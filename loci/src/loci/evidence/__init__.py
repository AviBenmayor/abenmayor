"""Closure evidence: web search and the classification rules that turn a
search hit into a provenanced verdict (D98, GTM-170).

See `loci.model.poi_evidence` for the precedence rule that decides whether a
row written here can override a POI's base status, and
`sql/033_poi_closure_evidence.sql` for the schema this package writes.
"""
