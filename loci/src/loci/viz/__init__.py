"""Map and chart output.

Palette rules: sequential for DNCI; DIVERGING for the residual (zero is a real
midpoint); bivariate 3x3 for the transit-x-residual overlap. Load the `dataviz`
skill before writing chart code. CONTEXT.md 5.

The coverage-bias chart is not an appendix item. If the finding survives it, it
is the most persuasive artifact in the set.
"""
