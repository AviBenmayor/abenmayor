# __RQ_ID__ — __TITLE__

Scaffolded __DATE__ by `loci research new`. Fill every section from the interview and
the seed before marking `question: done` in STATUS.yaml — this file's job is to make
SEED.yaml's constraints legible to a reader who will never open the YAML.

## Question as asked

TODO — the owner's question, close to verbatim.

## Interview rulings

TODO — every ruling from the interview that shaped the seed's `constraints:` list,
one bullet each, in the owner's own terms. Every constraint in SEED.yaml should be
traceable to a line here.

## Definitions

TODO — the terms this RQ makes precise (e.g. what counts as "favorable," how an
outcome is measured, what a censored observation means here). A later reader should
never have to guess what a word in the results means.

## Tiers / unit

TODO — unit of analysis, and the data tier(s) this RQ runs at (e.g. ZIP long-panel vs
address-level calibration), with the history depth each tier covers.

## Out of scope

TODO — what this RQ deliberately does not answer, so a later session does not
re-litigate it or silently expand the question.
