# CHECKPOINT — Loci

**Running project memory.** Append at the end of every working session.

This is deliberately *not* CONTEXT.md:

| File | Holds | Changes |
|---|---|---|
| `docs/CONTEXT.md` | The charter — thesis, sources, method, threats. The *what and why*. | Rarely, and deliberately |
| `docs/CHECKPOINT.md` | State — what's done, what's blocked, what was decided when. The *where we are*. | Every session |
| `docs/QUESTIONS.md` | The build compass — research questions tiered by rigor, mapped to tickets, plus the homework list. | Per question, as answers land |
| `docs/TICKETS.md` | Work breakdown. Generated by `loci gen-tickets`. | Regenerate, never hand-edit |

**Last updated:** 2026-09-22 (session 44 — D134: D132/GTM-202's live re-run COMPLETE — blocked first at `address-gaps` by GTM-209 (bathhouse_sauna zero supply, no code path excluded it), owner ruled fix GTM-209 then finish the re-run; `signal_categories()` shared helper built, both call sites (address_gaps.py's guard/aggregates, supply_ratio.fit_baselines()) fixed and tested, full canonical order re-run end to end on hash ed55301203a4 → 1898163ac8ce (gated supply 133,356 → 130,536, −2.11%); 190,413 lot + 33,934 street addresses read a gap on the new hash (no valid prior snapshot to diff, since address-gaps had not completed since D121/2026-09-17); suite 2,819 passed / 1 skipped / 2 pre-existing-unrelated failures; GTM-202 and GTM-209 both Done; session 43 — D132: glossary audit — charter reconciled with the code on act/diligence, co-location, a fourth radius, claim layers; 16 terms defined, 10 overloaded words given canonical names; session 42 — D130: Deux Luxe and Fazenda operator pages, Fazenda rent MISS on floor area, retailarea-0 rule; session 41 — D131: camera and aerial instruments landed (owner list item 3, built 2026-09-17, recovered 2026-09-21): weekly all-camera sweep + spool + scene hash, ortho 2022→2024 change on 462 Gowanus permitted lots, awnings on the D82 corridors (23 of 1,333), convertible stock 3,310 lots, DOT camera/VivaCity ask drafted; camera validation N = 45, ρ +0.41; three review pages await the owner's hand-check; session 37 — D126: tenant-space research closes FOIL-only; owner sends FOIL and product work is parked; session 35 cont. 2026-09-17 — D124: bathhouse_sauna admitted at G0 as a non-filtering signal, narrow slug, ingest/hash move deferred; session 35 — D122: DOE snapshot registered GTM-204, Bed-Stuy read finds the screen more family-heavy than MN+BK, not thinner; webmap single-business mode now NTA-scoped, 5e743c7; D120: D42 parked, D43 placebo (4 of 15 category-specific, CONTEXT ONLY), backlog realigned to charter v2, five Todo tickets; D123: bathhouse base rate 1.7/yr citywide, AC-1 Baseline field ruled; session 45 — source-candidate sweep, no D-id: six sources registered (Vexcel P3, Facteus P2, Dataplor P3, BatchData P3 on the paid wishlist; fema_nfhl and rebny_retail_reports as free `planned` sources) and a competitor capability-gap memo benchmarking 28 competitors (docs/competitor-capability-gaps-2026-09-22.md, mirrored to Notion)).

## ⚠️ SCOPE CORRECTION — retired 2026-09-16 (D116)
This banner (hex-era, "find hexes missing a daily-needs business") is superseded. Read
**`docs/CONTEXT.md`** (v2, 2026-09-16) for the current charter: owner as first customer,
take-it first, decision value gated on a dated survival label, Gowanus bathhouse as AC-1.

---


---

## How to resume

```bash
cd ~/Documents/Projects/abenmayor/loci
claude --continue
```

A **fresh session is required** to pick up MCP servers connected mid-session — this is how
the Linear tools become available.

Then say: **"read docs/CHECKPOINT.md and continue"**.

**Canonical pipeline order (D78 — MN+BK only; never `--borough ALL`):**

```bash
uv run loci street-frame            # D84: refresh the street points parquet (address-gaps consumes it; --no-street-frame to skip)
uv run loci address-gaps            # default MNBK; --borough MN then --borough BK if it runs long (~10 min each)
uv run loci address-demand --borough MNBK
uv run loci pipeline && uv run loci storefronts && uv run loci age-fit apply --category all
uv run loci supply-ratio --boroughs MN,BK        # NO --fit-baseline
uv run loci address-access --boroughs MN,BK      # ~9 min; address-gaps NULLs the seven access columns
uv run loci transit-profile --boroughs MN,BK --re-sweep   # re-apply after address-gaps; --re-sweep required after any frame change (D84)
uv run loci citibike address-measures --boroughs MN,BK
uv run loci address-character build --boroughs MN,BK && uv run loci storefront-pipeline openings --boroughs MN,BK
uv run loci dot-counts address-context
uv run loci address-legality build   # D97/D104: PLUTO zoning legality; re-apply after every address-gaps re-run
uv run loci revenue --boroughs MN,BK
uv run loci export-webmap
```

**Seed spec for D97-D100 (Ouroboros, immutable):** `.ouroboros/seed.yaml` — 23 acceptance criteria, deliver in order zoning → closure → search → report.

---

## Current state

Phase: D132/GTM-202's live collapse-fix re-run is COMPLETE (D134). It was blocked once, at `address-gaps` (EmptyCategoryError, GTM-209 — bathhouse_sauna, admitted zero-supply at D124, had no aggregate excluding it), then the owner ruled to fix GTM-209 itself rather than defer it: `model/conveniences.signal_categories()` (shared, cached) now backs an exemption in `address_gaps._refuse_empty_categories` and a signal-column mask in `compute_gap_metrics`'s gap_score/lead_category/n_missing, plus a matching zero-supply skip in `supply_ratio.fit_baselines()`; four tests pin the behavior. Full canonical order re-run end to end: supply_hash ed55301203a4 → 1898163ac8ce live (gated supply 133,356 → 130,536, −2.11%, matching the pre-run preview), address-gaps 332,041 rows with no error (190,413 lot + 33,934 street addresses read a gap; bathhouse_sauna correctly leads nowhere), age-fit/supply-ratio/access/transit/citibike/character/pipeline/legality/revenue/webmap all re-applied clean. No valid prior screen snapshot exists to diff against (address-gaps had not completed since D121, 2026-09-17). Suite 2,819 passed / 1 skipped / 2 pre-existing-unrelated failures (test_bike_od's own hardcoded matrix shape, a Codex peer's uncommitted public_signals.py). GTM-202 and GTM-209 both Done in tickets.py; QUESTIONS D24 answered. Charter glossary reconciled with the code (D132): *act* needs grade ≥B and *diligence* is the grade-C verdict; unresolved co-located pairs are NOT both counted (rule stands, code fix ticketed and hash-moving); a fourth radius (400 m straight-line, forecast/retrodiction only) is named and the memo's 500 m list is ticketed for removal; screen measures are REALIZED, only fitted quantities are MODELED; `ships_as: signal` is not honoured by the screen aggregates (ticketed, definition deferred). Item 2 landed (D125): licence non-renewal label + 400 m baseline, went-dark triangulation (4,668, unpromoted), rewind snapshots 2016–2025, tenure prior, pipeline 541,051 rows; P5 FAILS on the joinable statistic (restaurant 0.29 / bar 0.24 / grocery 0.04 vs 0.50), so survival-gate source #4 currently supports non-renewal only. Two SoHo operator pages issued and the first predictive page scored its first answer (D130): Deux Luxe $1.5M/yr and Fazenda $470k first-year counter sales; Fazenda rent $15,000/mo on <1,000 sq ft was a MISS on floor area, so operator pages now declare unknown floor area on `retailarea` 0 lots. camera and aerial instruments landed (D131, owner list item 3): every MN+BK camera swept weekly on Tue+Thu inside the three DOT windows (spool, scene hash, free vehicle classes; launchd plists rendered, NOT installed — owner step), camera validation meets the memo gate at the margin (N = 45, ρ +0.413; stays context per D76), ortho 2022→2024 change / awnings / convertible stock written as three ungated tables (analysis.lot_aerial_change 462, building_awning 1,333, lot_convertible 3,310) behind review pages the owner has not hand-checked; DOT data-sharing ask drafted. Overnight ingests (D121-3): LODES and ZBP landed; CBP raw landed but its report crashed on a stale 2023 cache; Citi Bike legacy re-run aborted at 2013-06 on a `NULL` dock-id marker (fix named, not re-run). tenant-space research is FOIL-only (D126): the owner sends the drafted LL157 FOIL; no tenant-facing listing, dossier, warehouse, or card product work is authorized. bathhouse_sauna remains admitted at G0 as a non-filtering signal (D124, GTM-198); ingest + hash move is the next announced step (D124-1). Warehouse reshaped and re-run on supply ed55301203a4 (D121): scope enforced in schema, catalog drift-checked, forecast 0.1.1+67e28b04 lot+street, file 5.17 GiB; survival gate re-sourced to SLA (restaurant/bar/grocery). AC-1 base rate enumerated — 1.7 bathhouse openings/yr citywide 2019–26, 3.4/yr since 2024, Baseline = citywide rate + named pipeline (D123, GTM-199); DOE Demographic Snapshot registered as a planned source for a first derivative on the child pool (D122, GTM-204); webmap single-business mode scoped to the picked neighborhood (5e743c7). Rewind prerequisites landed (D119): filings full history 989,174 rows, LL157 recode fixed (6.8% → 81.8%), DCWP intervals in; rung (iii) survival source being re-inventoried in wave two. Charter v2 landed (D116): owner is first customer, decision value gated on a survival label (AUC ≥ 0.65 by 2027-06-30), Gowanus bathhouse is AC-1 (bathhouse_sauna admitted at G0 via GTM-198). Challenger forecast null recorded (D118). Chains schema shipped (D114, GTM-189); page served from the repo (GTM-191 half). Auto-admission ruled and run (D113): candidates/admit/reject/auto-admit shipped (GTM-186–188), 741 auto rows, list 902. Re-baselined on supply ba944e18c57b (D112) and the hash pinned to a stored as-of after it proved clock-dependent (D115); D42 parked behind DOT validity, D43 placebo finds bar/fitness positive and childcare/pharmacy negative (D120); backlog realigned to charter v2 with five Todo tickets; investor re-review actioned and the first full allocator memo rendered at grade C (GTM-172 Done); Citi Bike plan complete — phase 2 OD leakage (D108) and phase 3 activity growth (D111) both CONTEXT ONLY, reader proven portable to Divvy; ground-truth session 1 recorded (D107, GTM-183); chains watchlist admission process ruled and built — two tiers (mechanical CANDIDATE pool + hand-admitted YAML), a candidate predicate, exclusions, tier 3 `watch` for pre-chain operators, and full auto-admission (D109–D110, D113); first-run backlog hand-triaged to a 38-brand batch add (161 brands), then 741 auto-admitted for 902 total; Legality label, closure evidence, address search and allocator report built (D97-D100, D104); reports and investor review completed (session 26, abenmayor-29, 2026-09-14); generator reworked per the investor review, three no-trade notes regenerated 2026-09-15, re-review pending in the log (GTM-172). GTM-48 coverage validation done on the address frame (D90) — P3 survives: design-weighted 7.6% [6.5–8.8] of MN+BK MISSING flags are real coverage holes (~40k of 523k), no income gradient once errors respect the 137 strata, holes track anchor absence not income, and fitness's apparent 35.6% hole rate was entirely a sports_club/marina Google type-map defect (D50), not missing supply. Owner ruled "yes to all 4" on the follow-ups: demote tailor_repair/hair_barber from headline (D30 precedent), adopt the numeric G9 Wilson-upper grade ladder, drop sports_club/marina from fitness's GOOGLE_TYPES, and run cross-category dedup before per-category dedup (GTM-153 proper) — all four ruled, implementation in progress (GTM-173/174/175); only the unresolved-co-located-pairs collapse question stays open (QUESTIONS D24). GTM-112's category-expansion checklist shipped (commit 766d9a2). Per-category density elasticity shipped (D70, GTM-138) — eight saturate, none cluster, supercedes D68's bars/cafés reading; density-signal annotation ready for address screen and grade design (C ≡ 1). Construction-activity evidence built (D72) — permitted splits active/lapsed/stalled from DOB feeds; Gowanus red-team downgrades the earlier reading (D72). Supply ratio vs MN+BK baseline per address×category on the principled set at 400 m network distance shipped (D73, GTM-143) — laundry 0.94× (normal not thin), pharmacy/convenience/hardware are the thin leads in Gowanus core. Evidence-graded recommendation card shipped (D74, GTM-144) — first Gowanus card says "do not act on this data" for 14 of 15 categories, all on economics. Development pipeline built and deployed (D62); storefront registry ingested (D67); headroom backtest shows no predictive power (D68 superseded by D70); supply set settled by principle (D52/D59) and re-run on MN+BK; laundry anchored (DCWP inspections), LL84 + StreetEasy laundry inputs landing; alcohol overlay live; E10 category expansion ticketed; bar age-fit shipped as a separate supply-revealed ranking signal (D63); subway ridership finally populated. Warehouse consolidated 34 → 26 objects (D61): address/address_category long form with address_gaps as a view, hex screen outputs retired; ACS age/race/education/household-size at address grain (D60); development-pipeline layer wired into the webmap (D62). Childcare anchor loaded (D65) and its age-fit now passes; pharmacy passes on the wrong coefficient (D66) — the three rulings landed (D69): childcare age-fit applied, pharmacy refused under the tightened F2, floor-anchor exception to D52 live, screen re-run on supply 06bb6f357cc1. Pharmacy anchored (D71) and its age-fit stays refused; eligibility gate REMOVED by owner ruling (D75), screen re-run gate-free on supply 767b28674e30; next: street-midpoint sampling frame and density ranking (owner directions, pending confirmation). Foot-traffic proxies at the address grain shipped (D76, GTM-147) — transit_entries_400m and jobs_400m within 400 m network distance; DOT validation ρ +0.79 headline, but contrarian verdict is card-context only (65% of Brooklyn addresses read zero), not a grade, not a denominator; day-type × daypart profile now shipped as a D76 addendum (analysis.address_transit_profile, analysis.address_entrance) — the AM/PM entry share reads residential vs destination catchment (Brooklyn 84.5% residential-type) but the levels still carry no time-of-day discrimination at DOT points, so the D76 card-context ruling stands unchanged (QUESTIONS X7, open). Growing-chain watchlist and monthly refresh shipped (D77, GTM-148) — 121 brands, first detect snapshot 2026-09, press enrichment live. Screen tables enforced to MN+BK only (D78); address-gaps refuses other boroughs. Storefront first-seen ledger shipped (D79) — analysis.poi_presence + poi_first_seen view, content-hash keyed (cluster_id is an input-order artefact, 0% survive a row-order shuffle), 227,548 locations backfilled with censored/NULL first-seen rather than a fabricated September wave; chains now keys on the ledger, not cluster_id. Storefront lifecycle filings and pipeline shipped (D80, GTM-152) — staging.storefront_filing (232,667 filings, 7 feeds × 8 stages) plus analysis.storefront_pipeline at bbl × business_name_key (135,912 rows), cross-agency lead time fitout→first_inspection strict 259 d / reconciled 221 d (92% sole-pair, so strict is the reference), 'gov_filing' first-seen kind, openings_pipeline_400m context measure real for 5 categories and structurally zero for the other 10; ticket GTM-152 opened to widen coverage and harden reconciliation. Site-revenue model v0 shipped (D81, GTM-150) — CEX spend pool × Huff-model capture share fitted per leave-one-ZIP-out fold, leakage calibrated to EC 2022 county receipts; restaurant alone passes backtest and grades C on the recommendation card; nine categories honestly not modelled (café/grocery/hair/nails/pharmacy fail placebo, laundry/convenience/bar fail baselines); Gowanus core restaurant revenue $1.08M–$2.66M and rent ceiling $11.2k/mo. Neighborhood character shipped (D82, GTM-154) — retail/corporate/industrial/residential at the address grain from PLUTO floor area + LODES jobs + zoning overlays within 400 m/100 m, urban-planner-revised after the first pass mislabelled prewar retail corridors, continuous retail_index rendered on the webmap with corporate/industrial hatched overlays; character is map colour and card context only, it enters no grade. Clusters ranked by walk-shed density (D83); street-midpoint frame built and scored as a second frame (D84). NYC DOT data landed as a source — bi-annual pedestrian counts (37 rounds 2007–2026), a 969-camera registry, per-address DOT context, and a local YOLO11n sidewalk-count sampler shipped as a shortlist-verification instrument, not a screen-wide layer (D85); first samples were a Sunday evening so the DOT validation is still N=0 pending weekday sampling. Post-raise data wishlist encoded as 35 machine-checked registry entries at $301,565/yr with a generated docs/PAID-SOURCES.md (D86), and the GTM strategy memo shipped with a contrarian red-team finding the value-prop math was D1's reverse-causality error in a costume — the retrodiction test (GTM-158, Urgent) now gates any decision-value claim (D87). Retrodiction ran (D88, GTM-158 Done): the frozen screen ranks retail streets, not unmet demand (entry AUC 0.866 vs 0.854 no-score), and LL157 go-dark returns a null, so Loci may claim cost of search only — survival ascertainment (GTM-161) is next; the recommendations ledger (D89) now tracks time-to-fill and fit against a documented rubric, first real check 2026-10-01. Four layers named end to end (D92, e578470): MODELED (gap score, supply ratio, demand, character, recommendations, forecasts), REALIZED (first-seen ledger, closures, filings, chain snapshots, DOT counts, camera frames), SCORED (retrodiction, recommendation fill checks, forecast outcomes) and LEARNED (FINDINGS.md, carrying capacity, portability, planner verdicts) — with a forecast ledger (analysis.forecast/forecast_run/forecast_outcome, sql/028) that issues and scores dated p_opening predictions monthly (2023-01 backtest AUC 0.899, 2026-09 live vintage scored 2027-09) and a legality-vs-herding addendum to D88 (own-category supply +1.18 [0.88, 1.49] net of legality, +1.40 [1.05, 1.75] inside the legal retail set, legality absorbing only 0.0002 of the +0.0126 AUC lift against other-category supply's ~48%). The learned layer is registered (D93, a9efca7/df5383d): FINDINGS.md's 14 graded findings, the carrying-capacity NYC-vs-national curve and CBP↔POI portability ratio, the portability audit's per-grade minimum input set, and the planner-review protocol with three send-ready packets — the project's compounding, machine-checked asset. Charter names the four layers (D95). Forecast ledger now carries the supply-set identity in its model version and a vintage-retention rule (D96, 0.1.1+3cd0e269 issued on a7c531ebca17), with a third re-run onto the post-Windclimb final hash 467cd5969200 finishing at session close and a re-issue onto it in progress; Citi Bike phase 1 shipped end to end — 44-month panel, address-grain measures, DOT validation, map layer — and marked Done (D102, GTM-166); the first-seen ledger key migration that D101's dedup required is applied and verified with zero fabricated ledger events (D103), alongside a second, unrelated schema-application bug found and fixed the same night (f8142e0). Chain watchlist admission ruled (D109): two tiers, candidate predicate, exclusions, incumbents off the lead list; All'Antico Vinaio and Super Burrito added, two brand-key splits aliased, 2026-09 snapshot rebuilt (20,989 brands); admission pipeline ticketed GTM-186–191. Tier 3 `watch` ruled (D110): pre-chain operators at 1–2 locations with an intent signal, internal-only until graduation, Tavily cap 60→68; Bathhouse/Mink Padel retrospective shows filings are food-and-drink by construction (GTM-192). D117: the recommendation loop has its first scored operator datum (rent, hit) and a second-site search; economics stays C until sales arrive.
M1 coverage validation is done for hardware/fitness/clinic (D29): hardware "not disproven" (5%
true-hole rate, Google's type is wider), fitness partially confirmed (~21% true hole remains after
removing geometry artifacts), clinic excluded from headline claims until re-anchored (D30). D3
walk-threshold sensitivity done for Manhattan (D31): the category mix is not stable across walk
windows. Axes 1–4b exist as expansions; Axes 3 (premium) and 4b (ignition) are uncommitted-quality
spikes now committed under E9. D6 reach rule is BUILT and verified (monotone on real data, D34) but
NOT publishable: contrarian review (D34) shows the p80 quantile calibration fixes the gap rate at
20% per category by construction, so the ranked list is a quantile artifact. D8 calibration
comparison is DONE (D35): store-to-store spacing and every p80 variant are rejected; external
per-category walk-time tiers with a max-ratio lead rule are recommended but the tier assignment
must be pinned to a citable source before the owner adopts it. Conveniences check is wired as
`loci conveniences` with tests; fair-value spend parameters are grounded in real BLS 2024/2023–24
tables; comps (O7/O8) remain blocked on listing-site access. Current focus: the lead-viability rule
(supply floor, excess minimum, or D7 density-class reach). The D34 acceptance battery was re-run at address level (D39): monotone only with a fixed gate, exactly-one lists are knife-edge, ranking must be continuous; reach table ADOPTED (D41: reach_tiers.yaml, continuous ranking); address_gaps.py rewritten and shipped citywide run (D44). Owner's market-reach question answered (D43): adopted tiers are a floor; Manhattan's revealed standard is 3–7× tighter; density-class reach (D7) is the refinement.

### Progress (E1 well underway)
Done this session: GTM-17 (DOHMH anchor + adapter framework), GTM-10 (Census key, verified),
GTM-12 (LODES vintage), GTM-21 (H3 grid, 8,321 cells), GTM-18+GTM-13 (DCWP), GTM-19 (NYS DOS).
GTM-16 (OSM adapter, hardened with mirror rotation + fail-loud).

**Data in the DB now** (`data/loci.duckdb`): analysis.hex = 8,321 cells; staging.poi =
**177,783 POIs** across all 15 categories (Overture 133,965 + DOHMH 30,619 + NYS DOS 13,199),
**un-deduped**. Adapters: dohmh, nys_dos, dcwp(empty), osm_overpass, overture_places — all done.
OSM is NOT full-loaded into the main DB (Overpass is slow; Overture covers the base). Load OSM
citywide only when the E4 coverage-bias cross-check needs it.

**Done since:** GTM-20 dedup (161,092 canonical), GTM-22 PLUTO controls (units_res 3.735M ✓),
GTM-24 ACS dasymetric (pop 8.48M ✓, households 3.30M ✓). The full supply-model input set now
exists in `data/loci.duckdb`: hex (8,321) + hex_controls (6,858) + hex_demographics (5,978) +
poi_dedup. Remaining E1: GTM-15 FSQ (low priority), GTM-23 MTA (needs E2 walk graph),
GTM-25 SI leverage (model-time), GTM-26 ship W1 map.

**E2 in progress (code written + unit-tested; awaiting the walk-graph download to run on real data):**
- GTM-27 walk graph — `src/loci/score/walkgraph.py`, OSMnx over an extended NYC bbox,
  retain_all=True (keeps Staten Island / Rockaways), lz4 Overpass mirror. **Downloading now**
  (large; process alive). Saves data/interim/walk_graph.pkl.
- GTM-28 access engine — `src/loci/score/access.py`. **Design refinement:** the DNCI needs
  per-hex COUNTS, which a multi-source Dijkstra can't give (collapses to nearest), so it uses
  scipy C-level Dijkstra from hex nodes in batches, bounded at 1200 m, then numpy counting.
  **Caught a real bug:** manual edge-mirroring double-counted lengths (OSMnx already has both
  directions) — csr_matrix SUMS duplicates, so every distance was 2×. Fixed (min per pair).
- GTM-29 k_c calibration — DONE. Per-tier k, first essential ≈ 0.55.
- GTM-30 DNCI — `src/loci/score/dnci.py`. Weighted geometric mean; the crux test proves an
  essentials-gap hex scores 0.25 (arithmetic would say 0.45+). Awaiting real access data to run.

Once the graph lands: `build_access` → `build_dnci` → the DNCI map (GTM-32, the W2 ship).
27 tests pass.

**DNCI computed on real NYC data** (analysis.hex_dnci, 24,963 rows = 8,321 hexes × 3 thresholds):
top hexes = Manhattan core (validated), median 0.03/0.27/0.61 at 5/10/15-min, 0.4% false-zero
floor among populated hexes. **Pushed to origin/loci/scaffold.** Next: GTM-32 render the DNCI
map (W2 ship), then E3 (supply model + residual on the now-complete input set).

**E1: 13/15 done (remaining: GTM-15 FSQ low-priority, GTM-23 MTA, GTM-25 SI leverage).
E2: 6/6 DONE.** The Errand Map (DNCI choropleth) is shipped:
https://claude.ai/code/artifact/ccf0d67a-bb2b-459d-a557-0b25584c81cc

**THE RESIDUAL WORKS (E3 core done).** Supply model R²=0.60 (P1 ok); all controls correctly
signed; Moran's I=0.587 → spatial error model (transit + density robust, comm_far/income were
spatial confounds). Top-20 opportunity list is defensible: Jamaica (#1, 6.5 FAR headroom),
Rockaways, East New York dominate; UES-Lenox Hill is the surprise; only Inwood Hill Park needs
an audit look. Done: GTM-23/33/34/35/45. hex_dnci now carries residual + opportunity.

**CAUSAL TEST DONE — the naive thesis is empirically REJECTED (honest null/reversal).**
P2 β=+0.069 (wrong sign, p=5e-16); pre-trend breaks parallel trends (gap = momentum, not latent
demand); placebo clean. Loci is a valid descriptive tool; the investment inference is not
supported. Recorded as CONTEXT §0. Done: GTM-36/37/40/41/42. This is the project's central result.

**Remaining E3 (robustness + honesty):** GTM-38 (validate LODES vs establishments), GTM-43 (MAUP),
GTM-44 (weight sensitivity), GTM-63 (LODES allocation bias), GTM-39 (add rent/permit outcomes),
GTM-46 ship. Then E4 (coverage validation P3, needs Google key; memo; artifact).

Old remaining-E3 note: GTM-36 LODES panel loader, GTM-37 block→hex, GTM-38 validate
LODES vs establishments, GTM-39 outcomes (ACS growth + Zillow rent + DOB permits), GTM-40 growth
regression (does residual@t0 predict growth?), GTM-41 pre-trend, GTM-42 placebo, GTM-43 MAUP,
GTM-44 weight sensitivity, GTM-63 LODES allocation-bias sizing. Then E4 (validation + artifact).

Old next-note: The raw DNCI correlates with density/wealth
(endogeneity, D1). E3 regresses DNCI on density + income + transit + commercial zoning capacity
+ borough FE; the RESIDUAL is the opportunity signal. Prereqs: GTM-23 MTA transit control (now
unblocked — walk graph exists), then GTM-33 supply model, GTM-34 Moran's I, GTM-35 opportunity,
GTM-40 growth regression + its mandatory checks (GTM-41 pre-trend, GTM-42 placebo, GTM-43 MAUP),
and GTM-63 (LODES allocation-bias sizing) before trusting the pre-2020 panel. analysis.poi_dedup populated. Remaining E1: GTM-15 FSQ,
GTM-22 PLUTO (verifying), GTM-23 MTA, GTM-24 ACS, GTM-25 SI leverage, GTM-26 ship W1 map.



**Emerging category coverage** (which source feeds which bundle category):
- food (restaurant, cafe_bakery): DOHMH (near-census anchor) — SOLID
- hair_barber, nails_beauty: NYS DOS — SOLID
- grocery, convenience, pharmacy, laundry, bar, and all Tier-4: OSM (+ Overture pending)
- DCWP: nothing (D13)

### Done and verified
- **`docs/CONTEXT.md`** — the charter. Residual thesis, 15-category bundle with OSM tags +
  NAICS, source registry, method spec, 7 ranked threats, 8 open questions.
- **`src/loci/registry.yaml`** — 18 sources, machine-readable mirror of CONTEXT.md §3.
- **`loci check-sources`** — passes. Validates schema, budget ceiling, and CONTEXT.md
  drift. **It caught a real drift on first run** (MTA ridership `wujg-7c2s` was in the
  registry but missing from the charter table). Keep running it.
- **URL sweep** — 18/18 sources resolve 200.
- **DuckDB** — `data/loci.duckdb`, 9 tables across `staging`/`analysis`, verified.
- **`docs/QUESTIONS.md`** — 23 research questions in five rigor tiers (Measurement,
  Descriptive, Explanatory, Predictive, Causal) each mapped to the tickets that answer it,
  plus 23 homework items grouped by literature / data quirks / methods / tooling.
- **`loci check-questions`** — passes. Asserts every cited ticket title exists in
  `tickets.py`, every cited epic exists, statuses are in-vocabulary, and P1–P3 are each
  claimed by a question. Warns on load-bearing E3/E4 tickets with no question behind them.
  Wired into `make check`.
- **`loci` CLI** — `init-db`, `sources`, `check-sources`, `check-questions`, `gen-tickets` all work.
  `ingest`/`grid`/`score`/`model`/`export` raise `NotImplementedError` pointing at their
  epic.
- **Tickets** — 6 milestones, 61 issues (two exploratory E3 tests added in session 4), 219 points, generated by `loci gen-tickets` and
  **pushed to Linear** (GTM-5 … GTM-62). Verified: all 58 assigned to a milestone, none
  orphaned. E0 sits at 71% (5 of 7 done).
  Project: https://linear.app/avi-benmayor/project/loci-723fa10296fb

### Blocked
| Blocker | Blocks | Action |
|---|---|---|
| Scripted fetches of BizBuySell/BizQuest/LoopNet return HTTP 403 | Comps (O7/O8) has 59 BizQuest NYC listings via owner-approved human-paced browser session (commit fe59eb2); fill rates thin (cash_flow_sde ~14%) and only restaurant meets benchmarks.yaml's ≥8-row bar | Scripted sources remain 403; further listings require manual export or additional browser sessions; test_comps.py failing test tracks the fixture-only state |
| ~~`loci address-gaps` re-run NULLs the seven access columns~~ | RESOLVED (D78) — peer added `address-access` and `transit-profile` to the canonical re-apply order in How to resume | n/a |
| Access columns not on `analysis.address_gaps` view or webmap | Recommend card and webmap popups cannot show transit/jobs context | Move the ALTERs into `002_schema.sql` tail and extend `address_gaps_view_sql()` |
| ~~Chains monthly job not installed~~ | RESOLVED — `~/Library/LaunchAgents/com.loci.chains-refresh.plist` installed 2026-09-13, day 1 03:10, runs storefront-pipeline build → poi-snapshot → detect → research → render; logs under `data/chains/logs/` | n/a — the first honest snapshot lands 2026-10-01; verify its logs then |
| SLA pending licenses are a live queue with no history | D80 filing lead-time and the eventual `'gov_filing'` first-seen kind — the SLA API returns only current pending applications, so a skipped month vanishes on issue, same as the D79 rule | `make chains-refresh` already snapshots it; the launchd row above must land before 2026-10-01 so this runs unattended |
| Sidewalk validation needs weekday samples (not a blocker) | `validate` reads N=0 today because the first 183 frames were a Sunday evening | Sample the 15 DOT-adjacent cameras across AM/MD/PM on two weekdays, then `validate --round 2026-05` — ticketed GTM-157 (D85) |
| DB size 4.6 GB after two forecast cycles — no retention rule | Every monthly `make chains-refresh` issue+score cycle adds ~1.4 GB (D92); unbounded growth | Retention rule before the 2026-10 monthly run adds another ~1.4 GB — ticketed GTM-163 (D92) |
| `ANTHROPIC_API_KEY` unset in `loci/.env` and `anthropic` newly in pyproject | `loci report` falls back to ClaudeCliProse when unset; calls pass through to plan-billed billing | Owner adds the key when ready; interim: `claude` CLI present on PATH (verify with `which claude`) and ClaudeCliProse is selected automatically |

D8 tier source is **no longer a blocker** — resolved by D41 adopting reach_tiers.yaml with cited walk thresholds where they exist and owner norms elsewhere. Docker is **no longer a blocker** — removed from the critical path entirely (D9). Linear is
no longer a blocker — all 58 issues are pushed.

---

## Environment

| Component | Status | Notes |
|---|---|---|
| **DuckDB** | **Working, verified** | `spatial` + community `h3` both load. RTREE indexes, `ST_Read`, `read_parquet` confirmed. No container, no daemon. |
| PostGIS | **Abandoned** (D9) | Container torn down. Volume `loci_loci-pgdata` holds nothing of value — remove with `docker volume rm loci_loci-pgdata`. |
| Docker Desktop | Working, unused | 29.7.2 / Compose v5.5.0, wired up via user-level symlinks (D5). Kept in case it's wanted later. |
| `uv` / Python | Working | 3.12 target; `.venv` gitignored |
| Linear connector | **Working** | `claude.ai Linear`. 6 milestones + 58 issues live in project `loci-723fa10296fb`, team **Go To Market** (GTM-5 … GTM-62). |
| Census API key | **Working** | Set in .env; GTM-10 verified. ACS vehicle-ownership ingest (for D7) is a task, not a blocker. |
| `python-dotenv` | **Working** | Loads `loci/.env` in `cli.py` (`override=False`); fixed 2026-09-03 — previously `GOOGLE_PLACES_KEY` in `.env` was invisible to `loci validate --run`. |
| `tavily-python` | **Working** | `>=0.7` installed; used by the chains press-enrichment sweep (D77). |
| `TAVILY_API_KEY` | **Working** | Present in `loci/.env`. |
| launchd plist (chains monthly refresh) | **Working, installed 2026-09-13** | `~/Library/LaunchAgents/com.loci.chains-refresh.plist`; day 1 03:10, storefront-pipeline build → poi-snapshot → detect → research → render; logs `data/chains/logs/`. First honest snapshot 2026-10-01. |
| `analysis.poi_presence` + `poi_first_seen` view | **Working, populated 2026-09-13** | 227,548 locations; 52.3% source_date, 47.7% backfill_censored, 0 observed (D79). |
| `analysis.address_transit_profile` | **Working, populated 2026-09-13** | 1,654,545 rows, day_type × daypart, MN+BK (D76 addendum). |
| `analysis.address_entrance` | **Working, populated 2026-09-13** | 457,440 reachable-entrance pairs, persisted (rebuild 8 s vs 5 min) (D76 addendum). |
| `staging.storefront_filing` | **Working, populated 2026-09-13** | 232,667 filings, 7 feeds × 8 lifecycle stages (D80 stage one, commit 2979cc2). |
| `analysis.storefront_pipeline` | **Working, populated 2026-09-13** | 135,912 rows at bbl × business_name_key; `openings_pipeline_400m` / `openings_recent_400m` on `analysis.address_category` as context only, structurally zero for 10 of 15 categories (D80 stage two, commit 10a33e8). |
| `analysis.address` character columns (`retail_index`, `character_label`, `character_copy`) + NTA character view | **Working, populated 2026-09-13** | BK 34.7% retail_mixed / 62.2% residential / 1.9% industrial / 0.2% corporate; MN 58.2 / 25.6 / 0.0 / 15.5 (D82, GTM-154). |
| `onnxruntime` | **Working** | 1.30.0; runs the YOLO11n person detector for `loci sidewalk-count` (D85). |
| `data/models/yolo11n.onnx` | **Working, gitignored** | Exported once from ultralytics assets v8.3.0, sha256-pinned (D85). |
| `staging.dot_pedestrian_count`, `staging.dot_camera`, `analysis.sidewalk_count` | **Working, populated 2026-09-13** | 12,312 rows / 969 cameras / sidewalk sample counts (D85, GTM-157). |
| `analysis.address` DOT context columns | **Working, populated 2026-09-13** | 332,041-row address table including the street frame (D84 count, carried forward) (D85). |
| `docs/PAID-SOURCES.md` | **Working, generated 2026-09-13** | Mirrors the 35 `status: wishlist` registry entries; byte-identical drift check in `check-sources` (D86). |
| `docs/GTM.md` | **Working, published 2026-09-13** | Investor-lens draft + contrarian red-team, mirrored to the LOCI Notion page (D87, commit 0d1012c). |
| `loci gen-paid-sources` | **Working, wired into `make check`** | Regenerates docs/PAID-SOURCES.md from the registry (D86). |
| `staging.poi_closure` | **Working, populated 2026-09-14** | 228,455 NYC closures from the unfiltered Foursquare re-pull; 13,643 match ledger rows; ~3% ascertainment, categorically non-random (D88). |
| `closed_on` / `closed_src` on `poi_presence` | **Working, populated 2026-09-14** | First-seen ledger gains an observed-closure column where Foursquare ascertains it (D88). |
| `analysis.recommendation` / `analysis.recommendation_outcome` | **Working, populated 2026-09-14** | 16 recommendation rows, monthly outcome snapshots (opened/in_pipeline/none), solution_match_score rubric (D89). |
| `data/retrodiction/` | **Working, gitignored** | cohort/entry_panel/summary/go_dark parquet + json outputs of `loci retrodiction run|report|go-dark` (D88). |
| `docs/retrodiction-2026-09.md` | **Working, published 2026-09-14** | Full design, cohort, entry retrodiction, LL157 go-dark, Foursquare closures, verdict (D88, GTM-158 Done). |
| `analysis.forecast` / `forecast_run` / `forecast_outcome` (+ views `forecast_latest`, `forecast_surprise_nta`) | **Working, populated 2026-09-14** | model 0.1.0+f7d190df; vintages 2023-01 and 2026-09, 4,227,630 rows each; sql/028. **DB now 4.6 GB (was 1.8 GB) — retention needed** before the next monthly issue+score cycle adds ~1.4 GB (D92, GTM-163). |
| `webmap/data/forecast.json` map mode | **Working, exported 2026-09-14** | Off / Modeled / Realized / Surprise segmented control, `loci forecast-export` (D92). |
| `src/loci/model/carrying_capacity.yaml` (generated) | **Working, published 2026-09-14** | `loci capacity`; gated NYC curve + CBP-vs-POI ratio + national comparison (D93, docs/carrying-capacity-2026-09.md). |
| `docs/PORTABILITY.md` (generated, drift-checked) | **Working, published 2026-09-14** | `loci gen-portability`; registry gains `portability`/`feeds`/`degrades_to` on all 43 sources (D93). |
| `docs/FINDINGS.md` | **Working, published 2026-09-14** | 14 graded findings + rejections + portable/NYC-only split, commit a9efca7 (D93); Notion mirror. |
| `docs/planner-review.md` / `docs/planner-packets-2026-09.md` | **Working, published 2026-09-14** | Protocol + three send-ready packets (A legality/herding, B held-out corridors, C Gowanus fills), commit df5383d (D93); Notion mirror. |
| `staging.citibike_station_month` | **Working, populated 2026-09-14** | 1,400,477 rows, 44-month panel 2023-01…2026-08, 2,611 docks, 151.9M starts (D102, GTM-166, commits f7f3d29/ac914da). |
| `analysis.address_bike_station` + `bike_starts_400m`/`bike_ends_400m`/`bike_evening_ends_share_400m`/`bike_casual_share_400m` on `analysis.address` | **Working, populated 2026-09-14** | 590,953 address×dock pairs; 149,364 of 281,842 MN+BK lot addresses within 400 m of a dock (D102). `--re-sweep` mandatory after any address-gaps rebuild. |
| `webmap/data/bike.json` | **Working, exported 2026-09-14** | 2,491 docks, map layer beside transit (D102, commit ac914da). |
| `analysis.poi_key_map` | **Working, populated 2026-09-14** | `loci poi-keys plan\|apply\|guard`; 8,134 merged groups mapped, 0 fake openings/vanished-without-map (D103, commit 00eeca3). `make chains-refresh` runs the guard before every snapshot. |
| `analysis.forecast_run.supply_hash` | **Working, populated 2026-09-14** | sql/030 ALTER; NULL on rows written before D96 landed; 2026-09 vintage `0.1.1+3cd0e269` stamped `a7c531ebca17` — re-issue onto `467cd5969200` in progress at close (D96, ticket GTM-181). |
| `data/loci.duckdb` size | **5.0 GB as of 2026-09-14 23:15** (`ls -la data/loci.duckdb`) | Grew from 4.6 GB (D92) after the Citi Bike ingest and two more screen re-runs; `loci forecast prune` (D96, GTM-163) exists but has not had a real (non-dry-run) execution yet. |
| `GOOGLE_PLACES_KEY` | **Working** | Set in `loci/.env`; ledger 8,963/9,113 calls after D90 coverage validation run (commit a7c531ebca17). |
| `LOCI_GOOGLE_CALL_BUDGET` | **Working** | Set to 9113; remaining calls 150. |
| `anthropic` SDK | **Installed in pyproject** | version ≥ 0.28, but `ANTHROPIC_API_KEY` is unset in `loci/.env`; `loci report` falls back to ClaudeCliProse plan-billed when key is absent. |
| NYC GeoSearch | **Reachable, keyless** | `geo/geosearch.py` verified working; no auth required. |
| `claude` CLI | **Present on PATH** | `/opt/homebrew/bin/claude` (verify: `which claude`); used by ClaudeCliProse fallback when `ANTHROPIC_API_KEY` unset. |
| Interceptor (browser-driving CLI) | **Working** | Browser-only 1.0.1, Chrome store extension, one context; `interceptor status` → browser-only (D105/D107, GTM-183 Done). |
| Supply hash | **ed55301203a4, as-of 2026-09-16** | Reshaped and re-run supply (D121); prior ba944e18c57b → a0b8ac9a8a52 → ed55301203a4. |
| `data/loci.duckdb` size | **5.17 GiB, reclaimed 2026-09-17** | Was 8.97 GiB; COPY FROM DATABASE with FK-ordered inserts (D121). Pre-reclaim copy kept at `data/loci.duckdb.pre-reclaim-2026-09-16` (8.97 GiB) — owner to confirm delete. |
| Forecast vintage | **0.1.1+67e28b04** | Issued for lot AND street frames, 4,980,615 rows, 0 street points without a forecast, 17,535 street_no_homes; 7 vintages kept (D121). |
| Warehouse objects | **83** (staging/measure/score/ledger/calib, 3 `_screen` views) | Was 81 before sql/046 consolidation; catalog with layer/grain/key comments in `docs/WAREHOUSE.md`, drift-checked by `loci check-warehouse` (D121). |

---

## Decision log

Append-only. Each entry says what was decided **and why**, so a future session doesn't
relitigate it.

**Collapse rule (2026-09-15).** Entries dated **on or before 2026-09-11** (more than five
sessions back) that ran five lines or longer and no longer state a rule the current phase must
obey have been collapsed to a single `**Dnn — … → archive` line. Their **full original text,
unchanged**, lives in **`docs/CHECKPOINT-ARCHIVE.md`** under the same D-ids. The archive is
**append-only** and is **not read at session start** — the briefing agent reads this file only.
Open the archive when a stub's reasoning is actually needed, or when a stub is about to be
relitigated. Anything still load-bearing (scope rulings, live gates, traps the database will not
enforce) was kept verbatim here regardless of age; everything dated 2026-09-13 or later is
verbatim.

**D1 — The thesis is a residual, not a raw count; the causal framing later failed and was scope-corrected away.** *(2026-09-01)* → archive

**D2 — LODES WAC is the panel backbone; license-date reconstruction deferred to E5.** *(2026-09-01)* → archive

**D3 — Commercial zoning capacity (PLUTO CommFAR/ZoneDist) is a required supply-model control.** *(2026-09-01)* → archive

**D4 — Access is one multi-source Dijkstra per category, not ~7,400 per-hex isochrones.** *(2026-09-01)* → archive

**D5 — Docker CLI symlinked at user level to avoid sudo; moot for Loci since D9.** *(2026-09-01)* → archive

**D6 — Epics are Linear Milestones, not Projects.** *(2026-09-01)*
The Loci project already exists in Linear; mapping epics to Projects would have created six
competing projects. Milestones nest correctly and drive the overview progress bars.

**D7 — DNCI is a weighted geometric mean.** *(2026-09-01)*
An arithmetic mean lets a hex with fifty restaurants and no grocery, pharmacy or laundromat
score well — exactly the failure the project exists to detect. Mandatory adversarial unit
test in E2.

**D8 — h3-pg is not a dependency; superseded by D9, DuckDB's community h3 extension.** *(2026-09-01, superseded by D9)* → archive

**D9 — DuckDB replaces PostGIS.** *(2026-09-01)*
The only available PostGIS image ran **emulated** (amd64 on arm64), and the "service path"
rationale for Postgres was speculative. DuckDB removes the container, the daemon and the
emulation penalty. Verified before committing: `spatial` + community `h3` extensions,
RTREE indexes, `ST_Read`, `read_parquet`, schemas, composite PKs, FKs, CHECK constraints,
JSON and GEOMETRY columns all work.
**Caveat the database will not enforce:** DuckDB `GEOMETRY` carries **no SRID**. Everything
stored is EPSG:4326 by convention; metric work must reproject explicitly. Hold this in code.

**D10 — Three root directories named by audience: docs/, src/, data/; scripts became CLI subcommands.** *(2026-09-01)* → archive

**D11 — Linear label taxonomy collapsed 32 to 14 via LABEL_MAP in tickets.py.** *(2026-09-01)* → archive

**D12 — LODES panel proceeds without a crosswalk; pre-2020 years are retro-allocated, leakage filed as GTM-63.** *(2026-09-01)* → archive

**D13 — DCWP contributes zero daily-needs POIs; laundromats come from OSM/Overture instead.** *(2026-09-01)* → archive

**D13 — docs/QUESTIONS.md is a sanctioned fourth document with its own lifetime and drift check.** *(2026-09-01)* → archive

**D14 — Grid boundary is the 2020 NTA file; cells labelled by maximum areal overlap (hex era, see D38).** *(2026-09-01)* → archive

**D15 — OSM Overpass needs mirror rotation and must fail loud.** *(2026-09-01)*
The main overpass-api.de endpoint 504s frequently; the adapter rotates to lz4.overpass-api.de
first, then main, then kumi. Critically, it now RAISES if every mirror is down rather than
returning 0 — a silent 0-row ingest for a core source would be invisible corruption. General
rule for any live-API source: fail loud on total failure, never ingest a silent zero.

**D16 — DuckDB `ST_Distance_Sphere` reads POINT(x, y) as (latitude, longitude).** *(2026-09-02)*
Our geometry is stored (lon, lat) per D9, so the raw call computes every distance at the
equator: 1° of longitude at NYC came back as 111 km instead of 84 km, and a gap hex's nearest
hardware store as 359 m instead of the true 1,228 m. Caught by a sanity check after an
aggregate disagreed with a walk-graph sample. **Rule:** never call the function on stored
geometry directly; wrap both arguments in `ST_FlipCoordinates` (the `METRES` expression in
`validation/sample.py`, pinned by a unit test). Same family as the no-SRID caveat: the
database will not tell you.

**D16 — SNAP and SLA are the coverage anchors for the bottom-biased tiers.** *(2026-09-02)* → archive

**D17 — Manhattan has no validated gaps (hex era); superseded by D48's Manhattan+Brooklyn scope.** *(2026-09-02)* → archive

**D18 — "Best investment" is a feasibility-gated Investability Index (Axis 1), not homes-affected.** *(2026-09-02)* → archive

**D19 — Trajectory ("Rising") is a second axis the owner added, kept separate from D1.** *(2026-09-02)* → archive

**D20 — Real ACS income momentum replaces the LODES job-growth proxy in Rising.** *(2026-09-02)* → archive

**D21 — East New York sized as a rooftops play; its value-gym call was overturned by D23.** *(2026-09-02)* → archive

**D22 — Premium/destination amenities are Axis 3, an inverted trade-area method, not the daily-needs screen.** *(2026-09-02)* → archive

**D22 — Hosted read-only query app for sharing the dataset; built, then taken down (GTM-85).** *(2026-09-02)* → archive

**D23 — Maturity curve and 2033 projection are Axis 4, not a revival of D1; see D25.** *(2026-09-02)* → archive

**D23 — Gym/ENY recommendation overturned; gaps need category-radius and walkability-demand gating.** *(2026-09-02)* → archive

**D25 — The 2033 maturity projection fails its 10-year backtest; ships as ranking and scenario only.** *(2026-09-02, overnight — numbered D25 because a parallel session already used D22/D23 above)* → archive

**D26 — Ignition is exogenous: a curated catalyst layer, not zoning or trajectory (Axis 4b).** *(2026-09-02)* → archive

**D27 — Catalysts run on two clocks: construction 5-9 years, demographic tip 10-20 years and only if market-rate.** *(2026-09-02)* → archive

**D28 — Axis 3 v1 built; padel has zero supply rows; premium demand must be steeply top-weighted.** *(2026-09-02)* → archive

**D28 — Scope arc: one corrected pivot, then deliberate expansion toward a where/what/when investment dataset.** *(2026-09-02)* → archive

**D29 — Google "found at least one" is not a coverage hole; split every result three ways (see D90).** *(2026-09-03)* → archive

**D30 — Clinic is excluded from headline gap claims until redefined.** *(2026-09-03)*
Why: loci's clinic category deliberately excludes doctor's offices (foursquare_places.py) and no
external source reproduces that exclusion, so the clinic gap is unfalsifiable by construction.
Google's `doctor` type saturates at the API cap (median 20 results/hex). Decision: re-anchor to
licensed urgent care (NYS Article 28 / DOHMH) rather than drop; until then clinic is not cited.

**D31 — The walk threshold is not one citywide constant; refined by D33, settled by D41.** *(2026-09-03)* → archive

**D32 — Validator bug fixed (commit 877d162): canonical deduplicated local counts, and .env now loads.** *(2026-09-03)* → archive

**D33 — The "missing" rule is definitionally broken, not mistuned; monotonicity is the acceptance test.** *(2026-09-03)* → archive

**D34 — Reach rule lands the monotonicity fix but the p80 calibration is rejected; not publishable.** *(2026-09-05)* → archive

**D35 — Spacing and p80 reach calibrations rejected; external walk-time tiers recommended, adopted by D41.** *(2026-09-05)* → archive

**D36 — 0 m same-type spacing is mostly a snapping artifact; the real duplication is DOHMH turnover.** *(2026-09-05)* → archive

**D37 — Income enters the gap screen as an annotation, never as a filter (Meltzer & Schuetz 2012).** *(2026-09-05)*
Why: Meltzer & Schuetz (EDQ 26(1):73–94, 208 NYC ZIPs, ZBP 1998–2007) show low-income NYC neighborhoods (<80% of citywide mean HH income) are NOT under-supplied by count in necessity categories — groceries are MORE numerous per acre there (0.051 vs 0.036) though smaller — while food service, gyms (0.29 vs 1.04 per ZIP), bars and upscale chains concentrate in higher-income areas. So a missing gym or cafe in a low-income hex is plausibly demand-following, not conspicuous; a missing grocery or pharmacy there is a real gap. Loci's gap rule used no income at all. Decision: (a) new package data `src/loci/demand.yaml` classifies each of the 15 categories as `necessity` or `discretionary` with an `evidence: paper|assumed` field — paper-backed: grocery/convenience/pharmacy necessity, restaurant/cafe_bakery/bar/fitness discretionary; the other eight are owner priors marked `assumed` and must survive contrarian review before the flag is shown on the map; (b) every gap row now carries `median_hh_income`, `renter_share`, `income_class` (low if below 0.80 × citywide population-weighted mean over inhabited hexes) and `demand_caveat` = discretionary ∧ low-income, plus a `caveated_missing` list; (c) the lead-missing category prefers a non-caveated gap; (d) the annotation NEVER changes which hexes or (hex, category) pairs are gaps — tests/test_demand_caveat.py proves set equality with a demand-unaware re-derivation for both rules, and the monotonicity battery is untouched. Result on real data: window rule 726 hexes (unchanged), 987 pairs, 91 caveated (9.2%) — fitness 42% (58/138), bar 36%, all necessity categories 0%; reach rule 1,707 hexes (unchanged), 6,189 pairs, 918 caveated (14.8%) — tailor_repair 42%, fitness 41%, bar 38%, nails_beauty 35%, cafe_bakery 33%, restaurant 29%. Reading: nearly half of the fitness "gaps" sit in low-income hexes where the literature says gyms follow income, which weakens fitness as a headline category the same way D29/D30 weakened clinic. Why annotation not filter: filtering would bake demand-side assumptions into the gap set and hide the D8 calibration problem behind them; and the same paper warns that race predicts retail net of income (X6), so an income-only filter could launder under-provision as "no demand". Also recorded from the paper: transit and retail floor space do NOT explain the income disparity (low-income ZIPs have more of both), so subway access must not be used as an expected-supply covariate; count-based reach is one-dimensional vs size/diversity (D9); reusable robustness battery (exclude Manhattan, exclude tiny units, alternate cutoffs) and symmetric growth rate. Side fixes: `build_gaps` now inserts by column name not position; `gaps-sweep` read `missing_expected` by index and was fixed to `r[-1]`; stray `__write_probe__.txt` deleted.

**D38 — The gap screen's geography is the residential address, not the hex.** *(2026-09-05, owner)*
Why: the owner stated it plainly this session — "no more hexes." The address-level screen already
exists (`model/address_gaps.py`, committed 2026-09-02 with the Railway webmap: per PLUTO residential
lot, one multi-source Dijkstra per category, same-gap buildings clustered) but it was never recorded
as the geography of record, so the 2026-09-04/05 sessions built D6/D33–D35 on hexes. What carries
over unchanged: the reach rule's logic (a location is a gap for c iff its nearest c is beyond
reach(c); eligibility is per-category reach too), the monotonicity acceptance test, the D35 verdict
that spacing and p80 calibrations are unusable and external walk-time tiers are the candidate, the
D36 DOHMH duplication fix, and the conveniences engine (`compute_address_convenience`) which already
yields per-address nearest distance for all 15 categories. What does not carry over and must be
redone at address level: the D34 acceptance-battery numbers (gap counts, list stability, lead
distribution, coverage correlation), the eligibility gate's K-of-15 threshold, and the clustering
step that turns same-gap buildings into an actionable site. address_gaps.py's current rule (800 m
window + ≥80% expected) has exactly the D33 defect and must move to the reach rule. Hex tables
(analysis.hex_gaps, hex_gaps_reach) stay in place as history; nothing new is built on them. The
address-level conveniences check (owner-set norms) and the address-level gap screen (reach rule) are
now the same geography; whether they remain two claims or merge is decided with the D8 tier source.

**D39 — Address-level reach screen needs a fixed gate for monotonicity; ranking must be continuous, not exactly-one.** *(2026-09-05)* → archive

**D40 — Census ZIP Business Patterns added as a validation-and-calibration source, never a gap-flag input.** *(2026-09-05)*
Why: Meltzer & Schuetz measured NYC retail with ZBP establishment counts by NAICS; Loci had no establishment count independent of its POI feeds. Built: `census_zbp` registry entry, `src/loci/zbp_naics.yaml` (NAICS 2017 crosswalk for the 15 categories, tailor_repair single medium-confidence code), `sources/universal/census_zbp.py`, `loci ingest-zbp` → `analysis.zip_establishments` (37,804 rows, 6-digit NAICS, 217 NYC PLUTO ZIPs, CBP 2023) and `analysis.zip_category_establishments` (size bands <5 / 5–9 / 10–19 / 20+), `loci zbp-compare` → `analysis.zip_coverage_check`; tests/test_zbp.py (8 passing; full suite 80 passed + the known test_comps failure). Probe facts: the standalone `zbp` API stops at 2018; from 2019 ZIP data lives in the `cbp` endpoint; a comma-separated ZIP list works in one call (~14 s); `in=state` is rejected; suppressed cells are absent rows, not zeros. FINDING (QUESTIONS M9): Loci POI counts exceed ZBP establishments by a median 2–8× in most categories, and the overcount is concentrated in the license-registry-anchored categories (restaurant 3.2×, cafe 3.5×, hair 2.8×, nails 8.0×, bar 6.0×) plus fitness 5.9×, while OSM/Overture-only categories sit near 1 (childcare 0.85, clinic 0.95, laundry 1.3, pharmacy 1.5). Part of this is CBP's employer-only universe, but bank 2.2× and hardware 3.2× are not sole-proprietor trades, so POI duplication or stale records is the working hypothesis — the same mechanism D36 found in DOHMH. Consequence: every revealed-spacing reach value is biased tight in those categories and their gaps are under-reported; the D8 tier work should treat license-anchored categories as suspect until the per-source rerun lands. Geography: ZIP is coarse; POI→ZIP is a majority-vote hex→ZIP crosswalk from PLUTO and ZIP population is summed dasymetric hex population, so the ratios are diagnostic, not load-bearing. Hex tables are used here only as a crosswalk, not as gap geography (D38 respected).

**D41 — Reach table adopted: cited walk thresholds where they exist, owner norms elsewhere;
ranking is continuous.** *(2026-09-05, owner)*
Why: D35 showed external walk-time tiers are the only calibration whose per-category counts carry
information, D39 showed the tier assignment is load-bearing and any binary "exactly one missing"
list is a knife-edge, and the tier-source research found only 4 of 15 categories have a citable
walk threshold (grocery 800 m — USDA FARA 0.5 mi urban / NYC FRESH / Portland 20-min; pharmacy
800 m — Guadamuz & Qato 2021 low-income threshold; restaurant and cafe_bakery 400 m — Walk Score
full-credit radius), 3 have analogs (convenience 400, bar 400, fitness 1,200 vs CDC's 1 mi), and 8
have no walk-scale literature (laundry, hair_barber, nails_beauty, tailor_repair, childcare, clinic,
bank, hardware; clinic and bank standards are drive-based). Owner adopted `src/loci/reach_tiers.yaml`
as the reach table — cited values where they exist, the owner's conveniences.yaml norms for the
rest, every row carrying its basis and citation — and a CONTINUOUS ranking: per address,
ratio(c) = nearest_m(c) / reach(c), gap_score = max ratio, lead = argmax, n_missing = count of
ratio > 1. Tier edges therefore decide only the gap flag, never the order. The eligibility gate
stays fixed and reach-independent (≤800 m for ≥12/15; D39) so monotonicity holds. Units are
capped at 500 per lot for any unit-weighted ranking (D39 mega-lot artifact). reach.yaml (p80)
stays available as `--reach p80` for comparison only. Implementation: `model/address_gaps.py`
rewritten, `loci address-gaps`, `analysis.address_gaps`, tests/test_address_gaps.py — see Session
10. Caveats carried: the −0.6 coverage correlation (M1) is untouched by any of this; 8 of 15 reach
values rest on the owner's judgment, and pharmacy's 800 m is the one cited value that moves the
most gaps.

**D42 — NAICS 2022 adopted as the category-taxonomy anchor via `src/loci/categories.yaml`.** *(2026-09-05)*
The 15 category slugs (`src/loci/categories.py`) were ad hoc keys scattered across
reach.yaml/conveniences.yaml/demand.yaml/benchmarks.yaml/spend.yaml, the OSM tag map, and
NYC/NYS license-type maps, with no single place stating what each slug means in NAICS
terms — the vocabulary every downstream comparison (ZBP/CBP, SBA, LODES, Meltzer & Schuetz)
actually speaks. `categories.yaml` is now that pivot: one entry per slug with `naics_2022`,
`naics_2017`, `label`, and each adapter's native tag (`osm`, `overture`, `dohmh`, `nys_dos`,
`dcwp`, `nys_sla`). It does NOT classify individual POIs — no POI source publishes NAICS —
it only anchors definitions. NAICS 2022 reference files (Structure, Descriptions, and both
concordance directions) were fetched from census.gov/naics (direct file paths; the `/naics/`
landing and search-style URLs are Cloudflare-blocked at 403) and reduced to
`src/loci/naics/naics_2022.csv` (2,122 rows, 2–6 digit) so nothing needs to open an xlsx at
runtime. `tests/test_categories_anchor.py` checks every slug in use is anchored, every
naics_2022 code is real, and no two slugs share a 6-digit code.

Two corrections the crosswalk surfaced, neither caught before because nothing previously
joined these categories against a verified NAICS 2022 code:
1. **The retail-tier 2017→2022 recode moved three of our codes**, not just the two the task
   anticipated: convenience 445120→445131, pharmacy 446110→456110, **and** hardware
   444130→444140 (all real, confirmed against the concordance file — `naics_2017` in
   categories.yaml keeps the pre-recode codes for panel years before 2022).
2. **`registry.yaml`'s `lodes_wac.naics` block was internally inconsistent**: it listed
   4-digit codes as if LODES WAC published NAICS, but WAC only ever publishes 20 fixed
   sector columns (CNS01–CNS20, e.g. CNS07 = all of Retail Trade 44-45) — no WAC column is
   narrower than a full sector. The 4-digit codes there are aspirational targets, realizable
   only from ZBP/CBP; the block is now annotated to say so explicitly (recoded to 2022,
   pharmacy's prefix corrected 4461→4561) rather than silently implying a join that can't
   happen.

**D43 — The adopted tiers are a floor standard; mature Manhattan's market standard is 3-7x tighter.** *(2026-09-05)* → archive

**D44 — Address-level reach screen shipped citywide; ratio ranking dominated by tailor_repair and laundry (scope drift, see D48).** *(2026-09-05)* → archive

**D45 — The universal layer is universal in logic, NYC-hardcoded in constants (GTM-89 verified on London).** *(2026-09-05)*
Why: E8 assumed the universal core runs outside NYC; CLAUDE.md says verify before designing around a dependency. A Sonnet spike ran the real pipeline pieces on a central-London bbox with live Overture S3, Overpass and OSMnx calls, writing only to scratchpad. Category mapping populated all 15 categories (Overture 7,591 + OSM 4,334 normalized POIs, plausible: Waitrose/Boots/London pubs), `score/dedup.py` ran unmodified (9,688 clusters), and the walk-graph builder is already parameterized (16,051 nodes). But the Overture and Overpass adapters expose BBOX/CACHE_PATH only as module constants (monkeypatch needed), and `grid/build.py` hard-requires `nyc_nta2020.geojson` + EPSG:2263, so the H3 grid step is not runnable for another city today even though bare H3 clipping works. Anchor loss is the real second-city cost: 7 of 15 categories (restaurant, cafe_bakery, bar, hair_barber, nails_beauty, grocery, convenience) lose their near-census anchor (DOHMH, SLA, NYS DOS, SNAP) and fall back to raw Overture/OSM confidence — and D40/M9 shows those anchors are themselves inflating NYC counts, so a second city may be closer to truth in count and worse in coverage. Decision: GTM-89 closed as verified; one E8 ticket carries the six constant-level fixes (adapter constructor args, parameterized build_grid, generic region columns, injectable anchor map in source_rank). No E8 design work before it lands. No `sources.cities.nyc` import leaks into universal/, model/ or score/ — the isolation rule is holding.

**D46 — NYC only, for a long time.** *(2026-09-05, owner)*
Why: the owner said so plainly after the GTM-89 result. The second-city milestone (E8) is parked: the parameterization ticket from D45 is filed at Low priority and blocks nothing, and no session should propose E8 work or design for city-agnosticism beyond what the isolation rule already gives for free. Depth in NYC — address-level screen, anchor quality (D36/D40), demand-side annotation (D37), and the Axis 3/4 extensions — is the whole roadmap. Revisit only when the owner raises it.

**D47 — The POI overcount is mostly uncorroborated single-source records, not stale licenses; fitness and hardware have no license anchor (corrects part of D40).** *(2026-09-05)*
Why: `loci zbp-compare --by-source` (GTM-108) split M9's ratios by the canonical record's source and by whether any second feed corroborates the cluster. Only six sources are loaded (Overture, Foursquare, DOHMH, SLA, NYS DOS, SNAP); OSM Overpass, FDIC, NYS pharmacy and DOHMH childcare are still `planned` with zero rows, so D40's grouping of fitness (and hardware) with the license-anchored categories was wrong — both are Overture+Foursquare only. Dropping every single-source record moves restaurant 3.4→1.09, cafe_bakery 3.55→1.12, hardware 3.37→0.86, hair_barber 2.58→0.65 (so the corroborated core already matches or undershoots ZBP), while bar 5.85→1.54, fitness 6.8→1.74 and nails_beauty 8.9→1.77 keep a residual. Staleness is ruled out where testable (SLA: 0 of 3,199 bar licenses expired before 2024) and untestable elsewhere because the DOHMH adapter never fetches inspection/grade dates and the NYS DOS adapter never fetches `license_expiration_date` despite documenting it; SLA misclassification is ruled out because the adapter already keeps on-premises classes only; DOS booth-renter fragmentation is 1.8% of nails rows. Worst nails ZIPs include Elmhurst (37×) and Williamsburg (24×), not just CBDs, so it is not a daytime-population artifact. Reading: the residual in bar/fitness/nails is most likely dedup misses (registry owner-name vs storefront-name, and the 40 m match radius), i.e. the same mechanism as D36 but not confined to DOHMH. Decisions: (a) for the reach-tier calibration, run the supply set both ways — all canonical POIs vs corroborated-only (≥2 sources, or anchor+1) — and report the reach values under each; if they diverge materially, corroborated-only is the default; (b) the DOHMH and NYS DOS adapters should fetch their date fields so the staleness test can be run at all (feeds the D36 active-filter work); (c) M9 stays in-progress until the dedup-miss hypothesis is tested by name-matching a sample of nails clusters by hand or via Google.

**D48 — Scope is Manhattan + Brooklyn (dense, walking-dominant); leads are graded, not filtered; the D44 citywide run was drift.** *(2026-09-08, owner)*
Three owner rulings in session 13, made when the assistant presented the D44 lead-viability candidates as "which rule knocks laundry out of the top 200." (1) Scope: Manhattan and Brooklyn, as it has been throughout. The `--borough ALL` run in D44 was drift; Staten Island and Queens results should not surface. Density IS a factor, but the model optimizes for dense, walking-dominant areas first so it stays simple; driving-dominant areas are a future expansion, not a current constraint to design around. CONTEXT.md's "five boroughs" scope line is updated accordingly. (2) Method: refine the logic, never optimize for an outcome. Tabulating candidate rules by the category mix they produce in the top 200 is outcome-tuning and is rejected as a way to choose. (3) Shape: Loci is a gradient, not a filter. The output is a GRADED recommendation; when the grade is high enough the recommendation is to act, at a fair price (hand-off to the comps/fair-value work, O7/O8). Hard filters — a supply floor, a fixed excess minimum as a cutoff — are the wrong shape. The excess-minimum idea (D44 candidate b) is sound as a grade INPUT: absolute excess meters carry information the ratio loses. Numbers from the session-13 tabulation kept only as description: the citywide top 200 by max-ratio is entirely laundry, 186 of them on Staten Island, because laundry's 320 m reach is the tightest of the 15 categories; Manhattan+Brooklyn-only description is being produced separately. Supersedes the "lead-viability rule" framing of D44's next step; the next step is a grade design (data-scientist proposal, then contrarian), described on Manhattan+Brooklyn only.

**D49 — demand.yaml's necessity/discretionary split is derived from spend.yaml's BLS CEX income elasticity (cut: > 0.35 → discretionary), not owner priors; the 0.80 cutoff denominator is citywide MEAN household income; the caveat is continuous and MOE-gated.** *(2026-09-08, owner, on contrarian review GTM-109)*

Why: the eight `assumed` rows contradicted a sourced file three directories away — spend.yaml already carried a continuous BLS CEX elasticity per category, and no cut on that scale reproduced the hand-coded binary (tailor_repair 0.30 = hair_barber, classed opposite; nails_beauty 0.35 = grocery, classed opposite). The 0.35 cut reproduces all seven Meltzer & Schuetz paper-backed rows exactly and is the only cut in {0.30, 0.34, 0.35, 0.40, 0.45} that does, so it is identified, not chosen. It sends all eight former priors to necessity; nails_beauty and tailor_repair flip. The old denominator was a population-weighted mean of tract medians ($88,154, cutoff $70,523); the paper's quantity is citywide mean household income, household-weighted — ACS 2023 5-year B19025/B11001 over the five counties gives $127,894 ± $988 (cutoff $102,315), cached at data/interim/citywide_mean_hh_income.json and exposed as `loci citywide-income`. Median income MOE is 27% of the estimate and more than half of gap hexes sit within one MOE of the line, so the binary badge is retired: gaps.py now emits `income_ratio`, `income_ratio_moe`, `income_indeterminate`, `demand_caveat_text` (X6 disclaimer appended), and `demand_caveat`/`lead_missing` key off the MOE-confident test (ratio + moe < 0.80). Clinic is excluded from the annotation (D30). The `evidence` vocabulary now refuses `assumed`. Caveated pairs: window 91 → 119, reach 918 → 765 (stored D37 tables; the review's intermediate prediction of 582 under old denominator reproduced exactly). Annotation verified non-filtering by a test that runs the screen with it forced off and asserts identical gap sets. Not yet ported to addresses (GTM-110). Renderers must not truncate `demand_caveat_text` — the disclaimer is its tail.

**D50 — Google Places validation harness fixed (primary-type filtering, 20-result cap recorded, returned types persisted) and the type map corrected per the GTM-105 audit; clinic removed from the Google map; existing clinic and fitness validation rows are not usable as counts.** *(2026-09-08, GTM-105)*

Why: both call sites (validation/google_places.py, webmap/server.js) filtered on `includedTypes`, which matches any of a place's types, while loci assigns one category per place — a bakery-cafe counted under restaurant and cafe_bakery, a bodega under grocery and convenience, a salon under both hair and nails. Switched to `includedPrimaryTypes`. Nearby Search (New) caps at 20 results with no pagination, and `nearby_count` returned `len(places)`, so counts were right-censored: clinic hit the cap in 867/990 sampled hexes (87.6%), fitness in 151/990 (15.3%), hardware in 0. New columns `n_ground_truth_at_cap` and `ground_truth_types` (primary-type histogram, JSON) on analysis.coverage_validation; existing rows are NULL (unknown), not false. Mapping fixes: beauty_salon moved from nails_beauty to hair_barber (it is NAICS 812112, hair's anchor); cafe_bakery gains donut/bagel/ice-cream/juice/dessert/tea (loci's own DOHMH keywords already include them, so Google was narrower than loci and biased toward "gap survives"); fitness gains yoga_studio and sports_club; restaurant gains the Table A cuisine family under primary filtering (39 types, under the 50 cap, pinned by test); bar drops bar_and_grill; clinic removed (no Table A type isolates 621493; GTM-107 is the only path). tailor_repair keeps `tailor` but is marked not usable for headline claims (Table A has no shoe_repair). Drift test added between GOOGLE_TYPES and the JS map (JS was missing preschool, medical_lab, fitness_center). `loci validate` skips unmapped categories with a notice. Ledger stands at 2,978/3,000 calls; a corrected 15-category re-run at 990 hexes is ~14,850 calls (~$315) and needs a fresh budget before GTM-48 can run. D29's fitness "21% true hole" is unreliable in both directions until then.

**D51 — Trust factor dropped from the grade; all of Brooklyn in scope; in-building laundry a supply input.** *(2026-09-08, owner)* → archive

**D52 — Supply-set principle adopted; stale licences ruled out; booth-renter dedup rule; alcohol is a map overlay, not a category; pipeline re-run approved.** *(2026-09-08, owner)*
Findings (data-engineer, session 13, MN+BK, ZBP 2023): the ACTIVE filter is a no-op — NYS DOS has 0 expired licences statewide (min expiry = today), NYS SLA 0 expired, and a DOHMH "not inspected in 24 months" rule removes 0.4% of canonical POIs; this closes D47(b): licence staleness does not explain the overcount for nails/hair. CORROBORATED (≥2 sources) brings restaurant 3.62→1.13×, cafe 3.73→1.20×, hardware 3.72→0.99×, tailor 3.88→1.12× ZBP, but collapses convenience to 0.16×, childcare 0.15×, clinic 0.10× because their anchors are not loaded — corroboration measures feed overlap, which is thickest in dense/rich areas, so applied globally it manufactures gaps. Dedup radius is not the cause (40→100 m changes counts <1%); the causes are aggregator-only rows (≈30% of canonical bar and fitness are Foursquare-only) and booth renters (996 DOS nails licences, 20%, at a shared ~11 m coordinate under different names).
Rulings: (a) the supply set is chosen per category by a principle, not a table: where a category has a registry anchor loaded whose own coverage is ≥0.7 of ZBP, a lone aggregator record (cluster whose only member is Overture or Foursquare) is not evidence of a business and is excluded; where no qualifying anchor is loaded, all canonical records stay and loading the anchor is the fix. The 0.7 threshold is a module constant with its reasoning in the docstring. Categories currently without a qualifying anchor: laundry (DCWP wired but empty), bank (FDIC planned), pharmacy (NYS planned), childcare (DOHMH planned), clinic, fitness, hardware, tailor, convenience. (b) Booth-renter rule: same source (NYS DOS), same category, ≤15 m, name-blind collapse; nails −12.5%, hair −0.9%; explains ≈1/8 of the nails overcount; caveat that it also collapses genuine stacked storefronts in multi-tenant retail. (c) Alcohol: the NYS SLA feed already covers every licence that authorizes selling alcohol; it becomes a map OVERLAY (on-premises / off-premises liquor / off-premises beer, toggle + legend, MN+BK), not a 16th category. (d) Pipeline re-run approved after (a) and (b) are in code: dedup → distances → address-gaps MN+BK → export-webmap, with supply_set and supply_hash recorded in provenance; before/after reported as description per category, never as a top-N list.
Why: two defensible supply sets differed 6× and rank-correlated at 0.19; the principle applies corroboration only where corroboration means something, and it turns each unanchored category into a concrete plumbing task rather than a judgment call.

**D53 — The Google coverage validator's radius is now the gap screen's 800 m NETWORK threshold divided by NYC's MEASURED circuity (1.233), i.e. 649 m straight-line; the radius is config, is derived in code, and is recorded on every row.** *(2026-09-08, QUESTIONS M8, GTM-105 finding D)*

Why: (Ids D53/D54 are allocated because the concurrent ENY session took D51/D52 the same day; the two entries below are this session's.) M8 offered two fixes for the D29 geometry artifact. Option (a) — reuse analysis.hex_poi_distance at validation time — was rejected as not implementable and actively harmful: Google Nearby Search accepts only a circular locationRestriction, so (a) could only change loci's side, comparing a network-shaped set against a disc of the same nominal size and guaranteeing a one-sided loci undercount. Re-measuring Google's returns on the walk network is the only coherent version and cannot reuse hex_poi_distance (which holds loci's own POIs only); it would mean routing to every returned place. Option (b) keeps BOTH sides a disc and chooses the disc that best approximates the network-800 set. Circuity was MEASURED on loci's own data rather than taken from the literature: read-only over analysis.hex_poi_distance (16.2M pairs) joined to hex centroids and staging.poi geometry, the 949,334 pairs whose network_m falls in the 700–900 m band give median network_m/straight_m = 1.2332 (IQR 1.122–1.348); 1.2341 restricted to populated hexes. By borough: Brooklyn 1.226, Manhattan 1.232, Queens 1.233, Bronx 1.248, Staten Island 1.279 — a single citywide constant is used because the four dense boroughs sit within 1%. Cross-check: median straight-line distance among pairs at network 780–820 m is 648.8 m = 800/1.2332 to the metre. The value is at the low end of the literature's 1.25–1.30 for gridded US cities, as a dense grid should be. 800/1.233 = 649 m. Radius sweep against the network-800 set (recall / precision): 600 m 0.825/0.928, 640 m 0.893/0.881, 660 m 0.919/0.853, 800 m 0.992/0.629 — 649 m is the balance point with ~12% symmetric error, while the pre-D53 800 m disc put 37% of what the validator counted beyond the reach it was validating, one-sided toward "loci looks under-covered." Second benefit (D50 finding D): the disc's area falls to 66% of the 800 m disc, reducing the 20-result cap censoring. Implementation: src/loci/reach_tiers.yaml gains a `validation:` block (network_threshold_m 800, circuity 1.233, derived_radius_m 649, measured_on, circuity_basis, provenance and the sweep); loci.reach.load_validation_geometry/validation_radius_m derive radius = round(threshold/circuity) and refuse a circuity below 1.0 or a pinned derived_radius_m that contradicts the derivation; validation/google_places.py, validation/sample.py, plan(), and webmap/server.js (which reads derived_radius_m from the same yaml) all resolve to that one value. analysis.coverage_validation gains a nullable radius_m column: NULL means a pre-D53 row measured at 800 m, and NULL and non-NULL rows must never be pooled. sample.recount_local re-measures each row at its own COALESCE(radius_m, 800) — recounting an 800 m row at 649 m would have manufactured an undercount. tests/test_validation_radius.py (10 tests) pins the derivation and fails on any yaml/code drift across all call sites including server.js. No Google calls spent; ledger still 2,978/3,000. Existing coverage_validation rows are 800 m rows and stay usable only against each other; the corrected re-run (D50, ~14,850 calls) will be the first at 649 m.

**D54 — ACS vehicle-ownership ingest lands (B08201 primary, B25044 tenure-split cross-check); renter_share finally gets its MOE. The D7 renter_share pre-test was negative.** *(2026-09-08, CHECKPOINT next action 10, QUESTIONS H-L7)*

Why: next action 10 required testing renter_share before the vehicle ingest. The test (read-only, MN+BK, hexes with population ≥ 100, n = 1,875): renter_share's partial Spearman with reach outcomes net of population density is 0.13 (POI/km²), 0.07 (present_count), −0.07 (median nearest m) and −0.02 (max_ratio, p = 0.37); it adds under 2 points of R² over log density; in Manhattan the sign flips (renter-heavy hexes walk slightly farther at fixed density). Renter_share proxies density (ρ = 0.62 citywide) but not mode (ρ = 0.09 within Manhattan), and its ACS MOE (17% of estimate; hex median 0.072) leaves 30% of MN+BK hexes indeterminate at a 0.70 cut. H-L7's owner-occupancy finding replicates citywide and dissolves under a density control. So the ingest is not optional. src/loci/grid/acs.py::build_acs now pulls B08201_001E/M and _002E/M → `zero_vehicle_hh_share[_moe]`; B25044_002/003/009/010 E/M → `zero_vehicle_owner_share[_moe]` / `zero_vehicle_renter_share[_moe]`; and B25003_001M/_003M → `renter_share_moe`. All cells verified against the 2023 ACS variables.json, plus a runtime cross-table check (B25044 owner+renter totals vs B25003 total; raises above 2% aggregate discrepancy). Propagation: extensive quantities apportioned tract→hex by PLUTO unit share exactly as population/households already were; MOEs use root-sum-of-squares for apportioned sums, then the ACS-handbook proportion-MOE formula (`_moe_proportion`, with the documented negative-subtraction fallback). Raw response cached at data/raw/acs/tracts_2023.json (one call per vintage). Schema: 7 new nullable columns via ALTER TABLE ADD COLUMN IF NOT EXISTS; registry.yaml mirrors the variables. Rebuild: 5,978 hexes, row count unchanged. Headline: citywide median zero_vehicle_hh_share 0.321 (MOE 27.8% of estimate), MN+BK median 0.585 (MOE 14.2%, n = 1,960). Within MN+BK, pop ≥ 100: Spearman(zero_vehicle_hh_share, pop density) = 0.519, Spearman(zero_vehicle_hh_share, renter_share) = 0.712 — it correlates MORE with renter_share than with density, so whether it is a genuinely distinct mode signal is next session's question; no threshold or class was built. Caveat the database cannot enforce: proportion MOEs on hexes with a near-zero apportioned housing-unit denominator can exceed 1 (one hex at households ≈ 1.8 shows renter_share_moe 5.3); any consumer sorting or filtering on these MOE columns must also gate on a minimum apportioned denominator. Tests: tests/test_acs.py 8 (6 new).

**D55 — Plan to add the missing daily-needs categories; anchor laundry first via DCWP; LL84 and listings become laundry inputs.** *(2026-09-08, owner)*
Owner: "lets plan to add in all of those missing categories. for now lets anchor laundry." Candidates (from the session-13 gap list): liquor & wine stores, dry cleaners (if not inside laundry), dentists & eye care, veterinarians & pet supply, shipping & mail, specialty food (butcher/fish/produce/non-cafe bakery), dollar & variety stores, florists/bookstores/phone repair, auto repair & gas (likely out of scope for a dense walking screen). A planning agent drafts per-category NAICS, anchor source, aggregator vocab, reach basis, demand class, ZBP mapping and risk, with an ordering and a drop list, and adds tickets under a new milestone in src/loci/tickets.py; nothing is pushed to Linear until the owner reviews the plan. Laundry is anchored first: the DCWP "Legally Operating Businesses" adapter exists but its ingest was empty; it is being fixed to emit laundromats into a pending table, merged into staging.poi after the D52 pipeline run finishes, then a laundry-scoped re-run. Laundry supply inputs now on disk: LL84/LL133 benchmarking laundry hookups (staging.ll84_laundry, analysis.address_laundry; 30,966 BBLs; clears 710 of 43,726 MN+BK laundry leads = 1.6%, because 92% of laundry leads are <6-unit buildings which LL84's 25,000 sq ft threshold never covers; field names resolved by display name because Socrata transposes the in-unit/common-area columns across vintages), and advertised laundry from StreetEasy listing pages via Tavily (registry `streeteasy_listings`, verified; pilot in progress). NYCHVS 2023 says 74% of 1-unit and 50% of 2-unit MN+BK dwellings have an in-unit washer: for small buildings in-building laundry can only ever be a rate, and it belongs in the grade as a demand haircut, not as supply.
Why: the laundry category's dominance is substantially small buildings being counted as laundromat demand; anchoring supply and adding the demand rate are the two logic fixes, and neither is a filter.

**D56 — `analysis.address_demographics` lands: address-grain ACS income, tenure and vehicle ownership, taken directly from each PLUTO lot's 2020 census tract via a BBL lookup — no spatial join, no apportionment. It replaces `analysis.hex_demographics` as the demographic carrier under D38; hex_demographics is frozen history.** *(2026-09-09, owner: "do it, no more hex work")*

Why: the owner's 2026-09-09 correction — "why do we keep talking about hexes?" — was that D49/D53/D54 had all landed on hex tables the address screen never reads, so none of it reached anything the owner looks at. The missing piece was an address-level demographic carrier. An address sits in exactly one tract, so the tract's ACS values and their MOEs are taken as-is: no apportionment, no interpolation, the least-modelled input possible. Tract assignment is PLUTO's own `bct2020` field on the lot (the same field `grid/acs.py` already trusted for the hex weights), so address → tract is a BBL lookup against the raw PLUTO extract, not a point-in-polygon join; no TIGER download. Columns: tract GEOID, median_hh_income + moe (B19013), renter_share + moe (B25003, ACS-handbook proportion MOE via `_moe_proportion`), zero_vehicle_hh_share + moe (B08201), zero_vehicle_owner_share / zero_vehicle_renter_share + moe (B25044), population and households (B01003/B11001) for reference, acs_year. Built from the existing raw ACS cache (no new Census call). Results: 767,337 rows, 767,331 with a tract (99.9992%; the 6 without get NULLs, never dropped); every address sharing a tract carries an identical income (0 tracts with more than one distinct value — proves lookup, not apportionment); 2,225 distinct tracts. Citywide median household income $91,389, MN+BK $83,083; median zero-vehicle share 31.2% citywide, 52.4% MN+BK; median income MOE 25.7% of estimate; 53.0% of addresses sit within one MOE of the D49 threshold ($102,315). Caveat the database cannot enforce: population/households are the tract's FULL count repeated on every address in it — summing them across addresses is an N-times overcount; rates are safe per address or as medians. Implementation: src/loci/model/address_demographics.py, DDL in 002_schema.sql, `loci address-demographics [--dry-run]`; tests/test_address_demographics.py 6 tests. Note: the concurrent session's migration 008 separately put a hex-interpolated median_hh_income onto address_gaps the same day; the annotation in D57 deliberately reads the tract-direct value here instead.

**D57 — GTM-110 done: the D49 demand annotation now exists ONLY at the address grain, as a sibling table that cannot filter. The hex_gaps annotation is frozen history.** *(2026-09-09, GTM-110)*

Why: D49 built the continuous, MOE-gated demand annotation on model/gaps.py's hex screen and left "not yet ported to addresses" as its own open item; D38 had already made the residential address the unit of analysis, so the annotation was living on the one geography the project no longer uses. The port is a NEW table, analysis.address_demand, not a column set on analysis.address_gaps — D48 says the output is graded, never filtered, and a sibling table keyed by address_id makes that mechanical rather than conventional: model/address_demand.py opens address_gaps read-only and has no write path to it, so nothing it computes can move gap_score, lead_category, n_missing or membership in the missing set. Verified on the real run: address_gaps' row count (767,337; 281,842 MN+BK) and hash-sum checksums of gap_score, lead_category, n_missing and eligible are byte-identical before and after the build. Grain: one row per (address_id, category, run key), the run key being (reach_hash, supply_hash) carried from the address_gaps run being annotated — the two inputs that decide what "missing" and "nearest" mean (D41 reach table, D52 supply set), so an annotation row detached from them is uninterpretable; the delete-then-insert is on the run key, not the borough. Rows are emitted for every category whose ratio > 1 (the continuous D39/D41 reading of "missing") plus the address's lead_category always; lead rows are the only ones that can carry ratio <= 1. Columns mirror D49 exactly: demand_class (derived from spend.yaml's BLS CEX elasticity at demand.yaml's 0.35 cut, never hand-coded), income_ratio, income_ratio_moe, income_indeterminate, demand_caveat, demand_caveat_text, acs_year, run_at. The caveat fires only on the MOE-confident test (income_ratio + income_ratio_moe < 0.80) AND a discretionary, annotatable category; unknown MOE means no assertion, and clinic can never be caveated (D30). The numerator is the address's TRACT median household income from analysis.address_demographics (D56), deliberately NOT the hex-interpolated median_hh_income that migration 008 added to address_gaps the same day. MN+BK run (reach_hash f6339bf9d842 tiers, supply_hash c4b847b82ffd principled): 627,439 rows over 281,842 addresses; 107,009 caveated (17.1%); 286,159 income-indeterminate (45.6% of rows — the same "more than a coin flip" fact that retired the binary badge in D49, reproduced at address grain); caveated rows by category bar 65,874, cafe_bakery 33,440, restaurant 6,955, fitness 740 (the entire caveat-eligible set); 48,888 of 267,114 eligible addresses (18.3%) have a caveated LEAD category. Rows with ratio > 1 among eligible addresses total 421,218, exactly sum(n_missing) in address_gaps — the missing set is reproduced, not reinterpreted. 3,740 rows have no income and 6,257 no MOE; all fail closed. Consequence: model/gaps.py and the hex_gaps/hex_gaps_reach demand columns are FROZEN HISTORY; gaps.py keeps its private _ratio_moe and _caveat_text rather than being rewired (D38: the hex path takes no edits); loci.demand.ratio_moe is the shared home of the formula and tests pin the two implementations and the two caveat wordings against each other. Renderers must not truncate demand_caveat_text — the X6 disclaimer is its tail. NOT yet rendered: viz/webmap_export.py does not read address_demand; the remaining step is one join on (address_id, reach_hash, supply_hash), filtered to is_lead. CLI: `loci address-demand [--borough MNBK|MN|BK|ALL] [--dry-run]`, default MN+BK per D48. Tests: tests/test_address_demand.py 12; suite 297 passing.

**D58 — The Google coverage validator's sampling frame is gap ADDRESSES, not hex centroids; the 2,970 existing coverage_validation rows are the frozen pre-D38 hex frame and must never be pooled with address rows.** *(2026-09-09, GTM-48)*

Why: D38 made the address the unit of analysis and froze the hex tables, but validation/sample.py still drew hex centroids stratified by income decile from analysis.hex_demographics — so GTM-48 would have spent ~$300 of Google budget validating coverage at points no lead refers to (a hex centroid can sit in a park, a rail yard or mid-block), against a screen that no longer speaks in hexes. draw_sample now draws rows of analysis.address_gaps, restricted to MN+BK (D48) and to eligible lots, stratified income decile × missing-this-category. The income decile is NTILE(10) over analysis.address_demographics.median_hh_income — the tract's own ACS median looked up directly on the lot's BBL (D56), no apportionment. The missing/present split is the point: measuring Google's enumeration only where loci says a category is missing cannot separate coverage bias from a real hole (D29 / CONTEXT 7.1 P3), so the present half of each stratum is the control. Both sides of every row are measured at the lot's own lon/lat at the D53 649 m radius, so the Google count and the local canonical count are like-for-like by construction. The sample is address-weighted by default (one lot, one draw); `--unit-weighted` draws proportional to units_capped (Efraimidis–Spirakis, without replacement) for reading the sample as a statement about households behind a gap. analysis.coverage_validation gains nullable address_id and borough; h3_index stays and is NULL on address rows. The hex-era PRIMARY KEY (h3_index, category) had to be removed — a DuckDB PK column is implicitly NOT NULL and there is no ALTER TABLE DROP CONSTRAINT, so the table physically could not hold an address row; sample.ensure_address_frame() migrates an existing database (rename → re-apply 002_schema.sql → copy rows back by column name; guarded, no-op once migrated, never fires on --dry-run) and run() maintains uniqueness itself by deleting the row's own key before inserting. The 2,970 rows written 2026-09-02/03 are the pre-D38 hex frame, all at the legacy 800 m disc (radius_m NULL): different unit, different geometry, different radius. They stay for the D29 hardware/fitness/clinic split and are usable only against each other — every statistic must filter on one frame (h3_index IS NOT NULL or address_id IS NOT NULL), never pool. Code only: no Google call made, ledger still 2,978/3,000. Plan on the current data (20/stratum, 14 mapped categories, clinic skipped): 5,185 addresses over 265 strata = 5,277 calls at 649 m. Two facts the plan surfaced that GTM-48's budget request must carry: grocery has ZERO missing addresses in the eligible MN+BK frame (200 control calls buying no gap observations — drop it or accept it as a pure control), and fitness/pharmacy/nails_beauty/hardware have income deciles with no missing address at all, so their missing arm is thinner than 10 strata. Also: plan()'s est_cost_usd treats the 5,000 free calls as untouched; 2,978 are already spent, so the true incremental cost of a 5,277-call run is nearer $170 than the printed $8.86 — budget off the ledger, not that number. CLI: `loci validate --per-stratum N --boroughs MN,BK [--unit-weighted] [--seed S] [--dry-run]`. Tests: tests/test_sample.py 16 (11 new); suite 297.

**D59 — Supply-set principle applied and re-run; laundry anchored via DCWP; alcohol is an overlay, not a category.** *(2026-09-09, owner)* → archive

**D60 — ACS age, race/ethnicity, education and household size added, and the tract-direct address table is the ONLY address-demographics source; the hex-copied columns are gone.** *(2026-09-09, owner, GTM-129)*
Owner asked for age, race/ethnicity, education and household size. grid/acs.py now pulls 118 E+M variables (every cell verified against the live 2023 variables.json; fetched in 3 chunks of ≤48 because the Census API caps a request at 50): median age (B01002); under-18, 18–34 and 65+ shares from B01001 summed across sexes, with 35–64 deliberately left as the residual so a fourth column cannot read as independent; white, Black and Asian non-Hispanic and Hispanic-any-race shares (B03002); bachelor's-or-higher share of 25+ (B15003); average household size (B25010) and one-person-household share (B11016). MOEs travel with every field: RSS for apportioned and multi-cell sums, the handbook proportion formula for shares, the incumbent weighted mean for the two intensive fields. A B01001-vs-B01003 per-tract population cross-check (>2% aggregate raises) joins the D54 B25044/B25003 one. Population-weighted hex figures against known NYC values: white NH 31.4% (~31), Black NH 20.7% (~22), Asian NH 14.6% (~15), Hispanic 28.5% (~29), college 39.7% (~40). Why the decision matters: these landed first on hex_demographics and were then COPIED onto address_gaps by containing hex — doubly modelled (tract→hex by unit share, hex→address by containment) and a step function at hex edges — while a parallel session built D56's tract-direct analysis.address_demographics the same day. Two tables answered one question. Ruling: the tract-direct table is canonical because a lot sits in exactly one tract and there is nothing to apportion; it was extended to the same 20 columns by IMPORTING acs.py's cell lists and helpers, the 36 hex-copied columns were stripped from the gap table, and a drift test fails if any hex demographic lacks an address twin. Unit-weighted citywide from the canonical table: white NH 35.0%, Black NH 19.1%, Asian NH 14.4%, Hispanic 26.7%, college 44.6%, median age 39.0, avg household 2.47 (units, not people, are the weight, so these differ from the hex figures by construction). Also caught: `loci address-demographics` had never been re-run after the extension landed, so every new column was NULL on all 767,337 rows until this session. Caveat the DB cannot enforce: share MOEs exceed 1 on a handful of near-empty denominators; gate on population before filtering on any *_moe. A catchment-weighted variant (demographics over the walk-shed rather than the lot's tract) is logged in QUESTIONS.md, not built.

**D61 — Warehouse consolidated 34 → 26 objects: address long/wide split with address_gaps as a generated VIEW, hex screen outputs dropped, prototypes and orphans removed, laundry evidence merged; zip_coverage_check deliberately kept as a table.** *(2026-09-09, owner, GTM-129)*
Owner, on seeing 34 objects: "we need to really watch for proliferation of tables." Inventory showed the sprawl in the address layer — wide per-category tables that were pivots of an existing long table, a 200-row prototype (address_convenience) beside the table that replaced it, two orphans nothing wrote (address_gaps_prev, main.p), four hex screen outputs frozen since D38 (hex_gaps, hex_gaps_reach, hex_dnci, empty hex_outcomes), and two laundry-evidence tables with two views. Six items approved; five done, one rejected with reasons. Done: (1) one address-demographics source (D60); (2) orphans dropped; (3) hex screen outputs dropped, their writers and CLI commands retired under D38, the foundation hex tables kept because the address path still reads them for transit, jobs and reach; (4) `loci conveniences` is a read-only query over the long table — no second Dijkstra pass, nothing persisted; (5) the principled shape: analysis.address (one row per address: identity, summary, provenance stamped ONCE) + analysis.address_category (767,337 × 15 = 11,510,055 rows: nearest_m, ratio, and the D57 demand-annotation columns, which address_demand.py now writes by UPDATE only, with a pinned test asserting its SET-list is disjoint from the screen's own columns) and analysis.address_gaps as a VIEW generated from categories.yaml carrying every old column including the six provenance ones, so webmap_export, sql/004–006 and the supply-set tests work unchanged; (6a) address_laundry_evidence(bbl, source) replaces the two laundry tables and two views with one of each. REJECTED (6b): turning zip_coverage_check into a view over zip_coverage_by_source would silently drop 12 of 2,034 rows — the poi_count=0 "Census sees establishments, Loci found none" rows, the table's strongest undercoverage signal — because the by-source table inner-joins onto canonical POIs; reasoning is in the 002_schema.sql header. The bug the work surfaced: address_gaps_view_sql() had been designed but never wired into db.init_schema(), so a fresh test DB crashed at 004 with "address_gaps does not exist" (58 failed / 20 errors, reported by a parallel session and reproduced exactly); fixed by creating the view immediately after 002, since DuckDB resolves a view's query at CREATE time. Full pipeline rebuilt for all five boroughs on the live DB after a backup; non-filtering proof exact on the rebuild (count{ratio>1, eligible} = sum(n_missing) = 1,389,900); supply_hash unchanged (e2b7ef55e607). Full suite 333 passed. Rule that now lives in code rather than table boundaries: analysis.address + address_category hold exactly ONE run; comparing supply sets means a parquet snapshot between runs, never two copies in the warehouse. Standing rule from the owner: before creating a table, run the inventory; pivots and subsets become views, new measures extend the existing grain, provenance is stamped once on the parent.

### 2026-09-16 — Session 20 (executive session on Fable): first operator answers, second El Punto site search, D16 flip correction (D117)
- The owner's friend answered on the El Punto page (WhatsApp, Turkish): rent $4,000/mo — a hit on the pre-registered $3.5k–$6.5k; food cost lower than Lion's Milk (page assumption reversed); evenings wine-and-dine; a breakfast-burrito + Cuban-coffee daytime plan. Sales/staff/orders still pending; he will read the other pages.
- Owner: "we aren't getting more feedback so we need to use what we have" → the second-site search ran on the rent datum and the concept profile; a fourth owner page "A second El Punto" (artifact 27517f73) joins Lion's Milk (77aa486a), El Punto (2f9162ae) and Graham Avenue (b176da82).
- The site-search agent found the D16 lat/lon flip in the two Graham memos' straight-line distances; both memos, both affected owner pages and the Stone Street report were corrected and republished the same day; D117 records the rule.
- 09-14/15: revenue v0.2 re-fit and re-applied on the final supply set (closure gate + cross-category dedup; supply_hash 467cd5969200; restaurant ε 0.6 → 0.4; commits 44fca62, e61c239), sequenced with sessions cf/29/88 (fit → their reports → applies → export); 379 Broome model revenue filled in ($1.41M, capacity-bound).
- Coordination: abenmayor-af holds D116 (CONTEXT v2 rewrite) and appends after this pass; abenmayor-cf/29/88 landed D94–D104 this week; GTM-153 (Lion's Milk double-count) closed via the cross-category dedup under the owner's "finer category wins" ruling. Nothing pushed to the remote.

### 2026-09-09 — Session 14 (concurrent with the ENY/query-app session): the address port after "no more hex work" (D56–D58), open tickets reframed to address level, ticket-coverage hook
- Owner: "why do we keep talking about hexes?????" then "do it, no more hex work." Everything this session had landed on 2026-09-08 (D49, D53, D54) sat on hex tables the address screen never reads. Ported in one pass: address_demographics (D56), address_demand for GTM-110 (D57), validator sampling frame on gap addresses (D58). Decision ids: the other session took D51/D52/D55 the same days; this session's are D53/D54/D56/D57/D58 (renumbered in code comments and docs after each collision).
- Owner: "rewrite existing Linear tickets to be address level." All 47 open tickets audited from tickets.py: 13 rewritten (GTM-48/49/50/56/58/71/72/90/104/106/107/110/111), 1 canceled (GTM-91, the dasymetric step it skipped no longer exists), 29 already address-level or geography-free, 4 left for the owner (GTM-60/61/62 are E5 deferred items of the rejected thesis; GTM-85 has no definition in tickets.py). Pushed to Linear 2026-09-09; GTM-110 marked Done.
- Owner: "make sure we have a hook moving forward AND retroactively create tickets and move them to DONE." `loci check-tickets` added (every decision-log entry from D29 on must be cited by a ticket definition or listed in tickets.py RULINGS; `--hook` emits Claude Code Stop-hook JSON); wired into `make check` and a repo-level Stop hook. Retroactive Done tickets generated from tickets.py for the uncovered decisions and pushed. (Details in the same-day commit and the check-tickets output.)
- Memory: feedback note saved — anything landing only on hex tables is not done.
- Nothing committed at the time of this entry; the other session's uncommitted work is interleaved in the same tree.

**D62 — Residential development pipeline built from DCP Housing Database + DOB certificates of occupancy: analysis.dev_pipeline at job grain, 12 pipeline-exposure columns on analysis.address by UPDATE, pipeline overlay on the webmap.** *(2026-09-09, owner)*
Owner asked (2026-09-09) "build the pipeline layer with DOB permits and CO dates", after the density-shock investigation (QUESTIONS T7) found Loci had no way to say which gap addresses are about to gain residents. Source decision: the DCP Housing Database project-level file is br6q-ssj3 (NOT hg8x-zxpr, which is HPD affordable production) — version 25Q4, data as of 2026-01-21, 109,076 jobs with unique job_number, 100% geocoded with BBL/BIN on MN+BK; completion is CO-derived by DCP; datepermit present on 454/457 MN+BK new-building jobs ≥10 units, so DOB NOW filings (w9ak-ipjd) and Permit Issuance (ipu4-2q9a) were NOT ingested. Both DOB CO feeds (bs8b-p36w BIS, pkdm-hqz6 DOB NOW) refresh daily and were joined on job_number to override DCP's stale status: 436 MN+BK jobs / 11,407 units are still "Permitted" in DCP but hold a CO issued after DCP's cutoff. Traps: pkdm-hqz6 CO date is text (MM/DD/YY h:mm:ss AM); bs8b-p36w contains a literal 2105-11-05. Inventory before building (D61 rule): 26 objects, none at job grain; ignition.py's 198k DOB filings were never persisted. So ONE new table, analysis.dev_pipeline (one row per DOB job: a genuinely new grain), 48,762 MN+BK jobs (BK complete 21,979 / permitted 5,124 / filed 5,222 / withdrawn 3,807; MN complete 6,923 / permitted 2,236 / filed 2,068 / withdrawn 1,399). Measures extend analysis.address by UPDATE only — units_permitted_400m, units_completed_24mo_400m, units_completed_60mo_400m, nearest_large_project_id/_units/_stage/_date, pipeline_asof etc. (12 columns), reusing score/access network-distance helpers; NOT address_category, because exposure is category-independent and would write 15 identical copies. Pinned test: PIPELINE_COLUMNS ∩ ADDRESS_SCREEN_COLUMNS = ∅ and ADDRESS_SCREEN_COLUMNS == address_gaps.ADDRESS_COLUMNS. Bug found and fixed: db.init_schema() builds the address_gaps view right after 002 and before 011, so on the live DB the view bound to columns that did not exist and every connection raised BinderException; the ALTERs now live at the tail of 002 (same class as the D61 bug). Live run after backup (data/loci.duckdb.bak.20260909-182444): 281,842 addresses annotated in 2m45s; non-filtering proof exact before and after (422,904). Of 177,078 eligible gap addresses, 9,591 have ≥100 units permitted within 400 m and 2,198 have ≥300; top 15 are FiDi–Battery Park City and Carroll Gardens–Gowanus, lead category laundry. Ground truth passes: Gowanus max permitted 2,102; Downtown BK–DUMBO max completed-60mo 5,099; Greenpoint 3,234; largest jobs name 111 Wall St (1,569u), 175 3rd St Gowanus (1,071u), 260 South St Two Bridges (666u), 25 Water St (1,320u complete 2025-02). Two honest misses: Two Bridges has only 5 gap addresses; Park Slope's Fourth Avenue build-out peaked 2016–21 so only its tail falls in the 60-month window. Webmap: "Development pipeline" overlay (net_units ≥ 50, stages permitted / partially complete / complete ≤60 mo; filed and withdrawn never drawn): 589 jobs (MN 210, BK 379; 108,528 homes), sized by band 50–99/100–299/300+, coloured by stage, hollow ring = temporary CO; gap popups carry the exposure; a min-units-permitted selector filters the gap layer; legend derives the as-of line from the data (max filed/permit date 2026-01-20). Data dir 42 → 50.6 MB; nearest project dictionary-encoded per file. Suite 333 → 364. CLI: loci ingest-dcp-housing, loci pipeline, loci export-webmap, all --dry-run. Caveats the DB cannot enforce: (1) DCP vintage lag — the forward pipeline is a FLOOR; (2) TCO ≠ finished building, co_type is the filter; (3) zombie permits: 1,348 jobs / 9,468 units (23% of permitted units) permitted >5 yr ago and never completed — units_permitted_400m is exposure, not a forecast; (4) withdrawn-after-permit kept in the table, excluded from measures; (5) negative net_units (demolitions, alterations) ingested, measures count only ≥1; (6) units_complete is gross, net_units net — never ratio them; (7) 24mo ⊂ 60mo, never add; (8) a job is not a masterplan (Greenpoint Landing, Pacific Park are several points); (9) no under_construction stage, deliberately; (10) `loci check-sources --urls` not yet run for br6q-ssj3. Also logged this session: the registry describes lodes_wac as annual 2002–2023 but analysis.hex_panel holds only 2002/2013/2023 — registry drift, fix the temporal line.


### 2026-09-05 — Session 10: D6 reach rule verified, then rejected on calibration
- Briefing found the checkpoint header stale: D6 was framed as next-session work but was already
  built in the working tree on 2026-09-04 (reach rule, reach table, CLI, schema, monotonicity test),
  uncommitted and undocumented. Header and next actions brought current.
- Sonnet verification run: 61 tests pass, 1 fails (tests/test_comps.py::test_cushion_negative_when_rent_exceeds_cash_flow
  — comps stream, not D6). reach-table reproduces reach.yaml exactly. `loci gaps --rule reach`
  writes 1,707 gap hexes; reach×0.8 gives a strict superset — monotonicity holds on production data.
- Contrarian review verdict: not publishable. p80 calibration fixes the gap rate per category by
  construction; list membership is a quantile artifact; lead rule inverts investment relevance; gap
  flag correlates −0.67 with local POI density. Recorded as D34; replacement calibration is D8.
- Three other uncommitted work streams from parallel sessions surfaced, none in CHECKPOINT or
  QUESTIONS until now: (a) model/comps.py + benchmarks.yaml + tests/test_comps.py — an O7/O8
  comps model with ZERO real listings (BizBuySell/BizQuest/LoopNet all return HTTP 403;
  data/benchmarks CSV is a header-only stub) and one failing test; (b) model/conveniences.py +
  conveniences.yaml + sources/cities/nyc/addresses.py — an address-level owner-set-norm
  "convenience" check that cites a "CHECKPOINT D-conveniences" entry which does not exist;
  (c) spend.yaml — fair-value engine parameters (BLS CEX national fallback) referencing an
  `analysis.site_fairvalue` model "another session is building". Owner directed mid-session that all
  four morning streams (reach, comps, spend, conveniences) be picked back up; O9 is therefore
  answered 'keep' for conveniences and spend, and comps stays open on data access.
- Stray file src/loci/__write_probe__.txt (content: a probe timestamp) left by a write-permission
  test; not deleted, flagged for the owner.
- D8 calibration comparison run by the data-scientist agent (D35): spacing and p80 rejected,
  external tiers + max-ratio lead recommended, owner decision pending on the tier source.
- Data-engineer check on the 0 m spacing (D36): 86% snapping artifact; residual duplication is
  DOHMH closed-establishment turnover; MATCH_METERS 40 m vs docstring 25 m drift.
- BLS unblocked: bls.gov returns 403 to curl from this network (edge block, robots.txt included)
  but WebFetch on a different egress retrieved the real workbooks — CEX Table 3004 (NY-Newark-Jersey
  City MSA, 2023–24) and Table 1101 (national quintiles, 2024), verified OOXML, filed under
  data/raw/bls/ (gitignored). spend.yaml rewritten: annual_spend = NY MSA figure for grocery 7,033,
  restaurant 3,509, cafe_bakery 702, bar 556, hair_barber 407, nails_beauty 291 (national kept as
  annual_spend_national, ratio 1.13–1.32); pharmacy 658 and fitness 935 updated to real national;
  quintile breaks corrected to [29,932, 57,452, 94,511, 155,925] (old values were 20–27% too low)
  and index to [0.160, 0.412, 0.715, 1.166, 2.538]; laundry/tailor, hardware, childcare/clinic/
  bank carry mapping_notes (no clean CEX line). No code reads spend.yaml yet; the fair-value spec
  and `analysis.site_fairvalue` model it cites exist nowhere in the tree or any branch.
- Comps still blocked: BizQuest (BizBuySell's sister site) index pages ARE reachable via Tavily and
  its robots.txt allows `User-agent: *` on listing pages with Crawl-delay 2, but every listing detail
  page (where financials live) returns 403 to both fetchers; 8 fetches total, 0 rows, nothing
  fabricated. Options for the owner: the manual export in benchmarks.yaml, or a human-paced session
  in the owner's own browser (not attempted without explicit go-ahead).
- Conveniences wired by the developer agent: `loci conveniences --borough --limit --dry-run` (cli.py),
  summarize/convenience_summary helpers in model/conveniences.py, tests/test_conveniences.py (5
  tests incl. monotonicity). Suite: 66 pass, 1 known fail (test_comps). Manhattan 300-address dry
  run: 87% of addresses (unit-weighted) satisfy all 15 norms; every category ≥94%. Caveat: the check
  is near-saturated in Manhattan by construction; its information is in the outer boroughs.
- Also fixed: build_gaps return annotation. Stray src/loci/__write_probe__.txt still present, still
  flagged.
- Duplicate decision IDs in this log (two entries each for D13, D16, D22, D28) noted; not renumbered.
- Nothing committed; the working tree now holds the four streams plus this session's verification
  work — the owner decides what to commit.
- Owner clarified late in the session: NO more hexes — the address is the unit of analysis (D38).
  address_gaps.py (committed 2026-09-02) is the geography of record; today's hex-level D34/D35
  numbers are method evidence, not results. An address-level battery run was dispatched.
- Address-level battery run (D39): naive port breaks monotonicity (fixed gate required); external
  tiers still the only informative calibration but tier-driven; exactly-one list unstable at any
  geography (Jaccard 0.41–0.58) → rank continuously; mega-lot clustering artifact.
- Tier-source research done: only 4/15 categories have a citable walk threshold; literature ~25% wider than the owner's norms; Walk Score argues for continuous decay. Proposed reach_tiers.yaml + docs/reach_sources.md filed, NOT adopted.
- Owner adopted the recommended tiers and continuous ranking (D41); developer agent dispatched to
  rewrite address_gaps.py with the fixed gate, reach_tiers.yaml, ratio ranking, capped-unit
  clustering, CLI and tests.
- Owner asked what the free market dictates in complete Manhattan areas; answered (D43, docs/market_reach_manhattan.md): every category's p80 is 14–59% of the adopted reach; density-class reach (D7) is the follow-up.
- Address screen ported and run citywide (D44): 767k addresses scored into analysis.address_gaps; tailor/laundry dominate leads; lead-viability rule is next. 93/94 tests.
- Comps collection (owner-approved browser session, human pace, BizQuest): 59 listings across 14
  categories (restaurant 8, laundry 6, cafe/grocery/convenience/pharmacy 5 each, bar/hair/fitness/
  childcare 4, nails/hardware 3, clinic 2, tailor 1, bank 0 — branches are not sold there). Fill rates:
  gross_revenue 70%, rent 59%, sqft 58%, cash_flow_sde 14% (hidden behind sign-in on most listings).
  Two agents stalled on browser tool calls; the second run added 10 rows. Log:
  data/benchmarks/collection_log_2026-09-05.md. Only restaurant meets benchmarks.yaml's >=8-row bar;
  the cash-flow-based rent tiers in comps.py will mostly fall through to the occupancy-ratio fallback.

### 2026-09-09 — Session 13 (cont., ENY/query-app session): D52 re-run, DCWP laundry anchor, alcohol overlay, E10 plan, listings sweep
- Supply-set principle (PRINCIPLED) applied and re-run on MN+BK: anchor coverage qualifies nails_beauty 3.04, bar 1.85, restaurant 1.46, convenience 1.43, cafe_bakery 1.23; 36% aggregate dedup (5 anchored categories); lead_category shift 23% of eligible addresses (D59).
- Laundry anchor: DCWP Inspections (jzhd-m6uv) emits 1,561 active MN+BK retail laundries; anchor_coverage 1.06 → QUALIFIES; 546 new (existing 1,015 within 40 m); DCWP merge queued after webmap fix.
- Alcohol overlay (D52c) live: 16,126 active SLA licences in MN+BK (on_premises 10,757 / off_premises variants 4,352 / unclassified 617), toggleable map layer; no touch to gap/POI layers.
- Category expansion E10 milestone (GTM-112…117): dentist (NPPES verified), specialty_food (NYS Ag&Markets), eye_care (NPPES partial), dollar_variety, vet_pet (no anchor), mechanics (Urgent); liquor stays map overlay, dropped from E10; tickets pushed to Linear 2026-09-09.
- StreetEasy sweep approved at full scope: Bay Ridge pilot 222 listings (52 BBLs, 41 laundry leads annotated, 2.2% of pilot); owner approved FULL sweep all MN+BK laundry leads (~34k calls / $294), ≥6-unit addresses first, paced, parquet sink.
- Webmap POI layer fix in progress: poi_dedup.is_canonical → analysis.poi_supply (supply-set aware), excluded records drawn distinctly; validator sampling frame (D58) gap ADDRESSES stratified income×category.
- Suite 250+ passing; concurrent-session API limit deaths on 09-08 evening; resumed 2026-09-09 morning.
- 13:19 DCWP laundries merged + dedup + score + address-gaps MN/BK + export-webmap (supply_hash e2b7ef55e607); laundry anchored 1.42×; gap share 26.8→27.4% with 18.3k cleared / 19.8k new (see D59 post-merge note).
- Exclusive files committed as 4b5a9e2 (70 files); shared docs committed by the concurrent session as d540988/5bb63a3; cli.py, registry.yaml, CONTEXT.md, Makefile left to session abenmayor-cb, which is mid-refactor turning analysis.address_gaps into a view (58 tests red on fresh DBs until it lands; live table intact).
- E10 tickets pushed: GTM-112…117; D59 covered by GTM-128 (pushed by the concurrent session). StreetEasy sweep relaunched detached at ~13:40 (data/interim/listings_sweep/2026-09-09-mnbk).
- All Opus/Sonnet/Haiku agents hit the account session limit twice (18:20 on 09-08, 17:30 on 09-09); final steps were run directly.
- Webmap: **All opportunities** mode (top of the business list) — pick a neighborhood and every gap address in it draws colored by lead_category, legend carries per-category gap counts for that NTA, popup lists every missing category with its ratio; per-NTA files (107, 15 MB, largest Canarsie 694 KB) + index, same supply_set/hash provenance; NTA is chosen, never inferred from the viewport. Canarsie: 9,939 gap addresses, bar 9,201 / cafe 5,703 / laundry 5,166 / convenience 2,955 / bank 2,831. 32 webmap tests green.
- StreetEasy sweep: crashes every ~80 addresses on macOS OSError 49 (connection abort), not Tavily/StreetEasy (a hand extract renders with the amenities block). Supervisor `data/interim/listings_sweep/2026-09-09-mnbk/supervise.sh` relaunches launch.sh on exit; resume skips BBLs already in part files (listings.fetched_bbls). Yield at 75 addresses: 42 with unit pages, 106 listings with amenities, 132 credits (pilot rate). ~50 credits wasted on one premature manual stop.
- **Owner ruling 17:58: StreetEasy sweep capped at the ≥6-unit stratum, then merge.** Rationale: ~30k of the 33k laundry leads are <6-unit lots where listings almost never exist (pilot) and NYCHVS already gives the in-unit washer rate; the tail would cost ~9 days and ~33k credits for near-zero yield. Relaunched with `--min-units 6`: 2,829 targets, 2,685 remaining; supervise.sh now runs `loci ingest-listings --merge` on completion (merge.log). Observed cost so far ≈1.6 credits/address; ~650 credits spent before the cap incl. aborted starts. Caveat: the resume key is BBLs present in part files, so addresses that yielded zero listings are re-searched after a relaunch (~1 credit each).
- 2026-09-10 overnight: sweep reached 1,170 of 2,829 ≥6-unit addresses (~1,900 credits total), then five consecutive attempts stalled early — each died on a Tavily-side connection reset after ~5 addresses and lingered 1–2 h (sampled: blocked in an SSL socket poll on an open connection). Fixes: (1) listings.py `_post` timeout 240→90 s and a network error is now a recorded failed call that backs off instead of crashing the run (commit 72a5f8d); (2) supervise.sh v2 kills an attempt with no progress for 15 min; (3) sweep PAUSED at 09:12 and scheduled to resume at 16:30 with `--pace 5 --cooldown 120` (resume.pid) — flushed runs show extract failure 17–26%, the stalled attempts never flushed a part so their rate is unknown. Remaining: 1,659 addresses. Merge still automatic on completion (merge.log).
- **2026-09-11 14:48: StreetEasy sweep of the ≥6-unit stratum COMPLETE and merged** (all 2,828 targets; 4,813 credits logged across every attempt ≈ $38 at $0.008; paused 04:07–10:26 after four overnight hangs; the 09:00 auto-resume did not fire and was started by hand). Landed: staging.listings 3,724 listings over 1,210 BBLs (90 active / 3,634 past; 1,812 in-building, 1,087 in-unit, 6 explicit none, 1,310 silent); analysis.address_laundry_evidence source='listing' rows for 1,210 BBLs beside 30,966 'll84' rows. Coverage: 1,192 of 2,835 eligible MN+BK ≥6-unit laundry leads (42%) now carry advertised-laundry evidence. The <6-unit strata were deliberately NOT swept (owner ruling 2026-09-09: a rate from NYCHVS, never a per-address fact). Ops lessons for any future sweep: Tavily trickles at night (≈25 s/call vs 8 s by day) and ~hourly hangs slip past the read timeout — add a hard per-call deadline; the fetch log flushes per part, so a hang before the first part re-spends up to 24 searches per relaunch. Next use: the laundry demand haircut in the grade (LL84 + listings + NYCHVS rates).


### 2026-09-10 — Session 17: Childcare age-fit tested and refused by its own gate (D64); age_fit is now a per-category registry (395 passed 1 skipped)
- Owner extended age_fit to childcare per QUESTIONS D15 reasoning that supply-revealed placebo independently reproduces CEX note's two passing categories from a different source; registry refactor complete.
- Childcare fails F2 gate in Brooklyn at 0.767 [0.545, 1.078], count spec significantly wrong-signed at 0.542 [0.396, 0.741]; F2 now tests sign as well as interval.
- No DOHMH childcare anchor loaded; outcome built from Overture+Foursquare supply the screen reads; aggregator under-coverage cannot be ruled out.
- Under-5 share enters at +0.783; should become a warehouse column in next ACS pass.
- Bar unchanged; all 281,839 bar rows bit-identical (digest 5bec24a6ed2c8d14); pharmacy remains untested.
- Built: age_fit.py registry pattern, per-category stale hashes/fit files, loci age-fit fit|apply --category {bar,childcare,all} with scoped reset.
- Suite 384 → 396 (one deliberate skip: childcare ordering test skips because no curve exists).
- GTM-134 ticket generated and pushed to Linear.

### 2026-09-10 — Session 16: Bar age-fit shipped as supply-revealed ranking signal (D63); subway ridership fixed; hex_panel audit (GTM-131)
- Owner asked for age to inform the score; CEX survey route analysed and rejected (docs/age_demand_fit.md); NYC market-revealed curve analysed (docs/bar_age_nyc.md); age_fit_bar built and applied (D63, GTM-131).
- Subway ridership loader written; hex_panel found to be CNS07+CNS18 only, CNS18 must be excluded from bar controls.
- Coordinated with two parallel sessions (D62 commit 6265a0b, GTM-130).
- Suite 364 → 384 tests.


### 2026-09-09 — Session 14: ACS demographic extension (D60), warehouse consolidation (D61), GTM-129 pushed, 333 tests
- ACS demographic extension (D60): age, race/ethnicity, education, household size pulled into grid/acs.py (118 E+M variables, 3 chunks of ≤48 due to Census API cap); median age, three age cohorts, four race/ethnicity combinations, bachelor's-or-higher, average household size, one-person-household share with full MOE propagation (RSS, handbook proportion formula, weighted means). Population-weighted hex figures validated against known NYC values; tract-direct analysis.address_demographics table built as the canonical carrier (tract GEOID lookup from PLUTO BBL, no apportionment); hex_demographics frozen; drift test enforces parity between hex and address tables; existing NULL columns in address-level work caught and fixed after the extension landed.
- Warehouse consolidation (D61): 34 objects → 26 through elimination of wide per-category pivots, orphans, hex screen outputs (D38 retired them), prototypes, and laundry-evidence redundancy; address_gaps becomes a generated view over analysis.address + address_category long tables (D57, 767,337 × 15 = 11.5M rows with demand annotation by UPDATE only); zip_coverage_check kept as table (poi_count=0 rows are signal, not noise); address_gaps_view_sql() bug fixed (never wired into db.init_schema); full rebuild on live DB; supply_hash unchanged; full suite 333 passed.
- Opus agent killed mid-item-3 by account rate limit (17:30); resumed on Sonnet.
- Three parallel sessions coordinated via cross-session message notifications; all reached the messaging limit twice (18:20 09-08, 17:30 09-09).
- GTM-129 ticket (D60/D61 work) pushed to Linear; retroactive Done tickets for all remaining decisions generated and pushed.

**D63 — Age informs the RANKING of bar gaps through a market-revealed New York curve, not the CEX survey: `age_fit_bar` from the SLA composition specification, non-filtering, with its own F2 gate enforced in code.** *(2026-09-10, owner, GTM-131)*
Owner, 2026-09-10, restating the request `docs/age_demand_fit.md` had refused: "a bar gap in the Upper East Side where avg age is 60+ vs a bar gap in the East Village where avg age is 25 — the East Village bar should score higher." The BLS CEX route stays rejected on its own numbers: alcohol spend is $643 per household nationally and $851 in the NY metro, but those are means over households *including non-drinkers*, alcohol is among the most under-reported lines in the survey, there is no age-by-metro table so the metro level and the age shape cannot be crossed, and the household shape peaks at reference-person 45–54 — the budget of a household, not the behaviour of a drinker; the owner's own gloss, "I spend $100 for 1 date", is exactly the distance between the two, and the resulting multiplier had a p10–p90 spread of 0.013 against a median MOE of 0.140 (dispersion 0.10) and scored the UES *above* the East Village. The replacement is measured on this warehouse: the median East Village address has 197 on-premises SLA licences, 55 bar-type licences and 87 canonical bar POIs within 400 m against Carnegie Hill's 49, 11 and 14 — 4.0×, 5.0× and 6.2× — so the market has already answered the question at 6× on the category-specific measures. Of the specifications `docs/bar_age_nyc.md` tested, exactly one was shipped: the COMPOSITION spec, outcome log(1+bar-type) − log(1+on-premises) within 400 m of the tract's residential centroid, on adult 18–34 and 65+ shares plus log units, log(1+CNS07 retail jobs), log walk-to-subway, log median income, renter share and borough FE, Conley 2 km spatial-HAC SEs over 1,065 MN+BK tracts; it reproduces at b(w18) = +0.404 (Conley se 0.203, t +1.99) and b(w65) = −0.484 (t −1.74), Carnegie Hill → East Village 1.298 [1.123, 1.500]. It was chosen because it is the ONLY specification whose two age coefficients both carry the owner's sign — every count specification has w65 *positive*, because the UES is old, rich and dense — and the only one significant in Brooklyn (b18 +0.596, b65 −0.662, t +2.44, ratio 1.451 [1.196, 1.761]), where 98.2% of the bar-lead gap set actually lives; the bar-POI count's Brooklyn CI grazes 1 at [1.011, 2.178] and Manhattan's composition effect is null at 1.124 [0.950, 1.331], so pooling alone would have calibrated where the signal is and shipped where it isn't. That criterion (F2) is now enforced in code rather than documented: `loci age-fit fit` recomputes the Brooklyn-only Conley CI and the dispersion gate and exits non-zero writing nothing if the CI includes 1.0 or the spread fails to clear its own median MOE — currently 3.61× (spread 0.200 against 0.055), a 36× improvement on the test that retired the predecessor. The caveat ships with the column and must render untruncated: it is SUPPLY-REVEALED (fitted on where bars already are, so demand and residential sorting are not separated), a low value is never evidence a neighbourhood does not want the service, and resident age is a proxy for a bundle — spatial-block CV says age adds +3.2–3.6% out of sample over a density-only model in Brooklyn but ≈0% once income, renter share, walk-to-subway and retail jobs are measured directly, and it transfers *negatively* (−1.1 to −1.7%) from Brooklyn to Manhattan. Stated honestly, the effect on the output: over the 67,223 bar-lead gap addresses in 82 clusters the multiplier runs p10/p50/p90 = 0.899/0.962/1.038, cluster Spearman 0.955 and address Spearman 0.983, and the top-50 cluster list changes by four clusters (Jaccard 0.923, two in and two out, max rank shift 20) — enough to answer the owner's question, little enough that gap_score still dominates, spread 1.66 against 0.20. Two data findings logged on the way: `analysis.hex_controls.subway_riders_2024` was NULL on all 8,321 rows for the plain reason that nothing ever wrote it — the column landed ahead of its loader and the registry entry was still `planned` — now fixed by `grid/mta.build_subway_ridership` chunking the ~110M-row hourly feed by calendar month because a whole-year server-side aggregation times out (8,321 hexes, 424 complexes; Staten Island correctly NULL, its nearest station being SIR); and `analysis.hex_panel` is not total LODES employment but exactly two NAICS sectors, CNS07 retail (303,835 jobs, 2023) and CNS18 accommodation-and-food-services (332,020), of which CNS18 must NEVER be a control for bars because food-service payroll *is* the outcome in employee form. Built: `age_fit`/`age_fit_moe`/`age_fit_source` on `analysis.address_category` (bar rows only; NULL, never 1.0, elsewhere — "no curve" is not "a neutral curve") and `age_fit_lead`/`age_fit_lead_moe`/`gap_score_fit` on `analysis.address`, all by UPDATE with both SET lists asserted disjoint from the screen's own columns before either statement runs; the generated `address_gaps` view exposes the three address-grain columns, appended; `loci age-fit fit` and `loci age-fit apply`, both with `--dry-run`; 20 tests in `tests/test_age_fit.py` (suite 364 → 384) covering the non-filtering proof (re-verified on the live DB: Σgap_score 938,319.24, Σn_missing 1,389,900, ratio>1 count 3,044,584 all unchanged), the disjointness pin, the NULL-vs-1.0 pin, the F2/F3 refusal-to-write and its non-zero exit code, and East Village 1.117 ± 0.090 > Carnegie Hill 0.834 ± 0.083 on the live curve.

**D64 — `age_fit` extends to a REGISTRY of per-category curves, and childcare's curve is refused by the same gate that admitted bar: the composition spec fails F2 in Brooklyn at 0.767 [0.545, 1.078], so childcare does NOT ship.** *(2026-09-10, owner, GTM-134)*
Owner extended age_fit to childcare per QUESTIONS D15, on the reasoning that the supply-revealed placebo independently reproduced the CEX note's two passing categories with the same signs from a different source — the strongest available evidence that b(w18) is category-specific rather than generic urbanity — with the F2/F3 gate applied without relaxation, because two sources agreeing on a sign is corroboration and not a licence to skip the test. The spec mirrors bar deliberately: composition rather than count, `log(1 + canonical childcare POIs within 640 m) − log(1 + ALL canonical POIs within 640 m)` off the tract's residential centroid at the category's own reach tier, so that "this is a dense commercial strip" cancels out of numerator and denominator mechanically the way on-premises licences cancelled it for bar, with `under_18_share` as the primary demand regressor (children, not the age of adults) and bar's two adult shares retained so the placebo b(w18) stays comparable; controls, Conley 2 km SEs and the 1,065-tract MN+BK sample are identical to bar's. It failed. On 1,065 tracts (R² 0.551) b(under_18_share) is −0.205 pooled (Conley se 0.388, t −0.53) and −0.769 in Brooklyn (se 0.393, t −1.96), giving a Brooklyn contrast of 0.767 [0.545, 1.078] on East Village → Borough Park — the interval includes 1.0 and the point estimate is on the wrong side of it — while Manhattan runs the opposite way at +2.199 (t +2.98, ratio 2.586 [1.505, 4.446]), so the two boroughs are on opposite mechanisms even more sharply than bar; F3 passed comfortably at 1.49× (spread 0.117 against median MOE 0.078), which makes F2 the sole binding failure, exactly as the note predicted it would be. The contrast endpoints were derived from the panel rather than chosen — 79 MN+BK NTAs carry ≥500 addresses, and the extremes on unit-weighted under-18 share are East Village 0.069 (1,927 addresses) and Borough Park 0.464 (7,434) — and the count specification, fitted as a reported robustness check, is significantly wrong-signed at −1.862 in Brooklyn (t −4.52, ratio 0.542 [0.396, 0.741]), a CI that excludes 1.0 by rejecting the hypothesis rather than confirming it; F2 now tests the SIGN as well as the interval so that trap is closed in code, and nothing was switched. Two readings of the negative sign both forbid shipping: Borough Park's median address sits 375 m from the nearest canonical childcare against East Village's 205 m despite nine times the child share, which is either aggregator under-coverage of Hasidic home-based and yeshiva daycare — M1 wearing a costume, and childcare has NO registry anchor at all (`category_anchor` coverage 0.000; all 4,302 POIs are Overture plus Foursquare, so `in_principled` degrades to `in_all` and the outcome is built from the same supply the screen reads) — or a real desert, in which case the multiplier would rank the neighbourhoods with the most children DOWN, the D49/X6 hazard verbatim. Even had it passed it would have moved nothing: the 10,222 childcare-lead gap addresses sit in only 11 clusters, the multiplier runs p10/p50/p90 = 0.920/0.989/1.062 against a median MOE of 0.086, and cluster Spearman is 1.000. The under-5 robustness regressor, computed at tract level from B01001_003E + _027E in the raw ACS cache, enters at +0.783 and drives under_18_share more negative (Brooklyn −0.975, ratio 0.708 [0.513, 0.978]) — under-5s raise childcare composition, 5–17s lower it, net under-18 negative — so `under_5_share` and its MOE should be added to `analysis.address_demographics` in a future ACS pass, deliberately not in this one. Bar is untouched and proved so twice: the refit after the registry refactor reproduces every committed coefficient and the inputs hash `5b1374406fea` exactly, and applying both curves leaves all 281,839 bar rows bit-identical (digest `5bec24a6ed2c8d14`), with Σgap_score 938,319.24, Σn_missing 1,389,900 and 3,044,584 ratio>1 pairs unchanged; pharmacy remains open. Built: `CURVES`/`CategorySpec`/`ContrastSpec` in `model/age_fit.py` with radii read from `reach_tiers.yaml`, a k-term multiplier and delta-method MOE that raises rather than defaulting when a regressor has no ACS margin, per-category stale hashes and fit files, `loci age-fit fit|apply --category {bar,childcare,all}` with a reset pass scoped to what was requested so a gate-failed category is NULLed rather than left stale, and 12 new tests (suite 384 → 396, one deliberate skip: the childcare ordering test skips because no curve exists, so a future re-fit cannot ship without clearing it). Prerequisite to re-test childcare: load DOHMH child-care-centre inspections (dsg6-ifza) as the category's anchor.

**D65 — The childcare anchor lands and overturns D64: Borough Park was aggregator under-coverage, not a desert; the re-fitted childcare gate passes; under_5_share added; two rulings pending (D52 floor-anchor veto, screen re-run).** *(2026-09-10, GTM-135)*

The dataset D64's next action named, `dsg6-ifza`, turned out to be titled "(Historical)" with a portal note saying it reflects data as of 2019-05-14, a max inspection date of 2023-04-24 and no coordinates at all — the D55 trap again — so the source was repointed at the live DOHMH roster `gy3q-4tzp` ("Active NYC Health Code Regulated Child Care Programs", rowsUpdatedAt 2026-09-09): 2,755 programs, `dcid` 1:1 with rows, 99.56% geocoded by the publisher, ACTIVE applied at source because preliminary, suspended and closed programs are excluded from the file, so no D36/D47 staleness proxy is needed or even possible. It covers group settings only — Article 47 GCC and Article 43 school-based, the latter kept deliberately because it carries the yeshiva and parochial pre-K — while home-based Family and Group Family Day Care is licensed by NYS OCFS and appears in no NYC feed, which makes this anchor a FLOOR on childcare supply and gives the asymmetry that matters: a gap it closes was under-coverage, a gap that survives it is still not proof of a desert. Canonical childcare went 4,302 → 6,066 (1,035 of the 2,743 anchor centres sat within 40 m of an existing POI, 1,708 were new) and `anchor_coverage` went 0.000 → 0.85 vs ZBP, so childcare now qualifies; a required fix on the way was `score/dedup.py::source_rank`, which had no childcare branch, so the registry source had fallen to the tail rank behind Overture and an aggregator's geometry would have stayed canonical for every anchored cluster. Borough Park's median address moved from 298 m to 104 m from the nearest childcare with 3 → 31 sites inside 640 m, against East Village's 144 m → 140 m and 14 → 18: the 375 m D64 reported was the aggregators missing 151 Borough Park programmes across 97 buildings, and the re-fitted gate now PASSES with b(under_18_share) flipping from −0.769 to +3.299 in Brooklyn (pooled +3.753, Conley t +7.23), a contrast of 3.556 [2.416, 5.235] and F3 dispersion 3.33× — Manhattan and Brooklyn now agree instead of running opposite. The childcare curve is fitted but NOT applied, pending the owner. `under_5_share` (+MOE) was added to both demographic tables from cells already in GETVARS, so no ACS refresh was needed; Borough Park is 0.1438 against East Village 0.0253 on a citywide unit-weighted 0.0570, it enters the childcare fit at BK ratio 2.832 as a reported robustness check, and it is a strict subset of under_18_share, so the four age shares do not partition and must never be summed (test pinned). Two things need a ruling before the screen is re-run: because childcare now qualifies, D52's veto switched on and `in_principled` fell 4,302 → 2,657, which deletes lone-aggregator records with no registry able to corroborate them in exactly the neighbourhoods where OCFS home-based care dominates — `score/supply.py`'s own rule says a category landing in 0.5–0.9 (childcare is at 0.85, the first ever in that band) is the signal to load a better anchor, not to move the threshold, but no better anchor exists in any feed, so the proposal is a per-category `anchor_is_floor` flag under which lone-aggregator records are retained; and address-gaps / address-demand were deliberately NOT re-run (they would clear the age_fit columns), so the screen still reads the pre-anchor supply — a re-run would cut Brooklyn eligible addresses beyond 640 m of childcare from 2,237 to 1,605 (−28%). Suite 443 passed / 1 deliberate skip.

**D66 — Pharmacy clears the F2/F3 gate on the 18–34 coefficient rather than its own primary regressor; applied under the gate as written, with a ruling requested to tighten F2 to the primary regressor; age-fit provenance re-keyed to the live supply hash.** *(2026-09-10, owner, GTM-136)*

Tested under the D64 gate with no relaxation: the same composition shape at pharmacy's own 800 m reach tier, outcome log(1 + canonical pharmacy POIs within 800 m) − log(1 + ALL canonical POIs), `age_65_plus_share` as the primary demand regressor with `age_18_34_share` beside it — both raw bands, because a renormalized twin of the same variable in one spec is a collinearity trap and a test now refuses it — on bar's identical controls, 1,065 MN+BK tracts and Conley 2 km SEs. It clears F2 in Brooklyn at 1.585 [1.236, 2.032] on a contrast the panel derived (79 NTAs with ≥500 addresses; Williamsburg 0.059 unit-weighted 65+ → UES–Carnegie Hill 0.295) and clears F3 at 3.01× (spread 0.264 against median MOE 0.088), R² 0.436 — but b(age_65_plus_share) is −0.300 pooled (Conley se 0.319, t −0.94), +0.264 in Brooklyn (t +0.90) and −0.424 in Manhattan (t −1.40), insignificant everywhere and wrong-signed in two of three, while b(age_18_34_share) is −1.128 pooled (t −3.71) and −1.234 in Brooklyn (t −3.02), so roughly five-sixths of the Brooklyn log-contrast is "fewer 18–34s" and one-sixth is "more 65+". That is a real, category-specific finding — pharmacy is the exact mirror of bar on the same tracts and controls, the §5c placebo ordering reproduced inside the shipped registry — but it is NOT evidence that pharmacy demand rises with age, and a column that passes on a variable other than its stated demand driver is the GTM-109 defect: a number wearing a label it did not earn. The count spec agrees rather than conflicting (Brooklyn 1.687 [1.329, 2.142]), so unlike childcare nothing was switched; it was applied to the live DB under the gate as written, where it changes almost nothing — 1,527 pharmacy-lead gap addresses in 2 clusters, multiplier p10/p50/p90 = 0.935/1.030/1.211, top-50 cluster Jaccard 1.000 — and F4 clears (Williamsburg 0.868 ± 0.091 vs Carnegie Hill 1.115 ± 0.133) with bar bit-identical at digest `5bec24a6ed2c8d14` and Σgap_score 938,319.24, Σn_missing 1,389,900, 3,044,584 ratio>1 pairs unchanged. RULING REQUESTED, proposed by the session: tighten F2 so the primary demand regressor's own Brooklyn Conley CI must exclude zero with the stated sign — bar (w18 t +2.44 in Brooklyn) and the re-tested childcare (under_18 t +7.23) survive, pharmacy does not and would be un-applied with these numbers recorded. Pharmacy has no registry anchor (anchor_coverage 0.000 vs 1,330 ZBP; 3,540 of 5,536 canonical POIs single-source, 63.9%), and the NYS Board of Pharmacy registry remains the planned fix. Found and fixed on the way: `model/age_fit.py` read its `supply_hash` off `analysis.address`'s screen stamp instead of recomputing it, so after the D65 anchor rebuild a fit file recorded a supply (e2b7ef55e607) it was not estimated on (live f1b12ab55878) and the next address-gaps run would have tripped a FALSE staleness alarm on bar; the fit now records `score.supply.supply_hash(con)` live with the screen stamp kept as an informational `screen_supply_hash`, `check_fit_is_current` compares against the live hash, three tests pin it, and all three fit files were re-stamped with identical evidence (bar coefficients reproduce to full precision; n_target 2,299 / 5,536 / 2,657 unchanged). Built: `PHARMACY_SPEC` in `CURVES`, raw ACS bands in `AGE_MOE_COLUMNS` and the tract panel, an `adult_shares` robustness refit so b(w18) is placebo-comparable, CLI `--category` help derived from the registry, 7 pharmacy tests + 3 provenance tests; suite 443 passed / 1 deliberate skip.

**D67 — NYC DOF Storefront Registry ingested: analysis.storefront at storefront×filing grain, analysis.storefront_latest view, seven vacancy-exposure columns on analysis.address by UPDATE; no rent data exists at address grain anywhere public.** *(2026-09-10, owner, GTM-137)*
Owner asked whether Loci has vacant-storefront and asking-rent data. Research pass: the only address-level public vacancy source is the DOF Storefront Registry (Local Law 157, NYC Open Data 92iy-9c3n); asking rents exist only at corridor grain (REBNY/CBRE PDFs) or inside marketplaces (LoopNet/CoStar, Crexi) whose terms forbid scraping; HUD USPS vacancy is tract-grain and gated. Probe findings that changed the design: "Reporting Year" is a text label — the real key is Filing Due Date (11 filings), five of them vacant-only supplements; pooling them with the annual full-universe filing reads as 100% vacancy. vacant_6_30_or_date_sold is not mixed (only YES exists; sale date separate). Y/YES and N/NO are a form change. No storefront identifier; unit blank 87% of rows; a premises files four rows/year legitimately — deduped on ingest. Tax Class 1 essentially absent (0.27% MN+BK), so rowhouse corner stores under-covered. 4,780 MN+BK rows are (0,0). Lease expiry published 2024-06-03 filing only, then dropped. Inventory (D61 first: 26 objects): ONE new table analysis.storefront, one row per storefront per filing (253,519 MN+BK rows, 31,797 premises, premises_id = BBL|unit stable); derived per-premises fields in VIEW analysis.storefront_latest; seven measures by UPDATE only on analysis.address (vacant_storefronts_400m, storefronts_400m as denominator so absence/no-filing distinguishable, nearest_vacant_storefront_m/_id/_business, nearest_vacant_lease_expired, storefront_asof), disjoint from screen columns and D62 pipeline columns; network metres via D62 helper. Snapshot = one filing (full universe wins), pinned by test. Two double-count bugs caught in live run (backup data/loci.duckdb.bak.20260910-165510): pooled 2025-06-03 annual with 2025-02-15 supplement (13.79% vacancy), fixed to one filing (9.93%); same in storefront_latest. Snapshot 2024-12-31 — MN 23,411 storefronts / 2,607 vacant (11.14%); BK 14,907 / 1,200 (8.05%); 38,318 / 3,807 total. Of 177,078 eligible MN+BK gap addresses, 109,311 (61.7%) have ≥1 vacant storefront within 400 m and 44,048 (24.9%) have ≥3; non-filtering proof byte-identical. Ground truth: 335 E 54 St bodega cluster → 26 vacant within 400 m network; Fulton Mall 15+ within 300 m; Gowanus/Carroll Gardens up to 14. Suite 443 passed. Caveats DB cannot enforce: self-reported, non-filing invisible; annual snapshot not a listing; NO rent and NO square footage anywhere public; primary_business_activity on vacant is "NO BUSINESS ACTIVITY IDENTIFIED" — prior use 84% recoverable by ARG_MAX over filings, premises-level only; construction_reported counted vacant; vacant_storefronts_400m ⊂ storefronts_400m, never add; only 36 of 3,806 vacant carry lease expiry. Webmap "Vacant storefronts" overlay shipped the same day: 2,750 vacant premises drawn (MN 1,857, BK 893; 1,708 vacant two-plus consecutive years; 61 construction-reported), legend denominators MN 2,607/23,411 (11.1%) and BK 1,200/14,907 (8.0%) recomputed from the data, snapshot 2024-12-31 (full filing 2025-06-03, DOF vintage 2026-04-09) derived not hard-coded; gap popups carry vacant/registered within 400 m and the nearest vacant storefront with prior use (unknown for 16%) and lease expiry ("not reported" for all but 36); a filter for gap addresses with ≥1/≥3 vacant storefronts nearby. Laundry gap layer: 33,198 of 73,335 addresses have ≥1 vacant storefront within 400 m, 9,958 have ≥3, and 9,769 have ZERO registered storefronts nearby — the reason the denominator ships in every popup. Data dir 50.6 → 58.4 MB; storefronts.json 195 KB. Suite 443 → 463 (one pipeline test had been silently shadowed by a duplicate name, revealed by ruff F811). Caveat: the overlay's consecutive-year run is anchored at the snapshot year, whereas storefront_latest anchors at each premises' own last observation (often the 2025 supplement, which has no denominator).

**D68 — Headroom (slack) has no predictive skill; bars, cafés and restaurants cluster rather than saturate; the grade carries two regimes per category, and headroom is descriptive only, never a forecast.** *(2026-09-10, owner, GTM-138)*
Owner hypothesis (Gowanus): the category exists but thinly and thousands of residents are arriving, so "more of the same type" could work — intensive-margin headroom question meant to feed fair-price/grade design; owner asked to check Greenpoint too. Backtest same day (data-scientist, Opus; report scratchpad headroom/headroom_backtest.md): headroom_2013 = norm density × pop_2013 − estab_2013 on ZBP ZIP × category (MN+BK, ZIPs with pop ≥5k; ZCTA ACS 2013/2023), outcome Δestab 2013→2023. Result: no out-of-sample skill — leave-one-ZIP-out, 1 of 14 categories beat persistence/drift/population-only, and that one (convenience) is 2017 NAICS 445110→445120 reclassification (grocery −25%, convenience +242%, sum flat). Structurally headroom is affine recombination of population and count, adds nothing conditional on them. For bar (Spearman −0.26) and cafe_bakery (t −4.23) the sign is NEGATIVE: ZIPs above norm added more — clustering, not slack. Greenpoint 11222 read "full" on bars/cafés/restaurants 2013 (−13.7/−3.2/−13.4), added +12/+27/+49. Forward headroom Gowanus ZIPs (11215/11217/11231) with 25,584 pipeline residents (2.0/unit): none for bars/cafés on headcount norm, +9–13 restaurants, +8–18 laundries, +10–17 hair, +5–8 pharmacies; Greenpoint +11 laundries, +4 pharmacies. Age-scaled norm (per 18–34 resident) flips Greenpoint restaurants −13.4→+33.7 (2013), −41.5→+10.9 (today). Ruling for grade: two regimes per category — chore categories (laundry/grocery/pharmacy/hair) behave like demand÷incumbents and pipeline adds directly; social categories (bar/café/restaurant) reward incumbent proximity and cap at spend per resident by age/income, not headcount. Headroom ships as descriptive ratio with interval (mature-ZIP bar density 0.00–1.84/1,000 around 0.33), in ONE supply universe (never ZBP norm vs Loci count; D40 gap 2–8×), age-scaled for bars — never a forecast. Next increment GTM-138: per-category clustering-vs-saturation coefficient.

**D69 — Three owner rulings land together: a per-category `anchor_is_floor` exception to D52 for childcare, F2 tightened to the primary demand regressor, and the screen re-run on the resulting supply — childcare ships, pharmacy is un-applied.** *(2026-09-11, owner, GTM-139)*
Ruling 1 amends D52 rather than relaxing it: `childcare` now carries `anchor_is_floor: true` in `categories.yaml`, because D52's veto rests on the premise that a dense registry's silence is evidence, and for childcare that premise is false by construction — the DOHMH roster `gy3q-4tzp` covers Article 47 and Article 43 group settings only, while OCFS-licensed home-based Family and Group Family Day Care is published by no NYC feed, so the registry could not corroborate a home-based provider however real it is, and the veto was deleting supply precisely where home-based care dominates (`in_principled` had fallen 6,066 → 2,657 the moment the anchor qualified at coverage 0.846). The flag suspends only the lone-aggregator veto: the anchor still ranks first for canonical geometry and name, still feeds `anchor_coverage`, still qualifies, and `in_principled` for childcare returned to 6,066 — equal to `in_all` — moving citywide principled supply 138,403 → 141,812 and the live `supply_hash` f1b12ab55878 → 06bb6f357cc1; the flag is persisted on `analysis.category_anchor` and read by a new append-only migration `sql/013_floor_anchor.sql` so the view stays self-contained SQL, and it is folded into `supply_hash` so a rule change that moved nothing today still changes the stamp. Three tests pin the semantics — flag off, the veto still fires on the identical fixture; flag on, the lone aggregator is retained and `in_principled == in_all` for that category with the CORROBORATED ⊂ PRINCIPLED ⊂ ALL nesting intact — plus a drift check that refuses a floor flag on any category with no registry source measured in it, because a floor is a floor *under* a roster and flagging a category whose anchor was never ingested would be silently wrong the day one landed.

Ruling 2 splits F2 into F2a (the Brooklyn contrast CI excludes 1.0 with the stated sign, unchanged) and F2b (the primary demand regressor's own Brooklyn Conley CI excludes zero with that same sign), because F2a is a contrast over the whole age block and pharmacy cleared it in D66 at 1.585 [1.236, 2.032] while `b(age_65_plus_share)` carried a t of +0.90 — five-sixths of the contrast was "fewer 18–34s" — which is the GTM-109 defect, a number wearing a label it did not earn; the gate message now names which sub-test failed, a fit missing its Brooklyn SEs fails closed rather than skipping, and a fixture in which the contrast passes and the primary CI includes zero is refused by test. On the new supply, bar reproduces D63 exactly and passes (w18 +0.404 pooled, +0.596 Brooklyn t +2.44, contrast 1.451 [1.196, 1.761], dispersion 3.61×); childcare passes on materially stronger coefficients than D65's, `b(under_18_share)` +2.744 pooled (Conley se 0.446, t +6.15) and +2.302 in Brooklyn, East Village → Borough Park 2.990 [2.125, 4.208] pooled and 2.353 [1.662, 3.330] in Brooklyn, R² 0.601, dispersion 3.24× — different from D65's 3.556 because the floor anchor changed both the numerator and the all-POI denominator of the composition outcome; pharmacy is REFUSED at `b(age_65_plus_share)` +0.396, se 0.284, t +1.39, CI [−0.161, +0.954], and the D66 figure of +0.90 does not reproduce because it predates the anchor — the point estimate moves with the denominator, the verdict does not.

Ruling 3 applied both surviving curves and retired pharmacy's: its stale fit JSON was moved out of `data/interim` (a gate failure leaves yesterday's file in place by design, and `apply` then refuses on staleness rather than proceeding), pharmacy's 281,839 `age_fit` rows are NULL, childcare's 281,839 are populated with MOEs, and bar's 281,839 are unchanged in count. The screen was then re-run citywide in the canonical order — address-gaps ALL, address-demand ALL, pipeline, storefronts, age-fit apply — stamping supply_hash 06bb6f357cc1 on all 767,337 addresses; eligible rose 587,408 → 593,930 and Σn_missing 1,389,900 → 1,398,892, with the non-filtering identity exact on the new run (count{ratio>1, eligible} = 1,398,892 = Σn_missing), childcare-lead gaps falling 20,949 addresses in 45 clusters to 9,289 in 17, and bar-lead rising 144,731/230 to 148,221/240. That last number is the session's surprise and worth recording: eligibility is `present_count ≥ 12 of 15`, so +3,409 childcare POIs pushed 6,522 addresses over the gate and *raised* every other category's gap count on a run whose only supply change was childcare — D51's "the eligibility gate does no work" holds inside MN+BK and not outside it. The ranking barely moves: the top-50 MN+BK cluster list is identical by gap_score and gap_score_fit (Jaccard 1.000), bar-lead-only is 0.961 against D63's 0.923, and childcare's F4 ordering clears on the live curve with Borough Park at 2.306 ± 0.811 against East Village 0.718 ± 0.120 — passing, but with a band three times bar's, so childcare's multiplier must never render without its MOE. The UPDATE-only layers the rebuild cleared were re-applied and verified identical (pipeline 281,842/278,909; storefronts 281,842/281,835/9,625), `age_fit_lead` and `gap_score_fit` cover 267,327 addresses, the webmap was re-exported on the new gap set (142 files), and the D64 childcare live-ordering test that had been a deliberate skip now runs and passes: suite 503 passed, 1 skipped, that skip being pharmacy's live-ordering test, which is the state the ruling intends. The fix for pharmacy remains the NYS Board of Pharmacy registry, not a relaxed gate. *Correction (D71):* the "bar-lead 144,731 → 148,221" figures here count eligible addresses with bar as lead, including 17,329 that are not gaps; the bar-lead GAP count is 130,892, the delta +3,267, of which 1,832 came through the eligibility gate and the rest is childcare losing the argmax at already-eligible addresses, which is the ranking behaving correctly; see docs/eligibility_gate.md.

**D70 — Per-category density elasticity shipped as package data: eight daily-needs categories saturate, none cluster; supersedes D68's "bars/cafés cluster" reading; two gate rules added (unanimous-sign rejection; regime requirement relaxed).** *(2026-09-11, owner, GTM-138)*
Owner: "lets do it" on GTM-138. Built src/loci/model/density_elasticity.py + density_elasticity.yaml (config lives with the code), `loci density-elasticity fit [--dry-run] [--bandwidth-km] | show`, tests/test_density_elasticity.py (29 tests); suite 501 passed. Panel: 79 MN+BK ZIPs with 2013 pop ≥5k, ZBP 2013→2023 via analysis.zip_category_establishments, ZCTA ACS 2013/2023 population and income cached under data/raw/acs/ (same Census API as acs.py). Estimand per category: Δestab_2013→2023 on 2013 density (estab per 1,000 residents) with Δlog pop, log pop_2013, log income_2013, Δlog income, borough FE; HC3 and Conley 3 km SEs; Moran's I on residuals; leave-one-ZIP-out sign stability; standardized cross-category placebo (real |β_std| must exceed the 90th percentile of |β_std| from regressing the category's growth on OTHER categories' 2013 density). Classification: saturating / clustering / no_signal by |t_Conley| ≥ 1.96 AND LOZO sign stability ≥ 0.90 AND placebo. Result: NOTHING clusters. Saturating (8): childcare β −14.73 (t −2.10; only category with spatially autocorrelated residuals, Moran +0.40), clinic −13.57 (−4.77), laundry −10.82 (−4.20), grocery −8.79 (−4.76), tailor_repair −8.74 (−5.74), fitness −7.37 (−4.42), nails_beauty −6.58 (−3.80), bank −4.49 (−4.95). No signal (8): bar +3.47 (t +1.63, fails significance), cafe_bakery +6.20 (t +4.83, LOZO 1.00, but FAILS THE PLACEBO: standardized 5.354 vs p90 5.820 — 2013 restaurant density (6.18) and hardware density (5.88) predict café growth better than café's own, i.e. generic gentrification), convenience, hardware, food_retail (fails placebo by a hair, 2.726 vs 2.804), restaurant, pharmacy. Supersedes D68's reading that bars/cafés cluster; D68's chore-category conclusions stand and are strengthened. Two gate rules: (1) the log (proportional-growth) form Δlog(estab) was refused by REJECT_UNANIMOUS_SIGN — all 16 βs negative, and its only three "significant" verdicts (convenience, tailor_repair, childcare) were exactly the three series with base-year definitional breaks; the base count sits in its own denominator, so the form measures its own transform; the rule is symmetric (an all-positive table fails identically) and forces no sign on any category; the level form was adopted and the choice recorded in the YAML. (2) The original gate required a category in EVERY regime including clustering — a spec error that forces a sign at the portfolio level; relaxed 2026-09-11 to: placebo ran for every category, at least one category carries a signal of either sign, every regime in the allowed set; documented in the module docstring and YAML header, pinned by test. Grade usage recorded in the YAML header: saturating → demand pool ÷ incumbents, pipeline residents add directly; no_signal → residents only, incumbent count enters in neither direction; no category is scored with incumbents in its favour. Caveats: ZIP grain (11215 mixes Park Slope and Gowanus; 11249 merged into 11211); ZBP employer-only and noise-infused 2017+; grocery/convenience reclassification 2017 (also fitted combined as food_retail); revealed-supply panel; cafe_bakery and food_retail are hair's-breadth placebo calls to re-read if the panel is ever extended below ZIP grain. Gowanus reading: the chore stack (laundry, hair, nails, grocery) is supported; "more bars/cafés because the pipeline is coming" has no support at ZIP grain.

**D71 — Pharmacy anchor lands as the NYS Medicaid roster because the Board of Pharmacy has no bulk form; coverage 0.000 → 0.903, the pharmacy age-fit stays refused on its own regressor; the webmap renders age-fit with its MOE; the eligibility gate is measured and its D69 numbers corrected.** *(2026-09-11, GTM-140)*
Three D69 next actions closed in one pass. The pharmacy registry D66/D69 named — NYSED Office of the Professions establishment registrations — was probed and found to have no bulk form at all: a one-record verification page, no export, no API, `op.nysed.gov/data` 404s, and nothing on data.ny.gov or health.data.ny.gov, so the registry entry is now `excluded` with the probe result written into it; CMS NPPES taxonomy 3336C0003X was probed and rejected too (no coordinates, a 1,200-record enumeration cap that truncates Brooklyn, `status='A'` on 100% of returns against an active count 1.6–2× lower — the D47 stale-licence trap with a federal badge). The anchor is `keti-qx5t`, the NYS Medicaid Enrolled Provider Listing filtered to `profession_or_service='PHARMACY'`, defensible as a pharmacy roster in New York specifically because NYRx (2023-04-01) carved the pharmacy benefit for ALL Medicaid members into fee-for-service, so any pharmacy serving any Medicaid member must enrol and enrolment requires a current NYSED registration; weekly refresh, lat/lon published by the State on 100% of rows. Two silent bugs were caught by probing before building: `mmis_id` is per-PROVIDER not per-storefront (Ideal Care Pharmacy enrols 1621 and 811 Avenue U, 670 m apart), so the key is `mmis_id@address-slug`; and 8 rows carry a (0,0) null-island coordinate, dropped and counted rather than guessed. 2,416 records staged citywide (MN 448, BK 757, QN 719, BX 397, SI 95), hospital (1,270), clinic (142), specialty (11) and supervising-pharmacist (2,911) enrolments excluded by a closed vocabulary that raises on a sixth value and on a blank; `score/dedup.py` got the pharmacy branch (the D65 trap). `anchor_coverage` 0.000 → 0.903 against 1,330 MN+BK ZBP establishments, pharmacy qualifies; canonical pharmacy 5,536 → 7,105 `in_all` and 5,536 → 3,959 `in_principled` as D52's veto removed 3,146 lone-aggregator clusters (MN 534, BK 564, QN 599, BX 300, SI 94, 1,055 outside the res-9 grid); live `supply_hash` 06bb6f357cc1 → 767b28674e30, every other category bit-identical. `anchor_is_floor` deliberately left OFF: at 0.903 the file is above the "load a better anchor" band, the slice it misses skews affluent — the opposite of the bias that makes a veto dangerous — and unlike OCFS home-based care the true roster exists and is merely not downloadable. Caveat the database cannot enforce: `mmis_name` is the legal entity and the aggregators carry the awning, so 516 corroborated aggregator clusters within 40 m of a registry cluster are counted twice (~13% of principled pharmacy), under-stating pharmacy gaps; the fix is the DBA in NPPES `other_names` joined on the `npi` already stored, not a wider `MATCH_METERS`. The pharmacy age-fit on the new supply is still refused, more decisively — `b(age_65_plus_share)` Brooklyn +0.148, Conley se 0.307, t +0.48, CI [−0.454, +0.749] (D66 +0.90 → D69 +1.39 → +0.48): the anchor removed the excuse rather than rescuing the curve; and because +1,569 pharmacy POIs enter the ALL-POI denominator of every composition outcome, the bar and childcare fit files (stamped 06bb6f357cc1) are correctly stale and need genuine re-estimation before the next apply. The webmap now renders the age-fit: the gap layer carries `age_fit_lead`, `age_fit_lead_moe`, `gap_score_fit`, `gap_score` and the lead's `age_fit_source` in parallel arrays, with three states drawn distinctly — NULL (never applied), 1.0 with NULL MOE and source (identity: the lead has no curve, drawn unranked and never folded into the ramp), and a fitted value with ± always shown — behind a "supply-revealed; see caveat" expander that renders `AGE_FIT_DISCLAIMER` untruncated, plus a default-off toggle to reshade by `gap_score_fit` that is disabled with an explanatory tooltip for categories with no curve; `meta.json` gains an `ageFit` block listing exactly the categories with a fit file on disk, their timestamps, supply hash, median MOE, the caveat verbatim and a staleness warning (`loci export-webmap` does not yet echo that warning to the terminal). The eligibility-gate investigation (`docs/eligibility_gate.md`, read-only) found the gate binds nowhere in Manhattan, on 5.8% of Brooklyn addresses and on 20–60% of QN/BX/SI; four categories have reach longer than the gate's fixed 800 m window (tailor 960, clinic 960, hardware 960, fitness 1,200), so the gate's "present" and the screen's "present" are different predicates sharing a word; it corrects D69's record — the bar-lead 148,221 included 17,329 non-gaps, the gap count is 130,892, the delta +3,267 of which only 1,832 came through the gate, and inside MN+BK the whole event is 359 addresses — and it shows the gate's importance to the deliverable is 0.93 by units and 0.32 by gap score, so the cluster-ranking choice is the larger open question. Recommendation, pending the owner: long-term a POI-free built-form gate (PLUTO retail floor area within 800 m; AUC 0.873 vs 0.856 against the DOF Storefront Registry, with a pre-registered NTA-holdout test and an externally derived threshold), interim the gate computed on `in_all` (one predicate; would have made D69's crossing exactly zero; moves the MN+BK deliverable under 2%). Suite 536 passed / 1 deliberate skip; check-sources ok at 32 sources. The screen has NOT been re-run on supply 767b28674e30; that second pass waits on the gate ruling.

**D72 — Construction-activity evidence on the development pipeline: permitted splits into active / lapsed / stalled from DOB permit issuance and DOB NOW approved permits; units_active_400m and units_stalled_400m ride `loci pipeline`; Gowanus red-team downgrades the earlier reading.** *(2026-09-11, owner, GTM-141)*
Owner: "our goal is to take this model and start to make recommendations. close the gap." A contrarian pass on the Gowanus reading (same day) graded the load-bearing claims: arriving homes A on record / D on timing (14 of 20 permitted jobs vested before the 2022-06-15 421-a deadline, none with a CO 40 months later); laundry-as-lead-gap D (of 17,628 homes at laundry-lead addresses in the wide box, 59% sit in 51+-unit buildings that carry in-building laundry and most of the rest are 1–5-unit rowhouses where NYCHVS says 50–74% have in-unit washers; LL84/listings evidence covers 1.9% and confirms laundry on 3 of the 12 largest flagged buildings; the DCWP anchor vetoes 25 of 83 candidate laundries as lone-aggregator); the supply counts the owner was shown were in_all not principled (restaurant 886→443, laundry 83→58; the chore-vs-social ratio 7.6:1 is at par with MN+BK 7.9:1 and survives only in the Bond–4th Ave / Douglass–9th St core, where pharmacy 0.07×, convenience 0.14×, grocery 0.29×, hair 0.33× baseline are all thinner than laundry 0.28× and bar is 1.12×); the "restaurant/bar takeover on a below-market lease" line had no data behind it and is withdrawn as a data claim; D68's forward headroom numbers had been quoted forward despite D68 ruling them descriptive only — the rejected-predictor pattern, flagged. Web research (Tavily) on the pipeline: 499 President (350u, Union Market grocery, Paulie Gee's, a PT clinic), 420 Carroll, 251 Douglass (occupied 2026-02) and 267 Bond (517u) are delivered/leasing; 300 Nevins (654u) and 545 Sackett (255u) are building; 175 Third St (1,071u, Life Time signed) closed financing only in June 2026 with first move-in ~2028; Gowanus Green (950 affordable) is stalled on remediation; 18 Gowanus projects use the state PILOT workaround for the lapsed 421-a; the rezoning's active-use mandate adds ~100,000 sq ft of storefront; Gowanus new-build retail asks ~$70/SF vs Court St $115 and Smith St $90 (REBNY H1 2025, both softening); CSO tanks online early 2029. Build (data-engineer): sources/cities/nyc/dob_permits.py using ipu4-2q9a (BIS permit issuance; TEXT dates in two formats in one column, byte-identical duplicate rows, parsed both ways, aggregates min/max) and rbx6-tga4 (DOB NOW approved permits; job number = 9-char prefix of job_filing_number); w9ak-ipjd rejected (coarser duplicates). No construction-start date exists in any feed — job_start_date is the declared start copied onto every renewal — so the column is activity_status (active = permit issued/renewed within 12 months of asof or expiration in the future; lapsed = expired 0–12 months, no CO; stalled = expired >12 months or permitted >5 years with no activity; complete; n/a when no permit row matched, never stalled), NOT an invented under_construction stage. sql/014_dev_pipeline_activity.sql: UPDATE-only columns on analysis.dev_pipeline (last_permit_issued, last_permit_expires, permit_evidence_source, activity_status); `loci pipeline-activity` writes dev_pipeline only; units_active_400m / units_stalled_400m added to PIPELINE_COLUMNS so the concurrent session's canonical re-apply order (`loci pipeline` → `loci storefronts` → age-fit) is unchanged; NULL not 0 when no evidence. Bug surfaced: dcp_housing.write_dev_pipeline used a positional INSERT…SELECT that the new columns broke (would have silently mis-mapped type-compatible columns); now a named column list. Live (asof 2026-09-11, backup data/loci.duckdb.bak.20260911-142920): MN+BK 7,364 permitted/partial jobs → active 2,244 (29,657u), lapsed 616 (1,382u), stalled 4,454 (9,195u), complete 4 (714u), n/a 46 (353u); stalled = 22.3% of permitted units, independently reproducing D62 caveat 3's 23%; validation: unit totals reconcile exactly, 0 addresses with active+stalled > permitted. Gowanus box: 13 active jobs / 3,433u, 7 stalled / 1,673u (33% of units; four never renewed once; B00625252 + B00649561 share BBL 3004710200 — one site, not two risks); 175 Third St B00680917 active (renewed 2026-03); Gowanus laundry-lead gap addresses: 80% mostly-active exposure. Webmap: the existing pipeline overlay should style dots by dev_pipeline.activity_status (not done; another session held webmap_export.py/index.html); the two gap-layer fields registered in PIPELINE_NOT_EXPORTED. Caveats: a renewal is not a shovel (separates abandoned from not-abandoned only); active + stalled ≤ permitted, never equal (lapsed and n/a are in neither); last_permit_expires is a MAX over work types; DCP-filed jobs that permitted after 2026-01-21 still read n/a (D62 caveat 1); rbx6-tga4 permit_status 'Signed-off' exists per work type and is deliberately not ingested (ticket it). The `pipeline-activity` CLI block was carried in the concurrent session's commit cdfb46c; the rest landed in this session's commit. Suite 554 passed. Standing ruling for recommendations from here: a recommendation card must grade each load-bearing claim (demand, arriving homes by activity, supply ratio in the principled universe, addressable households, space, economics) and may not say "act" while any load-bearing claim is at grade D.

**D73 — Supply ratio vs the MN+BK baseline per address×category on the principled set (400 m network), homes_400m, and a laundry addressable-homes haircut: laundry is NOT thin in the Gowanus core (0.94×); pharmacy (0.00×), convenience (0.40×) and hardware (0.72×) are.** *(2026-09-11, owner, GTM-143)*
Built by a data-scientist agent from the D72 red-team's three findings (in_all counts shown to the owner; "is there a gap" is the wrong statistic for a present-but-thin category; the laundry pool is mostly unaddressable). src/loci/model/supply_ratio.py + `loci supply-ratio [--boroughs] [--radius-m 400] [--fit-baseline] [--dry-run]`: for every eligible address × category, supply_400m = principled POIs within 400 m NETWORK distance (87 s for MN+BK by sourcing Dijkstra from the 39,083 distinct graph nodes that 282k addresses collapse onto, 611 calls; graph symmetry licenses the reversal, pinned by test); homes_400m on analysis.address (category-independent, so not duplicated 15× on address_category per D61); supply_per_1k and supply_ratio_vs_base on analysis.address_category by UPDATE only, SET lists disjoint. Baseline per category = MN+BK median supply_per_1k over 267,329 eligible addresses, shipped as src/loci/model/supply_baseline.yaml stamped with supply_hash 767b28674e30 (note: not the 06bb6f357cc1 of D69 — the set moved with D71's pharmacy anchor), aggregate estimator recorded alongside; tailor_repair's median is 0 so it uses the aggregate, stamped. `--fit-baseline` is the only path that rewrites the YAML and is deliberately NOT in the re-apply order. src/loci/model/laundry_haircut.yaml (hand-maintained, separate from the generated baseline): addressable_homes_400m_laundry = Σ units × (1 − p_in-home): 1-unit 0.74 and 2-unit 0.50 from NYCHVS 2023, 3–5 units 0.35, 6–50 units 0.15, 51+ units 0.85 as owner-adjustable priors; address_laundry_evidence overrides to 1.0. Re-apply command handed to the concurrent session: `uv run loci supply-ratio --boroughs MN,BK` at the end of the canonical order; re-run verified byte-identical (sum homes_400m 801,269,217; sum supply_400m 25,246,768), screen untouched. Gowanus core (Bond–4th Ave, Douglass–9th St; 1,831 eligible addresses, median 3,646 homes within 400 m): pharmacy 0.00× (961 core addresses have zero principled pharmacies within 400 m; 18 principled vs 29 in_all in the wide box), tailor 0.00×, convenience 0.40×, hardware 0.72×, laundry 0.94×, childcare 0.94×, hair 0.98×, grocery 1.07×, clinic 1.33×, nails 1.75×, restaurant 1.84×, bank 1.97×, cafe 2.89×, fitness 3.39×, bar 5.58×. Wide Gowanus box and Williamsburg both rank convenience and pharmacy thinnest too. The red-team's laundry 0.28× reproduces only on the laundry-gap subset, which is selected on laundry being absent — circular; unconditioned, laundry is normal for this city. Addressability: the median core laundry-gap address keeps 718 of its 5,622 nearby homes (13%) after the haircut; zero core gap addresses reach 1,500 addressable homes; 69 buildings of 51+ units hold 14,740 of the wide box's 40,275 homes. Ruling: the recommendation ranks categories by supply_ratio_vs_base on the principled set, never by gap count, and laundry is withdrawn as the Gowanus lead; pharmacy, convenience and hardware lead. Caveats: baseline is revealed supply (D6 circularity) — 1.0× means "normal for this city", never "correctly provisioned"; poi_supply is a view, hash stamped per row and in the YAML with a drift test; median vs aggregate estimators differ 15–50% (bar 5.58× vs 1.89×), both ship and the choice is the largest lever; three of five haircut priors are judgement, the 51+ prior drives the laundry result; only MN+BK written, other boroughs NULL. 46 tests; suite 600 passed / 1 skipped.

**D74 — `loci recommend` shipped: an evidence-graded recommendation card per area × category whose verdict cannot say "act" while any load-bearing claim is grade D; the first Gowanus card says "do not act on this data" for 14 of 15 categories, all on economics.** *(2026-09-11, owner, GTM-144)*
Built by a developer agent on the D72 ruling. src/loci/model/recommend.py + recommend_grades.yaml (owner-adjustable thresholds, load_bearing/context split, cheapest_check text) + `loci recommend --area NAME --bbox lat0,lon0,lat1,lon1 [--nta] [--category c]... [--format md|json] [--out PATH]`; docs/recommendations/ holds the cards. Seven sections per category, each graded A–D with the rule and the reason: (1) demand now (homes_400m, income vs MN+BK with MOE, 18–34 share; A if MOE < 20% else B; context); (2) arriving homes (units_permitted_400m split active/stalled/other from D72; B ≥ 70% active, C 40–70%, D < 40% or NULL; takes the WORSE of exposure-weighted and median-address share); (3) supply thinness (median supply_ratio_vs_base on the principled set with baseline and supply_hash; A if hash matches and n ≥ 200, B 50–199, else C; plus the D70 regime); (4) addressable demand (laundry haircut B on priors, A only when evidence coverage > 50%; other categories C "no haircut yet"); (5) space (vacant/registered storefronts within 400 m; B; D if nobody filed; context); (6) economics (comps_for: A ≥ 5 neighbourhood comps with cash flow, B ≥ 5 borough, C thin, D no cash-flow data); (7) coverage (anchor qualification, Google validation in the bbox; A validated, B anchored registry, C aggregator-only). Verdict = min grade over the load-bearing sections 2, 3, 4, 6, 7: "act" ≥ B, "diligence" C, "do not act on this data" any D, and every D section prints the cheapest check that would change it (site walk; Google check; 3–5 P&Ls). Never emits an expected profit; economics reports supportable rent only. Hash drift between supply_baseline.yaml and the live poi_supply warns and caps section 3 at C rather than refusing the card — argued (six sections never read the supply view; C already makes "act" unreachable; the cap can never raise a grade) and pinned. Tests brute-force all 4^7 grade combinations to prove "act" is unreachable with a load-bearing D and that context sections never move the verdict; synthetic in-memory warehouse fixture; 38 tests, suite 638 passed / 1 skipped. First card, Gowanus core (bbox 40.670,-73.995,40.682,-73.982): 14 of 15 categories "do not act on this data", all on economics (no cash-flow comps anywhere); hardware and tailor_repair reach "diligence" only because the listings CSV holds a couple of cash-flow rows; arriving homes C not B (72% of exposure-weighted permitted units active, median address 50% — the "80% mostly-active" line in D72 flattered the typical doorway); demand-now B (income $160,652 ± $35,726, MOE 22%); supply ratios per D73. What closes the gap from here, in order of leverage: (a) 3–5 P&Ls per category from businesses for sale in the area — BizQuest/BizBuySell block automated and browser access and that block is honoured, so this is a human collection into data/benchmarks/bizbuysell_nyc_listings.csv; (b) the Google coverage check on the core for pharmacy/convenience/hardware (GTM-48 budget, ~$170, owner approval pending); (c) a site walk or per-work-type sign-off (GTM-142) on the 20 permitted jobs. The map overlay styling by activity_status remains a follow-up.

**D75 — The eligibility gate is removed by owner ruling; every address is in the universe, and the cap_m censoring it was hiding is now flagged rather than smoothed.** *(2026-09-13, owner, GTM-146)*
The owner's words (2026-09-13): "I 100% vehemently disagree with 'which addresses count at all'. If an address is truly in a super underdeveloped area, this would completely not count it." The gate existed for exactly one reason — D39 found that the literal port, eligible iff within reach of ≥12 of 15 categories, violated monotonicity because tightening a reach dropped addresses out of eligibility faster than they gained gaps, and a FIXED, reach-independent presence test (≤800 m for ≥12/15) restored it — so retiring it needed a replacement for that role, and with no gate at all the role is filled trivially: the gap set is `ratio > 1` read off each address's own nearest_m against a reach that is fixed input, so tightening any reach can only ADD pairs, and the test now asserts reach × 0.8 removes exactly zero pairs over the whole universe rather than inside a mask. `eligible` survives as an always-TRUE column for schema and query compatibility (`WHERE eligible` in new code is now a bug, and the three surviving `eligible_only` parameters — sample, recommend, supply-ratio's baseline fit — are documented no-ops that exist only so a restored pre-D75 snapshot is not pooled with a gate-free one); `present_count` survives as the descriptive count it always was and is a candidate ranking feature, while the feasibility question the gate was reaching for stays where it belongs, in model/invest.py's PLUTO CommFAR/RetailArea build-or-lease test, with nothing folded across. D51's `cap_m` = 2,400 m censoring artifact, which the gate had been accidentally suppressing, is now fully exposed and is handled in the open by a flag and not a rule: `gap_score` and `cap_m` are both unchanged, because a censored ratio is still the smallest value that ratio could take and leaving it in keeps the ranking monotone, continuous and conservative, while `analysis.address_category.censored` and `analysis.address.lead_censored` carry the fact through the `address_gaps` view into the webmap's gap layer and popup, which now says "nearest X beyond 2,400 m — the distance was not measured" instead of printing the cap as a walk; 143,973 (address, category) pairs sit at the cap and 55,971 addresses have a censored LEAD (SI 20,864 / 18.6%, QN 17,511, BK 11,852, BX 5,734, MN 10), tailor_repair alone accounting for 111,512 of the pairs. The screen was then re-run citywide on supply 767b28674e30 — the second pass D71 deferred, and a genuine re-estimation because the pharmacy anchor moved every composition denominator: bar and childcare both clear their gates on the new denominator (bar w18 +0.404, Conley se 0.203, dispersion 3.61×; childcare under_18_share +2.762, se 0.447, dispersion 3.23×) and pharmacy is refused again on its own primary regressor at t +0.48, CI [−0.454, +0.749]. Over all 767,337 addresses the non-filtering identity is exact — count{ratio > 1} = 3,012,430 = Σ n_missing — against 1,398,892 on the gated D69 run; gap addresses rise 452,491 → 626,145, bar-lead gaps 130,892 → 180,480 in 240 → 264 clusters and childcare-lead gaps 1,342 → 1,775 in 17 → 18 (correcting D69's "childcare 9,289" the same way D71 corrected its bar figure: both counted eligible addresses whose lead was that category, non-gaps included). Every one of the 173,407 formerly-excluded addresses turns out to be a gap — none was a served address the gate had merely mislabelled — and they are overwhelmingly outer-borough: QN 74,050, SI 69,887, BX 14,957, BK 14,509, MN 4, leading on laundry 57,799, bar 49,613 and convenience 28,717, which is the shape the owner's objection predicted. The canonical order was re-applied in full and verified — pipeline 281,842 / 278,909 with units_active and units_stalled at 281,842, storefronts 281,842 / 281,835, age_fit_lead and gap_score_fit 281,840, homes_400m and supply_ratio 281,842 — and the MN+BK deliverable barely moves: the top-50 cluster list by capped units has member-address Jaccard 0.926 against D69, the same nine clusters lead in the same order, and the F4 orderings hold on the live curves (bar East Village 1.117 ± 0.090 over Carnegie Hill 0.834 ± 0.083; childcare Borough Park 2.316 ± 0.818 over East Village 0.716 ± 0.120, a band still seven times bar's). One consequence needed its own sanctioned exception: `supply_baseline.yaml` was fitted over the 267,329 addresses the gate admitted, so although the supply set itself did not move the baseline's universe did, and it was re-fit once on the gate-free universe (n 267,329 → 281,842, `universe` now recorded as "all addresses with homes_400m > 0", `supply_hash` 767b28674e30 unchanged, asof 2026-09-13) before the final `supply-ratio --boroughs MN,BK`, which raised no drift warning. The webmap was re-exported on the new gap set (145 files, 83.6 MB) with `meta.ageFit` listing bar and childcare on supply 767b28674e30 and `stale: []`, and the suite stands at 794 passed / 1 skipped, that skip still being pharmacy's live-ordering test. The owner also stated two further directions, recorded here and not yet built: sample an address near the middle of every known street in the borough so that undeveloped streets are represented, and rank by density.

**D76 — Foot-traffic proxies land at the address grain (transit_entries_400m, jobs_400m): DOT validation reads ρ +0.79 headline, but the contrarian verdict is card context only, never a grade, never the supply-ratio denominator.** *(2026-09-13, GTM-147)*
Built: MTA hourly turnstile/entry ridership for Jun–Aug 2026 (64 weekdays), entries split evenly across entry-allowed entrances per complex, joined to LODES WAC block centroids for jobs; walk-graph Dijkstra at 400 m network distance on every MN+BK address (281,842 addresses, 0 NULL). Validation against the DOT pedestrian-count program (99 corridor points): all-99 transit ρ +0.793, jobs +0.790, homes_400m +0.372 — transit and jobs both beat homes_400m on the headline number. But the headline does not survive stratification: Manhattan-only homes_400m is ρ −0.209 (wrong sign), Brooklyn-only transit is ρ +0.561 with a 95% CI of [0.14, 0.83] — a floor low enough that the correlation cannot be trusted in the borough that carries most of the gap-address universe. In the lowest tercile of foot traffic, homes_400m (+0.686) beats transit (+0.622) — the proxy this column would ride alongside already does better exactly where a foot-traffic column would matter most. A binary any-station-within-400m flag alone gets ρ +0.547, meaning most of transit's signal is "is there a subway stop nearby at all", not a graded count. Endogeneity: ρ(transit, supply_400m) ranges +0.536 (restaurant) down to +0.236 (childcare) across every category, and ρ(transit, supply_ratio_vs_base) is +0.418 for restaurant — transit correlates with the supply it would be used to explain, the exact structure D1 was written to keep out of the model. Even-split bias: 59.7% of MTA entries land at complexes with ≥6 entrances, where the even split across entrances is least defensible (a single busy entrance vs five quiet ones reads identically).
Ruling: (a) card context only — the recommend.py context sections may show transit_entries_400m/jobs_400m, and a zero renders as "no station within a 5-minute walk", never as "low foot traffic"; (b) not an economics grade section — D74's seven-section grade does not gain an eighth section from this; (c) never enters supply_ratio_vs_base's denominator (D73) — that would let transit's endogeneity with supply contaminate the ratio it is supposed to be independent of. Graduation test before either restriction lifts: ≥100 non-corridor DOT-class validation points, Brooklyn-only ρ ≥ 0.6 with CI lower bound > 0.4, and a non-degenerate variable (800 m radius or distance-decay kernel replacing the binary any-station read).
Registry fix on the way: `68hr-j2j7` and `2de2-6x2h` turned out to be Socrata map VIEWS with no columns, not usable as ingest sources; the tabular ridership ids are `i9wp-a4ja` and `cqsj-cfgu`.
This supersedes CONTEXT.md §3.5's budget assumption that SafeGraph-class foot-traffic data costs $200–400: as of 2026, Advan via Dewey Data is $3,600/yr and academic-only, Placer.ai/Replica/Cuebiq are enterprise-only with no published self-serve price, and MTA turnstile data (the free historical alternative) is discontinued in favor of the hourly ridership feed used here. QUESTIONS C2's budget premise is marked superseded; C2's underlying behavioural-outcome question (does the effect appear in behaviour before residence) stays deferred regardless — free proxies answer "is there foot traffic," not "did foot traffic change first."

**Addendum (2026-09-13, commit 3020947):** owner asked for foot traffic broken out by day and time of day, not just a pooled 400 m count. Built `analysis.address_transit_profile` (long: address × day_type{weekday, saturday, sunday} × daypart{early 0–6, am_peak 6–10, midday 10–15, pm_peak 15–19, evening 19–24}), `analysis.address_entrance` (persisted reachable-entrance set, 457,440 pairs, cutting rebuild from a 5-minute graph run to 8 s), a wide view, and `transit_am_pm_share_400m` on `analysis.address`. Live MN+BK run: 1,654,545 profile rows, 110,303 addresses carry a share. MTA hourly ridership is grouped server-side by complex × day-of-week × hour (SoQL `date_extract_hh`), Jun–Aug 2026; conservation against the pooled D76 total is exact (worst relative error 4.4e-16). Brooklyn: 84.5% of addresses with a share read residential-type (AM/PM > 1, median 1.79); East Village reads 0% (a destination catchment); Gowanus core median 1.03 (mixed). Validation per DOT window: AM +0.620 / midday +0.644 / PM +0.807 (all 99 points), Brooklyn-only +0.583 / +0.403 / +0.568 — but the off-diagonal is sharper than the diagonal: the AM count is ranked better by pm_peak (+0.788) than by am_peak itself, so at DOT points every window is best ranked by whichever daypart is largest, i.e. station size, and the levels carry no time-of-day discrimination — the D76 ruling (card context only, never a grade or the supply-ratio denominator) stands unchanged for the levels. The share, being a ratio, is the object that might actually discriminate residential vs destination catchments and is carried forward as X7, open. Saturday/Sunday profiles are unvalidated (DOT counts weekdays only). Gowanus core weekday p50 by daypart: am_peak 1,868, pm_peak 1,823, evening 694; Saturday midday 1,215.

**D77 — Growing-chain watchlist ships as package data: 121 brands, a 2026-09 detect snapshot, and Tavily press enrichment; the honest growth measure needs a second snapshot.** *(2026-09-13, GTM-148)*
`src/loci/chains/watchlist.yaml` holds the 121-brand watchlist as package data (config lives with the code that reads it; `data/` is gitignored, so a hand-curated watchlist cannot live there without disappearing). `docs/CHAINS.md` is generated, not hand-written. Detect counts are a floor, not a census: only about 60% of brand-locations in the open-data sources carry any first-seen/opened date at all, and DOHMH, Overture, SNAP and DCWP carry none — `last_inspection_date` is deliberately never read as a proxy for first-seen, because a re-inspection of an existing location would silently manufacture a fake opening. Given that, a single snapshot can only report a count, not a trend; the honest growth measure is the month-over-month snapshot delta, which requires a second snapshot (2026-10) that has not been taken yet.
Seed findings from the first snapshot + press pass: Apollo Bagels is at 9 NYC locations (+5), plausible because its Industry City central bakehouse keeps each storefront low-capex (no on-site baking line to duplicate). Vital Climbing Gym is at 4 NYC locations with 0 net new — its last opening was November 2024 and its UES/West Harlem locations were acquired in 2021 — so it reads as a sales/acquisition prospect, not a growth signal, and the watchlist must not conflate "present and expanding" with "present, acquired, and static." Food/drink brands are roughly 80% of confirmed openings in the snapshot. Pharmacy chains show a net −42 locations (Rite Aid alone −35); the vacated boxes are being absorbed by discount grocers, gyms, and LaundryBee (~20 outer-borough laundromats on 20-year leases) rather than by other pharmacies — a category contraction, not a like-for-like replacement. 27 rows imported from an earlier pass were relabelled from `unverified` to `reported` because they carry evidence (a press hit or a corroborating count) behind them.
Press budget is enforced in code, not by convention: 60 queries per run, with a `--dry-run` path that plans the query set and shows the credit cost before any call is made.
Operational note: macOS's case-insensitive filesystem collided `docs/chains.md` with the already-generated `docs/CHAINS.md`, silently overwriting one with the other; the prose process doc is now named `docs/chains-process.md` to avoid the collision permanently, not just avoid it once.

**D78 — The screen scope is enforced, not defaulted: Manhattan and Brooklyn only.** *(2026-09-13, owner, GTM-149)*
The owner restated the scope in one line — "we are still focused on Manhattan and Brooklyn" — and the screen tables had quietly stopped obeying it: D61 ran `address-gaps --borough ALL` to attach the address-grain demographics, every re-run since matched that call out of habit, and `analysis.address` had been carrying all 767,337 addresses with `address_category` at 11.5 M rows ever since, so a reader of the deliverable was reading Queens and Staten Island alongside the two boroughs the project actually screens. A default was not enough, because a default is what D61 overrode: `SCREEN_BOROUGHS = ("MN", "BK")` now lives beside `BOROCODE` in `sources/cities/nyc/addresses.py` (the codes are NYC-specific, so `model/` and `score/` take the scope as an argument and still name no borough), `loci address-gaps` defaults to `MNBK` and REFUSES `--borough ALL` or any borough outside MN+BK before it opens the database, citing D48/D78, unless `--allow-out-of-scope` is passed for a deliberate documented run. Because `write_address_gaps` only delete-then-inserts the boroughs it is handed, a narrower re-run could never clean up after the wider one, so a new idempotent `prune_out_of_scope` removes out-of-scope rows from `address_category` and `address` together on every in-scope write; it took out 485,495 address and 7,282,425 category rows on its first pass and has returned zero ever since. The canonical order was then re-applied in full — address-gaps MN then BK, address-demand, pipeline, storefronts, age-fit apply (fits current on supply 767b28674e30, not re-fitted), supply-ratio, address-access, revenue, export-webmap — leaving `analysis.address` at 281,842 rows and `address_category` at 4,227,630, the non-filtering identity exact at count{ratio>1} = Σn_missing = 531,773 over 190,973 gap addresses, `lead_censored` at MN 10 / BK 11,852, bar-lead gaps 74,114 in 90 clusters and childcare-lead 52 in 1, and the UPDATE-only layers back at 281,842 / 278,909 / 281,835 / 281,840 with `access_run_at` NULL on nobody. The ruling changes the universe of rows and not one number inside it: Σgap_score is byte-identical at 412,007.8556131311, a 1,000-address sample matches the pre-run backup on all eight screen columns with zero differences, and the top-50 cluster list is identical by member addresses with no id relabelled. `analysis.address_demographics` deliberately stays citywide at 767,337 rows — it is the D48 data foundation, along with the raw sources, `poi_dedup`/`poi_supply` and the hex tables, because a POI in Queens is still the nearest pharmacy to an address in Brooklyn; `address_laundry_gaps` is a view and followed on its own. Layers keyed on `analysis.address` rows (the access columns, the transit profile) are rebuilt underneath by address-gaps and must be re-applied in the canonical order. Webmap re-exported (145 files, 83.6 MB) with `meta.ageFit.gapsSupplyHash` 767b28674e30 and `stale []`; suite 857 passed / 1 skipped.

**D79 — Storefront first-seen ledger: every storefront carries the month Loci first saw it, independent of source dates.** *(2026-09-13, owner, GTM-150)*
The owner asked that every storefront carry the month Loci first saw it, because only about 60% of chain locations had a source date at all (D77) — most POIs have none. Built `analysis.poi_presence` + the `analysis.poi_first_seen` view, `loci poi-snapshot --month` and `loci check-presence` (commit 46e50ff, sql/018). Key finding: the dedup `cluster_id` is an input-order artefact, not a stable identity — 0.00% of ids survive a byte-identical row-order shuffle — so the ledger key is instead a content hash of normalized name + rounded lon/lat + category, with the canonical `poi_id` folded in only for the 118 nameless (CJK/Arabic/generic) collisions (0.052%) that the content hash alone cannot separate. Backfill run 2026-09: 227,548 locations, 52.3% carry a real source_date, 47.7% are `backfill_censored`, 0 are `observed`, 100% coverage, and the run is idempotent (re-run mints 0, retires 0). The view deliberately returns a NULL `first_seen_month` for censored rows, so the backfill can never be misreported as a wave of September openings. `chains.brand_location` now keys on the ledger instead of `cluster_id` — two monthly snapshots keyed on `cluster_id` were not comparable to each other, which is exactly the growth-delta problem D77 flagged; `detect` now reports 60% dated-by-source / 0% observed / 40% censored rather than implying a clean count. Rule going forward: a skipped month is a permanent hole (an observation cannot be reconstructed after the fact), and `last_seen_month` falling behind is not evidence of closure. Why this shape and not per-source date columns: sources disagree with each other and most carry no date at all, so one durable ledger at the deduplicated-location grain is the only version that stays comparable across snapshots; consumers read the view, never the table directly.

**D80 — Storefront lifecycle filings and pipeline: stores can be dated from what they must declare to government, for all storefronts not just chains.** *(2026-09-13, owner, GTM-152)*
The owner asked whether stores can be dated from what they must declare to the government, for all storefronts not just chains. Lease signings are private (ACRIS records ~400 leases/yr vs 52,700 deeds; NYS DOS entities are holdcos, not per-store LLCs), so the answer is the filing chain. Stage one (commit 2979cc2): `staging.storefront_filing`, 232,667 filings across 7 feeds × 8 ordered stages (`liquor_application`, `fitout_filing`, `license_application`, `permit_issued`, `sign_permit`, `license_issued`, `liquor_active`, `first_inspection`; `outdoor_dining` reserved), new sources SLA pending (`f8i8-k2gm`), DOB NOW job filings (`w9ak-ipjd`; storefront filter drops 78%: new buildings, 1–3 family, non-GC/sign/PA work), DCWP applications (`ptev-4hud`); DOHMH promoted planned→verified; BBL match 97–99% DOB/DOHMH, 82–84% SLA (no BBL published; PLUTO address / nearest-30 m), DCWP 38% unmatched are out-of-city home-improvement contractors. Stage two (commit 10a33e8): `analysis.storefront_pipeline` at bbl × business_name_key, 135,912 rows (56,571 open / 79,341 not yet), NULL-BBL (24,147) and NULL-name (2,463) rows carried flagged per D75; lead time cross-agency only — fitout_filing → first_inspection strict N=67 median 259 d (p25 178 / p75 378), reconciled N=741 median 221 d; liquor_application → first_inspection N=22 median 73 d; fitout → license_issued N=35 median 183 d; same-agency clocks (DCWP 11 d, SLA pending→active 26 d) excluded because they measure the agency, not the store; reconciliation links 2,613 pairs but 92% are sole-pair-in-BBL so strict is the reference, not the larger reconciled N; ledger gains kind `'gov_filing'` (20,345 locations; censored 108,605 → 91,031) and only `opened_on` may set first-seen, never an application date; `openings_pipeline_400m` / `openings_recent_400m` on address×category as context only (real for restaurant, bar, cafe_bakery, grocery, pharmacy; structurally zero for the other 10 — must never be read as nothing is coming, it is a filing-blind spot); Gowanus core mean restaurant pipeline 0.27 vs East Village 1.78; category mapping is package data (20.6% of filings mapped; DOB descriptions are free text); top brands by government pipeline are wireless carriers, ecoATM, SP+ parking, GameStop, Home Depot, Family Dollar, Staples — i.e. the filings see national retail the press list does not, and vice versa; bugs: `brand_key(NaN)` fixed, `dohmh.classify("Steakhouse")` → `cafe_bakery` pinned as a known bug, not yet fixed (GTM-152). Why this shape: one filing-event staging table plus one pipeline grain so any consumer (ledger, address screen, chains) reads the same lifecycle; lead times reported strict and reconciled side by side so the number cannot outrun its evidence.

**D81 — Site-revenue model v0 shipped: CEX spend pool × fitted capture share, leakage calibrated to Economic Census 2022 county receipts, gated by a leave-one-ZIP-out backtest and a cross-category placebo; restaurant alone passes and now grades C on the recommendation card; nine categories honestly "not modelled".** *(2026-09-13, owner, GTM-150)*
Owner: "would love real data but can we try to predict what a business could make? how could we do that?" then "go ahead, start the build." Anchors verified before designing (Sonnet probe, scripts in the session scratchpad): Economic Census 2022 (api ecnbasic) publishes receipts and establishments by county × 6-digit NAICS — the finest real revenue truth anywhere public (Kings 2022 revenue per establishment: coin laundry $260,818, restaurant $925,896, bar $804,765, café $675,905, convenience $662,354, hair $190,777, nails $182,953, grocery $2,862,292); NAICS 446110 pharmacy is absent from the 2022 vintage at every geography, so EC 2017 ($4,332,756) is used and flagged; CBP/ZBP carry no revenue, and ZIP grain exposes establishments by employment-size band only (the standalone zbp dataset ended in 2018; 2023 ZIP data lives under cbp); CEX gives national spend by income quintile (Table 1101) and a flat NY-metro rebase (Table 3004), no table crosses metro × quintile; NYS DTF taxable sales reports all five boroughs as one row at 4-digit NAICS — unusable for Kings; a circulating $504k/laundromat trade benchmark is inconsistent with its own totals and 2.5× the census figure, so trade benchmarks are not trusted without a census check. Model (data-scientist, Opus; src/loci/model/revenue.py with every equation in the docstring, revenue.yaml spec, revenue_calibration.yaml generated by the gate): spend pool = households within 400/800 m × CEX spend by the tract's income quintile; capture share Huff-style with distance decay β and competition elasticity γ fitted inside every leave-one-ZIP-out fold with the sign free — the textbook negative sign was WRONG for 7 of 10 categories at ZIP grain (incumbents proxy "is this a retail street"), a result, not a bug, disclosed in the YAML with the grid widened after an in-sample ablation; leakage λ calibrated per county so predicted revenue of existing establishments sums exactly to EC receipts (λ_Kings vs λ_NY disagree 1.4–2.6× in every category — no single NYC constant; both carried). Gate: out-of-sample Spearman against CBP employees per establishment must beat a county-average baseline AND a homes-only baseline AND a cross-category placebo (another category's model must not predict this category's size as well as its own); `loci revenue fit` writes the calibration only for passing categories; `loci revenue --boroughs MN,BK` is the idempotent UPDATE of revenue_p25/p50/p75, rent_ceiling (× benchmarks.yaml occupancy ratio), revenue_model_version on analysis.address_category and is the last step of the canonical re-apply order (a no-op for unmodelled categories); recommend.py section 6 grades a modelled category C ("modelled, uncalibrated to local P&Ls"), never higher, so "act" still needs real comps. Result on the MN+BK-only table after D75/D78 (281,842 addresses, supply_hash 767b28674e30, home_rows_citywide stamped so a universe change can never move a calibration invisibly): RESTAURANT alone passes — ρ_oos 0.763, R² 0.577, β 0.50, γ −0.25 (agglomerative, stable in 65 of 73 folds), placebo pass (own 0.80 vs best rival café 0.79); café, grocery, hair, nails and pharmacy beat both baselines but FAIL the placebo — they measure commercial intensity, not category economics — and ship as "not modelled"; laundry, convenience, bar fail the baselines; fitness has 14 ZIPs < 15. Verdicts were identical before and after the rebuild; λ moved (pharmacy Kings −40%, convenience −45% with a sign flip). A σ-band bug was fixed between runs (fold-frequency-weighted RMS, restaurant σ 0.98 → 0.62, halving a spurious 4× band). homes_400m recomputed by this sweep matches D73's stored column on 100% of addresses — two independent sweeps, same answer. Gowanus core (1,831 addresses, median 3,646 homes within 400 m, 14,169 within 800 m): restaurant revenue p25/p50/p75 $1,077,090 / $1,673,663 / $2,657,475 = 1.81× the Kings average, driven by 39 incumbents at γ < 0 — a corridor reading, not an operator forecast; rent ceiling $133,893/yr = $11,158/mo at the generic 8% occupancy ratio vs the one local comp at $7,313/mo (1.53×); docs/recommendations/gowanus-core-2026-09-13.md regenerated with restaurant at C. Caveats the model cannot escape: county anchors blur Park Slope with Gowanus; CEX quintiles are national; the MN+BK-only universe drops real Queens/Bronx households from edge sheds; λ is right by construction and has NO out-of-sample test; the backtest target is establishment size, a revenue proxy only if revenue per employee is flat within category; p25/p75 is a parameter band, not outcome dispersion; a typical operator at a site, nothing about concept quality. 62 tests; suite 884 passed / 1 skipped / 1 lock-induced failure during the concurrent rebuild. Commits efae213 (code; the CLI block and ALTERs were carried in the concurrent session's c9059d8) and d7387dd (calibration + card).

**D82 — Neighborhood character: retail vs corporate vs industrial vs residential.** *(2026-09-13, owner, GTM-154)*
Owner asked to colour neighborhoods by whether they are retail- or corporate-dominated. Built at the address grain (never hex): PLUTO floor areas within 400 m network over ALL lots (`homes_400m`'s residential-only lot set would have called Midtown residential), LODES 2023 jobs split retail-facing (CNS07/17/18/19) vs desk-facing (CNS09–14/20) vs other (sector columns sum exactly to `jobs_400m`), commercial zoning overlays (C1-/C2-, C4–C8) within 100 m, transit AM/PM share as independent corroboration (median corporate 0.31, retail 0.92, industrial 1.08, residential 1.70). First pass (55e2116) passed the CBD sanity list but the urban-planner rejected it: Brighton Beach Ave, Cortelyou, Myrtle Ave, 7th Ave, Graham Ave, Broadway Hamilton Hts, Dyckman, Fulton St, Church Ave, Kings Hwy, 5th Ave Bay Ridge and Pitkin Ave all read ~0 retail_mixed because prewar ground-floor retail is not PLUTO RetailArea and small-shop strips have few payroll jobs; Bay Ridge VA and East Flatbush hospitals read corporate; parks and cemeteries got labels. Revision (1f115d6): zoning-overlay OR-route (≥ 20 commercially zoned lots within 100 m — executive's choice: ≥1 gave BK 52%, floor/share alone 20.5%, ≥20 gives 35%, every corridor flips at every setting, so the threshold buys discrimination not coverage), RETAIL_JOBS_FLOOR 1,000 → 300 and share 0.40 → 0.35, institutional lots (BldgClass I/M/P/W, LandUse 08) excluded from office area and jobs ≥ 5,000 required on both corporate routes, labels suppressed for parks/cemeteries/NTAs < 50 addresses (2,744 addresses; Stuyvesant Town is a casualty), continuous retail_index for the map, character_copy: corporate → "weekday-office catchment" with the caveat that corporate addresses have MORE homes within 400 m (median 5,682 vs 2,284) so daily-needs there are limited by rent, loading and ground-floor supply, not absent customers. Final mix: BK 34.7% retail_mixed / 62.2% residential / 1.9% industrial / 0.2% corporate; MN 58.2 / 25.6 / 0.0 / 15.5. Map (e99c808): single-hue sequential ramp on retail_index with quantile stops, corporate/industrial hatched overlays, suppressed NTAs as not-measured grey, never the pale end of the ramp. Ruling: character is map colour and card context only; it enters no grade. Caveat: LODES codes jobs at the payroll address (Brighton Beach 5,106 median jobs are home-health offices) — jobs_400m is suspect where jobs_other_share is high and homes ordinary. Follow-up ticketed GTM-154: retail_index saturation, the 20-lot threshold against a held-out corridor list, and character copy on the `loci recommend` card.

**D83 — Clusters are ranked by walk-shed density, not by capped units, per the owner's 2026-09-13 ruling.** *(2026-09-13, owner, GTM-155)*
The owner said "rank by density" and confirmed the reading: households per km² inside the walk-shed. What it replaces was never a decision — every cluster list in this project had been sorted by Σ`units_capped` since D39 capped mega-lots, because that was the first plausible weight to hand, and it silently ranked by SIZE, so forty low-rise blocks outranked four towers. The definition is `density_400m` = `homes_400m` ÷ `walkshed_km2_400m` at address grain, both written by the same `loci supply-ratio` sweep off the same Dijkstra rows so numerator and denominator can never be measured over different reachable sets. The walk-shed's area is the convex hull of the graph nodes within 400 m network metres, projected in EPSG:32618 and floored at a 50 m disc: NOT the nominal π·0.4² disc, because dividing by a constant ranks by `homes_400m` under a new name, and convex rather than concave because a concave hull needs an alpha knob that silently moves every area while the convex hull's error runs conservative — it spans water, parks and rail cuts, overstating the shed and understating the density. The measured shed's median is 0.267 km², 0.53× the nominal disc, and the density distribution is MN p10/p50/p90 14,418 / 22,810 / 39,825 against BK 4,218 / 8,444 / 14,020 units per km², carried everywhere with the caveat that this is a PLUTO `UnitsRes` register count with no margin of error and not an ACS household density. A cluster's density is the `units_capped`-weighted MEDIAN of its members' own, never Σhomes/Σarea, because members sit within 200 m of each other so their sheds overlap almost entirely and the pooled ratio counts the same homes and the same land once per member; the unweighted mean ships beside it so skew is visible. The ordering moved almost everything — top-50 Jaccard 0.087 against the old list, borough mix flipping from 32 Brooklyn clusters to 44 Manhattan ones, median cluster size from 912 addresses to 13.5 — while the gap set and `gap_score` are byte-identical by checksum, because this changes an ORDER and nothing else; `--rank-by units` keeps the old order selectable, `loci clusters` lists the ranked table, and the webmap's `meta.rankBy`, `clusters.json` and the NTA index carry the ordering with the caveat. The one open edge is that density ranks a one-address cluster against a 700-address one on equal terms, so nine of the top fifty are single doorways; `loci clusters --min-addresses N` exists for it and the floor is deliberately left unset, as the owner's to choose. Suite 1,044 passed / 1 deliberate skip.

**D84 — The screen now samples TWO frames: the residential tax lot and the street midpoint.** *(2026-09-13, owner, GTM-156)*
The owner's direction was literal — "for addresses, we should be sampling an address near the middle of every known street in the borough" — and the reading confirmed in docs/street_midpoint_frame.md is one scored point per intersection-to-intersection segment, scored by exactly the same engine as an address. The lot frame was built FROM RESIDENTS (D38), so a street nobody lives on yet was not low-scoring, it was absent: the Navy Yard, the Red Hook and East Williamsburg industrial blocks, Spring Creek. The source is NYC Street Centerline `inkn-q76z`, weekly; the keep rule `status='2' AND rw_type=1 AND nonped<>'V' AND at-grade` keeps 32,291 of 41,784 MN+BK segments (3,480.9 km), and the ladder plus a ±10% count guard print on every rebuild so the filter is auditable rather than asserted. L = 100 m — the smallest round value at or above the 81.9 m median block face, so a street point stands for at most one block face, the same spatial unit a PLUTO lot frontage occupies; splitting per geometry PART (not per segment) yields 50,199 points, MN 13,472 / BK 36,727. It is a `frame` column on `analysis.address`, not a sibling table, because a street midpoint is not a new grain (D61): within 50 m of a lot it reproduces that lot's lead 93.1% of the time at a median gap_score difference of 0.000, and a sibling table would fork every UPDATE-only annotation layer into two code paths; `loci street-frame` writes `data/interim/street_points.parquet` and `address-gaps` consumes it, delete-then-inserting BOTH frames in one write under one provenance stamp (`--no-street-frame` reproduces the pre-D84 screen). Clustering is WITHIN frame, ids namespaced `BK:bar:S12`, because clustering the union moved 429 of the 571 existing lot clusters and net-MERGED 12 — a point with zero residents becoming the bridge that makes two markets look like one is a dedup bug wearing a new costume. The non-filtering proof was re-run as D57/D61 ran it: every lot row's gap_score, lead, n_missing, cluster_id and homes_400m are byte-identical by md5 before and after, Σunits is unchanged at 2,074,244, all 571 lot clusters keep their id and membership, and reach×0.8 removes zero pairs in either frame. 4,874 street points (9.7%) have no lot within 100 m and are a different population, not more of the same — median gap_score 2.18 against the lot frame's 1.27, 6.33 missing categories against 1.89, 55% of them led by laundry — and they cluster into 586 street-only clusters, 369 with five or more members. The censored ones need no gate: 28 of those clusters have homes_400m 0 and therefore density 0, so D83's density ranking sorts Randall's Island, Floyd Bennett Field and Governors Island to the bottom for a stated reason rather than letting their `CAP_M/reach(laundry)` = 7.50 top an unfiltered list — reintroducing a gate would have been D75 in reverse. What stays pinned to `frame='lot'`: the supply baseline FIT universe (n unchanged at 281,842 — the apply is not pinned, a street point gets a real ratio against the lot-frame norm), the revenue calibration, leave-one-ZIP-out backtest AND apply (a units-0 point is not an observation of realised receipts; street revenue_* stay NULL, which is this model's word for "not modelled"), `loci recommend`'s area medians, validation/sample.py's income deciles, conveniences' per-address distribution, the webmap's neighbourhood roll-up, and — found during the build — the NTA character suppression denominator and roll-up, which would otherwise have moved every published D82 share with no building having changed. Operational: `transit-profile` now needs `--re-sweep` after any frame change (its own consistency check caught the stale entrance set), and `supply_baseline.yaml`'s `universe` string still reads "all addresses" until the next sanctioned `--fit-baseline` (its stored n = 281,842 already proves the lot universe). Webmap: street dots render hollow behind a default-off toggle, `clusters.json` carries frame (571 lot / 586 street). 33 tests; suite 1,137 passed / 1 deliberate skip.

**D85 — NYC DOT data: bi-annual counts as a source, camera registry, sidewalk counts.** *(2026-09-13, owner, GTM-157)*
Owner: "we need NYC DOT data, both the bi-annual and the camera data." Built: `staging.dot_pedestrian_count` (114 points × 37 rounds 2007-05…2026-05 × am/md/pm = 12,312 rows, exact vs the raw wide file; rounds are irregular — May 2019 only, Oct 2020 only, June 2024 — so trends use decimal years); `validate-pedestrian` now reads the table; registry role validation → control. Top on-street points 2026-05: W 34 St @ Broadway 37,109/round, Eighth Av @ W 44 35,488, Seventh Av @ W 32 34,465; Roosevelt Av @ Main St is the only riser in the top 10 (+121/yr); Midtown's negative 10-year slopes are largely the unrecovered 2020 level. `staging.dot_camera`: webcams.nyctmc.org/api/cameras verified — 969 cameras (MN 376, QN 208, BK 204, SI 100, BX 81), no auth, 352×240 JPEG ~23 KB, near-live (9 fetches 5 s apart → 9 different frames; 0 duplicates in 183 fetches at 10 s), `isOnline` is the string "true" on all 969 so it means published, not live. Per-address context on `analysis.address` (dot_point_id/m, latest am/md/pm, camera_id/m): straight-line EPSG:32618, a lower bound on the walk, never comparable to the network `*_400m` columns; median distance to the nearest count point MN 739 m / BK 1,416 m, to the nearest camera MN 234 m / BK 732 m; within 400 m of a camera MN 79.9% / BK 22.1%; camera density is DOT's arterial geography, not footfall. `loci sidewalk-count sample|schedule|stats|validate`: onnxruntime + YOLO11n (COCO person), `.pt` from ultralytics assets v8.3.0 sha256-pinned, exported to ONNX once (ultralytics publishes no `.onnx`), 0.027 s/frame CPU on the M4; counts only, frames never stored unless `--keep-frames` (`data/frames` gitignored; public imagery of identifiable people stays on the machine); `MAX_FRAMES_PER_RUN=720`; duplicate-frame skip makes a frozen camera one observation. First samples 2026-09-13 18:54–19:05, 61 frames each: 7 Ave @ 9 St pm_peak mean 0.94 / evening 1.12; Grand Army Plaza 1.91 / 1.82; 5 Ave @ 50 St 2.71 / 0.87 — Fifth Avenue busiest at 6 pm and empties after 7. `validate`: 15 cameras within 60 m of 14 count points, bridge midpoints excluded, per-counted-hour footing, N=0 today (Sunday vs weekday counts). Ruling: sidewalk counts are a shortlist-verification instrument (stock, not flow; per-camera field of view uncorrected; compare a camera with itself first), never a screen-wide layer; DOT counts are context and calibration. Why the shape: counts and cameras are new grains (tables); per-address context is columns per D61; the detector runs locally so no image ever leaves the machine.

**Addendum (2026-09-13, commit 089f1fe):** the DOT layer shipped on the webmap — count points sized by latest round with a trend arrow and a COVID-recovery caveat in the popup, and all 969 NYCTMC cameras with per-camera sidewalk-count summaries where sampled, off by default alongside the other context layers. The exported `webmap/data/dot.json` carried bare `NaN` tokens (from rounds with no comparable prior count for a trend) that `JSON.parse` rejects outright, silently breaking the whole map bundle rather than just the trend arrow; fixed by writing `null` for the incomparable case, with a test pinning the export to valid JSON so the failure mode cannot return unnoticed.

**D86 — Paid-data wishlist as registry entries.** *(2026-09-13, owner, GTM-159)*
Owner asked for a list of paid sources to integrate post-raise. Encoded as 35 `status: wishlist` registry entries (config with the code that reads it) with required cost/price_tier/price_note/license_note/closes_gap/priority/evidence/cost_basis, exempt from the $100 budget and the CONTEXT §3 mirror, mirrored instead by generated docs/PAID-SOURCES.md with a byte-identical drift check in `check-sources` (commit 9488a6d). Booked $301,565/yr (tier floors $213,200; verified subset $86,457); P1 $74,300: Advan Patterns Plus, Placer ($8k verified public order), SafeGraph Spend, CompStak, Google Places Insights, Data Axle API, LiveXYZ, Google Places Pro/Aggregate, Esri BA ($5,200 verified). Findings: license terms not price are binding — Dewey/Advan academic-only at any price, Claritas no derivatives, Placer no redistribution, Data Axle/Esri internal-use by default — so riders are a line item, not a rounding error; Google Places Insights holds monthly POI snapshots back to 2024-01 and could un-censor the 47.7% `backfill_censored` rows in the first-seen ledger (D79). Two non-purchases encoded as planned: NYC DOT VivaCity sensors (~100 sites incl. residential, announced 2026-06-04 — the ≥100 non-corridor points GTM-147 requires; a data-sharing ask, not a purchase) and Strava Metro (Loci does not qualify outside an agency contract). Why this shape: a wishlist that is not machine-checked rots; the registry already carries cost and bias per entry, so the wishlist is the same object with a status, not a new document.

**D87 — GTM strategy memo.** *(2026-09-13, owner, GTM-158/GTM-159)*
Owner asked for pricing, buyers, competitors, value prop, and whether anyone sells the model or the dataset. Investor-lens draft, then a contrarian red-team — five of seven attacks failed and were accepted — final at docs/GTM.md (commit 0d1012c), mirrored to the LOCI Notion page. Verified: Placer $31k signed public contract (civic band $8–27.5k), Buxton $35k model build + $15k/yr SCOUT, Esri BA $3,908–$8,367 list; Esri Retail MarketPlace discontinued (2017 data on 2010 geography, no replacement) so the industry's canonical retail-gap metric is unreplaced; NYC SBS already sources its 24,900-storefront count from LiveXYZ. Nobody sells address-grain city-wide daily-needs gap logic or the filings lifecycle; nearest substitutes are Placer void analysis (chain-level) and LiveXYZ (census). Rulings: ICP order brokers → lenders/feasibility shops (subcontracted $500–1,500 input into their $4,900 SBA study) → BID/SBS → 3–30-unit operators; chains are logos and the watchlist a lead list; the lead SKU is the $20k/quarter retainer (the only demonstrated willingness to pay); per-seat licence anchored on Reonomy $4,800 / GrowthFactor $2,400; the value prop is cost of search, not failure rates, and no decision value is claimed until the retrodiction test runs (GTM-158, Urgent — QUESTIONS T11). Moat is BBL reconciliation + operator, months not years, not a data monopoly; narrative is "consulting-funded data company"; sub-$1M NYC TAM makes the raise a bet on city #2 (E8). The memo carries the contrarian's three "we will not claim" sentences verbatim, and its pre-spend actions (license riders, a Places Insights trial, the VivaCity ask) are folded into D86's wishlist ticket (GTM-159).

**D91 — Site-revenue model v0.2: pool elasticity chosen by the gate (restaurant ε = 0.6; the unconstrained fit was degenerate at ε = 0), leakage anchored to the CBP size-band median instead of the EC mean, capacity ceiling from PLUTO retail area, site-level competition multiplier capped at 1.0; restaurant skill rises to ρ 0.82 and every level falls 3–5×; the v0.2 number is a floor for above-typical operators.** *(2026-09-14, owner, GTM-160)*

Owner, after the v0 model (D81) overshot Lion's Milk at 104 Roebling: "why would we send him something we know is wrong" → "build the fix. unless there is a difference in avg price, I wouldn't expect more than a modest difference between different cafés." Causes of the overshoot, ranked and quantified: revenue scaling linearly with the local spend pool (elasticity 1 by construction; the "scaled to your block" row multiplied the borough average by 2.6); λ anchored to the Economic Census MEAN receipts, which large operators pull up (Kings cafés mean $675,905 vs CBP band median $575,589; restaurants $925,896 vs $262,924); no capacity term; survivorship in the anchor; the ZIP-level agglomeration sign (γ < 0) applied at an address, turning 28 neighbouring cafés into a ×6.7 multiplier. Built (data-scientist, Opus; src/loci/model/revenue.py v0.2, v0 reproducible behind model_version; sql/025_revenue_capacity.sql): (1) ε per category on a 0–1 grid refit inside every leave-one-ZIP-out fold jointly with β, γ and a price-index exponent δ — the unconstrained argmax was DEGENERATE, ε = 0 in seven of nine categories, deleting the spend pool and leaving λ·(incumbent density)^−γ, i.e. retail predicting retail, the rejected D1 thesis; the cross-category placebo caught it (every category's model predicted every other's employment equally well). So ε is chosen by the gate: refit at each ε, ship the smallest that passes, floored at 0.3 (the owner's range and published elasticities). Restaurant passes at 0.6/0.7/0.8, ships 0.6 (placebo margin crosses zero between 0.5 and 0.6) — the backtest refuses the owner's 0.3–0.5. δ adds no skill for restaurant (ships 0); a little for bar, convenience, grocery, nails, pharmacy, which still fail. (2) λ calibrated so the predicted county MEDIAN matches the CBP size-band median × EC revenue per employee; λ_median/λ_mean restaurant 0.31 Kings / 0.77 NY. (3) revenue_cap_p50 = PLUTO retailarea (÷ the Storefront Registry count when a lot files several storefronts) × a per-category $/sq ft band; p50 = min(model, cap); capacity_bound flag; binds on 62% of MN lot rows, 1% Gowanus core, 0.2% BK. (4) γ stays free in the fit (its ranking skill) but the site-level multiplier is capped at 1.0 — neighbours may not raise a site above corridor-neutral; the ZIP-level sign does not transfer to an address. Two bugs fixed on the way: λ had been fitted on the uncapped share and applied to the capped one (Manhattan median restaurant $130k against a $1.67M anchor); numpy nan**0 == 1 gave the ε = 0 candidate a larger evaluation sample than every other. Result: gate unchanged — restaurant alone passes (ρ_oos 0.763 → 0.819, R² 0.68, beats county-average 0.573 and homes-only 0.629, placebo own 0.823 vs bar 0.818); nine categories fail in both versions. Levels: 104 Roebling restaurant p50 $2,494,887 → $494,500 (cap $700,000, not bound); 376 Graham (El Punto Cubano Express, confirmed by the owner's contact as the former Lion's Milk owner's new restaurant, opened ~2026-07) $1,282,676 → $369,429 (688 sq ft demise); Gowanus core restaurant $1,673,663 → $453,650, rent ceiling $11,158 → $3,024/mo, grade stays C. Decomposition: the median anchor is the largest correction (0.41× alone); capacity 0.28×/0.38× alone but binds neither p50 once the others land; ε 0.80×/0.97×; γ cap 0.81×/0.97× net. Standing caveat, now on the owner-facing pages: the median anchor describes a two-to-three-person shop, so for a 1,000 sq ft café running 62 hours with a full crew the v0.2 number is a FLOOR, not an estimate; the size term the model lacks is headcount, which only an operator's answer supplies. Pre-registration record for Lion's Milk (docs/recommendations/predictions/lions-milk-2026-09-13.md) now holds, all timestamped before any figure from the owner: raw v0 model $1.29M–$2.42M; judgment $650k–$1.25M central $900k; bottom-up menu × daypart traffic $1.35M–$1.69M (traffic is the exposure; the menu has no breakfast plates); cost-side read $933k–$1.04M (break-even $739k, $906k with a $90k owner draw; rent the least sensitive line; tip credit worth $62k–$74k); rent $8k–$12k/mo; v0.2 model $400k–$574k; owner evidence: "made more money during COVID" (consistent with a labour-fixed-cost-bound café). 376 Graham memo: block side-by-side with Roebling (same 4,000 homes, $142k vs $213k income, commuter-origin stop with a resident evening, weekends weak), levers ranked (resident evening, delivery to the 800 m ring, beer-and-wine not a bar, no café line, small fixed base), pre-registered bottom-up $871k–$1.16M settled / $653k–$867k ramp, rent $3.5k–$6.5k. Two owner-facing artifact pages published (Lion's Milk, El Punto). Suite 1,221 passed / 1 skipped; commit 3d2a936. Dedup defect ticketed as GTM-153 (a café with a DOHMH permit counted twice; category-assignment rule before per-category dedup).

**D88 — Retrodiction: the screen ranks retail streets; no decision value claimed.** *(2026-09-14, owner, GTM-158)*

Owner: "run the retrodiction." Design and results exactly as in docs/retrodiction-2026-09.md and the statistician review: cohort 12,572 dated openings 2023–24 MN+BK (81.5% food; dates are record-creation/licence dates, D80 lead 221–259 d); business-level survival NOT identified (every source is a current-state extract; Foursquare fetched open-only; pipeline is_open ⇔ ever opened; KM/Cox refused against a 48-event floor); entry retrodiction — screen frozen 2023-01-01 from the first-seen ledger, 12,000 lot addresses × 6 categories, logit with category FE, NTA-clustered SEs, NTA-blocked folds: AUC 0.866 [0.851, 0.881] vs 0.854 for the same model without the score (the honest lift +0.013; the +0.048 vs homes-only was a straw man the permutation null itself exposed), spatially structured placebo p95 0.8545, CD-blocked +0.014, calibration monotone; sign: log(1+supply_ratio_t0) +1.54 pooled, positive in every category, +1.17 [0.87, 1.47] after NTA fixed effects and other-category supply — own-category thickness predicts own-category entry within a neighborhood net of general density, i.e. agglomeration or herding, not unmet demand; restaurant own-category-gap −0.89 [−1.44, −0.33] survives Bonferroni (no restaurant within 400 m in 2023 → less likely to get one); LL157 go-dark (premises occupied 2022-12-31 → vacant 2024-12-31; strict n=12,713 / 1,074 events; attrition-as-event 16,163 / 4,524): AUC 0.549 vs 0.535, 0.544 vs 0.539; the score's sign flips with the outcome definition (+0.42 / −0.20) — a null; Foursquare re-pull without the open-only filter (b340d91): 228,455 NYC closures, 13,643 ledger rows gain closed_on, cohort 109 events, implied S(24) 0.994 vs a true ~0.75–0.80 → ~3% ascertainment, categorically non-random, excluded from the hazard gate; 54,190 pre-ledger closures are a future panel. Ruling — record the statistician's sentence verbatim: "The frozen 2023-01-01 screen predicts where 2023-24 openings landed out of sample (NTA-blocked AUC 0.866 vs 0.854 for the same model without the score, and above a spatially structured placebo at p95 0.8545), with a positive supply coefficient that survives NTA fixed effects and conditioning on other-category density (+1.17 [0.87, 1.47]) — so the screen ranks retail streets, not unserved demand, and Loci may claim cost of search only; decision value remains unclaimed because the one survival-adjacent outcome we can test returns a null and the business-level outcome remains untested rather than absent." Consequence: docs/GTM.md's value proposition stays cost-of-search; the recommend card's economics grades are unaffected; D1's reverse-causality worry is now measured, not assumed. Why the shape: the survival test was refused rather than printed because a 100%-survivor cohort would have produced S(24)=1.000 — the extract, not New York.

**D89 — Recommendations ledger: track time-to-fill and fit.** *(2026-09-14, owner, GTM-162)*

Owner: "start to track recommendations so that we can see how long it takes for the free market to fill those gaps and if they do it well (with our proposed solution)." Built (1c659a5, sql/026): analysis.recommendation (append-only by code invariant, card_hash idempotency; issued_on, anchor, category, proposed_solution, format_hint, grade, supply ratio / homes / gap score at issue, status open|filled|withdrawn|expired), analysis.recommendation_outcome per monthly snapshot (match kind opened|in_pipeline|none from the first-seen ledger and the filings pipeline, same category, 400 m straight-line, first-seen ≥ issued_on; days_to_fill; solution_match_score under a documented rubric — category 0.50 required, format 0.20, corroboration 0.15, independent 0.10, still_open 0.05; unavailable ≠ zero), views recommendation_latest and category summary (share still open at 12 months NULL until exposed); `loci recommend --record`, `loci recommendations backfill|check|report`; make chains-refresh ends with the check (order load-bearing). Backfilled honest history: D73 laundry lead issued 2026-09-10, withdrawn 2026-09-11 (0.94×); D74 Gowanus card as issued 2026-09-11 (13 D / 2 C — hardware and tailor C; restaurant recorded D because its C came after D81 and is not back-dated); the three D73 thin leads with solution and format hint. First check 2026-09: 15 rows all 'none' — the instrument, not the market (ledger left-censored before 2026-10; pipeline entries pre-dating issue excluded as the anti-reverse-causality choice). Owner decisions pending (GTM-162): radius, expiry, re-issue policy, rubric weights, pre-existing build-outs as context. Why the shape: a recommendation is a new grain (table), outcomes are monthly observations that persist ('none' is stored, not skipped), and honesty requires the first rows to be the cards actually issued.

**D90 — GTM-48 coverage validation on the address frame: P3 survives at 7.6% [6.5–8.8], no income gradient, and fitness's headline hole rate was a Google type-map defect — owner rules on all three follow-ups plus cross-category dedup.** *(2026-09-14, owner, GTM-48/GTM-112/GTM-173/GTM-174/GTM-175)*

GTM-48 ran on the address frame (docs/coverage-validation-2026-09.md is the source of truth for every number here — read it, cite it): 5,633 address×category rows (5,522 planned + 111 kept-and-flagged from a killed first attempt), seed 20260902, 20 per stratum, 649 m radius (D53), 14 mapped categories (clinic stays unmapped, D30); 5,985 calls ≈ $190, ledger 8,963/9,113 after the run (budget raised in `.env` from 5,000 to cover it). Design-weighted, respecting the 137 sampling strata: **7.6% [6.5–8.8] of MN+BK MISSING flags are coverage holes (~40k of 523k)** — the naive equal-weight pooled estimate of 10.5% was wrong, an artifact of pooling rows across strata of very different size, and the statistician's review (Opus) caught it along with two other defects in the first pass: row-level rather than stratum-level inference, and the wrong Google-type width for fitness. Income gradient is **not** distinguishable from zero once errors respect the 137 strata (stratum-clustered p=0.087, tract-clustered p=0.104, permutation p=0.141); only hardware shows a category-level income contrast surviving Benjamini-Hochberg correction. Holes track anchor absence, not income: anchored categories sit at ≤2%; tailor_repair 37.5% (a floor — no anchor exists for it); hair_barber 34.5% without beauty_salon in scope (44.5% with it); hardware 12.0% (a floor). **Fitness's apparent 35.6% hole rate was entirely sports_club and marina Google returns (yacht clubs, tennis clubs) — 0 of 180 sampled gym hits — a Google type-map defect inherited from D50/M6, not missing supply**; fitness is restored to the other anchored categories' range once GOOGLE_TYPES drops those two types.

Owner ruled 2026-09-14, "yes to all 4": **(1)** demote tailor_repair and hair_barber from headline coverage claims on the D30 precedent (the same treatment clinic got when its Google type could not validate it) — RULED, not pending; **(2)** adopt the numeric G9 Wilson-upper coverage-grade ladder — Wilson upper bound ≤10% → A, ≤25% → B, else → C — which on these rates grades grocery and hardware B and hair_barber/tailor_repair/clinic C, everything else A (QUESTIONS D22, GTM-174) — RULED; **(3)** remove sports_club and marina from fitness's GOOGLE_TYPES, validator-only — it changes what the validator counts as a fitness hit, not loci's own fitness category or its POI adapters (QUESTIONS M14, GTM-173) — RULED; **(4)** run cross-category name+distance dedup before the existing per-category dedup, GTM-153 proper — the fix for the Lion's Milk defect flagged inline in D91 (a DOHMH restaurant and an Overture cafe_bakery listing 11 m apart, both open, both counted) (QUESTIONS M15, GTM-175) — RULED; implementation of all four and the resulting supply re-fit/re-run are recorded in a later session-log entry, not this one. **Two items remain genuinely open, not part of this ruling:** whether unresolved co-located POI pairs (2,527 groups / 7,597 POIs; collapsing would remove 4,270 more; default OFF) should collapse or count as-is (QUESTIONS D24), and the three `categories.yaml` anchor-vs-adapter contradictions from Next-actions item 21 (bank NAICS 522110 vs credit unions; hardware 444140 vs home centers; clinic) — these concern the ZBP anchor measure, not the Google calls, and stay unresolved.

Also found and ticketed: the validator's `est_cost` printout ignores calls already spent on the ledger — it printed an estimated $16.70 for this run when the true incremental cost, given the ledger's prior state, was closer to $177, a regression of the same class of bug GTM-127 fixed once already for a different validator estimate (GTM-171).

Also this session: **GTM-112's category-expansion fail-closed checklist shipped** (commit 766d9a2, docs/CATEGORY-EXPANSION.md — 12 gates with cited thresholds, a dentist dry-run — plus tests/test_category_registry.py). Its findings are folded in here because they bear on the same coverage-grade question: G9 had no numeric threshold before ruling (2) above; clinic can never reach coverage grade A because it has no Google type (D30); `loci check-categories` does not exist yet; the `supply.py` anchor-fallback failing OPEN (rather than closed) on a missing anchor is deliberate, and the checklist records the refusal as a requirement rather than an oversight.

Why the shape: this is the P3 validation the charter's acceptance criteria gate on (CONTEXT.md §1.4) — it had to be re-done once the statistician found the pooling and inference defects, and the ruling on its three follow-ups plus the independently-flagged dedup defect belonged in one sitting rather than three.

### 2026-09-14 — Session 24: retrodiction verdict (D88), statistician review, LL157 go-dark, Foursquare closures, recommendations ledger (D89)
- Retrodiction run and reported (docs/retrodiction-2026-09.md, commit 1c659a5): entry test AUC 0.866 vs 0.854 no-score, sign positive (screen ranks retail streets, not unmet demand); LL157 go-dark test added after statistician review corrected the survival non-identification finding — null result, sign flips with the attrition definition; Foursquare closure filter removed and re-pulled (commit b340d91) — 228,455 NYC closures, closed_on lands on 13,643 ledger rows, ~3% ascertainment, excluded from any hazard gate.
- Statistician review (scratchpad statistician_retrodiction.md): corrected the headline comparator from homes-only (+0.048) to the honest no-score refit (+0.0126); added NTA-FE and other-category-supply conditioning (+1.17 [0.87, 1.47]); flagged the LL157 rejection as wrong and specified the go-dark test that unlocked a real (if null) survival-adjacent outcome.
- Recommendations ledger (D89, commit 1c659a5): analysis.recommendation + recommendation_outcome shipped with the monthly fill-check rubric; first check all 'none' by construction (left-censored before 2026-10).
- Linear: GTM-158 moved to Done with the verdict comment; GTM-161 (survival ascertainment) and GTM-162 (recommendations owner decisions) opened, both E2 · Access Engine.
- Coordination: three peer sessions active this window — D90 reserved by abenmayor-cf (GTM-157 camera sampling), D91 shipped by abenmayor-84 (site-revenue model v0.2).
- Commits: 1c659a5 (retrodiction + recommendations ledger), b340d91 (corrections + go-dark + closures).

**D92 — Four layers: modeled, realized, scored, learned — and the forecast ledger.** *(2026-09-14, owner, GTM-163)*

Owner framing: "predicted/modeled (not what the world reflects but what it could) vs realized/actual." Ruling: the warehouse and the docs name four layers — MODELED (the screen: gap score, supply ratio, demand proxies, character, recommendations, forecasts), REALIZED (the first-seen ledger, closures, filings lifecycle, chain snapshots, DOT counts, camera frames), SCORED (retrodiction, recommendation fill checks, forecast outcomes — where modeled meets realized on a schedule), LEARNED (FINDINGS.md, carrying capacity, portability, planner verdicts — what holds, what travels). Built (e578470): analysis.forecast / forecast_run / forecast_outcome + views (sql/028), model 0.1.0+f7d190df, vintages 2023-01 and 2026-09 (4,227,630 rows each), 2023-01 scored at 12 months AUC 0.899 vs 0.860 no-score, Brier 0.095, pooled calibration gap 0.12 hiding per-category level errors; own_gap_flag −0.71 [−0.98, −0.45]; no NTA surprise survives Bonferroni; the map's Off/Modeled/Realized/Surprise control; `make chains-refresh` issues and scores monthly; the ledger forecasts ENTRY, never viability.

Also record the legality-vs-herding test as the D88 addendum (from the statistician memo): +1.18 [0.88, 1.49] after NTA FE + other-category supply + present-day legal-capacity controls; +1.40 [1.05, 1.75] inside the legal set; legality sets the level (16% vs 72%) but absorbs 0.0002 of the +0.0126 lift while other-category supply absorbs ~48% — reading: "the screen ranks sites inside the legal retail set; it does not merely rediscover where retail is legal, and it still measures co-location, never unmet demand"; caveats: retail_area_400m partly post-treatment, legality variables present-day PLUTO; docs/GTM.md updated (069f0f4).

Why: naming the layers makes the scoreboard the product's compounding asset and keeps "what could be" from being sold as "what is".

**D93 — The learned layer: findings register, carrying capacity, portability audit, planner review.** *(2026-09-14, owner, GTM-164)*

Owner: "backtesting and improvement of the model as well as research; what did the model learn; what can other cities learn from NYC; how many small businesses can a certain amount of people support; density vs revenue; run things by urban planners; critical inputs to expand to new cities." Built: docs/FINDINGS.md (a9efca7; 14 findings with decision ids, grades, portability, overturn conditions; five most portable: agglomeration sign, legality-vs-herding decomposition, filings lifecycle ordering, ledger design, honesty-grade architecture; two most likely overturned: the +0.0126 entry lift, restaurant revenue v0.2); carrying capacity (`loci capacity`, docs/carrying-capacity-2026-09.md): gated fits — only grocery (flattens ~16,104 residents in 400 m) and convenience saturate, seven accelerate (open shed), six proportional; per-1k-residents at p50 density: restaurant 3.85, grocery 1.68, hair 1.39, nails 1.00; CBP-vs-POI ratio per category (clinic 0.94× … fitness 7.42×) is the portability parameter; NYC vs 107 metros with NYC held out: tailor 11.3×, laundry 3.4×, nails 2.2× above, restaurant/cafe/clinic on the curve, bar 0.60× below; nearest metros by dense population LA, Chicago, Philadelphia; D70 (growth) and this (levels) agree on 2/15 — different questions; D91's capacity ceiling binds 62% of Manhattan vs 7% of MN+BK; portability audit (`loci gen-portability`, docs/PORTABILITY.md drift-checked; registry portability/feeds/degrades_to on all 43 sources): universal 7 / national 8 / state 6 / city_open_data 15 / city_unique 6; minimum input set per grade — "act" needs a paid economics input in every city including NYC; the load-bearing portable input is a permit STATUS date; Chicago/LA/Philadelphia reach D today, C at best, none has a storefront registry; every calibrated constant is NYC-fitted; planner review (df5383d): protocol + three packets, verdicts enter QUESTIONS.md and the decision log; Notion mirrors for FINDINGS.md and the packets.

Why: research outputs that are not registered with grades and overturn conditions rot; the register is the learned layer's ledger.

**D94 — Co-located POIs and the closure gate.** *(2026-09-14, owner rule: "any time we have 2 businesses in the same address, we should do a check if one of them closed down"; owner then ruled "keep the closure gate on and re-fit the baseline"; commit a67f03e)*

Built: a tri-state predicate `poi_is_open` (open|closed|unknown) in `src/loci/model/poi_presence.py` — closed only on a source-published closure (Foursquare `closed_on` via `analysis.poi_first_seen`; DCWP `active_basis` out_of_business/unable_to_locate/no_evidence_of_activity; DOHMH closed_at_last_inspection; SLA/DOS licence expiry < today), open only on a published active status fresher than 731 days, else unknown; D79 preserved throughout (absence is never a closure; `last_inspection_date` is never a first-seen date; ledger rows are never deleted). `analysis.poi_colocation` (sql/029, a view not a table) groups same-category exact-coordinate POIs — `staging.poi` carries no `address_id`, so coordinate stands in for the building geocode. Gate wired into `score/supply.py`: `GATE_CLOSED=True` removes evidenced-closed POIs from the canonical supply set; `COLLAPSE_UNRESOLVED=False` — unresolved same-coordinate pairs count as-is and are flagged, since two restaurants in one large building can legitimately share a coordinate. `loci colocation` report added at cli.py EOF; 47 tests.

Real data (all boroughs, principled set): 140,235 canonical POIs → 2,982 removed (2.13%) — laundry 1,020, restaurant 635, fitness 391, grocery 272, hair 170, bank 118, bar 90, cafe 84, hardware 50, nails 48, clinic 40, childcare 32, pharmacy 19, convenience 9, tailor 4. Same-category coordinate groups: 4,530 (12,497 POIs, 8.91% of canonical) — 506 one_closed, 34 all_closed, 1,463 both_open, 2,527 unresolved (7,597 POIs); collapsing the unresolved groups would remove a further 4,270 (3.11%) but was NOT applied (QUESTIONS D24 stays open). Restaurant groups: 2,257, 52% resolved, half by a published DOHMH verdict.

Two named cases the gate honestly does not catch: Okozushi at 376 Graham (DOHMH last inspected 384 days ago, no published closure, so "open" under the rule — needs a fourth "stale" state, GTM-176) and the Lion's Milk double count (DOHMH restaurant vs Overture cafe_bakery, 11 m apart, different categories, so outside same-category colocation entirely — fixed downstream by D101, not by this gate).

DCWP verification (why laundry stays in, not a licence-lapse artifact): every laundry POI comes from the DCWP INSPECTIONS feed (jzhd-m6uv) — the licence roster contributes zero laundries, since its 6 "Laundries" licences expired 2023-12 — so Out of Business (1,289) / Unable to Locate (12) / No Evidence of Activity (27) is an inspector's field verdict, not a licence lapse; aggregator twins are uninformative for laundromats (0.6% of closed carry a twin vs 1.7% of open, versus 30% for closed DOHMH restaurants); the storefront registry has no laundry activity code; median 530 days since verdict with no re-inspection. Ruling: keep.

Baseline re-fit: supply_hash 767b28674e30 → 9a11a2f5b9e7 (n 281,842 unchanged); every baseline median moved under 1%; the supply-ratio apply touched no screen column. Screen re-run #1 (canonical order incl. character, openings, dot-context): identity exact (Σn_missing 645,015 → 656,387), gap addresses 224,993 → 227,892, laundry-led gaps +6,261 (52,064), bar −1,145, cafe −997, convenience −857; lead_censored unchanged (BK 14,245 / MN 705); top-50 clusters Jaccard 0.30 vs before, driven by laundry-led clusters entering; suite 1,520 passed. age-fit apply refused the stale curve (fitted on 767b2867) — refit deferred to re-run #2 (D101).

Why: co-located POIs are usually succession or a stale record, not two live supply points; counting both inflates supply and hides gaps; the fail-safe is the tri-state predicate so a missing measurement never becomes a fabricated closure.

**D95 — Charter: the four layers.** *(2026-09-14, owner)*

The wording naming the four layers (MODELED / REALIZED / SCORED / LEARNED) was added to
CONTEXT.md §4 (subsection 4.7) at the owner's approval. Why: it makes every future claim
declare whether it is modeled, realized, scored or learned, so "what could be" is never sold
as "what is" (D87/D88).

**D96 — Forecast re-issue on the gated supply; supply hash in the model version; retention rule.** *(2026-09-14, owner ruling: "keep the closure gate on and re-fit the baseline"; commit ce68d53, GTM-163)*

Consequence of D94's ruling for the forecast ledger: `model_version` now hashes `supply_hash` + the closure-gate flag alongside the existing feature list/fit-window/horizon/radius/support-floor terms (0.1.1), so a supply re-fit changes the version by construction even when the model form itself did not change; the supply hash a run was fit on is stamped onto `analysis.forecast_run.supply_hash`. `loci forecast issue` refuses to reuse an existing `(month, version)` without `--force`, printing the supply hash first. `loci forecast prune [--keep-vintages 3]` deletes prediction rows only — the newest 3 vintages per `model_version` are kept, never a vintage with an open horizon or no scored outcome, never `forecast_run`/`forecast_outcome` (the ledger itself); dry run by default, real deletion only with `LOCI_FORECAST_PRUNE_REAL=1`. Verified empirically against DuckDB 1.5: neither `CHECKPOINT` nor `VACUUM` shrinks the `.duckdb` file on disk — only an offline rebuild would (out of scope for a routine prune). Full design: docs/chains-process.md "The supply-set identity is part of the version" and "Retention".

Re-issued the 2026-09 vintage as `0.1.1+3cd0e269` on supply `a7c531ebca17` at 22:47, after screen re-run #2 (D101): 4,227,630 rows, `ships=true`, scored nothing new (horizon still open). `webmap/data/forecast.json` re-exported from it (commit ef9517e).

Discrepancy found after issuing: one further `poi_status` change (Windclimb, unknown→closed, the D98 closure evidence) landed after re-run #2 was declared final and moved the live supply hash to `467cd5969200`, so the vintage just issued is already one hash behind. Re-run #3 onto that hash finished at ~23:10 (supply_hash `467cd5969200` on every row and in the baseline); a peer began re-issuing the 2026-09 vintage on it at close of this session — **re-issue running at close, result to be appended** (as of this write, `analysis.forecast_run` for 2026-09 still shows only `0.1.1+3cd0e269` on `a7c531ebca17`; the next session should record the new `model_version` once it lands, and mark GTM-181 Done). Group rule adopted (also recorded in D101): a "final" signal freezes `poi_status` writes too, the same way an in-progress re-run freezes the supply hash — this is the D94/D101 failure mode repeating one layer up.

Also recorded here — one definition of "still open" (commit e695d52): retrodiction's cohort closure events, the recommendations rubric's `still_open` component, and the webmap's realized-closure layer all now read `poi_is_open` (D94's tri-state predicate) instead of raw `closed_on`/`last_seen_month` logic; an adoption test greps the three modules for any remaining raw logic. Cohort closure events move 109 → 190 under the predicate (Foursquare `closed_on` + DCWP `out_of_business` + DOHMH status + lapsed SLA/DOS), still ~5% of true closures (D88 stands); `unknown` is never an event and never a zero in any of the three readers.

Addendum (abenmayor-cf, 2026-09-15): the 2026-09 vintage was re-issued on the final supply — analysis.forecast_run model_version 0.1.1+f1cb6628, supply_hash 467cd5969200, issued 2026-09-14 23:27, 4,227,630 rows (the 0.1.1+3cd0e269 vintage on a7c531ebca17 stays as history); forecast score, forecast-export and citibike export ran with it (map data commit 5bbb39e). After abenmayor-84's revenue v0.2 re-apply on 467cd5969200, `loci export-webmap` re-exported the map: 148 files, 121.3 MB, ageFit curves and screen supply both stamped 467cd5969200 (commit 4b6841f). GTM-181 done.

**D97 — Commercial legality is a label, never a gate.** *(2026-09-14, owner)*

Decided: analysis.address gains PLUTO columns zonedist1, overlay1, overlay2, landuse, ownertype, histdist, landmark (extending the address grain, no new table) and a derived legality verdict: commercial | grandfathered | ineligible. Ineligible = (R district with no C1/C2 overlay, OR park/open-space land use, OR institutional owner type) AND no open commercial POI at the address. An open commercial POI makes the address grandfathered and eligible. Ineligible addresses stay in the universe and stay scored (D75 holds); they are excluded from the recommendation ledger and top-N lists, and gap markers render greyed with a "not commercially zoned" tag. Historic district and landmark are card labels only and never exclude (D82 holds).

Why: a friend of the owner saw gap markers on Washington Square North rowhouses and MacDougal Alley (R6, no overlay) where no as-of-right commerce exists; investigation showed the map draws gap markers with no legality check at all, that East 8th St markers nearby are legitimately C1-7, that NYU parcels are institutional rather than illegal, and that the Greenwich Village Historic District restricts facades, not use, so landmarks are the wrong exclusion signal. The open-POI clause exists because much of the Village and brownstone Brooklyn trades as legal non-conforming use in R districts.

**D98 — Closure evidence sources and precedence.** *(2026-09-14, owner)*

Decided: two new positive-evidence closure sources join poi_status: Google Places business_status CLOSED_PERMANENTLY (via the existing budget-capped proxy) and first-party or news web evidence. Every evidence row stores verdict, url, source name, evidence date, retrieval time and query; verdicts auto-apply with that provenance. Between conflicting sources the newest evidence date wins. Checks run on demand for every "unknown" POI in a generated report's supply set, inside that report's cap, and via a budgeted `loci verify-closures` batch command; never citywide unbudgeted. The webmap export must read the closure-gated supply view.

Why: Windclimb at 74 Richardson St (a soup café, not a climbing gym) is carried only by Overture and Foursquare, so the tri-state predicate correctly returns "unknown" and cannot close it, yet a first-party web post states the restaurant was sold (now D's Grab and Go). D79 (absence is never evidence) is preserved because these are presence-of-evidence sources. The export finding matters because webmap_export.py still reads the ungated analysis.poi_supply, so even a "closed" verdict would not remove a marker today.

**D99 — Address search uses NYC GeoSearch snapped to Loci.** *(2026-09-14, owner)*

Decided: the webmap gets a search box backed by NYC Planning Labs GeoSearch (free, keyless, returns BBL); results snap to the nearest Loci address row within 50 m and open its card; out-of-universe addresses are refused with "not in Loci coverage" and get no report.

Why: keyless and city-official beats Google autocomplete's per-session cost and vendor tie; snapping guarantees search lands only on scored data; refusing out-of-universe addresses avoids degraded half-reports.

**D100 — On-demand allocator report.** *(2026-09-14, owner)*

Decided: `loci report <address>` and a "Generate report" button on the address card produce, asynchronously, a markdown memo under docs/recommendations/ rendered in a map side panel. Reader is a capital allocator. Four mandatory sections: category call + confidence with falsification test; supply with open/closed/unknown status and basis; demand + catchment economics; legality, web-sourced rents/leases, risk, exit. Sources are the warehouse, Google Places and web search; prose is one Claude API call against a fixed outline, with no owner edit step. Hard cap default $1.00 per address enforced in code before any paid call, 30-day cache, --dry-run and --no-cache flags, spend ledger. Acceptance is the investor agent reviewing three reports plus call-counting tests proving cap and cache.

Why: the El Punto page proved the format's value but was hand-written from scratchpad scripts and not reproducible; an allocator reader needs the trade, risk and exit, not a data card; the cap and cache follow the standing "enforce spend budgets in code" rule.

**D101 — Cross-category name+distance dedup.** *(2026-09-14/15, owner ruling 4 "yes to all 4"; category-precedence ruling "go with B"; commit 13f0fec, sql/032, 19 tests)*

`score/dedup.py` is now ONE global union-find. Cross-source pairs of different category merge iff their distinctive name cores are equal and non-empty and NOT (both carry trade words whose sets are disjoint), within MATCH_METERS 40 m; same-source cross-category merging stays off by default (CROSS_CATEGORY_SAME_SOURCE=False; 1,741 such pairs are the DOS booth pattern). Audit: the loose names_match rule measured 0.870 precision [0.790, 0.922] on 100 hand-labelled merges (~2,340 wrong of 18,023 candidates); candidates were scored and the shipped rule measured 0.970 [0.915, 0.990] on a fresh 100 (~390 wrong of 12,928), recall ~0.84 — precision bought with recall deliberately. A 25 m cap was tested and rejected (true merges median 15.8 m, 28% beyond 20 m; the cap lowered precision). Merges: 12,928; clusters 227,548 → 219,133 (−3.7%).

CATEGORY_PRECEDENCE = "finer_food" (owner: DOHMH "restaurant" is a permit class, not a retail category; where the anchor says restaurant and an aggregator member says cafe_bakery or bar, the finer category wins, with DOHMH counted as corroboration, not overridden): canonical counts move restaurant 84,065 → 79,028, cafe_bakery 19,344 → 19,150, bar 12,516 → 12,294, hair 15,601 → 13,714, nails 24,009 → 23,905, grocery 15,504 → 14,998, convenience 7,891 → 7,650; relabels restaurant→bar 4,209, restaurant→cafe 1,981, hair→nails 2,365, grocery→convenience 459. Variant A (anchor-wins) was measured and rejected: bar→restaurant 4,986, cafe→restaurant 1,687. sql/032 redefines `analysis.poi_supply` to read the cluster's category (d.category) and join `category_anchor` on it; `poi_supply_status`/`poi_colocation` follow.

Ledger keys (category is part of the key): under variant B, 12,011 keys gone, 3,596 minted (3,514 same-canonical relabels + 82 canonical changes from transitive bridges); abenmayor-88 ran the key migration (sql/035, commit 00eeca3) before the snapshot — 8,134 merged groups, 0 fake openings, 0 vanished-without-map, ledger 219,133 = clusters (recorded fully in their D103). Lion's Milk is now one cluster (62729), category cafe_bakery, with the DOHMH row canonical — the double count named in D94 is fixed. Consequence worth a look: cafe_bakery now QUALIFIES as an anchored category (has DOHMH members, coverage 1.507), so its principled set is now registry-backed (gated cafe 8,932 of 19,150) — flagged as a QUESTIONS entry, not decided.

Screen re-run #2 (after `loci dedup`, the key migration, a poi-snapshot by 88, `supply-ratio --fit-baseline`, canonical order, `age-fit fit` + apply): supply_hash 9a11a2f5b9e7 → a7c531ebca17; baseline medians vs gate-only: bar +13.5%, cafe +17.9%, nails +5.2%, hair −15.3%, restaurant −14.2%; identity exact (643,670); gap addresses 227,892 → 223,107; Σgap_score 497,559 → 485,856 (−2.35%); lead-gap deltas: bar −7,327, cafe −987, convenience −428, laundry +2,716, restaurant +205, hair_barber lead 0 → 158; lead_censored BK 14,422 / MN 720; age-fit refit: bar pass (dispersion 3.61×), childcare pass (3.25×), pharmacy refused (F2b CI [−0.441, +0.750]); applied to 281,840 lot rows; all UPDATE-only layers ran full; top-50 Jaccard 0.28 vs gate-only (cluster ids renumber per run — caveat).

Incident: the first attempt of re-run #2 halted at age-fit apply because the live supply hash moved mid-run (7132ff7c1d57 → a7c531ebca17) — abenmayor-29's `verify-closures` (28 Google Places calls, $0.90, 112 evidence rows on 28 POIs, 2 closed verdicts) changed poi_status at 22:08; the hash guard worked as designed. Re-run restarted with a hash check at three points, all a7c531ebca17. Structural finding: `db.init_schema` applies every `*.sql` on disk in sorted order INCLUDING uncommitted files, so a peer's uncommitted migration goes live on any session's next write connection — abenmayor-88 will propose a HEAD-tracked-only guard as an owner decision (recorded as a QUESTIONS entry, not decided here; ticketed GTM-179). Rule adopted by the four sessions active this window: any write that can change poi_status or the supply set must be announced first, because it moves the supply hash under a running re-run.

Second hash move, same night: abenmayor-29's Windclimb verdict (the closure evidence named in D98 — the 74 Richardson St soup café carried only by Overture/Foursquare, resolved by a first-party web post) landed AFTER re-run #2 closed out, moving the live supply hash again to a7c531ebca17 → 467cd5969200. A third canonical re-run onto 467cd5969200 is in progress; its numbers (identity check, gap-address delta, lead-gap deltas, age-fit refit) will follow in a small follow-up edit to this entry once it finishes, not a new decision id. Group rule extended after this second move: a "final" signal (an evidence verdict marked final, not just recorded) freezes `poi_status` writes too, the same way an in-progress re-run freezes the supply hash — a verdict landing mid-re-run is exactly the D94/D101 failure mode repeating one layer up, this time on closure evidence rather than dedup.

Why: DOHMH's "restaurant" permit class was silently absorbing genuinely finer categories (cafe, bar), the same defect D91 flagged inline for Lion's Milk; cross-source, cross-category dedup with a name-core+trade-word rule closes it without over-merging distinct trade names that happen to sit close together.

Re-run #3 (2026-09-15, to 467cd5969200 after abenmayor-29's Windclimb verdict) completed with the hash guard equal at all three checks: supply_baseline.yaml re-fit (every median moved 0.00% — one POI has no material effect), street-frame, address-gaps (223 s), address-demand, pipeline, storefronts, age-fit fit (bar and childcare pass, pharmacy refused on F2b, as before) and apply (281,840 lot rows), supply-ratio, address-access, transit-profile, citibike address-measures (the fifth UPDATE-only layer, bike_run_at null = 0 on 281,842 lot rows), address-character, storefront-pipeline openings, dot-counts address-context; supply_hash 467cd5969200 on all 332,041 rows; identity exact; gap addresses 223,107 and Σgap_score 485,855 and every per-category lead-gap count identical to re-run #2. The run surfaced a latent defect (fixed, commit f8142e0): storefront_pipeline.ensure_schema applied sql/020 alone, which re-creates the VIEW analysis.poi_first_seen in its 020 form without closed_on/closed_src/is_closed, so every canonical run ending in `storefront-pipeline openings` silently stripped the columns the closure gate (sql/029) reads and broke supply_hash and two live tests; it now re-applies 027 after 020, the same pattern as poi_presence.ensure_schema. Rule: a module that applies a single migration file must re-apply the newest definition of every view that file touches (GTM-182 carries the follow-ups). Full suite after the fix: 194 passed on the affected files; earlier full run 1,773 passed / 7 skipped.

**D102 — Citi Bike phase 1 (GTM-166).** *(2026-09-14, owner: "yes absolutely. make sure we dont forget to finish this"; commits f7f3d29, ac914da)*

Built: `staging.citibike_station_month`, 1,400,477 rows, the full 44-month panel 2023-01…2026-08, 2,611 docks, 151,909,792 starts / 151,566,696 ends, member share 81.7%; dockless trips, cross-river ids (Jersey City/Hoboken leak into the NY file and are excluded by id pattern) and operator depots excluded and counted separately; start−end gap +0.23%. The completeness check distinguishes an interior outage day (2026-02-23, a system-wide zero from a storm, kept in the divisor — owner call still pending, see Next actions) from a truncated monthly file, which is refused. Pre-2021 data is refused outright: no station-id crosswalk exists before the 2021-02 schema. Seasonality: 45k (Feb 2026) → 183k (Jun 2026) starts per weekday; docks grew +20.9% 2023-01→2025-08.

Address grain (live, computed after screen re-run #2): `bike_starts_400m` / `bike_ends_400m` / `bike_evening_ends_share_400m` / `bike_casual_share_400m` on all 281,842 MN+BK lot addresses — 149,364 sit within 400 m of a dock, and 132,478 of those read a true, persisted zero rather than a NULL; 590,953 address×dock pairs stored in `analysis.address_bike_station` (`--re-sweep` is mandatory after any address-gaps rebuild, same rule as transit). Coverage: 46.9% of BK addresses sit within 400 m of a dock vs 34.9% near a subway entrance, so Brooklyn's non-zero foot-traffic coverage rises 34.9% → 60.2% once bike is added (63,205 BK addresses have a dock but no entrance). Gowanus core bike starts read p50 265 vs East Village 1,286 — the REVERSE of the transit ordering, so bike is evidence bike is not a restatement of transit coverage, not a second read of the same signal.

DOT validation: ρ +0.684 all-camera / +0.275 Brooklyn-only; per-window off-diagonal correlation ties the diagonal, so — same verdict as transit (D76) — this is card context only, never a grade and never a denominator. Licence: NYCBS data-use policy permits derived measures, forbids redistributing the raw dataset. Map: `webmap/data/bike.json` (2,491 docks); registry entry `citibike_tripdata`. GTM-166 marked **Done**; phases 2 (OD leakage, GTM-167) and 3 (activity growth + Divvy portability adapter, GTM-168) stay open.

Owner calls still outstanding: whether the 2026-02-23 storm day stays in the per-weekday divisor (QUESTIONS, below); whether bike dayparts extend `analysis.address_transit_profile` or get a sibling table (two builders would then write one DELETE scope — watch-table-proliferation caution applies).

Why: the owner asked for this explicitly and flagged the risk of it being dropped ("make sure we dont forget to finish this") — it is the first free, city-official, non-transit foot-traffic signal, and the Gowanus/East Village reversal is the first evidence any of Loci's access measures are NOT just a restatement of the transit signal.

**D103 — First-seen ledger key migration for the category-precedence dedup.** *(2026-09-14, following D101's owner ruling; commit 00eeca3, sql/035)*

Because category is part of `location_key`, D101's dedup (variant B, finer-food-category wins) re-minted keys wherever a cluster's canonical category changed: 12,011 keys went absent, 4,538 sat unresolved, collapsing to 8,134 merged groups. Without a migration the next monthly snapshot would have recorded every one of those as a fake opening or a fake disappearance, permanently, in the first-seen ledger — the exact fabrication D79 exists to forbid.

Built: `analysis.poi_key_map` and `loci poi-keys plan|apply|guard`, with three routes — relabel by `poi_id` (the source's own stable id carried the merge), relabel by location (a 40 m link rule), and merge into the surviving cluster keeping the earliest `first_seen` and latest `last_seen`; unmapped keys are never deleted. The snapshot guard refuses to run when absent keys exceed 0.5% AND ≥ 25 keys have no map entry (no-op on an empty ledger or a fixture without `poi_dedup`); `make chains-refresh` now runs the guard before every snapshot.

Applied and verified against a pre-migration copy of the warehouse: fake openings 0, vanished-without-map 0, first_seen moved later 0; ledger 227,548 → 219,133 rows = the post-dedup cluster count exactly; the snapshot carried all 219,133 by key with 0 minted and 0 gone. Dependents rewritten: `chains.brand_location` 11,189 rows (PK-collapsed 7,390, 92,008 → 84,618), `staging.poi_closure` 18 rows.

Incident: the agent's scratch verification run executed against the LIVE warehouse rather than a copy, because the write path ignored `$LOCI_DB` while the read path honoured it — fixed in the same commit; the result was independently re-verified afterward and a rollback copy of the pre-migration state was kept.

Caveats (ticketed as GTM-182, low priority — none break the migration's own guarantees above): `n_months_seen` is a max, not a history, so a merged key's month-count undercounts the true seen-span across its merged clusters; conflicting closures keep the latest `closed_on`, so a merge between two clusters with different closure verdicts silently drops the earlier one; 3,880 `gov_filing` rows folded to an earlier `source_date` (earliest wins), so `gov_filing` counts are not conserved across the migration; `sql/015`'s comment still reads `location_key = cluster_id`, stale now that category is part of the key.

Second migration-application bug found the same night (commit f8142e0, a peer): `storefront_pipeline.ensure_schema` applied sql/020 alone and re-created the VIEW `analysis.poi_first_seen` WITHOUT `closed_on`/`closed_src`/`is_closed` — so every canonical re-run ending in `storefront-pipeline openings` silently stripped the closure columns the D94 supply gate (sql/029) reads, breaking `supply_hash` and two live tests. Fixed by re-applying sql/027 after 020. Same root cause as the `init_schema` uncommitted-migration issue (D101, QUESTIONS D25, GTM-179): a schema-application path that doesn't track which migrations a VIEW/table actually depends on. Two independent instances of this class of bug in one session is itself a signal the fix belongs at the `init_schema`/`ensure_schema` level, not per-caller.

Why: a dedup rule that changes what a canonical key MEANS must migrate every downstream reader of that key or it silently corrupts the one ledger the project depends on never to fabricate an event — this is the same discipline D79 established, applied to a second kind of key change (category reassignment, not just cross-source merging).

**D104 — Legality rule corrected (grandfathering test: PLUTO retail evidence OR a commercial-category POI within 20 m whose poi_status <> 'closed'; commercial zoning always qualifies, institutional ownership and park landuse can only disqualify non-commercial zones).** *(2026-09-14, owner-ruled via contrarian review, supersedes the open-POI wording in D97; GTM-169, commit 3cd0e269)*

The legality rule (D97) as first built required an evidenced-OPEN POI within the buffer to call an address "grandfathered," and since 74% of POIs carry status unknown, "ineligible" was substantially a map of thin POI corroboration — violating D79 in the exclusionary direction. Three further defects in the first build: (1) the east-west pre-filter pad omitted `cos(latitude)` and reached 15 m, not 20; (2) institutional ownership was tested before commercial zoning, marking 1,151 NYCHA/HPD lots in C districts ineligible; (3) street rows with no BBL fell through to commercial by default. Also retracted: a builder claim that DuckDB `ST_Distance_Sphere` is broken; it takes `(lat, lon)` exactly as D16 documents, and the module now pins the flipped call against its haversine.

Ruling (owner, after contrarian review of model/address_legality.py): grandfathered = PLUTO retail evidence on the lot (retailarea > 0 OR bldgclass LIKE 'K%' OR 'S%') OR a commercial-category POI within 20 m whose `poi_status <> 'closed'` (open or unknown). Commercial zoning (C-prefix district or C1/C2 overlay) is decided FIRST and always yields commercial; institutional ownertype and park landuse can only make a non-commercially-zoned address ineligible. A fourth verdict 'unknown' applies when no PLUTO lot exists (the 50,199 street-midpoint rows); unknown is never excluded from recommendations and never greyed. Radius stays 20 m (typical lot width 6–8 m; 30 m spans three lots); the sensitivity table is recorded in `src/loci/model/address_legality.py`. Top-N and recs read paths filter ineligible at read time, not only at ledger insert.

Distribution after apply on 2026-09-14 (supply_hash 467cd5969200): commercial 49,036 (14.8%), grandfathered 19,102 (5.8%), ineligible 213,522 (64.3%), unknown 50,381 (15.2%).

Why: as first built the rule required an evidenced-open POI, and since 74% of POIs carry status unknown, "ineligible" was substantially a map of thin POI corroboration, violating D79 in the exclusionary direction (only 857 grandfathered citywide; 41 and 31 Carmine St, 35 Bedford St, 223 Franklin St and 156 Henry St, all real R-district storefronts, read ineligible). The three further defects were calculation bugs. Also retracted: a false builder claim about `ST_Distance_Sphere`.

### 2026-09-14 — Session 26 (abenmayor-29, Fable lead, agents did the work): Owner interview + Washington Square dig + Windclimb dig + legality contrarian + D104 ruled; D97-D100 decisions traced; tickets GTM-169..172 linked
- Owner interview (4 rounds, ambiguity 0.18) on three asks: recommendations on non-commercial land near Washington Square Park, closed businesses still counted (Windclimb, 74 Richardson St), address search plus an on-demand allocator report. Seed at `.ouroboros/seed.yaml` (23 acceptance criteria). Decisions D97-D100, tickets GTM-169..172.
- Washington Square dig: the friend saw gap markers, not graded recs; markers had no legality check (D75); Washington Sq N and MacDougal Alley are R6 no overlay, East 8th St is C1-7, NYU parcels institutional; the historic district restricts facades not use. PLUTO zoning fields were not in the warehouse at all.
- Windclimb dig: a soup café, not a climbing gym; Overture+Foursquare only, status unknown by design; first-party web post says sold (now D's Grab and Go); map export read the ungated view.
- Built and tested: sql/031 legality columns + verdict on the address grain as a canonical-order re-apply step (`loci address-legality build|stats`); sql/033 poi_closure_evidence + spend_ledger; model/poi_evidence.py (date precedence, newest evidence wins); evidence/{web_search,web_rules,verify}.py; GooglePlacesClient.place_status; `loci verify-closures` (--budget, --bbox/--area, --poi, --name, --dry-run); export reads the gated supply view; geo/geosearch.py; webmap search box with 'not in Loci coverage' toast; report/ package (evidence, enrich, prose, render, cache, ledger, run) with `loci report` and a map 'Generate report' button, async job routes (webmap/report_routes.js), side panel; ClaudeCliProse plan-billed fallback when ANTHROPIC_API_KEY is unset. Contrarian review of legality led to D104.
- Real spend: verify-closures Williamsburg sweep $1.00 (28 Places + 13 Tavily; Susan's Day Spa and Domo Williamsburg closed), targeted Windclimb $0.032 (closed via Google Places CLOSED_PERMANENTLY). Supply hash moved 7132ff7c1d57 -> a7c531ebca17 -> 467cd5969200 as a result; abenmayor-88's forecast vintage 0.1.1+3cd0e269 was stamped on a7c531ebca17 before the Windclimb flip (one-POI discrepancy recorded in D96).
- Process incidents: a CHECKPOINT decision without a tickets.py definition tripped the check-tickets hook tree-wide; db.init_schema applies every *.sql on disk including uncommitted ones (QUESTIONS D25 / GTM-179); a stamped-hash freeze must be declared to all sessions in one message before the first fit.
- Export + browser check PASS (AC-5, AC-13): full export on supply_hash 467cd5969200; Windclimb and Susan's Day Spa absent from the POI layers; convenience layer legality counts: ineligible 43,228 / unknown 12,293 / commercial 4,416 / grandfathered 2,288 (n=62,225); Washington Sq N card reads "not commercially zoned" and renders muted, 4 E 8th St normal, screenshot in session scratchpad. Defect found, not fixed: a searched address that is not a gap for the selected category opens a bare card without its legality line (openAddressCard only reads legality from the category's gap array).
- Three real reports generated with --no-closure-checks and plan-billed CLI prose (docs/recommendations/3027550006-, 3004260001-, 1005500023-2026-09-14.md), $0.072 total web spend; hash unchanged.
- Investor review (docs/reviews/investor-review-2026-09-14.md): 376 Graham Ave FIX-BEFORE-SEND, Gowanus BLOCKING, 4 E 8th St BLOCKING. AC-23 NOT met. Causes: the falsification test is printed from the forecast field and then denied by a prose rule; revenue band and rent ceiling are null in every pack (hypothesis to verify: the reports ran between abenmayor-84's revenue fit and its MN/BK apply, so address_category revenue columns were mid-rebuild); the D18 ECON minimum-catchment gate is never applied; web comps are off-corridor (Gowanus priced off Court St) and sources include Reddit/DNAinfo; internal ids and hashes leak into allocator-facing prose; the 200-870-row POI table is a dump. Reviewer's six ranked generator changes are in the review file.
- Seed status at session end: AC-1..AC-22 met; AC-23 open. Tests: 270 passed on this session's files, node 5/5.
- Real spend today: $1.00 sweep + $0.032 Windclimb + $0.072 reports = $1.104; prose plan-billed ($0 ledger).
- Continuation (owner chose to keep going, 2026-09-15 00:00-02:00): the investor review's six generator changes were implemented in report/{evidence,enrich,prose,render,run}.py (one falsification block from the forecast field; grade gate below C emits a one-page no-trade note and skips rents/leases searches; revenue band + rent ceiling carried and an underwriting paragraph against the D18 ECON minimum; named vacant-storefront, SLA/DOB pipeline and chains rows with the POI table moved to an appendix; web-hit filter with corridor scoping, dated pages, unit-bearing rents, banned domains and a rejected-hits list in the footer; legality as a planner's verdict with PLUTO spdist1 added to the legality columns, LPC fit-out, SLA 500-ft, floodplain "not loaded"; internal identifiers confined to a provenance footer). 114 report tests pass.
- Two regressions found by re-running and fixed: pandas nullable booleans from analysis.storefront crashed the evidence pack (`_flag`/`_isnull` guards, four sites, regression tests incl. a mid-streak NULL); the plan-billed CLI prose reply (truncated/fenced JSON) was dumped raw into section 1 while sections 2-4 read "unavailable" (robust parser with fenced/preamble/heading fallbacks, honest "could not be parsed" line, raw reply in the footer).
- The revenue NULLs in the 09-14 reports were confirmed a timing artefact (reports ran between abenmayor-84's revenue fit and apply); columns are populated now.
- Search-card fix (GTM-169/171): webmap/data/address_index.json now carries legality, histdist and landmark per BBL; a searched address shows its verdict even when it is not a gap for the selected category; browser-verified on 18 Bedford St and 21 Washington Sq N.
- Three reports regenerated 2026-09-15 (docs/recommendations/3027550006-, 3004260001-, 1005500023-2026-09-15.md), all grade-D anchors, all no-trade notes by design, $0.024 web spend, prose plan-billed; structural checks pass. Investor re-review: docs/reviews/investor-review-2026-09-15.md Verdict: 376 Graham Ave BLOCKING (footer's same-BBL check refutes its own lead category; "commercially zoned (R6A)" names a residential district instead of the C2-4 overlay; undated homepage cited under "no dated news"), 545 Sackett St FIX-BEFORE-SEND (no floodplain/Superfund line, 797 permitted units never sized), 4 E 8th St FIX-BEFORE-SEND (stock-photo page cited as news; the grocery-coded supermarkets that explained the convenience zero were dropped). Cross-cutting: no note says what would change the call; the closure-check disclosure was dropped; "no trade" and "no call" are conflated; the D18 ECON gate still never runs. AC-23 still open; five ranked changes in the review file; the full-memo path is not yet judgeable (all anchors grade D; 379 Broome St is the cheapest exercise).
- supply hash drifted to d993b3802c6a after re-run #3 (nine POIs, ledger/036 re-render); re-baseline is the first action of 2026-09-15 (abenmayor-cf running it at session end).

### 2026-09-14/15 — Session 25 (abenmayor-cf, continued): closure gate (D94), cross-category dedup (D101), rulings 1–3 shipped, GTM-157 evening window, 379 Broome pre-registration
- Rulings 1–3 shipped (commit 8383ccd): tailor_repair/hair_barber/clinic demoted from headline (`categories.yaml` `headline: false`; `recommend` never leads with them); numeric G9 coverage grade shipped (Wilson-upper ladder A≤10%/B≤25%/else C in `recommend_grades.yaml`, on-type recount against current GOOGLE_TYPES — grocery/hardware grade B, hair/tailor/clinic grade C, fitness regrades C→A off stored rows); sports_club dropped out of fitness's GOOGLE_TYPES plus the `server.js` mirror; `test_recommend` 24→50 tests. GTM-173/GTM-174 marked Done below.
- GTM-157 evening window: sampled 16:41–16:51 after the 16:02 batch failed on DNS, N=30, Spearman camera-vs-DOT rho +0.438 (p 0.015), md +0.349 / pm +0.495, excluding QBB @ Crescent St +0.461.
- 379 Broome St pre-registered report (docs/recommendations/379-broome-st-2026-09-14.md; address_id 1004710039; no gap, convenience 0.30× the only thin category, bar 14.3×; predictions: food site, revenue below the PLUTO cap band, weekend ≥ weekday; model revenue_p50 pending 84's revenue apply): the report exposed a legality-join defect — `has_open_commercial_poi` reads False beside an open café 12.6 m away — reported to abenmayor-29 (D97's legality label).
- D94 (co-located POIs and the closure gate) and D101 (cross-category name+distance dedup) recorded above in full, with both screen re-runs (#1 on 9a11a2f5b9e7, #2 on a7c531ebca17). Re-run #3 to 467cd5969200 completed 2026-09-15 with zero screen deltas vs re-run #2; ensure_schema view-regression fix f8142e0; peers released for revenue fit, forecast re-issue and export-webmap.
- Google Places ledger 8,963 → 8,991 (abenmayor-29's `verify-closures` calls, folded into D101's incident note).
- QUESTIONS.md: +3 (D25 init_schema guard, D26 cafe_bakery anchoring under B, D27 the ~390 residual wrong merges); M15 marked answered → D101; D24 stays open. Tickets: GTM-153/GTM-175 (cross-category dedup) and GTM-173/GTM-174 (fitness types, G9 ladder) marked Done; GTM-176/GTM-177/GTM-178 stay open; one new ticket opened for the `init_schema` HEAD-tracked-migrations guard.
- Re-baseline on d993b3802c6a completed 02:40, commit 17900c2; D106.

### 2026-09-14 — Session 24 (continued): the modeled/realized/scored/learned layers (D92), the forecast ledger, the learned-layer register (D93)
- Named and shipped the four layers (D92, commit e578470): the forecast ledger (`analysis.forecast`/`forecast_run`/`forecast_outcome`, sql/028, `loci forecast issue|score|report|distribution`), the webmap's Off/Modeled/Realized/Surprise control, and the legality-vs-herding addendum to D88 (scratchpad legality_vs_herding.md folded into docs/FINDINGS.md F11 and docs/GTM.md, commit 069f0f4).
- Registered the learned layer (D93): docs/FINDINGS.md (a9efca7, 14 graded findings + rejections + portable/NYC-only split), docs/carrying-capacity-2026-09.md + `carrying_capacity.yaml` (`loci capacity`), docs/PORTABILITY.md (generated, drift-checked, `loci gen-portability`), docs/planner-review.md + docs/planner-packets-2026-09.md (df5383d).
- Linear: three issues opened — GTM-163 (forecast retention, E2), GTM-164 (closed-catchment capacity fit, E1), GTM-165 (send planner Packets A/B, E2).
- Coordination: abenmayor-cf is landing a `poi_is_open` predicate for POI colocation (`src/loci/model/poi_presence.py`, `src/loci/score/supply.py` modified; `src/loci/sql/029_poi_colocation.sql`, `tests/test_poi_colocation.py`, `tests/test_category_registry.py` new, uncommitted this session) — the retrodiction cohort, the recommendations-ledger fill check and the forecast scorer should switch to it once merged (next action). Also uncommitted: docs/CATEGORY-EXPANSION.md, docs/coverage-validation-2026-09.md (peer work, not touched here).
- DB size flagged: 1.8 → 4.6 GB after one forecast issue+score cycle (~1.4 GB/cycle) — no retention rule yet; blocker added.

### 2026-09-14 — Session 24 (continued): forecast retention + re-issue on the gated supply (D96), Citi Bike phase 1 shipped (D102, GTM-166 Done), ledger key migration (D103)
Ran across the afternoon and evening in three coordinated threads alongside abenmayor-cf (D94/D101) and abenmayor-88 (key migration, migration-application bugs); four peer sessions total active this window.
- **Thread 1 — forecast retention and re-issue discipline (D96, GTM-163, commit ce68d53):** `model_version` now hashes `supply_hash` + the closure-gate flag (0.1.1); `loci forecast issue` refuses to reuse `(month, version)` without `--force`; `loci forecast prune --keep-vintages 3` ships (dry run by default, `LOCI_FORECAST_PRUNE_REAL=1` for real deletion); verified empirically that `CHECKPOINT`/`VACUUM` do not shrink the `.duckdb` file. Re-issued 2026-09 as `0.1.1+3cd0e269` on `a7c531ebca17` after re-run #2 (commit ef9517e re-exported the map); a poi_status change after "final" (Windclimb, D98) moved the live hash to `467cd5969200`, forcing a third re-run — result pending at session close (see D96, next action 0).
- **Thread 2 — Citi Bike phase 1 (D102, GTM-166, commits f7f3d29 then ac914da):** built the full 44-month panel and the address-grain bike measures the owner asked to not forget; DOT-validated at card-context strength only; map layer shipped; marked **Done** in Linear and tickets.py.
- **Thread 3 — first-seen ledger key migration (D103, commit 00eeca3):** built and applied `loci poi-keys plan|apply|guard` to carry D101's cross-category dedup through the ledger without fabricating opening/closing events; verified 0 fake openings, 0 vanished-without-map against a pre-migration copy.
- **Also landed this window:** the "still open" unification (commit e695d52) — retrodiction, the recommendations rubric and the map's realized-closures layer all read `poi_is_open` now, folded into D96.
- **Two incidents, both migration-application bugs with the same root cause:** (1) the ledger-migration scratch run executed against the live warehouse because the write path ignored `$LOCI_DB` (fixed same commit, D103); (2) `storefront_pipeline.ensure_schema` applied sql/020 alone and silently stripped closure columns from `analysis.poi_first_seen` on every canonical re-run, breaking `supply_hash` (peer fix, commit f8142e0, folded into D103) — the same class of defect as the `init_schema` uncommitted-migration gap (D101, QUESTIONS D25, GTM-179): schema-application code that doesn't track what a table/view actually depends on.
- Coordination: four sessions active this window (this thread; abenmayor-cf on D94/D101/rulings 1–3; abenmayor-88 on the key migration infrastructure and the second migration bug; abenmayor-29 on `verify-closures`/D98 closure evidence). The freeze-poi_status-on-final rule (first stated in D101) was reaffirmed here after the Windclimb hash move.
- DB size: 4.6 GB (D92) → 5.0 GB at close (`ls -la data/loci.duckdb`), from the Citi Bike ingest plus two further screen re-runs; retention code exists (D96) but has not had a real prune run yet.

### 2026-09-14 — Session 25 (abenmayor-cf): GTM-48 coverage validation (D90), GTM-112 category-expansion checklist done, GTM-157 first weekday camera samples, scope-map artifact
- GTM-48 ran on the address frame: P3 survives — design-weighted 7.6% [6.5–8.8] of MN+BK MISSING flags are real coverage holes (~40k of 523k), no income gradient distinguishable from zero at the stratum level, fitness's apparent 35.6% hole rate was a Google type-map defect (sports_club/marina), not missing supply. 5,633 rows, 5,985 calls ≈ $190, ledger 8,963/9,113. Full method and numbers: docs/coverage-validation-2026-09.md. See D90.
- Owner ruled "yes to all 4" on the coverage-validation follow-ups (D90): demote tailor_repair/hair_barber from headline (D30 precedent), adopt the G9 Wilson-upper ladder, drop sports_club/marina from fitness's GOOGLE_TYPES, and run cross-category dedup before per-category dedup (GTM-153 proper). Tickets opened: GTM-173 (fitness type map), GTM-174 (G9 ladder), GTM-175 (cross-category dedup) — all three now say the ruling is in and implementation is in progress. Implementation of all four and the resulting supply re-fit/re-run are recorded in a later entry (D94 territory). The unresolved-co-located-pairs collapse question (QUESTIONS D24) was NOT part of this ruling and stays open.
- GTM-112 done (commit 766d9a2): docs/CATEGORY-EXPANSION.md, 12 gates with cited thresholds, a dentist dry-run, tests/test_category_registry.py. Findings folded into D90: G9 had no numeric threshold before ruling (2); clinic can never reach coverage grade A (no Google type, D30); `loci check-categories` does not exist yet; supply.py's anchor-fallback failing OPEN on a missing anchor is deliberate and the refusal now belongs in the checklist.
- GTM-157 progress (not a decision): first weekday camera samples 2026-09-14 (Monday), all 15 DOT-adjacent cameras, midday 12:14–12:24 and evening 16:41–16:51, 61 frames each at 10 s, 1,830 rows added to analysis.sidewalk_count; the morning window was missed (session started 09:06). `sidewalk-count validate --round 2026-05`: N=30, Spearman camera mean persons/frame vs DOT people-per-counted-hour +0.438 (p=0.015) overall; md +0.349 (n=15, p=0.20), pm +0.495 (n=15, p=0.06); excluding QBB @ Crescent St (roadway-facing, 0.03 persons/frame vs DOT 1,970/h) raises it to +0.461. Second weekday (AM/MD/PM) is still needed per GTM-157. The 12:03 and 16:02 scheduled batches both failed on their first try (machine sleep; DNS failure to webcams.nyctmc.org) and were relaunched by hand — ticketed as GTM-170 (a launchd job, not a session scheduler, is the right mechanism).
- Also landing this session, recorded fully in a later D94 entry: the co-location closure check — `poi_is_open` tri-state predicate, `analysis.poi_colocation` view (sql/029), a closure gate on the supply set (2,982 evidenced-closed POIs out; unresolved pairs flagged, not collapsed), supply baseline re-fit on the gated universe (767b28674e30 → 9a11a2f5b9e7). D94 pending: closure gate + screen re-run.
- Scope-map artifact published (owner-requested): https://claude.ai/code/artifact/7d9896d0-7b09-4e1d-be63-9fbabfe03036 — 64 threads across the project: 25 active / 17 shipped / 11 stalled-blocked / 5 stalled-owner / 3 superseded / 2 refused / 1 deferred; contrarian read: 18 claim-bearing outputs vs 1 passed validation since D76.
- QUESTIONS.md: +6 (M14, M15, D21, D22, D23, D24). Tickets: GTM-48 and GTM-112 marked Done; GTM-157 updated; six new (GTM-173–GTM-178).

### 2026-09-13/14 — Session 19 (executive session on Fable): Lion's Milk and El Punto pre-registrations, revenue model v0.2 (D91)
- Owner asked to predict Lion's Milk revenue; pre-registered four estimates (raw v0 model, judgment, bottom-up menu × daypart traffic, cost-side break-even), then the judgment estimate after "why would we send him something we know is wrong"; rent, bottom-up, cost model, owner evidence all documented in the pre-registration record (docs/recommendations/predictions/lions-milk-2026-09-13.md, timestamped before any figure from the owner).
- 376 Graham identified as El Punto Cubano Express (owner's contact confirmed it as the former Lion's Milk owner's new restaurant, opened ~2026-07); pre-registered bottom-up revenue and rent memo.
- Two owner-facing artifact pages published (Lion's Milk, El Punto) with v0.2 numbers and the standing caveat: median anchor describes a 2–3 person shop; for a fully-staffed 1,000 sq ft café the v0.2 number is a FLOOR.
- Owner critique of the v0 overshoot (turned into D91): degenerate ε = 0 finding caught by cross-category placebo; median anchor the largest correction (0.41× alone); capacity ceiling and competition multiplier cap binds selectively (62% MN lots, 1% Gowanus, 0.2% BK); v0.2 restaurant skill ρ 0.82 (R² 0.68), all levels 3–5× below v0.
- Coordination: abenmayor-cb D83/D84 (lot-only pins in revenue/supply-ratio/recommend), abenmayor-88 D79/D80/D85–D89, id D90 reserved by abenmayor-cf.
- Commits: 59b475a, 42fd4f1, 7951adf, a8879a5, 6bad0df, 8e6a9fd, fb0a02f, 5c59317, 3d2a936; nothing pushed. Suite 1,221 passed / 1 skipped.

### 2026-09-13 — Session 23 (continued): paid-data wishlist (D86, GTM-159), GTM strategy memo (D87, GTM-158/GTM-159), DOT map-layer addendum (D85)
- Commits: 9488a6d (paid-data wishlist — 35 registry entries, generated docs/PAID-SOURCES.md, `loci gen-paid-sources`, drift check wired into `check-sources`), 0d1012c (docs/GTM.md — investor-lens draft + contrarian red-team), 089f1fe (webmap DOT layer — count points with trend/COVID caveat, 969 cameras with per-camera sidewalk summaries; fixed a bare-`NaN`-in-JSON export bug, pinned by test).
- Two tickets opened: GTM-158 (Retrodiction — Urgent, E2 · Access Engine, gates any decision-value claim in the memo) and GTM-159 (post-raise wishlist pre-spend actions — Medium, E1 · Ingest and Grid). QUESTIONS.md: T11 (retrodiction, Urgent) and O11 (buyer-segment pricing, no ticket — needs the first customer conversations).
- This session's own commit: CHECKPOINT D86/D87 + D85 addendum + this log, QUESTIONS +2 (T11, O11), tickets.py two new definitions (GTM-158, GTM-159).

### 2026-09-13 — Session 23 (continued): NYC DOT data — bi-annual counts, camera registry, per-address context, sidewalk-count sampler (D85, GTM-157)
- `staging.dot_pedestrian_count` (12,312 rows, 114 points × 37 rounds 2007–2026) and `staging.dot_camera` (969 cameras citywide) landed as new source tables; `validate-pedestrian` reads the count table; per-address DOT context columns (nearest count point / camera, distance, latest am/md/pm) populated on `analysis.address` including the street frame — 332,041 rows.
- `loci sidewalk-count sample|schedule|stats|validate` shipped: local YOLO11n ONNX person detector, 0.027 s/frame, no frame ever leaves the machine. First samples taken (Sunday evening, 3 cameras); `validate` reports N=0 against DOT's weekday counts because the only samples so far are a Sunday — weekday AM/MD/PM sampling at the 15 DOT-adjacent cameras is the next action, ticketed GTM-157.
- Commit d948b6d. Tree coordinated with two peer sessions working D83/D84 in the same window.

### 2026-09-13 — Session 25: street-midpoint frame built (D84, GTM-156)
- 50,199 street points from CSCL inkn-q76z scored beside 281,842 lots as frame='street'; lot rows byte-identical (md5), 571 lot clusters unchanged, 586 street-only clusters; 4,874 orphan points where no lot exists within 100 m. Full canonical order re-run incl. address-access, transit-profile --re-sweep, address-character, storefront-pipeline openings, revenue; webmap exported with hollow street dots (toggle default off).
- Lot-only pins added in revenue.py, supply_ratio.py (--fit-baseline), recommend.py, validation/sample.py, conveniences.py, webmap NTA roll-up, and address_character.py (abenmayor-88's file, named in the commit). Tree was mixed with abenmayor-88's DOT work; cli.py, registry.yaml, CONTEXT.md and Makefile staged by hunk.

### 2026-09-13 — Session 24: density ranking shipped (D83, GTM-155); street-midpoint frame verified and designed (docs/street_midpoint_frame.md)
- Owner confirmed both directions ("yes do both"). Density: walkshed_km2_400m + density_400m on analysis.address (UPDATE-only, same sweep as homes_400m), cluster_density = units_capped-weighted median, every cluster list ordered by it; top-50 Jaccard 0.087 vs the units order; gap set byte-identical; 24 tests; webmap clusters.json + meta.rankBy. Owner call pending: `--min-addresses` floor (9 of the top 50 are single addresses).
- Street frame (design only, no build yet): CSCL inkn-q76z verified (41,784 MN+BK segments → 32,291 kept by rule, L = 100 m → 48,969 points); 9.3% of street points have no lot within 100 m and are a different population (median gap_score 2.07 vs 1.27); 93.1% lead agreement with the nearest lot. Recommendation: `frame` column on analysis.address, cluster within frame, density ranking handles the censored park/island points. Build is next (D84).
- Coordination: collision on webmap_export.py with abenmayor-88's character layer resolved by holding the map pass until e99c808; abenmayor-84 confirmed recommend.py free.

### 2026-09-13 — Session 22: owner correction on scope — screen tables enforced to MN+BK only (D78, GTM-149)
- Owner: "DUDE WE ARE STILL FOCUSED ON MANHATTAN AND BROOKLYN" after a D75 report led with outer-borough counts; then "yes rerun manhattan and brooklyn only". Reporting rule recorded in memory: MN+BK numbers first and only.
- Built: `SCREEN_BOROUGHS` constant, `address-gaps` refusal of out-of-scope boroughs (`--allow-out-of-scope` override), idempotent `prune_out_of_scope`; four tests. Re-run MN+BK in the canonical order incl. address-access and revenue; 281,842 addresses, identity 531,773, checksums byte-identical to the D75 MN+BK subset; webmap exported. Suite 857 passed / 1 skip. GTM-149.
- Process: the build agent stalled twice on the stream watchdog (backgrounded waits) and was resumed; the Haiku doc agent truncated this file with a whole-file write and it was restored from HEAD (2dbe742) and edited by hand. cli.py commit order negotiated with abenmayor-88 (chains/transit) and abenmayor-84 (revenue).

### 2026-09-13 — Session 23 (continued): daypart transit profile (D76 addendum), storefront first-seen ledger (D79), watchlist seed rows fixed, D80 filings pipeline shipped
- Commits: 3020947 (day-type × daypart transit profile, D76 addendum), 46e50ff (storefront first-seen ledger, D79), 52aa9f4 (chains watchlist seed rows — Apollo Bagels / Vital Climbing Gym replaced with researched fields), plus earlier today 60d7b0b, 44fed6a, f6ee485, 2dbe742; docs commit a6ad4c5 (CHECKPOINT D79 + D76 addendum + this session log). D80 stage one lands as 2979cc2 (staging.storefront_filing, 7 feeds × 8 lifecycle stages) and stage two as 10a33e8 (analysis.storefront_pipeline, cross-agency reconciliation, `'gov_filing'` first-seen kind, `openings_pipeline_400m`).
- Chains watchlist published as a claude.ai artifact for the owner. Contrarian memo on foot-traffic proxies as a grade input — verdict recorded in D76 (card context only).
- Storefront-filings ingestion and pipeline (D80) now shipped and in the decision log: SLA pending licenses, DOB NOW job filings, DCWP license applications, DOHMH promoted inspections landing in normalized `staging.storefront_filing` with lifecycle stages (`src/loci/filing_stages.py`, `sources/cities/nyc/filing_feeds.py`, `sources/cities/nyc/socrata.py`, `model/storefront_filing.py`, `model/revenue_calibration.yaml`, `sql/019_storefront_filing.sql`, `tests/test_storefront_filing.py`), then `analysis.storefront_pipeline` at bbl × business_name_key with cross-agency lead times (see D80). Ticket GTM-152 opened for the 10-category filing-blind-spot fix and the sole-pair reconciliation caveat.
- Three Claude sessions coordinated commits by hunk in the same tree today (this docs pass, the daypart/ledger/watchlist session, and the D80 filings session).
- Neighborhood character (D82, GTM-154) shipped: address-grain retail/corporate/industrial/residential classification from PLUTO floor area + LODES jobs + zoning overlays (commit 55e2116), revised after urban-planner review found prewar retail corridors reading ~0 retail_mixed (commit 1f115d6, zoning-overlay OR-route + lowered retail-jobs floor + institutional-lot exclusion + suppression rules), and rendered on the webmap as a continuous retail_index layer with hatched corporate/industrial overlays and suppressed-NTA grey (commit e99c808). Ruling: map colour and card context only, never a grade. Follow-up ticketed GTM-154 (saturation, 20-lot threshold review, card copy).
- Chains-refresh launchd job installed (`~/Library/LaunchAgents/com.loci.chains-refresh.plist`, day 1 03:10): storefront-pipeline build → poi-snapshot → detect → research → render, logs under `data/chains/logs/`. Resolves the standing blocker; first honest monthly snapshot is 2026-10-01.

### 2026-09-13 — Session 18 (executive session on Fable): site-revenue model (D81)
- Owner asked how to predict what a business could make, and approved the build after anchor verification (Sonnet probe: Economic Census 2022 county × NAICS granularity, the finest real revenue truth public; CEX national spend by income quintile; NY-metro rebase; trade benchmarks not trusted without census check).
- Build: Huff-model capture share with distance decay β and competition elasticity γ fitted per leave-one-ZIP-out fold, sign free — textbook negative sign was WRONG for 7 of 10 categories at ZIP grain (incumbents proxy "is this a retail street"), not a bug; leakage λ calibrated per county to match Economic Census receipts (λ_Kings vs λ_NY disagree 1.4–2.6×, both carried).
- Result: restaurant alone passes the backtest gate (ρ_oos 0.763, R² 0.577, placebo pass), grades C on the card; café/grocery/hair/nails/pharmacy fail the placebo (commercial intensity, not economics); laundry/convenience/bar fail the baselines; fitness too sparse. Gowanus core restaurant revenue p25/p50/p75 $1.08M / $1.67M / $2.66M (1.81× Kings avg), rent ceiling $11.2k/mo vs one local comp $7.3k/mo.
- Coordination: abenmayor-cb applied the D75 gate removal and re-ran D78/canonical order; abenmayor-88 completed D79/D80 storefront filings and pushed sequencing to unblock the hook. All coordinate at revenue step (now the final step of canonical apply order: supply-ratio → revenue).
- Commits efae213 (code), d7387dd (calibration + card); nothing pushed. 884 tests passed / 1 skipped / 1 concurrent-rebuild lock induced failure.

### 2026-09-13 — Session 18: foot-traffic proxies (D76, GTM-147) and chains watchlist (D77, GTM-148) documented; CHECKPOINT/QUESTIONS/tickets pass
Commits landed by the two building sessions this session's docs describe: 60d7b0b (address-level transit_entries_400m/jobs_400m + DOT validation), 44fed6a (`loci chains` — watchlist, detect snapshot, press enrichment), f6ee485 (docs/CHAINS.md first render); plus this session's own docs-only commit (CHECKPOINT D76/D77, QUESTIONS C2 superseded + 4 new entries, GTM-147/GTM-148 ticket definitions). Three Claude sessions worked the repo concurrently today (this docs session, a foot-traffic/chains building session, and a revenue-model session per `loci/src/loci/model/revenue.py`); commits were staged by explicit path per session, with `cli.py` split at the hunk level where two sessions touched different command blocks in the same file. The daypart × day-type transit profile agent is in flight, not yet landed — see Next actions.

### 2026-09-10 — Session 16 (executive session on Fable): storefront registry (D67), headroom backtest (D68), map served locally
- Owner's operator-timing questions on pipeline map (Turtle Bay bodega cluster: 703 convenience gap addresses, best catchment East 54th St between 1st/2nd Ave, ~18,900 gap homes within 400 m; SNAP anchor under-covers delis in high-income Midtown East).
- Owner asked vacant lots / asking rents / marketplaces → research pass → D67 build (DOF Storefront Registry, Local Law 157, no rents exist publicly; only addresses/corridors/marketplaces).
- Gowanus headroom hypothesis → D68 backtest, Greenpoint as cautionary case (full in 2013, added most in 2023; not slack).
- Two Backlog tickets from T7 created earlier (GTM-132/133) committed by concurrent session in 37534cd.
- Coordination with two concurrent sessions (D63–D66 theirs; shared files committed by them in f83747b carrying this session's 002/cli/registry/CONTEXT hunks).
- Webmap served locally on :8080 (server killed once by memory pressure, restarted).
- Nothing committed at the time of writing (commit follows map overlay).

### 2026-09-11 — Session 17: Per-category density-elasticity coefficient (D70, GTM-138) — nothing clusters at ZIP grain
- Owner: "lets do it" on GTM-138 to resolve the "bars/cafés clustering" reading in D68.
- Built by data-scientist agent (Opus): src/loci/model/density_elasticity.py + density_elasticity.yaml (config lives with code), `loci density-elasticity fit [--dry-run] | show`, 29 tests; suite 501 passed.
- Result: NOTHING clusters. Eight saturate (childcare, clinic, laundry, grocery, tailor, fitness, nails, bank); cafés fail cross-category placebo (generic gentrification); bars insignificant. Supersedes D68's "bars/cafés cluster" reading; D68's chore conclusions stand and strengthened.
- Two gate rules encountered and documented: (1) log-form Δlog(estab) refused by REJECT_UNANIMOUS_SIGN (all negative, base-count collinearity); level form adopted. (2) Original gate forced clustering ∈ regime; relaxed to: every regime has ≥1 signal (either sign), placebo covers all categories.
- Concurrent session abenmayor-cb rebuilding warehouse (D69) during the fit, reading only the ZIP panel; no coordination needed.
- Owner asked what the clustering coefficient is → D70 (elasticity per category, none cluster); owner asked "what is the data saying around Gowanus" and whether a no-gap area can still support a business → synthesis given, then corrected by the contrarian pass (in_all vs principled counts; laundry pool unaddressable; forward headroom quoted despite D68; D68's "restaurant/bar takeover" claim had no data, withdrawn) → D72.
- Owner asked for BizQuest comps → BizQuest/BizBuySell refuse automated and browser access (honored, not circumvented; Tavily deliberately not used for them); three listings found on Craigslist (Boerum Hill burger restaurant $450k / $7,313 mo / 1,000 sq ft appended to the comps CSV; a 30-year liquor store near Barclays with a lease to 2048; a Gowanus one-chair hair studio), comps model thin everywhere.
- Premium services inventory for Gowanus (Whole Foods, Club Pilates ×2, Pure Barre, SLT, CrossFit, European Wax, Bouldering Project, brewery/nightlife cluster; none of the Equinox/SoulCycle/Barry's/Blue Bottle/Trader Joe's tier present that Williamsburg has).
- Owner directive "start to make recommendations, close the gap" → D72 built; supply-ratio + addressable-households increment dispatched; recommend-card design recorded in D72 standing ruling; concurrent session commits 2e94b49 (D69), cdfb46c (D71).
- D73 built by data-scientist agent from D72 contrarian findings: supply_ratio.py + `loci supply-ratio` on principled set at 400 m network range, homes_400m per address, laundry haircut (addressable homes only); Gowanus laundry 0.94× (normal not thin); pharmacy 0.00×, convenience 0.40×, hardware 0.72× are the leads.
- D74 `loci recommend` shipped with the first Gowanus card; session commits 79118be (D72 code), 96c3fc1 (D72 docs), 2fc0eb4 (D73 code), 203c4f5 (D73 docs), d9ec335 (D74 code) — nothing pushed to the remote.

### 2026-09-10 — Session 18: Childcare anchor (D65); pharmacy re-tested and applied on wrong coefficient (D66); three rulings pending

Coordinated with the storefront session (D67/D68 reserved for it). Childcare anchor: dsg6-ifza (Historical, 2019-05-14 max date, no coordinates) replaced by gy3q-4tzp live (2,755 programs, 99.56% geocoded, ACTIVE applied). Borough Park median distance to childcare 298 → 104 m (3 → 31 sites within 640 m). Under_5_share added to demographics from ACS cache cells already in GETVARS, no refresh needed. Childcare gate passes on under_18_share (3.556 [2.416, 5.235] contrast BK t +7.23) but refitted curve NOT applied, pending owner. Pharmacy age-fit tested: passes F2/F3 on b(age_18_34_share) = −1.234 (t −3.02 in BK), NOT on primary regressor b(age_65_plus_share) = +0.264 (t +0.90, wrong sign); applied under gate as written (1,527 address-lead gap, multiplier p10/p50/p90 = 0.935/1.030/1.211), changes almost nothing. Provenance fix: supply hash now live (score.supply.supply_hash) not screen-stamped; fit files re-stamped; check_fit_is_current compares against live hash (3 tests pin). Three rulings requested: (1) apply childcare (gate passes primary regressor D65). (2) Tighten F2 to primary regressor's own BK CI, sign-inclusive (would un-apply pharmacy D66). (3) D52 amendment: `anchor_is_floor` per category so lone-aggregator childcare retained (in_principled 4,302→2,657 otherwise); then re-run address-gaps/demand and apply all curves (D65). GTM-135/136, 443 tests passed / 1 deliberate skip.

### 2026-09-11 — Session 19: Three owner rulings applied; floor anchor (in_principled childcare 2,657 → 6,066); F2a/F2b split; bar passes, childcare passes (+2.744, t +6.15), pharmacy refused (t +1.39) and NULLed; screen re-run ALL, eligible 587,408 → 593,930, identity exact; eligibility-gate surprise; webmap re-exported; GTM-139; 503 passed / 1 skip; one agent stalled on the watchdog mid-pipeline and was resumed.

### 2026-09-11 — Session 20: Pharmacy anchor (NYSED no bulk form → Medicaid roster keti-qx5t, coverage 0.903), pharmacy age-fit refused t +0.48, webmap age-fit rendering closed (three states, MOE always, caveat untruncated), eligibility-gate analysis and D69 correction, CONTEXT rows, GTM-140, 536 passed / 1 skip; second pass (re-fit + re-run + export) pending the gate ruling.

### 2026-09-13 — Session 21: Owner ruling removes the gate; censoring flags; second pass gate-free (identity 3,012,430; 173,407 formerly-excluded addresses all gaps, outer-borough); baseline re-fit on gate-free universe; webmap exported; GTM-146; 794 passed / 1 skip; one agent stalled on the watchdog at the final checks and was resumed; two other sessions active (revenue model, chains)

### 2026-09-09 — Session 15 (executive session on Fable): density-shock investigation (QUESTIONS T7), development pipeline layer (D62)
- Owner's question (2026-09-09): "why don't we know when people are moving into each gap address?" revealed a four-agent stack: (a) research agent scanned for storefront opening/closing time series (none exists, Increment-2 work); (b) data-engineer assessed DCP Housing Database and DOB CO sources (available, recent, 436 jobs with CO post-DCP-cutoff); (c) developer built the pipeline layer (D62); (d) investor reviewed timing signals and lag findings from O5.
- Kill check on T7 framing: no opening/closing time series in staging.poi (only Foursquare opened_on, 2010 spike is launch date; SLA 3,205 recent-only records; DOHMH/DOS/SNAP/DCWP carry no date fields). Foursquare's opened_on is record-creation date, not store opening. LODES panel issue: registry describes "annual 2002–2023" but analysis.hex_panel has only three vintages (2002, 2013, 2023), cannot resolve <5-year lag.
- Investor answer (scratchpad 2026-09-09): lag is rational landlord optionality. A 300-unit project COing 2025-Q4 will NOT have retail open until Q3 2026 by design (landlord waits for residents, retail tenant underwriting requires lease-up, 12–18 month pre-opening). Value of delay ~$4–5M per project (foregone rent for 12 mo, marginal above the building's base value) paid mostly by the residential side (lower net operating income) and recovered via retail premium only in the top tier. Not a friction, not a timing window — a rational tradeoff. Fit: only asset owners running portfolio bets on retail density (e.g. Equity Commonwealth or Cousins operating as a developer) see the option. Developer-side amenity-retail analysis is a separate (Axis 1 investability) product; T7's causal framing is deferred.
- Owner reframing (2026-09-09, plain language): "I want to know which gap addresses are about to gain residents so I can offer a storefront lease nearby to the incoming demand." This is timing annotation, not causation. D62 builds it: nearest large project (≥50 units), stage (permitted/partial-CO/complete), and CO date sit on every address; the webmap shows which gaps are 12–24 months out from a density shock; the investor's three-decade portfolio rule applies.
- D62 built (data-engineer + developer agents): DCP Housing Database (br6q-ssj3, 109k jobs on MN+BK) joined to DOB CO feeds (bs8b-p36w + pkdm-hqz6, 436 jobs freshened post-DCP-cutoff). One new table analysis.dev_pipeline (job grain), 12 UPDATE columns on analysis.address. Webmap overlay "Development pipeline" (net_units ≥50, four stage bands, sized 50–99/100–299/300+, hollow ring for temporary CO). Data dir 42→50.6MB, suite 333→364. GTM-129 reframed to address level 2026-09-09; D62 built same day.
- T7 status: exploratory only, marked open. Timing catalysts from O5 (permit→construction 5–9yr, demographic 10–20yr market-rate-only). The contrarian caveats (2026-09-09, Opus notes) stand: no identified opening/closing series; license date ≠ store opening; flat count, more profitable unfalsifiable by the data Loci has; underwriting heuristic (permit→bid, CO+18mo→demand, 50–60%leased→signal) is provisional, unvalidated.
- Nothing committed at the time of writing.

### 2026-09-08 — Session 13: scope re-fixed to MN+BK, grade proposal drafted and red-teamed, POI layer on the map
- Briefing found doc drift (header at session 12, stale comps and D8 blocker rows, QUESTIONS O9); fixed.
- A Sonnet tabulation of the D44 lead-viability candidates ran citywide and framed the choice by top-200 category mix; owner rejected both the scope (MN+BK only) and the framing (refine logic, never optimize an outcome; Loci is a gradient, not a filter) → D48. CONTEXT.md scope line updated. QUESTIONS D10 added.
- Owner asked for more Opus; analysis and design now route to Opus, Haiku only for dictated edits.
- data-scientist (Opus) drafted the grade proposal on MN+BK (scratchpad grade_proposal.md); contrarian (Opus) red-teamed it (grade_contrarian.md) → D51. Key structural finds: 2,400 m censoring makes max ratio = 2400/reach; the eligibility gate is inert in MN+BK; the supply set (all vs corroborated) swings the result 6×.
- developer (Opus) added the known-business-location layer to the webmap: new `loci export-webmap` (src/loci/viz/webmap_export.py, --dry-run, --boroughs MN,BK default), both layers share the category filter, borough selector, corroborated (≥2 sources) vs single-source styling, 5 new tests, suite 125/125. The map now reads analysis.address_gaps (eligible ∧ ratio > 1) instead of the Sep-2 building files; old gap_buildings.json/nav.json/hexes.geojson are orphaned, not deleted (buildings_meta.json still read by model/invest.py). POI borough is assigned via H3 → analysis.hex (POIs carry no borough; 65,604 canonical POIs outside the grid dropped). Dry-run: MN 60,257 known locations (15,797 corroborated), BK 41,431 (11,472); laundry MN 1,044 known / 324 corroborated vs 1,216 gap addresses; BK 1,062 / 304 vs 72,091.
- A second session ran concurrently in the same tree (GTM-109 demand.yaml contrarian review: demand.yaml/demand.py/gaps.py/acs.py/002_schema.sql/google_places.py/sample.py + tests, all stamped 15:05). This session did not touch those files; commits must stage paths per session.
- Owner rulings at end: drop C, supply set next, all of Brooklyn, find in-building laundry (D51). data-engineer (supply set) and a research agent (in-building laundry sources) dispatched.

### 2026-09-08 — Session 13b (concurrent with the ENY/query-app session): GTM-109 and GTM-105 closed (D49, D50)
- Executive session on Fable; work chosen explicitly to avoid the concurrent session's files (comps, ZBP, DOHMH/NYS DOS dates, invest/rising, queryapp, address_gaps, webmap export). Decision ids D49/D50 allocated here; D48 belongs to the other session.
- Contrarian review of demand.yaml (Opus, contrarian agent) → D49. Rebase implemented (Opus): demand.py rewritten to derive classes from spend.yaml; grid/acs.py fetches citywide mean household income; gaps.py emits continuous MOE-aware columns; 002_schema.sql extended; `loci citywide-income` added; tests/test_demand_caveat.py 6 → 21 tests; tests/conftest.py pins the citywide mean so the suite stays offline. hex_gaps rebuilt byte-identical on gap sets (726 hexes / 987 pairs). hex_gaps_reach was found stale (reach_hash 01316e0d7cce vs reach.yaml f6339bf9d842) and rebuilt to 1,966 hexes / 5,053 pairs — that drift predates this session.
- Google type-map audit (Opus) → D50. Harness and mapping fixes implemented (Sonnet) in validation/google_places.py, validation/sample.py, cli.py, webmap/server.js, sql/002_schema.sql; tests/test_google_places.py, tests/test_sample.py updated; tests/test_google_types_drift.py new.
- Full suite at close: 125 passed. Nothing committed at the time of this entry; the owner decides the commit boundary given the concurrent session's uncommitted work in the same tree.
- Second half (owner: "continue"): M8 decided and implemented (D53); D7 renter_share pre-test negative and the ACS vehicle-ownership ingest landed (D54); stale-reach-table check done — no published number cited the old 1,707-hex table (all citations are dated log entries D34/D37, and the address screen reads reach_tiers.yaml, not reach.yaml), so nothing needed re-tabulating. Session-end suite: 154 passed, 4 failed — all four in tests/test_webmap_export.py, the concurrent session's untracked in-progress file (Binder Error on staging.poi.source_record_id), reproduced with this session's changes stashed. Still nothing committed.

### 2026-09-05 — Session 12: NAICS 2022 adopted as the category anchor (D42)

Owner asked whether census.gov/naics could serve as the industry classification; decided it should be THE anchor, not a reference. One Sonnet dispatch: vendored the official 2022 structure, descriptions and 2017<->2022 concordance files under `src/loci/naics/` plus a derived `naics_2022.csv`; wrote `src/loci/categories.yaml` (15 slugs -> NAICS 2022 codes, 2017 equivalents, and native OSM/Overture/DOHMH/NYS DOS/DCWP/NYS SLA tags); corrected the LODES entry in `registry.yaml` (2022 codes; WAC is 2-digit only); added `tests/test_categories_anchor.py` (slug coverage, code validity, no shared codes). Committed as 4436bae. Surfaced three retail recodes (convenience 445120->445131, pharmacy 446110->456110, hardware 444130->444140). Two follow-ups left open: `src/loci/zbp_naics.yaml` now duplicates the anchor's NAICS 2017 mapping and should be derived from or folded into `categories.yaml`; the Overture adapter maps `bank_credit_union` onto the bank slug although NAICS 522110 excludes credit unions (522130). Full test suite at close: see next-actions note. Ran in parallel with another session that committed sessions 10+11 (85e9196), the CLAUDE.md operating model (770a5b3) and QUESTIONS updates (e91e710).

### 2026-09-05 — Session 11: demand-caveat annotation and Meltzer & Schuetz framing
- Owner brought the Meltzer & Schuetz full text (EDQ 26(1):73–94); Fable directed session.
- Sonnet read the paper and briefed: low-income NYC neighborhoods are not under-supplied in count for necessities (groceries actually MORE dense there); gyms, bars, upscale chains follow income. Decision: income enters as annotation, not filter (D35).
- Developer agent built `src/loci/demand.yaml` (15 categories × necessity|discretionary × evidence field), updated `model/gaps.py` for demand_caveat annotation (income_class, renter_share, demand_caveat flag, caveated_missing list), and extended schema (analysis.hex_gaps now 987 pairs, 91 caveated at 9.2%; reach rule 1,707 hexes, 918 caveated at 14.8%). Tests: test_demand_caveat.py proves set equality (gap hexes/pairs unchanged); full suite 72 passed (14 in gap battery, 61 in reach monotonicity), 1 pre-existing failure in test_comps.py unrelated. Result-level findings: fitness 42% caveated (demand-following in low-income areas per paper); bar 36%; necessity categories all 0% caveated per assumption.
- Haiku updated `docs/QUESTIONS.md`: D8 (next calibration focus) carries forward; H-L2 (food deserts & fair-value) answered "Meltzer & Schuetz + reach recalibration"; H-L6–H-L12 added (Zukin 2009, Schuetz/Kolko/Meltzer 2010, Waldfogel 2008, Zenk 2005, Powell 2007, Haltiwanger/Jarmin/Krizan 2010, Chapple & Jacobus 2009); X6 (race net of income) added; X5 (Staten Island) accidentally renumbered during Haiku pass, then reverted so Staten Island stays X5.
- Notion Loci Reading List (user's workspace) updated with H-L6–H-L12 entries and full-text link on H-L2.
- Nothing committed; all work uncommitted alongside the session-10 streams.
- Later in the session (D40): Census ZBP/CBP ingested as a validation source; first POI-vs-ZBP comparison shows 2–8× overcounts concentrated in license-anchored categories (QUESTIONS M9). Concurrent-session note: decision ids D35/D36/D38/D39 were allocated by session 10 in parallel; this session's are D37 and D40.

### 2026-09-03 — Session 9: M1 corrected, Manhattan D3, validator fix
- Google validation run: 2,970 calls, ledger 2,978/3,000 (`loci validate --categories
  hardware,fitness,clinic --per-decile 99 --run`; `--per-decile` raised from 20 to 99 because gap
  hexes are only 3–7% of populated hexes so the default sample barely overlapped them).
- Contrarian review of the raw survival rates led to the geometry/coverage split (D29): SQL worked
  out in scratchpad, now reproducible via `loci validate --recount-local` without new Google spend.
- Clinic excluded from headline claims pending an urgent-care re-anchor (D30).
- Manhattan D3 sensitivity at a 5-minute walk window (`d3_manhattan_walk_threshold.py`, read-only):
  category mix flips vs the 10-minute screen (D31).
- Validator source-lookup bug found and fixed (commit 877d162, D32); 42 tests pass.
- Correction: the CENSUS_API_KEY blocker row was stale (key verified as GTM-10); removed 2026-09-03. Outstanding work is the ACS vehicle-ownership ingest for D7, not a key.
- Spikes committed (4206ccc); CHECKPOINT header fixed (aed7be0).
- Session operating model recorded in root CLAUDE.md: Fable directs, Haiku/Sonnet/Opus agents work.
- Owner added O7 (fair-value storefront rent) and O8 (buy vs build inflection) to QUESTIONS.md; both parked in Axis 1 behind O6; no tickets.
- Session closed 2026-09-04: branch `loci/eny-investment-and-query-app` pushed to origin (first push). Owner confirmed D6 as next session's starting point.

### 2026-09-02 — Session 8e: the distance table
- Owner asked whether every data point is stored and whether a graph DB would help. Answer:
  POIs yes, distances no — `hex_access` kept counts at three thresholds and threw the Dijkstra
  results away. Graph DB: no; the graph is a road network and the only operation is
  shortest-path, which scipy does in C in seconds (D9 stands).
- **`analysis.hex_poi_distance`** — every (hex, canonical business) pair within 30 min with
  its network distance. 16.2M rows, ~3 min via `loci score` (now implemented; `make nyc`
  runs dedup → score → gaps). `hex_access` is derived from it in SQL; regression check
  matched the previous build row-for-row. `gaps`, `gaps-sweep`, `spacing` read the table;
  the nearest-of-category query answers in 60 ms instead of a 2-minute graph run.
- **Size:** the file hit 2 GB. Measured: the table is 453 MB as parquet and 533 MB in DuckDB
  without a key, 1,640 MB with the (h3_index, poi_id) primary key, which also made the load
  7× slower. Dropped the key — uniqueness holds by construction — and swapped the table in
  place. DuckDB does not shrink a file, so `data/loci.duckdb` stays ~2 GB until recreated;
  a fresh `make nyc` lands near 600 MB.
- 39 tests pass.

### 2026-09-02 — Session 8d: threshold knob, spacing, and a distance bug
- `loci gaps --expected` and read-only `loci gaps-sweep`. 0.75/0.80 → 726 hexes; 0.85/0.90 →
  402 (only bank and hardware drop out); 0.95 → 30. Owner's call.
- `loci spacing` (QUESTIONS.md D5), rewritten on the **walk network** at the owner's
  correction (straight-line is wrong in NYC, §2.2). Reuses the access engine's graph, CSR,
  snapping and component pruning; 2,000 businesses sampled per category; ~2 min. Same-type
  spacing: median 0–32 m for food/salons/groceries/gyms, ~200 m for childcare and hardware;
  11% of hardware stores and 16% of tailors have no competitor within 10 min. Gap hexes: the
  nearest missing business is a median 860–1,030 m on foot (p90 1.1–1.4 km); 26 of 726
  hexes beyond 1.5 km, none beyond 4 km. The screen finds a 10-to-17-minute band.
- **Bug found (D16):** DuckDB `ST_Distance_Sphere` reads (lat, lon). Caught because an
  aggregate said 359 m where the walk graph said 1,228 m. Fixed in the validation sampler
  (the only caller), shared as `METRES_SQL`, pinned by a test. Numbers quoted in chat before
  the fix were wrong; nothing wrong was written to docs or the database.
- Dedup lead (H-D11): tens of thousands of same-category cross-source pairs within 25 m with
  different names — some food halls, some legal-vs-trade names of one business. Hand-sample.
- 38 tests pass. Nothing committed.

### 2026-09-02 — Session 8c: Foursquare in, ghosts out
- Owner accepted the Hugging Face gate and supplied `HF_TOKEN` (in `.env`; it is also in the
  session transcript — rotate it when convenient). NYC bbox extract cached in <1 min:
  821k rows → `data/raw/fsq_places_nyc.parquet`.
- Category map rebuilt from the real 1,279-label taxonomy: path-prefix rules for the
  restaurant / bar / cafe families, leaf labels elsewhere. Dropped on purpose: Deli (8.7k, a
  restaurant leaf that is usually a bodega in NYC — SNAP and DOHMH already anchor both sides),
  ATM, Doctor's Office (23k solo practices; would quintuple `clinic`), specialist physicians,
  contractor leaves. 191k rows map.
- **Ghost check, the important finding.** Foursquare's dates are strings; `date_closed` is
  almost never set. Corroboration by any other source, by `date_refreshed` year: 2012 → 5%,
  2019 → 11%, 2024 → 29%, 2025 → 38%, 2026 → 55%. Venues that closed simply stop being
  refreshed. Gate `LOCI_FSQ_MIN_REFRESHED=2024-01-01`: 109k of 191k mapped rows load, 54k
  canonical after dedup (299k → 224k, 25% collapsed).
- **Gap screen with everything in (SNAP + SLA + fresh FSQ): 726 hexes.** any-missing:
  bank 285, hardware 245, fitness 138, bar 93, clinic 91, childcare 60, laundry 31,
  pharmacy 21, convenience 17, grocery 2. The UNGATED Foursquare load had shown 468 hexes
  (hardware 165, fitness 79) — that drop was ghosts, not coverage. Real Foursquare effect:
  hardware 270→245, fitness 154→138, clinic 148→91.
- **Screen-design sensitivity, for the owner:** a category becomes 'expected' at 80%
  prevalence. Bank sits at 0.829 and hardware at 0.844 now; adding Foursquare pushed bank
  over the line, so 285 bank-missing hexes appeared that were not gaps yesterday. The
  threshold is a knife-edge for tier-4 categories — worth a sensitivity sweep (0.75/0.80/0.85)
  before publishing a top-N list. Filed on QUESTIONS.md M4; H-D10 asks whether Foursquare's
  'Medical Center' and 'Gym and Studio' mean what the bundle means.
- GTM-15 Done. 37 tests pass; `make check` passes. Webmap JSON still needs re-export.

### 2026-09-02 — Session 8b: anchors in the screen, Google budgeted, Foursquare blocked
- **Rebuilt dedup → access → DNCI → gaps with SNAP + SLA in** (after giving each anchor
  authority in its category in `score/dedup.py`). 189,957 POIs → 170,792 canonical.
- **Headline: bodega/convenience gap hexes 166 → 16.** Ninety percent of the #2 gap type was an
  OSM/Overture coverage hole, not a missing business. Hexes with a bodega reachable in 10 min
  rose 4,523 → 5,595. Recorded on QUESTIONS.md M1.
- Total gap hexes 648 → 691: SNAP pushed ~150 hexes over the ≥12/15 walkability bar, exposing
  their other gaps. Lead types now: hardware 166, fitness 144, clinic 112, **bar 103**,
  childcare 68, laundry 36, pharmacy 32, convenience 16.
- Bar 7 → 145 (any-missing) is the qualification effect, not a data fault: of the 145, only 14
  have a bar within ~300 m straight-line, 93 within ~600 m, and 123 reach one at 15 min. They
  are marginal 10-minute gaps, mostly in the newly qualified hexes. SLA canonical bars: 3,182;
  Overture: 5,147.
- **The webmap's exported JSON (hexes.geojson, gap_buildings.json, meta.json) predates this
  rebuild** and must be re-exported before the map reflects the new gaps.
- **Google Places is now sampled ground truth only, never a source** (Maps Platform terms
  forbid storing Places content; bulk enumeration would also cost thousands). New
  `src/loci/validation/`: budget-guarded client (persisted ledger, refuses without
  `LOCI_GOOGLE_CALL_BUDGET`, minimal field mask) + income-decile stratified sample storing
  counts only. `loci validate --dry-run` plans; `--run` spends. Default plan 200 hexes ×
  15 categories = 3,000 calls, inside the free tier. GTM-11, GTM-47 done.
- **Webmap per-click Google calls capped:** `GOOGLE_CLICK_BUDGET` (default 0 = off) on top of
  the rate limit; the front end explains the batch path instead of an error.
- **Foursquare BLOCKED on access.** The public S3 bucket was retired (LICENSE/NOTICE only);
  data is on the Places Portal (Iceberg token) or the gated Hugging Face mirror. Adapter written
  and unit-tested (`sources/universal/foursquare_places.py`, reads `hf://` with `HF_TOKEN`,
  caches the NYC extract once, reports unmapped leaves on dry run). GTM-15 in progress.
- 37 tests pass. `make check` passes (25 sources, 24 homework items).

### 2026-09-02 — Session 8: two anchor sources verified + adapters, five registered
Ran in parallel with the session-7 work; it did not touch the model, the map, or the scope.
- **Why this matters MORE under the re-scope:** `hex_gaps` calls a category "missing" when
  no POI is reachable. The #2 gap type is bodega/convenience (166 hexes) and grocery,
  pharmacy and bar all ride on OSM/Overture, the sources with the worst small-business
  coverage. Some of those gaps are data gaps. Anchors are how the screen tells the two apart.
- **USDA SNAP Retailer Locator** — verified: ArcGIS feature service, 16,624 NY rows, lat/lon
  on every row, `Store_Type` separates Supermarket/Grocery from Convenience. Adapter
  `sources/universal/usda_snap.py` (bbox-scoped, portable). Dry run: 8,975 stores →
  4,607 grocery / 4,368 convenience. Bias runs OPPOSITE to OSM (misses non-SNAP stores in
  affluent areas). GTM-66.
- **NYS SLA active liquor licences `9s3h-dpkz`** — verified: 24,711 NYC rows, 98.5%
  georeferenced; companion INACTIVE file `6dg3-2z7i` makes closures recoverable. Adapter
  `sources/cities/nyc/nys_sla.py`. Dry run: 3,199 bars. Mapping is conservative and OPEN
  (QUESTIONS.md H-D9): descriptions never say "bar"; emits Food & Beverage Business /
  Club / Cabaret / Bottle Club, skips Restaurant (DOHMH) and Additional-Bar riders. GTM-67.
- Registered as `planned` with bias lines: DOHMH childcare `dsg6-ifza` (3,014 centers;
  childcare is a 62-hex gap type), FDIC branches, NYS pharmacies (bulk access UNVERIFIED),
  MTA bus GTFS, HUD USPS vacancy. Registry now 25 sources; CONTEXT §3 mirrors it.
- Fixed `check-sources --urls`: 4xx was raised as an exception and skipped the 403
  allowance; HTTPError codes now compare directly, 202 counts as present. 25/25 pass.
- **NOT loaded.** Every attempt hit a DuckDB lock from session-7 processes
  (`loci.model.address_gaps`). To land them: `uv run loci ingest --source usda_snap_retailers`,
  `... --source nys_sla_liquor_licenses`, then re-run dedup → `build_access` → `build_gaps`
  and compare the convenience/grocery/bar gap counts before vs after. Then mark GTM-66/67 Done.
- Tickets regenerated: 63 issues, 225 points. QUESTIONS.md still frames the residual/growth
  thesis and needs a pass against the session-7 scope (owner's call).

### 2026-09-01 — Session 4: research questions
- Three-round interview to lay out research questions; wrote `docs/QUESTIONS.md` (D13).
- Added `loci check-questions` drift check and wired it into `make check`.
- Added two exploratory E3 tickets (`Residual convergence test`, `Retail lead/lag timing
  test`) and cross-stratified the validation sample by foreign-born share; regenerated
  tickets and pushed the delta to Linear.
- Pointers added in README.md and CONTEXT.md §9.

### 2026-09-01 — Session 3: GTM-12 resolved
- Confirmed LODES8 block vintage from the tech doc AND empirically from the 2002/2023 NY
  WAC files. Panel unblocked, no crosswalk (D12).
- Discovered the retro-allocation problem; filed GTM-63 and amended threat §7.4.
- Downloaded 2002 + 2023 NY WAC into `data/raw/lodes/` (4.1 MB).

### 2026-09-01 — Session 2: Linear push
- Created 14 labels, 6 milestones, 58 issues in project `loci-723fa10296fb`.
- Marked the five completed E0 items Done; E0 now reads 71% on the project overview.
- Collapsed the label taxonomy 32 → 14 (D11).
- Verified the push: 58 issues, all milestone-assigned, none orphaned.

### 2026-09-01 — Session 1: charter, scaffold, tickets, DuckDB, restructure
- 4-round Socratic interview; composite ambiguity 1.00 → 0.14.
- Research pass verified every data source and cost. Corrected two beliefs: the panel
  could not come from license data (D2), and total cost is **$0–100**, not the $100–500
  budgeted — leaving ~$400 headroom.
- Wrote the charter and full scaffold; all checks pass.
- Generated 58 tickets across 6 milestones.
- Found the `claude.ai Linear` connector already authenticated; removed a duplicate local
  server I had added.
- Found Docker Desktop installed but never initialized; wired it up (D5) — then removed it
  from the critical path entirely by switching to DuckDB (D9).
- Restructured from six root directories to three (D10).
- **Not done:** tickets not yet pushed to Linear.

**D105 — Ground-truth instrument: Interceptor-driven browser sessions against the owner's real logged-in Chrome, first use = verifying the recommendation ledger's 15 open anchors against what is physically at each address.** *(2026-09-14, session 27, abenmayor-cc; ticket GTM-183)*

Owner chose Interceptor (agent-driven CLI over the owner's real logged-in Chrome, github.com/Hacker-Valley-Media/Interceptor) as the tool for Loci's first ground-truth checks on ranked addresses. Owner chose the 15 ledger anchors as the subjects and "existing evidence table + one new observation table" as the evidence home.

Owner 2026-09-14: first session checks the four 2026-09-14 report addresses plus the Gowanus centroid; rows being added by a parallel agent (QUESTIONS D35).

Built: sql/036 (`analysis.address_observation`, `analysis.address_observation_miss`), `src/loci/model/ground_truth.py`, a `loci ground-truth` cli.py sub-app, `tests/test_ground_truth.py` (20 tests), `docs/ground-truth-protocol.md`. Grains: `address_observation` = one observed storefront per anchor per session, with a NULL-name 'vacant' row written when nothing is observed so absence is recorded, not inferred; the miss view = open, same-category, no `poi_presence` match by `name_key` within 40 m, with empty `name_key`s excluded so the tokenizer's blind spot is not reported as the model's. Match is any-category within 40 m; the miss view is deliberately same-category and narrower, so a mis-typed POI reads as a category error, not as missing supply.

Rules: human-supervised, human-paced session only — same footing as the BizQuest precedent (commit fe59eb2), never unattended. D79 preserved: a vacant/unknown observation writes no evidence row; 'closed' is written only from an explicit Maps label; a human reading "Open" off a Maps card may write 'open', unlike `web_rules.py`, which never emits open because a search hit not saying closed is not evidence of trading. `address_observation` is NOT wired into the screen, forecast, or grades — an instrument that scores the screen must not feed it.

Why path (b) on the evidence source: DuckDB 1.5.5 rejects `DROP`/`ADD CONSTRAINT` on a `CHECK` (probed, `NotImplementedException`), and `db.init_schema` applies every `*.sql` on disk (GTM-179/182), so widening the shared evidence table's `CHECK` by a copy-swap would execute inside peer sessions mid-write. Evidence rows instead use `source='web'`, `domain_class='maps_ui'` — `domain_class` already carries source kind and `poi_evidence.basis()` renders it with zero changes.

Finding: all 15 open ledger recs share one anchor (the 2026-09-11 Gowanus bbox centroid, `anchor_address_id NULL`), so a session against them is one look producing 15 category verdicts, not 15 address checks — recorded in QUESTIONS D35, now answered (owner: add the four report addresses as subjects).

Tests: 80 passed across test_ground_truth/test_poi_evidence/test_recommendation; 227 passed across the init_schema neighbours. Interceptor NOT installed. Warehouse writes this session: four `loci recommendations add` rows (376 Graham Ave, 379 Broome St, 4 East 8th St, 545 Sackett St; issued_by abenmayor-cc) — no closure evidence, no poi_status write by intent.

Addendum 2026-09-15 (hash incident, logged at the hash steward's request; cross-referenced from the re-baseline entry): creating sql/036 at ~23:47 on 2026-09-14 moved the live supply hash 467cd5969200 → d993b3802c6a even though 036 names no poi_status or evidence object. `db.init_schema` sweeps every `*.sql` on disk on any write connection; the next write connection was this session's four ledger adds, and the sweep's poi_status re-render flushed closure evidence already in the warehouse, flipping nine POIs to closed under every artefact stamped that night (baseline, address rows, age-fit, revenue calibration, forecast vintage, map). 036 was announced to peers and written last, but the ledger adds ran on top of it before the steward could take the first write — that ordering was the gap. Rule adopted: a new `sql/*.sql` file is itself a hash-moving event; announce it like a poi_status write, create it last, and the hash steward makes the first write connection after it. The four ledger rows carry supply_ratio_at_issue from reports fitted on 467cd5969200; the steward flags them if the morning re-baseline moves those ratios. Re-baseline is the steward's first action on 2026-09-15.

Addendum 2 (2026-09-15, after the re-baseline, D106 commit 084ca55): the steward's flag on the four 2026-09-14 ledger rows was verified. `supply_ratio_at_issue` is defined as the card's `supply_ratio_vs_base` (recommendation_ledger.py:528 ← recommend.py:541, the area median of `analysis.address_category.supply_ratio_vs_base`, the D73 count-based ratio), not `analysis.address_gaps.{cat}_ratio`, which is the distance ratio nearest_m / reach. Three rows were correct under that definition (4 East 8th and 545 Sackett convenience 0.0 = zero open principled POIs within reach, a real reading with the reports' own status-coverage caveat; 376 Graham bank 0.36 = area median, address value 0.35). The 379 Broome convenience row held the distance ratio 0.4908 where the count ratio is 0.309: a data-entry error by the agent that added it, not a hash move. Fixed 2026-09-15, ledger-only and announced: r-20260914-379-broome-street--convenience-6045f4 withdrawn with the reason recorded, re-issued as r-20260914-379-broome-street--convenience-401904 (supply_ratio_at_issue 0.309, issued_on kept 2026-09-14, solution text now labels both measures). Lesson: two measures in this codebase are both called "supply ratio" in prose; the ledger holds the count-based one, and the reports print both. Manifest regenerated: data/ground_truth/manifest-2026-09-15.json, 19 anchors.

Why: the ledger's monthly outcome check (D89) matches only against Foursquare first-seen and storefront filings, so nothing today can contradict a recommendation with what is physically at the anchor. The load-bearing output is `analysis.address_observation_miss`: an open storefront of the recommended category that the supply model does not hold is a direct hit on P3 (coverage-gap validity). A confirmed gap at the anchor does not by itself confirm the 400 m catchment gap; the protocol lists that and four other threats.

**D106 — One quiet-warehouse re-baseline instead of a fourth chase: every stamped artefact moved to supply d993b3802c6a in a single pass.** *(2026-09-15, owner: "continue"; abenmayor-cf)*

Decided: after the fourth hash drift of the day (D105: 467cd5969200 → d993b3802c6a, nine POIs flipped closed when sql/036's creation forced a poi_status re-render), all sessions were frozen and the hash steward ran the whole re-stamp chain once, including the peers' steps: `supply-ratio --fit-baseline` (77 s, every median moved 0.000%), street-frame, address-gaps (215 s), address-demand, pipeline, storefronts, `age-fit fit` (bar and childcare pass; pharmacy refused, Brooklyn 65+ CI includes 0) and apply, supply-ratio, address-access, transit-profile, citibike address-measures, address-character (345 s), storefront-pipeline openings (208 s), dot-counts address-context, `address-legality build` (3,449 s — the long pole; commercial 49,036 / grandfathered 19,102 / ineligible 213,522 / unknown 50,381), `revenue fit` (345 s; restaurant alone passes, ε 0.4, β 1.25, γ −0.5, ρ_oos 0.829) and `revenue --boroughs MN,BK` (223 s), `forecast issue --month 2026-09 --force` (1,050 s; model_version 0.1.1+ae3eadb3, supply_hash d993b3802c6a, 4,227,630 rows, AUC 0.8768 vs 0.8513 no-score; earlier vintages kept as history), forecast-export, citibike export, `export-webmap` (148 files, 123.6 MB, meta.json stamps d993b3802c6a for screen and ageFit). Hash guard equal at six checks. Result: the gap screen is byte-identical to re-run #3 (223,107 gap addresses, Σgap_score 485,854.61, Σgap_score_fit 401,798.02, every lead-gap count unchanged, lead_censored BK 14,422 / MN 720); only supply_hash and legality (previously NULL) changed on analysis.address; suite 1,876 passed / 1 skipped; commit 17900c2. abenmayor-cc is verifying its four 2026-09-14 ledger rows against d993b3802c6a (the re-run check compared a count-based ratio with the rows' distance ratio, so no move is established); outcome logged under D105. Why: three canonical re-runs earlier in the night were each chased by another session's write (a closure verdict, a key migration, a migration file's sweep); the fix was not a fourth chase but a freeze of every session followed by one pass that also ran the absent peers' steps (revenue, forecast) so nothing ended a hash behind. Rules confirmed: a 'final' signal freezes poi_status writes, migrations AND view re-renders; the steward runs the first write after any new sql/*.sql; peers' re-stamp steps are part of the canonical order, not separate follow-ups. Cross-reference D105 for the cause and D94/D101 for the two supply changes this re-stamps.

**D107 — First ground-truth session: 12 of 19 recommendation anchors have open supply of the recommended category inside 400 m; five warehouse POIs found Permanently closed; the instrument stays a verifier and is not wired into the screen.** *(2026-09-15, session 27 continued, abenmayor-cc; ticket GTM-183 Done, follow-ups GTM-185)*

Setup: Interceptor Browser 1.0.1 installed (pkg SHA256 verified, Developer ID Hacker Valley Media LLC, notarized); first extension install landed in Arc because the Web Store link followed the OS default browser — reinstalled in Chrome; one Chrome context. 57 page loads, human-paced (3 s waits), zero timeouts, dedicated tab group loci-ground-truth, owner present throughout.

Protocol change from the live probe: the coordinate pin URL lists no businesses; the reads that work are the category-nearby search (`/maps/search/<term>/@lat,lon,18z`), the address panel ("At this place"), and Street View. Manifest now carries nearby_url (per-category search term table), address_url (MapPLUTO street address by BBL, fallback rec area_label, then neighborhood), area_kind; write is atomic. Commit f579c08.

Results (run gt-2026-09-15-session1): verdicts supply_missed 12, confirmed_gap 7, inconclusive 0. Address anchors: 376 Graham bank — Cross County Savings Bank open 122 m away (cleanest falsification; building holds Thee Brooklyn Barber + El Punto Cubano, open); 379 Broome convenience — two open stores ≤150 m (confirms the pre-registered no-gap conclusion); 4 East 8th convenience — four open stores ≤300 m (the 0.0 ratio was a status-coverage hole); 545 Sackett convenience — nearest open store 407 m, but the parcel is a fenced construction lot, not a retail site. Gowanus centroid: 9 of 15 categories have open supply inside 400 m; bank/hair/pharmacy/hardware are gaps at 524–732 m; convenience (410 m) and laundry (405 m) sit within measurement error of the radius.

Closure misses: five POIs open in the warehouse (closed_on NULL, last_seen 2026-09) are Permanently closed on Maps: OKOZUSHI by Megumi (overture) and Okozushi (dohmh 50100674) at 376 Graham; Greecologies (overture) at the 379 Broome anchor itself, now Stone Street Café; Daphne European Skincare (foursquare) 375 Broome; Wild Ginger Vegetarian Kitchen (dohmh 41433964) 380 Broome. Control passed: Concord Hill (376 Graham) closed on Maps and already closed_on 2022-10-15 in the warehouse. Six POIs with no Maps listing recorded unknown, not closed (D79).

Warehouse effect: record wrote 103 address_observation rows, 41 poi_closure_evidence rows (source web, domain_class maps_ui; 6 closed, 35 open). The 6 closed verdicts moved the supply hash d993b3802c6a → ba944e18c57b (announced; abenmayor-db held writes; abenmayor-61 no objection; steward abenmayor-cf offline). Re-ingest under the 400 m match did not move it again. tests/test_supply_ratio.py::test_baseline_supply_hash_matches_the_live_poi_supply is red until the re-baseline; owner ruled "run it now" and the next session needing stamped artefacts owns the re-baseline on ba944e18c57b.

Fixes in the same window: name match 40 m → 400 m (the nearby read spans the catchment, so known POIs 120 m out were false misses: 63 → 45 candidates); `record --replace-run`; report NaN crash. 35 tests. Commits: f579c08 (manifest URLs, protocol), 74bc539 (400 m match, replace-run, report fix), plus this docs commit.

Addendum (2026-09-15, later): owner answered QUESTIONS D36 — Google Maps segments capped at 30 minutes with at least a 2-minute break between segments, owner present, never unattended; protocol §1 updated (d7c6fd1). The cap is a protocol rule, not code, because the browser half is a supervised session rather than a pipeline; the first session ran ~28 minutes.

Addendum (2026-09-15, later still): owner approved the free price channel — ground-truth reads copy the Google Maps price token per storefront verbatim into `address_observation.price_label` (nullable column added via an idempotent ADD COLUMN IF NOT EXISTS in sql/036; never parsed at ingest), report shows `n_priced`; protocol §1/§3/§4/§5 updated; commit b871dbb, 42 tests. Grounds: two budgeted Places probes (QUESTIONS D49; 47 calls, ~$1.65, ledger 8,992 → 9,039) found price on ~80% of matched food/bar places and on no service category, and `priceLevel`/`priceRange`/`rating`/`userRatingCount` all sit in the Enterprise SKU (+9.4% per call), so the browser side-effect is the only free source and is food-only in practice. No paid price line approved; no change to the D36 session cap or to scope (price is captured only for storefronts the protocol already reads). Owner declined a dedicated price pilot.

Reading of the miss view: 45 rows is a candidate list, not a count — chain-name mismatches and hits beyond 400 m dominate the sample; see GTM-185 (a)(b).

Why the decision: the instrument did what it was built for (P3 falsification at the anchor) and is kept as a verifier; it stays out of the screen, forecast and grades. Interceptor assessment: strong at structured DOM reads in the owner's own browser and at running from a text protocol on a cheap model; weak where Google's labels or imagery are thin; setup needs one probe per new site.

### 2026-09-15 — Session 27 (continued, abenmayor-cc): first ground-truth run scored, five closures found (D107)
- Ran the first live `loci ground-truth` session against Interceptor 1.0.1 on 19 anchors (four addresses + Gowanus centroid); 12 of 19 verdicts are supply_missed, 7 confirmed_gap.
- Five warehouse POIs found Permanently closed on Maps (376 Graham x2, 379 Broome x2, 380 Broome x1); wrote 41 poi_closure_evidence rows; supply hash moved d993b3802c6a → ba944e18c57b (announced, writes held by peers).
- Follow-ups opened as GTM-185 (a-d): storefront distance in the observation grain, name-alias reconciliation of the 45 miss candidates, deli/bodega search terms for convenience, and an anchor eligibility (building-class) check before an address-level recommendation issues.
- Next action: re-baseline on ba944e18c57b owed by the next session needing stamped artefacts.

### 2026-09-14 — Session 27 (abenmayor-cc): Interceptor evaluated for Loci, ground-truth instrument built (D105)
- Interceptor (github.com/Hacker-Valley-Media/Interceptor) evaluated as the browser-driving CLI for Loci's ground-truth checks; owner ruled it in, with the 15 ledger anchors as first subjects, later narrowed by the owner to the four 2026-09-14 report addresses plus the Gowanus centroid (QUESTIONS D35).
- Built sql/036, `src/loci/model/ground_truth.py`, `loci ground-truth` cli.py sub-app, `tests/test_ground_truth.py` (20 tests), `docs/ground-truth-protocol.md`. D105, ticket GTM-183.
- Next action: owner installs Interceptor and runs the first ground-truth session on the four address anchors plus the Gowanus centroid.

**D108 — Citi Bike phase 2 shipped: OD leakage per NTA × category, CONTEXT ONLY (GTM-167).** *(2026-09-15, session 28, abenmayor-db; ticket GTM-167 Done)*

[D108 details follow...]

**D109 — Chain watchlist admission: two tiers, a candidate predicate, exclusions, incumbents kept off the lead list.** *(2026-09-15, owner, GTM-186–191)*

The list had no intake: all 123 rows arrived in one seeded payload on
2026-09-13 (misread as 121 while a peer was mid-edit) and nothing had been
added since — until two more rows (All'Antico Vinaio, Super Burrito) landed
the same way on 2026-09-15, which is what forced the question. Owner ruled:
a brand enters the mechanical CANDIDATE pool at `locations_total >= 5` OR
`detect.flag_for` (≥3 new in 12m, or ≥2 new of ≤8), after aliasing, with
`n_sources >= 2`, not in an excluded class, and at least one movement signal
(opening, press hit, or filing) in 12 months — "5+" alone was rejected as
the primary gate because on the 2026-09-13 snapshot it would have admitted
2,341 unlisted brands including banks and gas stations while missing 244
flagged sub-5 brands (150 of them two-sourced) that `flag_for` exists to
catch, and 41 of the 123 already-admitted brands have no detect row at all
(nothing licenses a climbing gym) — so the count is a floor, not the
predicate. Excluding banks and health-system practice lines and requiring
12-month movement narrows the 2,341 to 697, the honest size of the backlog.
National incumbents (100+ locations: Dunkin', Starbucks, Chase) are admitted
rather than excluded, but with `sales_role: incumbent`, sorted out of the
broker lead-list default order and reserved for the D19 "brand X opening
nearby" recommend-card signal — a broker doesn't want "Dunkin' is
expanding," the card genuinely does. Hard exclusions, each with a reason:
banks, health-system practice lines, agent networks (MoneyGram/Western
Union — counters inside bodegas, not storefronts, and the fastest-"growing"
name in the first raw detect run), wireless carriers, parking, ATMs,
government/postal, and fuel-branded convenience. Contraction-tier brands
(Rite Aid, Walgreens) stay on the list as `sales_role: contraction` — a
pharmacy net-closure is the supply event that made a laundromat chain's
expansion possible, which is a lead in the other direction. First-run
backlog: the owner will triage the top 100 unlisted candidates by
`locations_new_12m` by hand; the rest waits for the 2026-10 snapshot delta
rather than a multi-session sweep of all 697. Quarterly re-verification is
the only path to `confidence: verified` — as of today 0 of 123 admitted rows
carry a `last_verified` date and 0 are verified, so the first quarterly pass
is the first time anyone here will have counted a store rather than read a
number off a seeded payload.

Caveat on every count above: they were read on the 2026-09-13 detect
snapshot. A 2026-09-15 rebuild under the fixed normalizer and the
post-D101/D103 deduped ledger moved the base materially (20,989 detected
brands vs 26,151, 660 flagged vs 436, 2,241 at 5+ locations vs 2,403) — the
snapshot shrank roughly a fifth on dedup alone, which is itself the reason
aliasing is C1's precondition and not an afterthought: the same rebuild
caught "Raising Cane's Chicken Fingers" (17 locations) still splitting from
"Raising Cane's" (14) as one chain the 2026-09-15 rebuild detects at 20 locations / 7 new in 12m once aliased (the dedup collapses the overlap rather than summing 17+14). All'Antico
Vinaio's two seeded rows also collapsed to one key at 10 locations / 0 new
12m under the fix. Treat 2,341 / 244 / 697 as illustrations of scale on the
2026-09-13 snapshot, not as the current backlog — re-derive them on the
first real `loci chains candidates` run (T1) against the rebuilt base.

Two questions remain open and are not part of this ruling: what evidence
promotes a row to `confidence: verified` beyond a store-locator count, and
whether to request a RetailStat quote now or wait for revenue (QUESTIONS
D46/D47).

Owner rulings, all on the recommended option: (R1) the revenue model's λ stays untouched — it is a six-term identification constant (true leakage, commuter inflow, non-household demand, CEX mapping error, unit-to-household gap, POI-vs-EC count gap) that an OD matrix cannot decompose, so wiring bike leakage into it would silently move every shipped revenue number; OD leakage ships as a table, a view and card measures only, with the Huff denominator as the entry point if it ever graduates (QUESTIONS D42). (R2) the ticket's literal "AM/PM share > 1" residential-dock rule is arithmetically backwards — measured, it types 59 of 2,611 docks residential because the 15–19 window carries more volume everywhere — replaced by the weekday start:end ratio-of-ratios (am_starts/am_ends)/(pm_starts/pm_ends), >1.2 residential, <0.8 destination, else mixed, <200 trips in the station-month unknown; on the whole panel residential docks originate 50.3% of trips, destination 33.2%, mixed 15.6%, unknown 0.9%. (R3) NTA-grain card values are stamped on every address in the NTA, labelled neighbourhood-wide, NULL never 0 where the NTA has no residential dock or too few trips.

Built: sql/037_citibike_od.sql — analysis.bike_od_leakage at (origin_nta, destination_nta, month, day_type, daypart, origin_type) with trips/member_trips/round_trips stored separately, view analysis.bike_od_leakage_evening (window = evening any day OR any daypart Saturday/Sunday, counted once, weekday pm_peak excluded as the commute home, share partitioned by origin × month), nullable bike_od_* columns on analysis.address and bike_od_supplied_share on analysis.address_category (filled only where ratio > 1, D39). Modules sources/cities/nyc/citibike_od.py (imports phase 1's SQL helpers, never redefines them; month is the unit of idempotence; a trip is dated by started_at so conservation against phase 1 holds in every station-month cell, shortfall 0.19–0.49% = the dockless/JC ends) and model/bike_od.py (dock→NTA via python h3 res-9 → analysis.hex, the same rule addresses use — NOT the DuckDB h3_latlng_to_cell, whose BIGINT never matches the VARCHAR h3_index and joins zero rows silently). CLI: loci citibike od-ingest / od-stats / od-measures / od-validate. 70 tests in tests/test_citibike_od.py and tests/test_bike_od.py.

Ingest: 44 contiguous months 2023-01..2026-08, 133 origin → 133 destination NTAs, 151,489,130 NY-dock-to-NY-dock trips, 2.25% round, ~5.24M rows, 14,439 distinct NTA pairs, 0 unmapped docks in every month; ~6 s per month, ~4.5 min for the panel (all 33 archives already on disk).

Red-team (contrarian, before the first measures write) found the supplied-share as first built was a Manhattan indicator: POIs per lot-frame address put 27 of 110 NTAs above median in all 15 categories and 36 in none (86.6% identical indicator across category pairs; MN 0.90 vs BK 0.13), and phantom NTAs (BX0401: 1 lot address, 728 open POIs) sat inside the measurable universe reading 100% supplied. Fixed before any write: density is POIs per 1,000 residential units; an NTA enters the universe only with ≥500 lot-frame addresses; the outside-universe NULL threshold tightened 0.5 → 0.2 and the dropped share stored as bike_od_outside_share; the 200-trip floor now applies to the supplied-share denominator; needs_rebuild watches address_category and keys on the live supply hash (bike_od_supply_hash), not the window label; the card names the denominator ("of the trips that leave here for a Manhattan or Brooklyn neighbourhood we can measure (X% went elsewhere)"), labels Queens/Bronx destinations by name, and the report carries the monthly IQR of top-share (3.1% at the median NTA, 6.6% at p90 — the pooled 12-month window is summer-weighted, Aug ≈ 2.5× Feb).

Measures (window 2025-09..2026-08): 124 origin NTAs above the trip floor, 10,479 pairs, 79 NTAs in the measurable universe; median out-of-NTA share 84.5%; 22.3% of outbound trips land outside the universe, which drops 74 origin NTAs from supplied_share (QUESTIONS D44) plus 2 under the floor; 332,041 MN+BK addresses stamped, 200,976 carry a destination, 131,065 NULL; 226,063 address_category rows carry bike_od_supplied_share (per-category medians: restaurant 0.806, pharmacy 0.428, bank 0.252). Card readings: Gowanus BK0601 — 31% of evening/weekend trips from residential docks stay, 69% leave; Bushwick BK0401 — 15% stay, 85% leave.

Validation (D76 graduation): destination-NTA evening/weekend inflow vs DOT PM/evening pedestrian counts, 43 NTAs over 76 count points: ρ +0.327 against the +0.50 bar; the density baseline (POIs per 1,000 units vs DOT) is +0.005, so the flow beats it. Category placebo: every category beats the null baseline (excess +0.211 to +0.824) — but the null collapsed to 0.6% once density was per-unit (only 2 NTAs are above median in all 15 categories), so that gate is unfalsifiable as built and proves nothing (QUESTIONS D43). **Verdict: CONTEXT ONLY.** Enters no gap_score, no supply_ratio_vs_base, no grade, no λ. The card says "riders," never "residents" (caveat 1).

Migration hazard handled: 037 was developed as 037_citibike_od.sql.draft (invisible to init_schema's *.sql glob), tests applied the draft's text to a temporary DuckDB, and the rename was announced to abenmayor-cc and abenmayor-61 and made only after abenmayor-cc's ground-truth writes completed. This is a working answer to QUESTIONS D25 (QUESTIONS D45). My writes did not move the supply hash: it is ba944e18c57b before and after (moved by D107, not D108). The baseline-hash drift test in tests/test_supply_ratio.py stays red until the re-baseline D107 assigns.

Earlier the same session: 035434b — citibike-ingest now reports system-wide zero-trip dates and states they stay in the weekday divisor (D28 surfaced, not resolved).

Phase 3 (GTM-168) is now unblocked on phase 2; it still waits on the entry-retrodiction panel re-run.

**D108 addendum (2026-09-15, session 28, abenmayor-db) — owner rulings on QUESTIONS D44 and D28.** D44: report every origin NTA's bike_od_supplied_share; the 0.2 outside-universe NULL cut (which dropped 74 of 124 origins) is removed on the D75 no-eligibility-gate footing, and bike_od_outside_share is always stored and printed on both card lines. The 200-trip floor on the inside-universe denominator stays: that is a no-denominator rule, not a gate. Measures re-run (commit 82db230): 332,041 addresses stamped, 200,976 with a destination, 242,772 address_category rows now carry supplied_share (was 226,063); per origin NTA the outside share is 27.2% at the median, 93.5% at the 90th percentile, 98.0% at worst; 17 origins are NULL under the trip floor. D28: the 2026-02-23 system-wide zero-trip day stays in the per-weekday divisor (an average weekday that month really included a day the docks were shut; dropping it would select on the outcome) — the behaviour 035434b already prints, no code change.

### 2026-09-15 — Session 28 (abenmayor-db, Fable executive; Opus/Sonnet/Haiku workers): Citi Bike phase 2 shipped, CONTEXT ONLY (D108)
- Focus chosen by owner: Citi Bike phase 2 (GTM-167). Committed 035434b (D28 console note) at session open.
- Opus design → three owner rulings by buttons → two parallel Opus builds (ingest, model) → Opus red-team → fixes → ingest (4.5 min) → measures → validation.
- Coordinated with abenmayor-cc (ground-truth writes, D107) and abenmayor-61 (chains, read-only until this session's done).
- Result D108: shipped, CONTEXT ONLY. Tests: 70 new, full suite green except the peer's baseline-hash drift test.
- Continued: owner chose Citi Bike phase 3 (GTM-168); rulings by buttons (back-ingest + two vintages; forecast entry only on all gates; Divvy probe + PORTABILITY entry; statistician ratifies the criterion). Opus design → Opus statistician pre-registration (P0–P12) → three parallel Opus builds (growth table, retrodiction test, Lyft-system reader + Divvy probe) → back-ingest 2021–22 (paused once for a peer) → growth-measures → confirmatory then primary retrodiction runs. Result D111: CONTEXT ONLY, Citi Bike plan complete. Also this sitting: D108 addendum (D44/D28 rulings, commit 82db230/2c99b74).
- Then: owner chose the legality long pole + re-baseline. Opus data-engineer diagnosed the nested-loop join and shipped the grid-bucketed match with a read-only byte-identity proof (c36cd18); Sonnet extracted the D106 canonical order; the lead ran the order as a timed script (one designed abort at age-fit's pharmacy refusal, resumed). D112: legality 3,449 s → 3 s; re-baseline on ba944e18c57b complete, drift test green. Peers: abenmayor-61 (chains, D113 pending), abenmayor-57 (CHECKPOINT/QUESTIONS restructure, in flight at write-up time).
- Then (into 2026-09-16): owner chose the investor re-review (GTM-172). Opus investor brief + Sonnet anchor search → four rulings by buttons → Opus developer built R1–R3 + ask 4 + the forecast_latest fix and ran three notes and the first full memo (Gowanus-core hardware, C). The memos exposed the clock-dependent supply hash; owner ruled to pin the as-of; Opus data-engineer built the pin. D115. Peers: abenmayor-61 (D113/D114, sql/039), abenmayor-57 (CHECKPOINT archive restructure 22b74cc, QUESTIONS sections 79001ad), abenmayor-cc (exited after 45b3156).
- Then (2026-09-16, later): owner chose D42/D43 modelling. Opus data-scientist (D42) and Opus statistician (D43) designs → four rulings (park D42; adopt the D43 package; NULL failing categories; blind ratification) → blind Opus statistician fixed thresholds → Opus developer built the placebo → two read-only runs (stratified, then spatial-block primary per the diagnostics rule). Then the owner asked for a QUESTIONS/Linear reorganisation: Sonnet triage → five Todo tickets picked → QUESTIONS reorganised, tickets.py realigned, Linear pushed. D120. Peers: abenmayor-af (D116 charter, D119), abenmayor-61 (D113/D114), abenmayor-57 (restructure), abenmayor-84 (D117).

### 2026-09-15 — Session 29 (abenmayor-chains, Haiku mechanical edit): Chain watchlist admission ruled and stamped (D109)
- Owner ruling on chain watchlist intake: two tiers (mechanical CANDIDATE pool and hand-admitted YAML), candidate predicate (≥5 locations OR `flag_for`, ≥2 sources after aliasing, not excluded, movement in 12m), eight hard exclusions with reasons, incumbents (100+) as `sales_role: incumbent` off the lead list (for D19 recommend card). Triggered by two ad-hoc adds (All'Antico Vinaio, Super Burrito). Raises Cane's aliasing issue: 17+14 locations collapsed to one key at 20 after the 2026-09-15 rebuild.
- Tickets T1–T6 opened: `loci chains candidates`, admit/reject commands, exclusion rule set, schema extensions (watchlist.yaml + brand_snapshot columns), trajectory_state classifier, press-domain refresh and Tavily split (GTM-186–191).
- Result D109: admission pipeline ticketed. First real `loci chains candidates` run re-derives the 2,341/244/697 backlog figures against the 2026-09-15 rebuilt snapshot. Two questions remain open (QUESTIONS D46/D47).

### 2026-09-15 — Session 30 (abenmayor-chains, Opus analysis): Chain watchlist tier 3 `watch` designed and ticketed (D110)
- Owner ask from QUESTIONS D48 (pre-chain detection, examples Bathhouse and Mink) designed as tier 3 on top of D109: one-or-two-location operators with an intent signal, read off `analysis.poi_first_seen` because `detect.MIN_LOCATIONS = 2` excludes single-site operators from `brand_latest` entirely.
- Retrospective run read-only against the warehouse: Bathhouse's second (Flatiron) opening was **invisible** — every filing feed except DOHMH starts 2024-09-13 and `press_hits` spans 45 days, so signals (a) and (b) are unarchived, not absent; the third site (540 Atlantic Ave) *was* visible 86 days ahead via the SLA pending application.
- Owner clarified Mink = **Mink Padel**, West Harlem. Loci holds **zero rows** on it anywhere. Padel Haus is in the warehouse only through its café; the Janus Property fit-outs on the block carry the landlord's name or none. Added signal (f), a fit-out free-text scan, on the strength of "Padel State" being findable only by its work description — the only in-warehouse route to premium categories, which is the owner's real point.
- Pool sized: 168 brand keys (22 with a not-yet-open second-site filing) out of 40,691 one-or-two-location keys in MN+BK, all 22 liquor applications — food-and-drink by construction. Ticket GTM-192 opened. Three owner rulings taken: Tavily `MAX_QUERIES` 60 → 68 (not a re-split), `watch` rows internal-only, Mink = Mink Padel.

**D110 — Chain watchlist tier 3 `watch`: pre-chain operators at one or two locations with an intent signal, internal-only until they graduate; Tavily budget 60 → 68.** *(2026-09-15, owner, GTM-192)*

The D109 candidate predicate starts at five locations or `flag_for`'s "2 new of ≤8", so by
construction it meets a brand only after it has expanded; the owner asked for the earlier
read — "identify chains before they grow. Thinking bathhouse, perhaps Mink" (QUESTIONS D48).
Tier 3 is `watch`: an operator with **one or two open MN+BK locations** plus **at least one
intent signal in 12 months**, not in a D109 excluded class. It must read
`analysis.poi_first_seen`, not `chains.brand_latest` — `detect.MIN_LOCATIONS = 2` means a
one-site operator has no brand row at all — keyed on `chains.normalize.brand_key` so a row
graduates to CANDIDATE without changing key, and explicitly **not** on
`poi_first_seen.name_key`, which is token-sorted while `storefront_filing.business_name_key`
is not. No category gate, for the same reason D109 admits null-`loci_category` rows and a
sharper one: Bathhouse's own POI rows split across `nails_beauty` and `restaurant`, so any
category filter would admit or drop the owner's headline example by coin-flip;
`pipeline_coverage` (GTM-189) carries `real`/`structural_zero` beside the count so a zero
for a spa reads as unmeasured, not as nothing coming. A `watch` row is **internal only**
(owner ruling): it never reaches the broker lead list or a D19 card until it graduates,
because the evidence behind it is one filing or one headline and putting a single-site
operator in front of a broker as a growing chain on that basis would cost the list its
credibility — the pool below has a named false-positive tail that makes the risk concrete.
Graduation is the D109 predicate firing, which is also the moment the row becomes externally
visible, with `watched_since` retained so the tier's lead time becomes measurable rather than
claimed; expiry is 18 months without a new signal, set against D80's own clock (fit-out →
first-inspection p75 = 378 days, so a 12-month expiry would drop operators still mid-build).
The retrospective is the reason the tier is scoped to signals (a), (b), (e) and a new (f),
and not (d):
**Bathhouse** is already a D109 candidate today (`locations_total` 5, flagged "2 new of only
5") but that is half artefact — three physical sites fragmented into five clusters, and one
of the two "new" locations is the **2019** Williamsburg site re-dated by its 2026-03-24
DOHMH row — and it fired in 2026-09, **32 months after** the Flatiron site opened in January
2024. At December 2023 the warehouse could see **none** of the D48 signals: the earliest
Flatiron filing is a DOHMH inspection dated 2024-04-02, three months *post*-opening, and the
deeper reason is structural — in `staging.storefront_filing` every feed except DOHMH begins
**2024-09-13** (SLA pending: 1,542 rows, 2024-10-01 onward), because they are live-status
snapshots with no history, so signal (a) can never be back-tested before late 2024 and can
only accrue forward. `chains.press_hits` is likewise a 45-day queue (443 rows, 2026-07-30 →
2026-09-12), so signal (b) is unarchived rather than absent; the Crain's Flatiron-expansion
piece that would have given ~12 months of warning was published before Loci was collecting.
The $35M Imaginary Ventures round (May 2026) post-dates the second opening by two years and
is no help for it. Where the tier would have won is the **third** site: the 540 Atlantic Ave
`liquor_application` is dated 2026-02-04 against a 2026-05-01 activation — **86 days** of
warning, and seven months ahead of D109. **Mink is Mink Padel** (owner, West Harlem, 439 W
127th St, Manhattan's first public padel courts, two outdoor courts in the courtyard of
Janus Property's converted Mink Building, opened 2025) **and Loci holds nothing on it** —
zero rows in `poi_first_seen`, `staging.poi`, `storefront_filing`, `storefront_pipeline` and
`press_hits`; the nearest ledger POI to the address is a barber shop 70 m away. That is the
more important half of the retrospective, because it is not a gap that accrual fixes: a
padel club with no food or drink enters no feed Loci reads. **Padel Haus**, by contrast, is
in the warehouse at three BBLs — but only through its café (DOHMH "Juice, Smoothies, Fruit
Salads" 2023-08-09; `liquor_active` 2026-03-10 / 05-08 / 05-12), i.e. the filings see the
bar, not the sport. And where a premium operator's landlord does file, the filing carries
the **landlord's** name: 429 W 127th St has DOB fit-outs under "Janus Property Company"
(2025-10-28, 2025-11-26 "convert the use and occupancy of the first floor"), and 418 W 127th
a 2024-11-12 fence-and-gate fit-out with **no business name at all** — no brand-key rule can
recover "Mink Padel" from either. Signal (e) also cannot fire, because "Mink" is the
building's name, not an operator family. One route does work and is new to this design:
the only filing in the warehouse that names padel is **"Padel State"** (73 West St BK, DOB
`fitout_filing` 2025-07-25, not yet open, null category) found by its work description —
*"temporary installation of Prefabricated Assembly Padel Ball Courts"* — so tier 3 adds a
signal (f), a monthly derived scan of fit-out free text, which is the only in-warehouse
route to premium categories and costs nothing. Pool size for
signal (a) alone, measured today: 40,691 brand keys have one or two open MN+BK locations in
the five filing-real categories; **168** of them (0.41%) have a 12-month filing more than
150 m from every existing site, of which **22 have not yet opened** — all 22 entered at
`liquor_application`, so the SLA pending feed is the whole workhorse. That number is a floor
with a false-positive tail, not an estimate: name-key matching across filings and POIs is
lossy (D80 mapped 20.6% of filings to a category; franchisee and operating-company filings
are invisible to the brand key), 19 rows were dropped for missing coordinates, the 150 m
rule is a proxy because the ledger carries lon/lat and not BBL, and the observed false
positives are named — CulinArt (a contract caterer), Watches of Switzerland (a retailer's
bar licence), FLORIDITA 4159 LLC, and generic keys like "el malecon restaurant" matching
19.9 km away — and, per the Mink Padel finding, the pool is food-and-drink **by
construction**: all 22 are liquor applications, so no gym, spa, climbing wall or padel club
can appear in it at any threshold, the same blind spot D109 records when it notes that 41 of
123 admitted brands have no detect row because nothing licenses a climbing gym. Budget:
(a), the "Brand II" hint for (e) and the new fit-out-text scan (f) are derived monthly
inside the existing filings pipeline at no new spend; (b) needs a new Tavily
`query_kind = 'watch'`, and the owner **rejected re-splitting the code-enforced ceiling and
ruled a raise instead — `research.MAX_QUERIES` 60 → 68**, keeping GTM-191's 12/24/24 intact
and adding 8 watch queries. The re-split would have funded tier 3 by stretching the
admitted-brand re-verification cycle from five months to seven, and D109 already records
that 0 of 123 admitted rows are `verified` or carry a `last_verified` date, so it would have
slowed the one process that has never yet completed once; the cost of the ruling instead is
explicit and small, 8 Tavily credits a month (~13%), still enforced in code, with discovery
first in `build_plan` so truncation eats watch triage before discovery. Signal (d),
expansion-role job postings, is dropped: no free source, highest noise, lowest yield. Human
cost ~30 minutes a month on top of D109's 45–60. Left open deliberately: whether tier 3
should accept categories the filings cannot see at all — if it does, (b), (c) and (f) are
its only routes and the honest expectation is a handful of rows a year, not a pipeline; the
recommendation is to accept them and make `structural_zero` render as "no filing coverage
for this category" rather than a silent zero. Not built.

**D111 — Citi Bike phase 3: activity growth adds nothing to entry prediction; CONTEXT ONLY; reader proven portable to Divvy (GTM-168).** *(2026-09-15, session 28, abenmayor-db; ticket GTM-168 Done)*

Owner rulings (all on the recommended option): (R1) back-ingest 2021-02..2022-12 and run two retrodiction vintages — t0 = 2023-01 (growth on a COVID-recovery base, the only line-for-line comparison with D88) and t0 = 2025-01 (clean growth, right-censored outcome); (R2) the feature enters the 2026-10 forecast vintage as model 0.2.0 only if every gate passes, otherwise CONTEXT ONLY (D76); (R3) Divvy scope = parameterise the reader per system, prove one Chicago month, write the PORTABILITY entry; (R4) the ship criterion is ratified by the statistician before any fit — the pre-registration (P0–P12) is in the run's report and reproduced in docs/retrodiction-2026-09.md's D111 section: one primary test (pooled 6 categories, t0 = 2023-01, member-only, direct, NTA-blocked CV), baseline FULL + docks_added_24m, identical rows and folds, floor = max(+0.005, placebo-Δ p95) with the CD-clustered two-sided 95% CI lower limit > +0.002, NTA-block-donation placebo run BEFORE the real fit, fold-seed stability (Δ > 0 in ≥ 18/20), 2025-01 confirmatory with a joint-resample compatibility CI, BH q = 0.10 over the declared family, Moran's I and effective-n diagnostics, co-movement language only.

Built (commit 692fde8; sql/038 landed after peer clearance, the D108 .draft pattern): analysis.address_bike_growth at address × asof_month — 12m-over-12m log-ratio of weekday MEMBER throughput within the 400 m reachable dock set, on a BALANCED dock set only (first_month ≤ M−23, last_month ≥ M; a dock born inside the window contributes to neither window), NULL below a 0.5 balanced share, detrended against the system (bike_growth_12m_rel is the feature), docks_added_24m stored as a separate control; CLI citibike growth-measures / growth-stats; validation/retrodiction.py D111 growth test with CLI flags on retrodiction run; sources/cities/lyft_bikeshare.py SYSTEMS (nyc_citibike, chicago_divvy) with nyc/citibike.py reduced to a binding over it and every NYC SQL string proven byte-identical; citibike divvy-probe. 69 new tests; suite 2,033 passed (the one failure is D107's baseline-hash drift test).

Back-ingest: 2021-02..2022-12 in two sittings (paused once for abenmayor-61's owner-waiting query — the ingest holds an exclusive DuckDB lock that refuses even read-only connections); panel now 67 contiguous months 2021-02..2026-08. Growth measures: asof 2023-01 — 1,224 of 2,684 docks balanced (81.8% of system activity), 626 born inside the window and excluded, 87,203 of 281,842 lot addresses carry a value; asof 2025-01 — 1,808 balanced (89.0%), 468 excluded, 116,188 carry a value; validation zero failures.

Confirmatory vintage 2025-01 (runtime 542 s): P2 attrition 59.0% of panel rows (4,917 of 12,000 addresses carry growth; retained addresses are denser and more retail — std diff +0.95 retail_index, +1.57 log_homes), so every claim is about the DOCK-MATURE CORE; effective n 2,668 distinct dock sets; feature Moran's I +0.81, across-NTA +0.30 (CD blocking binds). FULL 0.8755, baseline FULL+docks_added 0.8756, +growth 0.8756: Δ-AUC +0.0000; placebo Δ p95 +0.0002 so the floor is the absolute +0.005; CD-clustered CI [−0.0005, +0.0006]; fold-seed Δ positive 1/20; auxiliary R² 0.052 (the feature carries independent variation — it is simply uninformative); residual Moran's I 0.683 → 0.683; coefficient −0.42 [−1.29, +0.44]; all six per-category Δs "not a finding" under BH. Ascertainment rule truncated nothing (0 months).

Primary vintage 2023-01 (runtime 107 s; cohort 10,495; the model's own score lift reproduces D88 — entry AUC 0.8674 vs 0.8518 without the score, +0.0155): P2 attrition 69.3% of panel rows (3,678 of 12,000 addresses carry growth — the 2021–22 balanced dock set is smaller), effective n 2,058 distinct dock sets. FULL 0.8314, baseline FULL+docks_added 0.8312, +growth 0.8309: Δ-AUC −0.0004; placebo Δ p95 +0.0007 (floor stays the absolute +0.005); CD-clustered CI [−0.0020, +0.0013]; fold-seed Δ positive 10/20; auxiliary R² 0.285; residualised Δ +0.0001 (flagged: larger than the direct lift, itself a red flag under P6); residual Moran's I 0.679 → 0.680; coefficient +0.67 [−0.16, +1.50] — indistinguishable from zero, and opposite in sign to the confirmatory vintage's −0.42. Ascertainment truncated nothing.

Verdict: CONTEXT ONLY on both vintages — P3 fails (Δ below the floor, CI lower limit below +0.002), P8 fails (10/20 and 1/20 positive seeds), P7 moot. The Citi Bike activity series enters no grade, no supply ratio, no forecast (FEATURE_LIST and MODEL_SEMVER unchanged at 0.1.1). Failure ships as a finding (P12): the change in bike activity before t0 co-moves with nothing the entry model does not already carry — consistent with D88 (the screen ranks retail streets) and with phases 1–2 (levels and flows are card context only).

Portability (R3): one real Divvy month (2025-06: 678,904 trips, 386,811 member / 292,093 casual, dockless ends 156,772 = 23.1% REPORTED under a per-system gate, not dropped) landed as 13,600 station-month rows in a scratch DB with the day_type × daypart partition summing exactly; a latent CRLF header bug in the shared extractor (only Chicago's files trigger it) fixed; docs/PORTABILITY.md §8 (generated from src/loci/portability.py) says portable = the trip-file reader and the station-month grain travel, NOT Loci in Chicago (no address frame, walk graph, PLUTO, supply set → no address measures, no growth feature, no claim about Chicago retail).

Reporting quibbles to fix (QUESTIONS D50): the verdict text assumes the run is the primary vintage even when --asof is the confirmatory one; the lagged-growth pre-trend (P9) column was not built for the 2025-01 run, moot for a null contemporaneous effect but required before any positive result could ship; the run stamps supply hash d993b3802c6a from the baseline table rather than the live ba944e18c57b — provenance should read the live hash.

Citi Bike plan (owner 2026-09-14, "make sure we don't forget to finish this") is COMPLETE: phase 1 D102, phase 2 D108, phase 3 D111. GTM-168 Done.

**D112 — address-legality long pole removed (3,449 s → 3 s) and the canonical order re-baselined on supply ba944e18c57b (GTM-169; D107's owed re-baseline).** *(2026-09-15, session 28, abenmayor-db; ticket GTM-169)*

Long pole: EXPLAIN on the live warehouse showed the open-POI match in model/address_legality.open_poi_match_sql running as a NESTED_LOOP_JOIN — four inequality predicates and no equality, and DuckDB costed analysis.poi_supply_status (a four-CTE view over 3.8M poi_dedup rows) at ~1 row, so it evaluated ~6.8e10 address × POI pairs. Fix (commit c36cd18): both sides floor to 0.001° cells, the POI side fans to its 3×3 neighbourhood as a MATERIALIZED CTE (without MATERIALIZED DuckDB inlines the view nine times and is slower than the loop: 561 s vs 296 s on a 50k sample), the join is on cell equality, and the original bbox-pad + haversine predicates follow unchanged; assert_grid_covers_pad() refuses a cell narrower than the pad. Equivalence proven read-only before any write: EXCEPT ALL both ways is (0, 0) on the full universe (57,106 match rows) and at 2k/50k samples; the reference SQL is kept as open_poi_match_reference_sql and pinned by five new tests (bucket-boundary fixture, no duplicate fan-out, grid-covers-pad at 40.4–45.0N, loud failure, MATERIALIZED regression). Bare SELECT: 178 s → 1.0 s on the full frame. Standalone `address-legality build` after the fix: ~4 s wall-clock; inside the canonical order (step 15, after address-gaps wiped the columns): 3 s. sql/031's "~90 s" comment corrected. Option (a), a spatial R-tree, was rejected: no persisted index and D104 pins this module off ST_Distance_Sphere.

Re-baseline (this session's runner, per-step timings, the D106 order plus the D108/D111 re-sweeps after step 11): 01 fit-baseline 78 s (332,041 address rows, 4,980,615 address_category rows; median drift: not printed); 02 street-frame 1 s; 03 address-gaps 221 s (332,041 scored points: 281,842 lot + 50,199 street); 04 address-demand 4 s; 05 pipeline 57 s; 06 storefronts 73 s; 07a age-fit fit 6 s — bar and childcare pass and are written, pharmacy refused by its own F2b criterion exactly as D69/D71/D106 (the refusal exits 1 by design; the runner first treated it as fatal and was resumed from 07b); 07b age-fit apply 5 s; 08 supply-ratio 73 s; 09 address-access 46 s; 10 transit-profile 43 s; 11 citibike address-measures 3 s (590,953 station rows, 149,364 addresses measured); 11b od-measures --re-sweep 3 s (332,041 stamped, 200,976 with a destination, 242,772 address_category rows — identical to D108-addendum counts); 11c growth-measures --re-sweep 3 s (87,203 / 116,188 values at 2023-01 / 2025-01 — identical to D111); 12 character 68 s (D106: 345 s); 13 openings 48 s (D106: 208 s); 14 dot-context 2 s; 15 legality 3 s (D106: 3,449 s); 16 revenue fit 149 s (D106: 345 s); 17 revenue 82 s (D106: 223 s); 18 forecast issue 2026-09 --force 292 s (D106: 1,050 s; 4,227,630 rows issued, fit 360,000 rows / 12,000 addresses / 103 NTAs, blocked-CV AUC 0.8768, model_version 0.1.1+51bab17f); 19 forecast-export 16 s; 20 citibike export 3 s; 21 export-webmap 40 s (148 files, 123.6 MB, largest gaps/bar.json 15.93 MB). Total wall-clock 17:31:40 → 17:54:54 ≈ 23 min including the one-minute resume gap, against D106's ~98 min of timed steps.

Outcome: model/supply_baseline.yaml carries ba944e18c57b; analysis.address_gaps rows stamp ba944e18c57b; the forecast vintage is re-issued on it; webmap meta.json carries it; tests/test_supply_ratio.py::test_baseline_supply_hash_matches_the_live_poi_supply is green again (suite 2,139 passed, 1 skipped, 0 failed at 18:12 — the drift test is green for the first time since D107). Gap screen: 332,041 rows → analysis.address_gaps stamped supply_hash=ba944e18c57b (lot 281,842 points, 2,074,244 units, 189,378 with a gap, median gap_score 1.25, 603 clusters, 12,029 censored leads; street 50,199 points, 33,729 with a gap, median 1.28, 601 clusters, 3,113 censored leads); byte-identity against the D106 run was NOT checked this pass — the supply set moved (five closures, D107), so identity is not expected. Steward rule (D106: the steward takes the first write after a new sql file): sql/037 and 038 landed since D106, so this re-run was that first write, announced to abenmayor-61 and abenmayor-cc before it started; no poi_status write occurred, the hash is unchanged by this pass.

Rule for the runner: the age-fit refusal is a designed exit 1 and must not abort the order; the runner script (scratchpad rebaseline.sh) treats step 07a's rc=1 as success and supports resuming from a named step. Worth promoting to `loci rebaseline` (QUESTIONS entry pending — see Next actions).

**D113 — Chain watchlist auto-admission: the monthly job writes every candidate to the list as `auto`; the owner prunes.** *(2026-09-15, owner, GTM-186–188)*

Owner ruled full auto-admission after D109 left the list with no intake — 123 rows seeded 2026-09-13, two added ad hoc 2026-09-15, nothing clearing the candidate predicate became admitted. Machine: `loci chains auto-admit` runs the D109 predicate + exclusions monthly and appends every clear to watchlist.yaml as `tier: admitted`, `confidence: auto`, `decided_by: auto`, `evidence: []`, sorted below hand-vetted rows under a section "Auto-admitted this snapshot (nobody has looked yet)". Human: `loci chains reject <key> --reason` marks a row `tier: rejected` so it never re-surfaces blindly; rejected re-surfaces only if its detect count at the next snapshot doubles or a capital event lands. This supersedes D109's "only a person writes to the admitted watchlist" — the machine admits, the human prunes, at cost of noise reaching the page until the exclusion rule set matures. Built tonight (GTM-186–188): candidates/admit/reject/auto-admit CLI commands shipped; `exclusions.py` (bank/clinic/fuel/health-system/generic-key/geocode-defect guard, tests per class) and `generic_keys.txt` (48 seed entries plus compound names, drift test asserts each is a fixed point); `candidates.py` yields the monthly diff with reason per row. Live numbers 2026-09 snapshot, rebuilt twice after alias pass: 20,949 detected brands, 976 met numeric predicate, 143 excluded (68 bank/clinic, 38 generic key, 32 geocode defect, 5 fuel), 92 already admitted, **741 auto-admitted** — watchlist 161 → 902 rows. Of the 741, 276 entered on `flag_for` below 5-location floor, 49 on not-yet-open filing alone, 0 on press alone; 26 have `sales_role: incumbent` under the 100+ rule plus supermarket co-op banners (Key Food, Associated, Fine Fare, nine others) admitted regardless of count with reason "co-op banner: no single site-selector". Mitigation: alias pass collapsed ~29 key-splits; generic-name stoplist (17 compound names) and geocode-defect guard (n_boroughs = 0 with locations > 0) each flag categories; exact vs contains matching so "Shell Cafe and Grill" is not a gas station. Clinic exclusion stays, with the seven boutique-wellness rows (LaserAway, Ever/Body, Peachy, Bespoke PT, Lenox Hill Radiology, Callen-Lorde, Quantum PT) and two financial services (Chase, Municipal Credit Union) pinned in `exclusions.ADMITTED_AND_EXCLUDED` as documented exceptions. Caveat on every auto row: detect count is a floor with a false-positive tail; "Kennedy Fried Chicken" at ~180 is shared, not one operator; Teppanyaki One / Teriyaki One share an operator but stay separate. Nothing is `verified` (D46 still open). Incident logged 2026-09-15: a build agent ran `git stash` on the shared tree, reverting 127 peer files; recovered, verified, dropped; rule going forward — agent briefs forbid stash/checkout/reset explicitly, not just add/commit.

**D114 — Chain watchlist schema: hand-evidence fields on the YAML, three derived pipeline/press signals on the snapshot; the capital-event escape hatch is armed.** *(2026-09-16, GTM-189)*

Watchlist.yaml gains five optional hand-entered fields that detect/auto-admit never touch: capital_events (list of {kind ∈ fundraise|acquisition|pe_minority|franchise_rights|bankruptcy|closure_program, date REQUIRED, counterparty, amount, url}), signed_leases ({address required, signed_on, expected_open, url}), store_count_source (locator|press|filing|hand_count), expansion_contact_url, trajectory_state (hand override; accelerating|steady|decelerating|declining|static|unmeasured). The new validate() enforces enums, dates and a verified gate: `confidence: verified` requires store_count_source in {hand_count, locator} because a person looking is not a person counting stores. All 902 rows validate unchanged; 0 are verified today so the gate is armed but bites nothing retroactively. The capital-event arm of the rejection escape hatch (D113 left it dormant) is armed: a rejected row re-surfaces when any capital event is dated strictly AFTER decided_on (since, not ever — ruling out information present at the time of rejection). Three nullable columns land on chains.brand_snapshot: pipeline_filings_12m (all storefront_pipeline entries matching the key with entry_date in the 12 calendar months ending the snapshot month), pipeline_coverage ('real' | 'structural_zero' | NULL — real for restaurant/bar/cafe_bakery/grocery/pharmacy, structural_zero when the brand has no loci_category or data, NULL when the optional table is missing), press_hits_12m (same 12-month window on chains.press_hits). sql/039_chains_pipeline_signals.sql re-binds the brand_latest view (DuckDB binds at CREATE time) and switches detect's INSERT from `* ` to a named column list so the table widening is safe. Live deployment: `loci init-db` and `loci chains detect --month 2026-09` re-run 2026-09-16 — 2026-09 snapshot re-issued with the three signals populated: 20,948 brands after a further PureGym alias, 13,638 in filing-covered categories, 7,310 structural_zero, 2,325 with a filing in the twelve-month window, 43 with a press hit (the press table is a 45-day queue, so that is a floor). Tests: +56 fixture-based (watchlist 20→47, candidates 57→70, detect 19→29, render_html 11→17); suite 2,322 passed / 1 skipped.

**D115 — Investor re-review actioned (GTM-172 Done; first full memo at grade C) and the supply hash found to be clock-dependent — owner ruling: pin the as-of to the baseline.** *(2026-09-16, session 28, abenmayor-db; ticket GTM-172 Done)*

**D117 — Second El Punto site search delivered on the evidence in hand; first scored operator datum (rent $4,000/mo = hit); straight-line distances in scratchpad analyses must use METRES_SQL — the D16 lat/lon flip was found in two owner memos and corrected.** *(2026-09-16, owner, GTM-193)*
Owner: "we aren't getting more feedback so we need to use what we have." First operator datum (WhatsApp, Turkish, translated): El Punto rent $4,000/mo — a HIT on the pre-registered $3,500–$6,500 (central $4,800; log error 0.18) and "the shop's biggest advantage is the rent", matching the page; food cost at El Punto LOWER than at Lion's Milk, reversing the page's assumption (Cuban plates cost less than bought-in pastries plus specialty coffee); evenings "wine and dine"; plan: daytime Cuban coffee + breakfast burritos from a warmer (the 07–09 outflow test in a grab-and-go format distinct from the café next door); foot traffic judged higher than Roebling because the door is on the L (not scored — pass-by at the door and shed-level station entries are different measures). Sales, staff and orders still unknown, so economics stays grade C and no revenue figure went on the owner page. Site search (data-scientist, Opus; docs/recommendations/el-punto-second-site-2026-09-16.md): 213,813 MN+BK lot rows → storefronts_400m ≥ 10, a vacancy or a vacant FOOD-SERVICES storefront ≤150 m, >1.5 km from 376 Graham, subway entrance ≤250 m, AM-outflow profile (am/pm > 1.2), on-premises licences 17–64 (re-anchored to Graham's true 39), no Cuban/Caribbean/Latin/Spanish DOHMH venue within 400 m (DOHMH carries no Cuban/Jamaican/Dominican/Puerto Rican label in MN+BK — only Caribbean 362, Latin American 409, Spanish 204) → 47 strict / 88 parity blocks on a 150 m grid, de-duplicated to ≥400 m apart; similarity = equal-weight RMS z-distance to Graham on log homes, family (HH size + under-18), Spanish, WFH, income spread, 18–34, transit shape, evening trade. Top 3: 570 Franklin Ave at Fulton (another Graham: 4,109 homes, 1,154 weekday evening entries vs Graham's 1,039, vacant FOOD-SERVICES unit at 1000 Dean St 208 m, 7 vacant storefronts, 99 active vs 6 stalled pipeline units; card C/diligence); 106 Clifton Pl (richer, entrance 82 m, restaurant ratio 0.67×, but one vacant storefront within 400 m; card C); 444A Greene Ave (vacant storefront at the lot, strongest AM outflow 1.69, but 33.8% under $50k and 41 stalled vs 22 active units → card D/do not act). Placebo: without the 1.5 km exclusion the next four blocks in all of MN+BK sit 22–445 m from Graham's own door — the metric finds Graham on its own. Stability: doubling transit/family weights keeps the top five identical. Rent bands $2,200–$4,600/mo on ~690 sq ft (the owner's revealed ~$70/sq ft inside it); the v0.2 rent ceiling ($1,600–$2,830) sits BELOW the rent he pays and calls cheap — D91's floor in dollars. Owner page "A second El Punto" published (artifact 27517f73). Correction found on the way: the 09-13 Graham site memo and the 09-14 neighborhood memo computed straight-line distances with ST_Distance_Sphere on raw ST_Point(lon, lat); DuckDB spatial reads (lat, lon) — the D16 flip that db.py's METRES_SQL guards. Their "400 m disc" was a 1.44 km × 0.30 km N–S sliver (~2.7× the area). Warehouse columns (homes_400m, supply_400m, storefront_*, pipeline_*) are unaffected — they come from the walk graph. True-disc values at 376 Graham: on-premises licences 39 (was 92; 1 bar not 3), Roebling 112 (was 211), 379 Broome 210 (31 bars; the 363 I had supplied to that report was flipped); Cuban/Caribbean/Latin/Spanish venues within 400 m 2 (Caribbean 0 — Los Primos is 456 m); 318 Graham vacancy 240 m (was 77); shed demographics on the true disc: under-18 13.6% (was 16.2), 18–34 42.9% (35.4), Spanish at home 13.9% (22.0), WFH 30.4% (25.2), under $50k 21.4% (31.4), over $200k 31.2% (19.4), persons/HH 2.06 (2.11) — the neighborhood memo's own 216 m MAUP column had predicted this within a point. Directions of every conclusion survive; levels do not: the Spanish-signage and kids'-plate levers weaken, the WFH lunch-plate lever strengthens, family/evening/delivery/morning levers stand. Both memos carry a dated CORRECTION section; both owner pages (El Punto, Graham Avenue) were corrected and republished the same day with a disclosure; the Stone Street report's licence figure corrected; the peer session whose 39/112 I had wrongly disputed was told it was right. Rule from here: every scratchpad straight-line distance uses METRES_SQL / ST_FlipCoordinates, and every such script asserts a known pair (Graham→Roebling = 976 m) before reporting. Commits 82cff0b, 3dd70ca, d25f964, b4720b1.

Investor re-review (docs/reviews/investor-review-2026-09-15.md), four owner rulings 2026-09-15, all on the recommended option, built in commit 8674c1b: (R1) NO CALL is a distinct verdict for data failure — closure checks disabled, unresolved co-located pairs, no gradeable category, unknowns ≥ opens in the lead category — and carries a re-look date (first of next month); NO TRADE keeps the falsification trigger; the section-0 null is untouched; the closure-disclosure lines and the "what would change this" block now render on the note path as well as the full path (the scrubber only ever deleted prose sentences, never the rendered test). (R2) A provenance footer that contradicts the headline (the same-BBL check) stops the run before the cache read and before any spend — exit 3, no file; the 376 Graham case that shipped a self-refuting note now refuses. Precondition fix: the check excludes the memo's own previous copy. (R3) `loci report --category` override, threaded to the lead-category choice; demoted categories (tailor_repair, hair_barber — D30) refused unless --allow-demoted. Review ask 4 done cheaply: zoning + overlays, special district, LPC, SLA-500ft and the flood line render under the verdict on notes. Ask 5 (dated-article filter letting a homepage and an image-search page through) deferred as cosmetic. Bug found en route: fetchdf() returns NaN for NULL VARCHAR and NaN is truthy, so `x or "—"` crashed the first full memo; fixed.

forecast_latest defect: sql/028's analysis.forecast_latest tie-broke same-month vintages by ORDER BY model_version DESC — lexicographic on a git sha, so 0.1.1+f1cb6628 (frozen 2026-09-14) beat D112's 0.1.1+51bab17f ('f' > '5'); now ORDER BY frozen_at DESC first; applied as CREATE OR REPLACE VIEW, returns 51bab17f. p_opening was byte-identical across the 0.1.1 vintages, so a label bug, not a number bug.

Anchor search (read-only, targeted): grade C is reachable only for restaurant (site-revenue model passes its gate), hardware and tailor_repair (BizBuySell comps: 61 rows citywide) — every other category is capped at D by economics (no cash-flow evidence) and addressable_demand is capped at C for all but laundry. Every high-gap_score address (bar/laundry/convenience/bank leads in Sea Gate, Spring Creek, Canarsie, Sheepshead Bay) grades D on economics. Eight grade-C rows found; the two thinnest restaurant hits (Gravesend-Homecrest 0.56, Windsor Terrace 0.53) are legally ineligible (R4/R5B, no overlay); the only C row with a thin ratio and commercial zoning is Gowanus-core hardware (0.78, ledger r-20260911-gowanus-core-hardware-e110ee). Owner picked it.

Runs (prose via ClaudeCliProse — ANTHROPIC_API_KEY unset, `claude` on PATH; --no-closure-checks throughout, ratified: the 17 planned Places calls would have written closure evidence and moved the hash): 3027550006 REFUSED (R2 working as ruled); 1005500023 and 1004710039 regenerated as NO CALL, grade D (convenience), re-look 2026-10-01; 3004260001 --category hardware rendered the first full memo, grade C, docs/recommendations/3004260001-2026-09-16.md ($0.024). Headline: "Hardware is the lead category at this Gowanus lot and it earns a C: the verdict is diligence, not deployment … the warehouse counts hardware supply at roughly twice the base rate for a catchment like this, and that headline deserves skepticism … none [of the four nearby listings] has a confirmed open status." Cosmetic follow-up: section 4 prints the raw PLUTO spdist1 code ("G") instead of the district name. Report tests 68 passed; suite 2,223 passed with the drift test red — see below.

THE HASH IS CLOCK-DEPENDENT. All four memos stamp supply hash 18eb5ab24629, not D112's ba944e18c57b, and no warehouse write had occurred: model/poi_presence.py's open predicate defaults `today = dt.date.today()` for the 731-day evidence-age window and for licence expiries, so poi_status — and score.supply.supply_hash, which hashes it — rolls at any midnight that ages evidence out. Verified read-only 2026-09-16: live hash 18eb5ab24629 vs baseline yaml ba944e18c57b. Consequences: D112's re-baseline was off the live hash within six hours by clock alone; every "freeze the hash" rule since D105 has been freezing a moving target; the drift test goes red on a schedule. Owner ruling 2026-09-16: PIN THE AS-OF TO THE BASELINE — poi_status and supply_hash take an explicit as-of date stored with the baseline, so the hash moves only on real evidence writes; advancing the date becomes a named step in the canonical order, announced like a poi_status write.

Pin built (Opus data-engineer, read-only proof first): the date entered SQL-side — poi_presence.poi_is_open defaulted today="current_date", which colocation_view_sql bakes into analysis.poi_supply_status 24 times and DuckDB evaluates at QUERY time. Proof with temp views pinned to a date literal: supply_hash @ 2026-09-15 = ba944e18c57b (reproduces D112 exactly), @ 2026-09-16 = 18eb5ab24629; twelve principled POIs flip between the two dates — nine open→closed on NYS DOS licence expiries dated 2026-09-15 (7 nails_beauty, 2 hair_barber; all leave supply) and three open→unknown on DOHMH evidence aging 724→732 days past 731 (2 restaurant, 1 bar; none leave supply). Design: a one-row table analysis.supply_asof(pin, asof_date, set_at, set_by, reason) is the single source of truth; ASOF_SQL = "(SELECT asof_date FROM analysis.supply_asof)" replaces every current_date in poi_is_open / poi_status_basis / poi_status() / poi_evidence.poi_status_date_sql and in the regenerated sql/029 views; read() raises rather than falling back to the clock; supply_hash(con, set, asof=...) added with the asof deliberately NOT in the hash blob so ba944e18c57b stays reproducible; `supply-ratio --fit-baseline` now writes supply_asof beside the wall-clock asof; db.init_schema ensures the table BEFORE the sweep because 029's views bind it at CREATE time. Migration sql/040_supply_asof.sql (developed as .draft, landed after abenmayor-61's window). CLI `loci supply-asof show | advance [--to] [--dry-run] [--force] [--reason]` — advance prints the hash before/after and the status flips, refuses backwards moves, and honours data/SUPPLY_FREEZE or LOCI_SUPPLY_FREEZE (the first machine-enforced freeze; until now 'freeze' was protocol only). Landing = one hash-RESTORING write (init_schema seeds 2026-09-15 and re-renders 029's views: 18eb5ab24629 → ba944e18c57b); the baseline yaml did not need re-stamping; no re-fit and no canonical re-run. 19 new tests; 153 across the presence/colocation/evidence/supply-ratio files; the drift test green at the pinned date and a second test asserts asof+1 may differ without failing. Caveat the DB cannot enforce: a pinned as-of goes stale on purpose — advance it on a schedule with the canonical order behind it; the freeze marker binds `supply-asof advance` only, not direct evidence writes (QUESTIONS entry). Side note for the forecast-challenge peer: their untracked tests/test_forecast_challenge.py needed one `supply_asof.ensure_table(con)` line to bind the table in its scratch warehouse (added; their 2 failures + 2 errors are unrelated numpy issues).

**D116 — Charter v2: rewrite CONTEXT.md around the owner as first customer, decision value behind a dated survival gate, cost of search as the earned claim, Gowanus bathhouse as AC-1.** *(2026-09-16, owner rulings via AskUserQuestion buttons; session 34, abenmayor-af, Fable executive; Opus charter draft, contrarian redraft review, Sonnet verification; ticket GTM-194 Done)*
The charter still opened with the causal residential-growth thesis rejected at D1, and its acceptance criteria were never rewritten after the scope correction — the project had been busy without a testable point. v2 (docs/CONTEXT.md) states purpose (find a candidate address worth taking and decide whether to take it; take-it first, help-someone-fill-it fallback), the owner as first customer with external segments a market-research question (zero discovery calls to date, docs/GTM.md), D88 verbatim as the earned claim ("What we sell is cost of search, not better decisions."), decision value as the goal behind a closed three-source survival gate with pre-registered floors (AUC ≥ 0.65) and dates (2027-03-31 / 2027-06-30 / 2027-06-30), a revisitable stop rule that self-amends 2027-07-01, six acceptance criteria of which four resolve on dated outcomes, AC-1 = admit `bathhouse_sauna` via the GTM-112 fail-closed checklist then pre-register the Gowanus decision (the D110 Bathhouse retrospective and GTM-192's structural blindness — every not-yet-open filing enters at `liquor_application`, so no gym, spa or padel club can appear at any threshold — are cited as why the warehouse holds nothing on this today), the three radii kept apart with MAUP-as-radius restored (§9.12), dead ends listed (§8) so no session relitigates them, and the old causal thesis moved to a Superseded appendix (A1–A8) rather than deleted. Contrarian review (redraft verdict, 14 findings) applied and verified by a Sonnet pass; one item stays open — the bank 0.67x/1.97x divergence between the two Gowanus cards (QUESTIONS D55).

Four owner rulings by buttons, 2026-09-16: (1) the §3.1 stop rule is revisitable, not permanent — it self-amends 2027-07-01 rather than closing the project outright; (2) admit `bathhouse_sauna` through the GTM-112 checklist (AC-1 step 1); (3) take-it-first ranks above help-someone-fill-it (AC-2 demoted under it); (4) the survival-label AUC floor is raised from the draft's 0.60 to **0.65** — owner: "a label that only just beats a coin flip would not change a decision."

Owner's verbatim words on purpose and on the Gowanus bathhouse, 2026-09-16, governing CONTEXT §2: "I am not sure yet. Originally I built this without a customer in mind so I do need to do market research. I built this as a test to see if there is indeed a market opportunity that exists then to either go in to that myself or help someone else fill it. I think I need to reorient and validate this with a customer. In fact, almost model aside, I am very bullish on there needing to be a bathhouse in gowanus and I am not sure if I should try to tackle that myself."

Retires the ⚠️ SCOPE CORRECTION banner that had sat at the top of this file since the hex era ("find hexes missing a daily-needs business", 726 gap hexes) — replaced in place with a two-line pointer to docs/CONTEXT.md v2; no other header content removed.

**D118 — Challenger forecast model: gradient boosting fails the pre-registered floor; `loci forecast challenge` harness shipped read-only.** *(2026-09-16, session 34, abenmayor-af, Fable executive; Opus data-scientist design, Sonnet developer build; ticket GTM-195 Done)*
The owner asked whether the recommendation engine should be more intelligent. The only fitted model is the forecast logit (the opening label, D92/D96), so a HistGradientBoosting challenger on the full t0-true feature set plus the withheld jobs/transit features was judged purely predictively on the same NTA-blocked folds against six pre-declared criteria: ΔAUC ≥ +0.010, a paired NTA-clustered bootstrap CI excluding 0, lower Brier and log loss, calibration gap ≤ 0.15, no per-category regression, and residual Moran's I not higher. Run on the 2026-09 vintage / supply ba944e18c57b / model 0.1.1+51bab17f, 360,000 rows, 12,000 addresses, 103 NTAs: rung1 (logit, 4 features) AUC 0.8768; rung2 (logit + 3 features) 0.8787; rung3 (GBM) 0.8799 with anachronistic features / 0.8794 without. ΔAUC +0.0031, NTA-CI [0.0015, 0.0049] — excludes 0 but misses the +0.010 floor. Brier 0.1214→0.1197 and calibration gap 0.0245 both pass; Moran's I 0.4498→0.4359 does not worsen. **FAILS** on W1 (the ΔAUC floor) and W5 (per-category no-regression: bank, childcare, hardware and laundry regress more than 0.02; only 6 of 15 categories improve against the ≥8 bar). Conclusion: model form and feature richness are not the ceiling on this screen — the opening label is. No future session should re-run "try XGBoost" (or any other model-form swap) against the same label without first landing a new one.

Harness shipped read-only: src/loci/model/forecast_challenge.py (655 lines), CLI `loci forecast challenge --month [--sample-n] [--no-anachronistic] [--tune]`. Opens the warehouse via `connect_read(read_only=True)` with no write path to `analysis.forecast*`; warehouse row counts verified identical before/after (25,365,780 / 6 / 8,455,260). Output JSON lands in data/forecast_challenge/ (gitignored). scikit-learn>=1.5 newly declared. 15 new tests; full suite 2,328 passed / 1 skipped. The row-order dependence of `_nta_folds` (fold assignment was sensitive to input row order) is pinned down by a dedicated test.

Process: owner's ML question → briefing → challenger design (Opus data-scientist) → build and live run (Sonnet developer) → null result recorded here; the owner then stepped back to the higher-order charter question (D116).

**D119 — Rewind backtest prerequisites: full-history filing backfill, LL157 activity recode crosswalk, DCWP licence intervals, and the pre-registered 2020→2023 rewind (fourth survival-gate source, with its identity and vocabulary defects recorded).** *(2026-09-16, session 34, abenmayor-af; ticket GTM-203 Done)*
Owner (2026-09-16): success is not "did a storefront open"; the measure must be a business moves in, of the kind Loci named, and survives more than a few years, and backtesting should rewind NYC to 2020 and ask what Loci would have said should exist, what came true, and when the gap closed. Same day: "never ever ever limit data pulls".

Probe (Opus data-engineer, read-only) found only the LL157 storefront registry reaches 2020 in the warehouse, and only because every filing feed was clipped by our own 24-month ingest window, not by the city. Also found a live defect: DOF re-coded LL157's primary_business_activity between the 2023-08-15 and 2024-06-03 filings and ingest carried it verbatim, so every activity comparison crossing that boundary was wrong by a factor of twelve and analysis.storefront_latest was serving the recoded labels.

Landed (commit 70cbd55, supply hash ba944e18c57b unchanged at every step): WINDOW_MONTHS clip removed, no date predicate by default, 232,667 → 989,174 filing rows across eight feeds, DOB BIS ipu4-2q9a added as the sole DOB source covering 2020; recode crosswalk derived from the premises crosstab, a clean 16-cycle permutation with one fixed point, era 2024-06-03..2025-08-15 reverted by DOF 2026-02-15, year-over-year persistence 6.8% → 81.8% with every other pair within 0.5 pt, activity_canonical on 248,316 rows with raw retained, D67 prior-use labels corrected at unchanged counts; analysis.licence_interval from the full DCWP roster (72,451 rows, all statuses carried, no closure decision); `loci rewind {coverage,recode,licence-intervals,checks}`; a positional INSERT on analysis.storefront replaced by named columns because the table had two column orders depending on CREATE vs ALTER; 67 tests, suite 2,395 passed.

Pre-registration (Opus statistician, before any result): entry rung ΔAUC ≥ +0.008 at 12 and 24 m, NTA-blocked, both t0 = 2021-01-01 (primary) and 2020-01-01 (declared pandemic regime arm; a 2020-only pass does not satisfy the gate); category-correctness as the joint event against the block-modal category, +0.05 top-1 and κ ≥ 0.10; survival AUC ≥ 0.65 (owner) AND ΔAUC ≥ +0.02 over a category × borough hazard AND decile calibration ≤ 0.10 AND sign-stable across two closure definitions (Expired counts only with no successor licence within 180 days; strict arm excludes Expired), 36 m, cohort-stacked 2016–2022; time-to-close descriptive only (Turnbull, incident vacancies, Peterson bounds). Dates 2026-12-31 / 2027-03-31 / 2027-06-30. DCWP licence-status intervals admitted to CONTEXT.md §3.1 as source #4 under these floors.

Recorded defects that re-source rung (iii) before any fit: the DCWP roster publishes no status-change date (surrender/revocation are bounds, only expiry is observed); its licence vocabulary maps to exactly one Loci category (6 rows; laundry is licensed via DCWP inspections, a different vocabulary); the licence → ledger join matches 0.9% because licences carry the legal entity and the ledger the awning name, and staging.poi holds zero roster rows so the licence-number rung is structurally empty; SLA publishes no historical file. Wave two (in flight) ingests the rosters as POIs and inventories a dated start and end per category. Also found: FEEDS key nyc_sla_liquor_licenses vs registry nys_sla_liquor_licenses, one letter apart, 24,850 rows under the wrong spelling (own ticket).

Owner's verbatim words for the CHECKPOINT entry: "i am not sure the measure of success is as simple as did a storefront open. if the space is available, someone will move in as long as the neighborhood isnt shitty. we also cant measure if the type of business we predict should fill it is the correct one. we have to make sure a business moves in and succeeds more than a few years. our backtesting should look at an older version of NYC (what did NYC look like in 2020 then 2021. what did we identify should exist in 2020 that became true in 2021 and when did the gap close, etc.)" and "your goal is to have AS MUCH AS DATA AS POSSIBLE!" / "never ever ever limit data pulls".

**D120 — D42 parked with its pre-registration; D43 category-specificity placebo built, thresholds fixed blind, run: four categories are category-specific (bar and fitness positive, childcare and pharmacy negative), the rest unresolvable at this power; backlog realigned to charter v2 with five Todo tickets.** *(2026-09-16, session 28, abenmayor-db)*

D42 (owner ruling: PARK). Design (Opus data-scientist, read-only): an outside-option term Ω_o = θ_c·resid_o added INSIDE the Huff denominator's K_self normalisation (additive, not a scaling of in-catchment mass, so an isolated site still leaks); NULL origins → Ω = 0 → the shipped model byte-identical; λ re-absorbs the mean of Ω per fold so only cross-sectional dispersion is a claim (R1 preserved); θ grid nests θ = 0. Identification measured on the live warehouse: Spearman of bike_od_out_share vs retail_area_400m −0.159, vs jobs_retail −0.122, vs own dock trips −0.308 — thick-retail NTAs RETAIN riders, so the term would arrive as an incumbent-density proxy with the flattering sign (D1 in a mobility costume); median out_share 0.849 is largely NTA geometry (an evening ride is longer than an NTA); Ω is NTA-wide and can never separate two addresses on one street. Pre-registered pass criterion (restaurant confirmatory; bar/cafe exploratory under BH q = 0.10): ρ_oos ≥ 0.829 + 0.010, ZIP-clustered CI lower limit > +0.002, Δρ > NTA-donation placebo p95, θ > 0 in ≥ 18/20 seeds, cross-category placebo still passes, λ_Kings/λ_NY split does not widen; in-fold residualiser on [log area, log perimeter/√area, log dock count, retail_area, jobs_retail, log homes]. Parked because D42's own gate (DOT convergent validity ρ ≥ +0.50) re-measured +0.327 on the re-baselined warehouse, the charter v2 (D116) puts decision value behind the survival gate and makes cost of search the earned claim, and phases 1–3 returned context/context/null. Full text: docs/retrodiction-2026-09.md §12.

D43 (owner rulings: adopt the residual-rank placebo as the gate; per-category verdicts; NULL a failing category's stored share at the next re-sweep; an UNREAD agent fixes thresholds before any run). Diagnosis: the old null intersected fifteen medians while the statistic was one marginal — only 2 of 110 NTAs were above median in all 15 categories, so the null was 0.6% and every category "beat" it; a correction to D108: the cross-category density ρ 0.76/0.94 quoted there was from the superseded per-address density — live per 1,000 units it is mean +0.240, max +0.854, and the shipped supplied_share off-diagonal is mean +0.171 (min −0.697 fitness↔pharmacy). Design: per destination the rank-percentile of log1p(density_c) residualised on log1p(other-14 density); per origin W_c = trip-weighted mean rank − 0.5; null = permutation within density-quintile × dock-tercile strata (dock siting endogenous), one shared uniform matrix across categories; PASS = BH q = 0.10 over ONE family of 15 ∧ destination BCa 95% CI excludes 0 ∧ |median W| ≥ 0.05; MDE = 2.8·σ̂₀ with every non-pass printed "cannot resolve" when underpowered; childcare/clinic/bank/tailor_repair/hardware flagged expected-uninformative as negative controls with a kill rule (≥ 2 positive passes ⇒ confounded, no stored number); spatial-block permutation becomes primary if Moran's I is significant for ≥ 3 categories. Thresholds fixed by a blind statistician from the design alone (B = 10,000 permutations, 2,000 bootstrap, floor 0.05 ≈ 4 rank positions of 79). Built in commit d3ca906 (report-only, nothing stored, no hash effect; 26 tests).

Run (2026-09-16, window 2025-09..2026-08, 110 origins / 79 destinations / 41 CDs): diagnostics — Σw² p50 0.114 (n_eff ≈ 8.8 destinations per origin; mean vector n_eff ≈ 40), σ̂₀ 0.0235 → MDE 0.066 > 0.05: UNDERPOWERED for the floor; strata 5×3 median size 5 (no collapse); 110 distinct origin weight vectors, median cosine 0.091; Moran's I significant for 11 of 15 → spatial-block primary. Spatial-block verdicts: PASS bar +0.140 (p 0.025), fitness +0.109 (p 0.025), childcare −0.149 (p 0.025), pharmacy −0.146 (p 0.013); kill rule 0/5; cafe_bakery +0.060, restaurant +0.099 (low power, aux R² 0.77), hair_barber +0.058, laundry +0.045, grocery −0.070, and the rest cannot resolve. Stratified-permutation primary agreed on childcare and pharmacy and left bar/fitness inconclusive (BH). Reading: evening/weekend riders from a gap area go where BARS and GYMS are denser than expected and where CHILDCARE and PHARMACIES are sparser — the leisure/daily-needs split the design predicted (D108 caveat 5), now with a falsifiable test behind it. Caveat: spatial-block p resolution is 1/79 ≈ 0.013 (three passes sit at exactly 2/79). No NULL verdict, so resweep_failing_categories clears nothing; the four passing categories are the only ones licensed to be read as category-specific — the card line for the other eleven should say "unresolved at this power". CONTEXT ONLY stands (DOT ρ +0.327).

Backlog realigned to charter v2 (owner rulings, same day): QUESTIONS.md Open split into Prioritised (P1/P2) and Parked (P3) with a tier line per entry (P1: M13, T10, D24, D55, D56, O1, O2, T12; P2: D3, D13, D19, D32, D54, O6, O7, O11, X10, C3; P3 the rest); C1/C2 (the causal retail→growth thesis) moved to Dropped under D116; M1, M9, D2, X2 to Answered (D90, own dated answer, D88 ×2); O3/O5 marked parked. tickets.py: GTM-161 (survival outcome) raised to Urgent; GTM-58/50/49 Urgent → Medium; all 29 open tickets in E6 Premium Amenities, E7 Maturity/2033, E8 Second City, E9 Ignition demoted to Low ("parked, not cancelled"); GTM-181 marked Done (D112's re-issue); five new tickets queued to Todo in Linear — bathhouse_sauna checklist (AC-1 Step 1, Urgent), Gowanus bathhouse base rate (D56, Urgent), filing-blind amenity anchor (T10, High), Gowanus bank ratio reconcile (D55, High), co-located pair policy (D24, High): GTM-198, GTM-199, GTM-200, GTM-201, GTM-202 (all Todo, assigned to the owner); 33 priority updates pushed, 0 failed. D55/D56/D24/T10 carry Answered-by lines naming those tickets and stay open until the work lands.

**D122 — NYC DOE Demographic Snapshot registered as a planned free source (GTM-204) for a first derivative on the child pool; Bed-Stuy read-only check finds the screen's present-day composition read does NOT match the Furman/Gothamist "families replaced by childless couples" story, and the disagreement is informative.** *(2026-09-16, session 35, abenmayor-map)*

Why: the owner shared a Gothamist piece (2026-09-16) citing the NYU Furman Center — Bed-Stuy's Black population share ~75% (2000) → 38% (2024), P.S. 25 closed 2025 at 54 students — glossed by the reporter as families with children replaced by childless couples. Two things were done. (1) Source: Loci's demand composition (D60, D63/D65/D69 age-fit) rides on one ACS 5-year vintage (2019–2023), so it has no first derivative on the child pool at all. The DOE Demographic Snapshot is an unsampled October register per school by grade, 2013-14 onward (Open Data ids s52a-8aq6, c7ru-d68s, vmmu-wj3w, nie4-bv6q; 2022-23 through 2024-25 are NOT on Open Data yet, DOE InfoHub only), joined to addresses through the 2024-25 elementary zone polygons cmjf-yawu, never by school point. K–2 registers lead under_5_share by three ACS vintages; that is the one thing it adds. It enters as `role: control`, `status: planned`, CONTEXT columns only (`k_enrollment_zone` and a 5-year change), no grade, no score, no growth thesis — enrollment is seats not resident children (charter/private/out-of-zone leakage, high in Bed-Stuy; no charter flag on the snapshot itself), and it says nothing about the 0–4 cohort childcare serves. Registry entry, tickets.py definition and Linear GTM-204 (E1, Medium) landed; check-sources and check-tickets clean; QUESTIONS D57. (2) Bed-Stuy read (read-only on the frozen warehouse, stamp ed55301203a4; BK0301 West 6,310 lot addresses / BK0302 East 8,741; refs Borough Park BK1202, East Village MN0303): on every composition column Loci carries Bed-Stuy is MORE family-heavy than the MN+BK norm — under_18 0.211 ±0.010 vs 0.175, under_5 0.064 vs 0.054, avg_hh_size 2.58 vs 2.30, one-person households 0.305 vs 0.384 (78th/61st NTA percentile on under_18) — and simultaneously young-adult-heavy (18–34 0.323, 79th pct) and old-light (65+ 0.103 vs 0.164); the story's "thin" is a comparison with 2000, which the warehouse cannot make. Childcare: 56 gap addresses of 15,051 (0.4% vs 1.5%), 8.87 principled sites per 1k under-5 vs MN+BK 8.90 — supply proportional to the present pool, neither gap nor overhang (caveat: the DOHMH anchor is active-only, so a closed program vanishes rather than reading vacant). Bar/café/fitness: bar 2.04× baseline (West 2.45×), fitness 1.47×, café 1.04× (West 1.59×, East 0.79×), bar gap 14.4% vs 40.2%; bar-lead addresses rank UP after age-fit (29.4 → 35.9th pct) and childcare-lead DOWN (59.8 → 45.8th, n=45); p_opening bar 50.9% vs 37.5%, café 53.6% vs 43.9% — under D88 that is herding on thick supply, so the lifestyle opportunity the story implies is already filled in the West and the residual sits in the East (bar 21.6%, café 10.5%). What the story would not predict: bank is the lead gap for 42.3% of Bed-Stuy addresses (35.8% gap, median nearest 542 m); the thin categories are health and chores (clinic 0.41×, pharmacy 0.52×, West 0.00×; nails 0.56×), not lifestyle; LL157 storefront vacancy median 10.0% vs Brooklyn 5.1% — if there is an overhang it is retail generally; and East/West is the real structure (West retail_mixed, 32% Black; East residential, 53% Black, AM/PM 1.91). No household-with-children cell exists in GETVARS (B11003/B11005 absent) — "childless couples" can only be inferred, and both proxies point the other way. Scratch CSVs in the session scratchpad only; nothing written to the warehouse.

(D121 id was claimed before D122 landed)

**D121 — Warehouse audit and reshape; laundry source_rank fix; supply hash ed55301203a4; forecast vintage 0.1.1+67e28b04 for lot and street frames; survival gate re-sourced to NYS SLA licence intervals; eleven ingest caps removed.** *(2026-09-17, session 34 cont., abenmayor-af; ticket GTM-205 Done)*

Owner (2026-09-16): "make sure we are organizing and storing data as efficiently as possible so that we are retrieving it in the correct manner — can we audit our database?" and, the same day, "never ever ever limit data pulls".

Audit (Opus data-engineer, read-only): 76 objects vs D61's 26; analysis.storefront double-counting (414,884 rows / 106,008 storefronts, vacant_only and full filings pooled); the MN+BK screen enforced nowhere in the schema with a live leak in validation/retrodiction.py; the forecast_latest lexicographic-vintage bug still live in webmap_export; 13 positional INSERTs (3 SELECT *); poi_closure join fanning out 3.7×; ~1 GiB of analysis.forecast re-derivable; a one-address report pulling 615k rows into pandas; 50,199 street-frame points with no demographics, forecast or bike measures; six unread objects (three confirmed).

Owner rulings by buttons: keep every forecast vintage but drop features_json/forecast_id and reclaim; enforce the borough scope in the schema on screen tables; consolidate now; street points get everything the lot frame has; storefront grain = storefront row keyed (storefront_id, filing_due_date); NULL-borough rows keep-and-flag; fix the laundry source_rank now and re-run tonight.

Landed (commits b0665c0 wave two, d2135a2 reshape): laundry branch in score/dedup.source_rank (the inspection anchor had ranked below an expired DCWP roster licence); DCWP roster POIs ingested (+6, none canonical, no laundromat gated out); supply as-of advanced to 2026-09-16 as an announced step (9 licence-expiry closures, 3 evidence-aged unknowns); supply hash ba944e18c57b → a0b8ac9a8a52 → ed55301203a4; one canonical re-run on ed55301203a4 incl. fit-baseline, bike growth 563,684 → 664,082, revenue re-fit, legality 6 s; TIGER tl_2020_36_tract ingested (5,411 tracts, no clip) so every street midpoint carries tract demographics (332,041 addresses); forecast vintage 0.1.1+67e28b04 issued for lot AND street frames (4,980,615 rows, 0 street points without a forecast, 17,535 street_no_homes; 7 vintages kept); export-webmap 148 files; sql/045 (scope CHECKs, one borough vocabulary: 155,261 long-form rows coded, storefront row grain, NULL-borough flag) and sql/046 (three dead views dropped, three _screen views; objects 81 → 83 across staging/measure/score/ledger/calib); forecast_outcome re-keyed on its natural key; features_json/forecast_id dropped; address and address_category rebuilt sorted; demographics clipped 767,337 → 281,842 lot rows then re-extended to 332,041 with streets; warehouse catalog with layer/grain/key comments, docs/WAREHOUSE.md generated by `loci gen-warehouse` and drift-checked by `loci check-warehouse` so "inventory before adding" is mechanical; 13 positional INSERTs → named columns with a lint test; retrodiction borough leak closed; webmap vintage pick by frozen_at; vacant-storefront count made deterministic; prune preview double-count fixed; sql/005 cross-run budget claim corrected (a resumed pull could authorise 4× its cap); report path 615k → 7.6k rows, 15.0 s → 3.8 s; file reclaim 8.97 → 5.17 GiB via COPY FROM DATABASE with FK-ordered inserts (DuckDB 1.5.5 does not topologically sort FK children), old file kept at data/loci.duckdb.pre-reclaim-2026-09-16 for the owner. Two agent-caused bugs caught and fixed: a false-green address_order probe, and the coded borough zeroing every positive in the forecast fit panel (found because the logit went singular; a guard now refuses a constant outcome). Eleven wave-two caps removed in code (DOF storefronts 5 boroughs, SLA inactive file + nyc_/nys_ key fix, Foursquare stale retained not promoted, licence identity on POIs, registry windows, Citi Bike 2013-06 with legacy crosswalk, MTA all months, CBP 1986–2023, ZBP 1994–2023, LODES 2002–2023); long ingests detached overnight to staging.* only.

Survival gate re-sourced (statistician v2 pre-registration): the DCWP roster was the wrong source (no status-change date; licenses contractors/tobacco/secondhand). Per-business survival clocks exist for restaurant, bar, grocery (NYS SLA original_issue_date → non-renewed expiration with no successor licence at the BBL within 180 d; active + inactive files) and provisionally pharmacy (448 SLA-subset intervals, capped below 25% coverage before the fit); laundry needs DCWP inspections first_inspection_date carried (P6, predicted to fail on 2023-07 feed depth); cafe/hair/nails/childcare/convenience/clinic/fitness/bank/hardware/tailor dropped before the fit in writing (no per-business identity or end event in any NY source). Comparator = category × borough × licence-class + time-since-issue splines (SLA terms are 2–3 y). Grocery expiry counts as closure only if P5 PPV ≥ 0.50 against an independent closure signal. P3′/P5/P6 due 2027-01-31; gate date 2027-06-30 stands. A pass buys "the Loci score predicts SLA licence non-renewal in three categories".

Owner's verbatim words for the CHECKPOINT entry: "I also want to make sure we are organizing and storing data as efficiently as possible so that we are retrieving it in the correct manner - can we audit our database? after that, I want to make sure we are building ALL additional calculations on existing data AND making the most out of camera and satellite data. take this list one at a time and WORK WITH ME"

Suite 2,669 passed / 1 skipped.

Disclosures: the Phase A agent ran `git checkout -- src/loci/sql/028_forecast.sql` on a file only it had modified — a rule violation, recorded here rather than papered over. Seven agents stalled on a harness transport error mid-run and were resumed with no data loss. The full test suite was killed once by a 600 s harness limit and re-run green by the executive.

Done 2026-09-17 (CHECKPOINT D121). Pushed to Linear 2026-09-17 as GTM-205.

**D123 — Gowanus bathhouse base rate enumerated for AC-1 (GTM-199 Done; QUESTIONS D56 answered): 1.7 openings/yr citywide 2019–2026 (95% 0.9–2.9), 3.4/yr since 2024; Baseline field = citywide rate + named pipeline, by owner ruling.** *(2026-09-17, session 36, abenmayor-db)*

Method: two parallel Opus passes on ONE shared definition agreed with GTM-198 (abenmayor-1d, D124) — narrow slug: public-admission heat/water bathing sold per visit (bathhouse, sauna, steam, banya, hammam, Korean spa/jjimjilbang, thermal/plunge pools); excludes nail/med/massage-only, hotel-guest-only spas, gym and residential-building saunas; membership-only clubs flagged not counted; day spas out UNLESS bathing is the primary offer (owner ruling 2026-09-17, R2: Juvenex, Great Jones Spa, Fountain of Youth count). Web pass: 27-row facility table with a dated source URL per opening; warehouse pass: read-only at the pinned as-of 2026-09-16 on supply ed55301203a4 (raw staging.poi by name terms and source codes, first-seen ledger D79, storefront filings at the D121 grain).

Opening series (five boroughs, dated): 2015 1 (cityWell Gowanus) · 2016–18 0 · 2019 1 (Bathhouse Williamsburg) · 2020–21 0 · 2022 2 (QC NY, World Spa) · 2023 1 (Spa Club K-town) · 2024 2 (Bathhouse Flatiron, Othership Flatiron) · 2025 3 (Brooklyn Bathhouse, AIRE UES, Othership Williamsburg) · 2026 to Sep 4 (Lore NoHo, The Altar, Bathhouse Atlantic Ave 2026-05-16, Connexion Flushing 2026-08-21). 14 in 2015–2026; 7 chain / 7 independent; 5 over 15k sqft. Closures: Premier 57 (c. 2020), Sandoony Midwood (by 2022); cityWell's bathing suspended 2025–26 (massage only). Base rate 2019-01..2026-08: web-reconciled 13 / 7.67 y = 1.70/yr (Poisson 95% 0.90–2.90); 2024-01..2026-08: 9 / 2.67 y = 3.4/yr (1.5–6.4); ledger-alone floor 6 dated = 0.78/yr (0.29–1.70). MN+BK 11 of 13. Open communal stock at the as-of: 24 citywide / 20 MN+BK (MN 12, BK 8, QN 4) with four rows flagged unverified (Neck Road, Fountain of Youth, Mermaid's seasonal closure spanning the as-of, The Altar's bathing-primary flag) — 20 / 17 without them. Per 100k residents ≈ 0.02/yr; per 1 km catchment-year ≈ 0.01 — the "predicting zero resolves nothing" trap, which is why the owner ruled (R1) that the pre-registration's Baseline field carries the CITYWIDE rate plus the named pipeline, not a catchment rate.

Pipeline (2026-09): Reunion Bathhouse LLC, 272 Seigel St BK — fit-out approved 2026-08-12, ~2027-Q2 by the D80 259-day median (filings found it; no POI feed carries it and press had "address undisclosed"); SaunaLounge LLC, 45 S 3rd St BK — SLA 2026-06-24; Othership UES 201 E 86th, 14k sqft, 2027; Schwet 78 Franklin, public opening unconfirmed; SUND Brooklyn early 2027; Bathhouse NJ/CT 2027; excluded-but-in-catchment: Life Time, 175 Third St Gowanus Wharf, 2028, rooftop hot/cold pools + saunas. Gowanus catchment (BBL 3004260001, 545 Sackett St): Bathhouse Atlantic Ave 540 Atlantic 0.79 km straight-line (L-class, from $29, opened 2026-05-16) is the only operating communal per-visit site; cityWell 496 President 0.23 km with bathing dark; Brooklyn Bathhouse 3.56 km; Reunion 5.2 km. Multi-city: NYC LEADS the national wave — comparable US cities opened 0–2 communal per-visit sites each in 2021–26 (Chicago 0, LA 0, DC 0, Miami 0–1, Austin 1–2, Philadelphia 1 on 2026-09-25); Toronto/Montreal are the other active markets; Bathhouse raised $35M (2026-05), Othership ~$20M. So the 12 NYC openings of 2022–26 are a local-plus-Toronto phenomenon feeding a national pipeline of announcements, not one node of a broad wave.

Source anatomy (feeds GTM-198's checklist, cited there as ours): Overture `public_bath_houses` (63 rows) is 100% NYC Parks comfort stations — never use it; real bathhouses sit in Overture spas/health_spa/day_spa (→ nails_beauty) and are findable only by name; FSQ `Bath House`/`Sauna` and Overture `sauna`/`onsen` are UNMAPPED in categories.yaml, so Brooklyn Bathhouse, Lore and Othership Williamsburg never enter staging.poi; DOHMH in-house CAFÉ permits are held by nine bathhouses and give a gov_filing first-seen trailing opening by 0–5 months; FSQ date_created LEADS opening (World Spa listed 2019-03, opened 2022-12); DCWP nothing. Coverage: raw feeds carry 12/14 post-2019 openings (misses: Lore — unmapped; Connexion — after both snapshots) and 7/7 pre-2015 stock; a first-seen within ±3 months of press exists for 9/14; name-term enumeration alone finds 6/13. GTM-198's category definition and anchor verdict (abenmayor-1d, D124, cited — D123 and D124 cross-reference: D124 owns the definition and vocabulary, D123 the base rate): DOHMH pool permits (Health Code Art. 165, 3kfa-rvez; sees Bathhouse Flatiron, the site D110 said every feed missed) + the café permits = a composite floor anchor, P1 CONDITIONAL; G3 unmeasurable (NAICS 812199 has 849 MN+BK establishments vs ~45 venues); ships as a non-filtering signal, narrow slug, headline:false.

Unverified (owner or a later pass): Schwet's public opening; open status of Neck Rd Russian Baths, Mermaid Spa, Brooklyn Banya; the seven Queens Korean saunas (Woori Healing Sauna NY, licensed 2026-01, may be an uncounted 2026 opening); Premier 57 and K-Town closure dates. The pre-2022 near-zero is partly a search-coverage artefact for outer-borough Korean/Chinese water spas — the rate is a floor.

What the base rate licenses: scoring a dated AC-1 prediction against 1.7/yr citywide with a named competitor set. What it does not: any statement that Gowanus is under- or over-supplied — that is the trade-area sheet (D116-2). Deliverable: docs/bathhouse-base-rate-2026-09.md (one headline number, house order). GTM-199 Done.

**D124 — `bathhouse_sauna` admitted at G0 of the category-expansion checklist as a NON-FILTERING SIGNAL, narrow slug (day spa out unless bathing is the primary offer); P1 anchor CONDITIONAL (DOHMH pool permits 3kfa-rvez + DOHMH café permits, a composite floor), G3 unmeasurable on NAICS 812199; three exact-set pins widened by owner ruling; ingest and hash move deferred to an announced step.** *(2026-09-17, session 35, abenmayor-map; GTM-198)*

Why: AC-1 Step 1 (CONTEXT.md, D116) is admitting the category through GTM-112's fail-closed checklist, and the checklist's own pass-or-fail is the finding. P1 (anchor), probed live: DOHMH Pool Inspections 3kfa-rvez (Health Code Art. 165 bathing establishments, 1,221 permits 2019–2025, street address + BBL/BIN + point) is a real government act and SEES Bathhouse Flatiron (UNKNOWN BATHS TWO LLC, 6 W 22nd, insp. 2025-09-10 — the site D110 said every feed missed), R&T, Spa 88, Aire, Akari, Banya NY, Russian Baths, World Spa, Spa Castle — but carries no facility-type column (keyword subset only, GTM-114's rule), sweeps in hotel/condo spa pools, and cannot see pool-less sauna studios; GTM-199's DOHMH in-house café permits (nine bathhouses hold one; gov_filing first-seen trails opening 0–5 months, FSQ date_created LEADS badly — World Spa listed 2019-03, opened 2022-12) are the second leg. DCWP licenses nothing here (verified, 49 categories); DOB job filings are a live PIPELINE signal (four 2026 bathhouse fit-outs: Perspire 15 Hanover Pl, 24 E 13th, 316 E 86th, 60 Orchard) for the GTM-192 tier-3 watch, not an anchor. G3 cannot read regardless: NAICS 812199 (Other Personal Care Services) holds 849 MN+BK establishments in CBP 2023 against ~21 real venues, a denominator 20–40× the category, so any coverage ratio is unmeasurable, not a floor. Under checklist §4 an unanchored category ships only as a non-filtering signal: `headline: false`, `ships_as: signal`, never leads a card, its supply layer exists to enumerate venues for the A8 trade-area sheet and the D123 base rate — not to flag gaps, because the prevalence premise fails on its face (reach ceiling 1,200 m, basis analog to fitness's CDC 1-mile measure, against a 15–30 min catchment; "no walkable bathhouse" is true almost everywhere in MN+BK). That is CONTEXT AC-1 point 5 confirmed, and it resolves Step 1: the Gowanus question moves to Step 2, the hand trade-area sheet. Definition pinned verbatim in categories.yaml and rendered to docs/CATEGORIES.md: public-admission heat/water bathing (bathhouse, sauna, steam, banya, hammam, jjimjilbang, per-visit thermal pools); excludes nail/med/massage-only, hotel-guest-only, gym and residential saunas, membership-only clubs (flag, don't count), and day spas UNLESS bathing is the primary offer (owner via GTM-199: Juvenex, Great Jones Spa, Fountain of Youth count). Narrow beat wide because wide would move Overture spas 3,485 + day_spa 737 + health_spa 95 and FSQ Spa 2,516 rows (~35% of nails_beauty's ~19k) out of nails_beauty, moving its hash and D14 ratio and re-opening D52's nails dedup, for a distinction the definition draws by hand. Vocabulary (peer-verified in GTM-199): OSM leisure=sauna, amenity=public_bath; Overture `sauna`, `onsen` — NEVER `public_bath_houses` (60/63 NYC rows are Parks comfort stations, now excluded and pinned); FSQ leaves "Sauna" and "Bath House" (were UNMAPPED, so Brooklyn Bathhouse, Lore and Othership never reached staging.poi); Google Table A sauna, public_bath; plus a pinned name-term include/exclude regex applied ONLY inside Overture spas/day_spa/health_spa and the FSQ "Spa" leaf — the only way the narrow split can be applied inside the spa sub-tags, mirrored byte-for-byte between categories.yaml and sources/base.py with a drift test. The next POI ingest will emit ~18 Overture + ~32 FSQ rows and pull ~43 rows out of nails_beauty — that ingest MOVES THE SUPPLY HASH and is deferred to an announced step; nothing was written to the warehouse tonight (hash stays ed55301203a4). Spend: no CEX line isolates bathhouse admissions; income_elasticity 0.40 on "Fees and admissions" (fitness's line, `reliable: false`) → DISCRETIONARY at the 0.35 cut, derived not set. Three owner rulings widened exact-set pins, dated in the tests: demand-caveat set +bathhouse_sauna (five), demoted/headline:false set +bathhouse_sauna (four), D43 bike-OD family re-registered 15→16 (P8 forbids exclusion; two synthetic fixtures moved from seed 7 to seed 9 because the n_dest × 16 draw dropped clinic's W to 0.17 — a fixture artefact). G7 density elasticity: a dry-run fit on 812199 said `saturating` (β −21.6, Conley t −5.05, LOZO 1.00) but that is a label for tattoo/tanning/day-spa establishments, not bathhouses; the shipped row is `not_fitted` pending the real fit after ingest, and the owner declined to pre-refuse the label. Fit artifacts that cannot exist pre-ingest (supply_baseline, carrying-capacity, G9 grade) are pinned as `UNFITTED_PENDING_INGEST == {"bathhouse_sauna"}` exact sets, so they fail the moment the ingest lands without the constant being emptied; G9 coverage confirmed C on the live warehouse. Two safety changes: a fail-closed guard in model/address_gaps.py (`EmptyCategoryError`) so a registered category with zero mapped supply makes `loci address-gaps` REFUSE rather than silently become lead_category at every address (it would have — nearest matrix sits at DIST_LIMIT); and the sql/036 address_observation CHECK is widened via a new dry-run-by-default `migrate-warehouse --step observation_category_check` instead of a 048 file, because db.init_schema applies every sql/*.sql on every write connection and a plain file would have auto-run on a peer's next session unannounced (migration 048 released). Full suite 2,693 passed / 34 failed — every failure is a peer file in flight (poi_presence.closed_on, licence_event classification, filing_categories); zero are this decision's. G1–G12 plan with commands and predictions is in the GTM-198 ticket thread; expected: G3 fail/unmeasurable, G8 ratio ≈ 0.00× nearly everywhere, G11 grade D (no comps, `revenue.yaml benchmark_only`), G12 legend colour #c02f8f landed.

---

**D126 — Tenant-side commercial-space research closes FOIL-only; do not build a listings, rent-card, or dossier product.** *(2026-09-17, owner; Session 37)*
The owner asked whether Loci should answer a tenant-side market question. Three research threads and the investor/contrarian memo at `docs/retail-space-search-market-2026-09-17.md` found a search problem, but not the one initially assumed: active listings exist, while rents/terms remain opaque and inventory is unlisted or stale. Local Law 157 requires collection of lease-start/expiry, escalation, concession, and average monthly rent-per-square-foot fields, but the law's explicit public surface is a searchable address/vacancy dataset plus aggregate reports; it does not establish public premises-grain rent publication. The narrowly scoped next evidence action is the ready-to-send FOIL at `docs/asks/foil-ll157-rent-fields-2026-09-17.md`.

Both product verdicts are negative. There is no demonstrated tenant or broker willingness to pay; predecessors either died or pivoted into broker-led workflows; a tenant-acquisition product would displace the survival gate and AC-1 work that charter v2 makes load-bearing. The only flip is a premises-grain response releasing usable rent/term fields. Until that happens, do not build a listings site, vacancy/rent card, tenant dossier, warehouse ingestion, or related product surface. D67 stands; this decision supersedes nothing.

**D127 — Owner reopens the tenant-side listing map; public broker pages may be collected with provenance, but LoopNet/CoStar remains authorized-export-only and advertising silence remains unknown.** *(2026-09-18, owner; Session 38)*
This explicitly supersedes D126's product prohibition after the owner asked to build the listing map and collect directly from broker/owner sites. The first bounded implementation collects the complete listing indexes exposed by Sinvin and RTL/Winick, preserves page URL/fetch time/raw rent and space strings, geocodes the advertised address, and extracts positive kitchen, venting, gas, bathroom, HVAC and sprinkler claims with matching snippets. A missing claim is NULL, never FALSE; a listing is an advertisement, never an inspection or proof of current vacancy. The map is a standalone gold overlay and never changes a gap, grade, recommendation, or known-supply count.

The 2026-09-18 snapshot contains 170 pages (84 Sinvin, 86 RTL), all geocoded: 161 carry a rent field but 159 say only “Please Inquire” or “Upon Request”; two disclose a numeric asking rent; 57 carry at least one extracted amenity claim. This is not “every available listing in NYC”: it is a source-bounded snapshot of two brokers and is strongly Manhattan-selected (147 MN, 9 BK, 13 QN, 1 SI). LoopNet's current terms prohibit scraping/data extraction and compiling its product into another listing service, so browser interception does not create authority. Loci refuses live LoopNet/CoStar collection and accepts it only through an explicitly authorized CSV import whose contract/permission/export provenance is mandatory on every row. The next evidence action is a licensed export/API or written permission, not an Interceptor workaround.

**D128 — The public commercial-listing snapshot clears 1,000 source postings and a separate 1,000 address/space overlap diagnostic; coverage is broad, not complete.** *(2026-09-18, owner; Session 39)*

The owner's explicit floor was more than 1,000 NYC listings. The gated build now contains **1,214 distinct source postings**: Leasync 1,003, Sinvin 84, RTL/Winick 86, Titan 26, and Verada 15. All 1,214 carry a mapped point; borough distribution is MN 727, BK 307, QN 100, BX 75, SI 5. Rent coverage is 1,197 raw fields and 639 machine-readable numeric asks; 505 postings carry at least one positive kitchen, bathroom, venting, gas, HVAC, or sprinkler claim. Exact repeated `(source_id, listing_id)` rows are removed before the `--min-listings` gate.

The headline posting count is not represented as unique premises. A deliberately conservative overlap diagnostic on normalized advertised address plus square-foot range contains **1,010 address/space fingerprints**; it is still not a unique-space estimate because two same-size suites can collide and one suite can advertise different divisible ranges. The JSONL retains the posting grain and every source URL rather than destroying that evidence with an uncertain merge.

The main recovery was methodological, not a relaxed city filter: 107 Leasync records had publisher coordinates inside the checked-in official NYC borough polygon but an ambiguous or conflicting address-geocoder result (for example, unhyphenated Queens house numbers). Those rows retain the publisher point with `location_basis = publisher coordinate + NYC borough boundary` and no invented BBL; 1,107 rows have a GeoSearch address/BBL match. Out-of-city publisher points remain excluded. Titan admits only JavaScript inventory rows explicitly marked `available`; Verada follows only the 18 links rendered in its current-listings section, not its 174-URL historical sitemap. KSR and RIPCO were rejected because their terms prohibit automated access; EagList was rejected because robots.txt disallows this crawler class and `/search`; OfficesAndSpaces remained rejected under its extraction ban. LoopNet/CoStar remains authorized-export-only under D127.

Artifacts: `data/commercial_listings/latest.jsonl`, `webmap/data/commercial-listings.json`, `src/loci/sources/cities/nyc/commercial_listings.py`, and `tests/test_commercial_listings.py`. The default command now covers all five public sources; `--min-listings` gates distinct source postings before either output is written. Focused verification: 16 tests passed; Ruff passed on the source module and test file; GeoJSON contains 1,214 features and no nested non-scalar properties.

**D129 — Listing coverage means clean Manhattan/Brooklyn storefront spaces, not ads: 1,057 spaces clear the 50% gate against a 1,823-listing market benchmark.** *(2026-09-18, owner; Session 40)*

The owner narrowed the product to Manhattan and Brooklyn lease inventory, excluded office and industrial inventory, required stale and duplicate listings to be removed, and set a 50% coverage goal. The benchmark is CommercialCafe's 2026-09-10 active retail-lease totals: Manhattan 1,082 plus Brooklyn 741 = **1,823**, so the gate is 912 clean spaces. This is an estimated, not census, denominator: marketplace classification and Loci's storefront/service inclusion rule are not perfectly identical. The source URLs and benchmark date ship in the GeoJSON metadata so the percentage cannot detach from its denominator.

The gated 2026-09-18 artifact contains **1,057 distinct active lease spaces**, or **58.0% estimated coverage**: Manhattan 668 and Brooklyn 389. The build began with 1,962 exact source postings after adding the new live inventories to D128, then removed 121 office/industrial postings, 10 non-lease postings, 44 explicitly inactive postings, 308 outside the two target boroughs, and 161 unknown/non-storefront uses. The surviving 1,318 in-scope postings collapse to 1,057 likely spaces, merging 261 syndicated duplicates. A merge requires the same normalized building (BBL where available, otherwise normalized address) and exact advertised square-foot range; explicitly conflicting floors or named suites remain separate. Every merged row preserves all contributing source IDs, listing IDs, URLs, content hashes, retrieval times, permission bases, rent variants, contacts, and positive feature-claim snippets.

Coverage now comes from 13 permitted public sources: 12 direct broker/owner sites plus the Leasync marketplace — Leasync, Sinvin, RTL/Winick, Titan, Verada, MOD CRE, United American Land, Mona, ARA, Walker Malloy, Roseman Retail Group, Tri State Commercial, and Meridian Retail Leasing. The artifact has 932 rent fields, 591 numeric asks, 392 spaces with amenity claims, and 1,056 mapped points; one valid target record remains in JSONL without a verified point and is honestly absent from the map. Meridian discovery uses its WordPress listing sitemap and Tri State uses the same public first-party JSON endpoint as its property page; both are robots-allowed. Bounded GET retries now cover transient page and GeoSearch resets.

`loci commercial-listings` applies exact-posting dedup, scope/freshness filtering, then space-level entity resolution before either gate. `--min-listings` now measures clean distinct spaces, not source ads; `--min-coverage` defaults to 0.50 against `--market-denominator 1823`, and failure writes nothing. LoopNet/CoStar remains authorized-export-only under D127. The map remains a non-scoring overlay and now displays every merged source URL and conflicting asking-rent variant.

**D131 — Camera and aerial instruments (owner list item 3): every MN+BK camera is swept weekly; free imagery yields three lot-grain heuristics, each ungated behind an owner review page; nothing here enters a grade.** *(2026-09-21, owner; built 2026-09-17 in Session 41, agent stalled before reporting, recovered and landed 2026-09-21)*

**Scope read (urban-planner memo 2026-09-17, in the session scratchpad; verdicts carried here because the memo is not in the repo).** Cameras: the NYCTMC feed covers **3.4%** of the 332,041 MN+BK address points within 100 m and **16.6%** within 250 m (30.8% at 400 m); 83% of the universe has nothing within 250 m and Brooklyn, which carries the gap universe, is 90% uncovered — any camera column is a Manhattan-arterial column. The honest camera measure is *relative sidewalk activity by daypart at the camera itself, against its own history*: a stock, never a flow, never a cross-camera comparison (field of view unpublished; DOT staff "may reposition them"). Aerial/satellite, verified source by source: NYC 6-inch orthos (CC BY 4.0, biennial leaf-off, 2024 latest) and Building Footprints are the only free sources with an address-grain measure nothing holds today; NYC LiDAR 2017/2021 gives height/volume for the A8 floorplate gate; **Sentinel-2 / Landsat measure nothing at address grain** (a pixel is two storefront bays); NAIP is dominated by the orthos; Overture buildings are ODbL share-alike; **Google Street View is contractually closed** (ToS 3.2.3(c) — a storefront index is the named prohibited act, and historical imagery has no API); Mapillary is the only dated ground-floor source and its NYC coverage is unverified; OTI's Cyclomedia/EagleView imagery is an ask. The one thing imagery could do for the survival label — a dated tenant A → vacant → tenant B change for the identity-less categories — **cannot be done at scale for MN+BK from free sources**; it can be done at a pre-registered decision site (Mapillary history, the owner's dated photographs, a camera ROI) as `address_observation` rows. LL157 already gives premises-grain vacancy every six months for filers.

**Owner ruling (2026-09-17, buttons): sample EVERY Manhattan + Brooklyn camera (580 in staging.dot_camera) weekly, over the memo's advice of ≤ 60.** The memo's Regime A (one frame per 10 min across each DOT window, two weekdays: 20,880 frames/week, 0.5 GB, 9 CPU-min) at all cameras, not its ≤ 60-camera cap; `--max-cameras` exists as the owner's throttle and defaults to ALL ("never limit data pulls"); the cadence and windows are constants, not flags. The exposure is recorded verbatim in `model/sidewalk_count.py` from memo §1b: *"The binding cost is legal: DOT sent Traffic Cam Photobooth a cease-and-desist on 2024-11-06 for "unauthorized use of NYC traffic cameras" (https://www.404media.co/traffic-cam-photobooth-cease-and-desist); the feed is not Open Data. My ruling: Regime A at <= 60 cameras (validation pairs plus shortlist anchors), not 580, until terms are read or an ask goes in beside the VivaCity one."* The privacy rule, memo §1e verbatim: *"Privacy rule: persons are counted in memory and never stored; no frame is kept without `--keep-frames` (QA-only, gitignored, local); no face, plate or track is ever produced; rows are aggregate per camera x timestamp."* The ask is drafted for the owner to send: `docs/asks/dot-camera-and-vivacity-2026-09-17.md` (research-use terms for the stills, aggregate-only retention, a fixed User-Agent, stop on request; plus the VivaCity sensor data already ticketed as GTM-159).

**Built.** (1) `loci sidewalk-count sweep --window am|md|pm [--max-cameras N] [--dry-run]`, `flush`, `report --camera|--nta`, `schedule --install|--uninstall|--status`: rows spool to `data/sidewalk/spool/<run_id>.parquet` after every tick and flush later with ON CONFLICT DO NOTHING, so a sweep never waits on the warehouse lock; each run's mean 9×8 thumbnail is dHashed and compared with the previous run's (Hamming > SCENE_CHANGE_BITS → `scene_changed`, `report` cuts a camera's history at its last change); the four free COCO vehicle classes (bicycle, car, bus, truck; motorcycle deliberately excluded) ride along as `n_car` etc., validated against nothing (sql/053; `vision/person_detector.py`). (2) `loci footprints ingest` → `staging.building_footprint` 1,083,030 polygons / 818,190 BBLs / 13 tagged Demolition (sql/054; registry `nyc_building_footprints`, `nyc_orthoimagery`, `nyc_lidar`). (3) `loci aerial pull|change|awnings|convertible` on the OTI WMTS tiles at z20 (0.149 m/px): 2022 668,444 MN+BK tiles pulled with 0 errors, 2024 676,755 tiles with 598,374 fetched and **67,802 errors (10%, resumable — the pull skips cached tiles)**, 12 GB under data/raw/nyc_orthoimagery. Change = share of lot pixels moved > 35 grey levels after a robust gain/offset match, with a normalised cross-correlation guard added after brightness alone flagged 60% of unchanged rowhouse blocks (the sun moved), classed unchanged / under_construction / cleared / new_roof; awnings = a dark strip on the street face of each footprint on the twelve D82 corridors; convertible = one-storey / parking / vacant / garage floorplates ≥ 500 m² from footprints + PLUTO + LiDAR roof height.

**Results, all three "ungated: owner review pending" in the catalog and card context only.** `analysis.lot_aerial_change`: **462** Gowanus permitted lots (D72) — unchanged 410, under_construction 36, new_roof 12, cleared 4; review page `data/aerial/gowanus_change_review.html`, 100 lots with 2022 | 2024 crops and the DOB status (memo §5 row 3 gate: agreement ≥ 0.8 with DOB status on 100 hand-checked lots). `analysis.building_awning`: **1,333** corridor buildings faced, **23 with an awning face (1.7%)** — a near-null that says nadir imagery at 15 cm does not see awnings under sheds and trees, not that the corridors lack them; review page `awning_review.html`, 100 faces (gate: precision ≥ 0.8 vs LL157 occupied premises). `analysis.lot_convertible`: **3,310** candidates (one_storey 1,675, vacant 902, parking 492, garage 241; 133 in the Gowanus box); review pages `convertible_review.html` and `convertible_gowanus_review.html`, top 30 each (gate: hand-check top 30).

**Camera instrument, live.** First sweep 2026-09-17 pm: 580 cameras, 17 ticks, 8,700 fetched, 8,663 unique rows, 1,166 fetch errors, 580 scene hashes first-seen. `analysis.sidewalk_count` now holds **12,490 rows over 584 cameras** (183 on 09-13, 1,830 on 09-14, 3,805 on 09-17, 6,672 on 09-18). Defect found on recovery and fixed today: the sweep planned 17 ticks "to 19:00 local" and ran 16:13 → 22:39 because each tick's work (4–15 min with the endpoint timing out) was added to a full 600 s sleep and nothing checked the clock — 6,349 of the run's rows are stamped `evening`, correctly, and are kept; the loop now sleeps the remainder of the interval and stops at the window's close (`ticks_run` in the report; test `test_a_slow_sweep_stops_at_the_window_close_not_at_the_planned_tick_count`). The owed weekday AM (09-18, 900 rows) and md (09-17, 914 rows) samples at the 15 validation cameras were taken; `loci sidewalk-count validate --round 2026-05`: **N = 45, Spearman ρ = +0.413** (camera mean persons/frame vs DOT people per counted hour; +0.401 vs the raw window count); per window am +0.38, md +0.31, pm +0.50 (N = 15 each, none significant alone). The memo gate (ρ ≥ +0.4, N ≥ 45) is met at the margin; the reading stays card context (D76), never a grade or a denominator. launchd: the three plists are rendered to data/launchd (Tue + Thu at 07:00 / 12:00 / 16:00) and **not installed** (`schedule --status`: installed False, loaded False) — `loci sidewalk-count schedule --install` is the owner's step.

**Overnight ingests (D121-3) verified.** LODES: 22 vintages on disk 2002–2023 (19 fetched, 4 already present; `data/raw/lodes/manifest.json` records the 19 fetched only). ZBP: `analysis.zip_establishments` **8,202,645 rows, 26 years 1998–2023**. CBP: every raw file landed (county totals 1986–2023, county × NAICS 1998–2023, ZCTA 2018–2023) but the run ended in `ValueError: cannot convert float NaN to integer` in `pull_every_year`'s report assembly because `data/raw/cbp/cbp_2023_county.parquet` is a 2026-09-14 cache written by the pre-D121 single-year puller — no `year`/`naics_vintage` columns and no absent sidecar — so the concatenated frame carries NaN years for 2023's 30,666 rows; nothing was lost (the command writes raw parquet only), fix = `loci ingest-cbp-national --years 2023 --refresh`, and the cache read should treat a parquet without `year` as stale. Citi Bike: `citibike ingest --start 2013-06` aborted after 39 s on its first month — **18,059 of 577,703 rows (3.13%) in 2013-06 carry the literal string `NULL` as end-station id** (the 2013 export's null marker), the station-id regex reads it as a foreign dock and the 1% out-of-system guard raises; `staging.citibike_station_month` is unchanged at 2021-02..2026-08 (67 months, 1,911,066 rows) and the legacy roster/crosswalk tables hold 0 rows. Fix = `NULLIF(trim(id), 'NULL')` (and `'\N'`) in `lyft_bikeshare.legacy_projection_sql` so those rows count as dockless ends (report-only for NYC), then re-run 2013-06..2021-01. Not re-run this session.

**Landing.** cli.py was reconciled from HEAD plus item 3's hunks only (a peer's uncommitted ruff reformat of the whole file and their commands are left in the tree, not committed). Suite 2,792 passed / 3 failed, all three a peer's in-flight work (the 16th slug: `test_bike_od` placebo (16,16) and `test_street_frame` two-frames 4,980,615 ≠ 16 × 332,041; and `public_signals.py`'s positional INSERT). `check-sources` green (50 sources); `check-warehouse` red only on eight uncatalogued `staging.public_*` tables from the same peer thread. Supply hash **ed55301203a4 unchanged** — no poi write anywhere in this thread. Ticket provisional: GTM-206 pending the owner's push.

**D125 — Four calculations on existing data (owner list item 2); the v2 survival pre-registration checks run early: P5 fails on the joinable statistic, source #4 supports licence non-renewal, not survival, pending an owner ruling.** *(2026-09-17, landed 2026-09-21, session 34, abenmayor-af; ticket GTM-208 Done; D125 id was claimed before D126–D131 landed)*

Owner (2026-09-16): "I want to make sure we are building ALL additional calculations on existing data", item 2 of three.

Inventory (Opus data-scientist, read-only): the survival label was sitting unread in analysis.licence_interval (126,869 intervals, 48,443 with an observed expiry, 45,255 category-mapped) with no measure or model reading it; ~750k full-history filings never aggregated (sign permits 102,563 and issued permits 500,383 entered no measure); LL157 read only at its latest filing; 79,182 stale Foursquare venues of which 20,888 sit within 30 m of a registered storefront; six hex-era modules unreachable from the CLI. Owner chose all four candidates and the dead-code removal by buttons.

Landed (commit bbaa988): (1) analysis.licence_event 43,863 SLA licences for restaurant/bar/grocery/pharmacy (issue → non-renewed expiry with no successor at the BBL within 180 d; business and premises arms; expiry_future and 1900 sentinels dropped) plus a 400 m address measure (n_licences, n_nonrenewed, 5-y rate vs the category × borough baseline: restaurant MN 35.0% / BK 36.9%, bar 20.0 / 20.9, grocery 31.5 / 35.2); the v2 pre-registration checks run early: ≥ 200 events restaurant 3,966 / grocery 957 / bar 376 PASS, pharmacy 6 auto-dropped; P3′ (licence → scored lot, floor 70%) grocery 71.6% PASS, restaurant 65.2% and bar 59.2% FAIL (nearest lot ≤ 50 m would impute 99%, so any fit must state scores are imputed); P5 (expiry means closure, floor 0.50) joinable PPV restaurant 0.29 / bar 0.24 / grocery 0.04 ALL FAIL, so per the pre-registration the outcome is "SLA licence non-renewal" and context only; the determined PPV (closed vs seen trading after) restaurant 0.52 / bar 0.53 pass and grocery 0.07 fails, printed beside it; which statistic binds is an owner ruling pending; no hazard fit by design. (2) Went-dark triangulation: analysis.closure_triangulation 4,668 corroborated (4,580 stale + LL157 vacancy flip, 86 stale + licence end, 2 all three; 74,514 stale-only NOT written per D79; an activity-class compatibility guard halved 9,978 → 4,668; flip_shared_by flags 3,071); promotion would flip 298 poi_status rows and move the hash; NOT promoted, owner ruling pending. (3) analysis.supply_snapshot 418,583 rows for t0 = 2016..2025-01-01 via supply_as_of, backfill-censored share stamped per t0 (48.6% → 30.5%), counts validated. (4) analysis.storefront_tenure 50,565 premises / 191,976 premises-years / 34,099 turnovers with 400 m address measures; storefront_pipeline rebuilt on the full-history filings 135,912 → 541,051 rows (1989–2026) with twelve category-blind fit-out/sign/permit/liquor counts at 6/12/24 m (D88 costume risk stated: card context only). Three context lines on the recommendation card, each tagged "context, not a grade". momentum.py, rising.py, dnci.py removed; invest/premium/ignition kept (imported); CONTEXT.md A9 records it. Seven catalog entries, WAREHOUSE.md 94 objects. Supply hash ed55301203a4 unchanged at every step (hash guard exits 2 on a move). Suite 2,742 passed.

Done 2026-09-17 (CHECKPOINT D125). Pushed to Linear 2026-09-21 as GTM-208.

**D130 — Two SoHo operator pages issued under the AC-6 loop (Deux Luxe, Fazenda); Fazenda is the first PREDICTIVE page; its rent guess scored a MISS on floor area, so operator pages must declare "floor area unknown" on `retailarea` 0 lots instead of assuming one.** *(2026-09-17, owner; Session 42)*

Decided: (1) "Deux Luxe, on paper" — 384 Broome St, BBL 1004800041 (its own lot, not 1004710039), restaurant (wagyu burger counter), open since 2025-05, no SLA licence; ONE number $1,500,000/yr sales before tax (bottom-up 190 weekday orders × $23; the revenue-v0.2 p50 $819,875 is pre-registered as LOW because it is floor-area × a typical-restaurant band), rent $14,000/mo on 1,171 sq ft, staff $450k, break-even $1.11M. Record `docs/recommendations/predictions/deux-luxe-broome-2026-09-17.md`; page https://claude.ai/artifact/M74a39pHk9NU4JXiF1LGqT. (2) "Fazenda, on paper" — 177 Mott St, BBL 1004807502, café + beer bar inside a menswear store, opened summer 2026, first-seen 2026-09 backfill_censored (DOHMH permit only; absent from Overture 2026-09-01 / FSQ 2026-09-02; no SLA row despite the beer-bar billing). Owner asked for it PREDICTIVE: ONE number $470,000 first-twelve-month coffee-counter sales (clothing excluded, unpriced), scored 2027-09-17, band $300k–$750k; survival p = 0.75 still serving food on 2027-09-17, scored against the ledger only (cafe_bakery is one of the eleven categories dropped before the §3.1 survival fit, counts for nothing under AC-3). Record `docs/recommendations/predictions/fazenda-soho-2026-09-17.md`; page https://claude.ai/artifact/9ZggYDYXLXoinKFGv5sMFx. (3) First scored Fazenda answer (owner, verbal, 2026-09-17): rent **$15,000/mo exactly on under 1,000 sq ft** vs guessed $37,500 on 2,500 sq ft — MISS on rent (outside $22k–$60k) and MISS on area. Logged in the record's §7 and in `docs/recommendations/predictions/fazenda-soho-2026-09-17-rent-answer.md`; frozen sections and the published page unchanged. (4) Rule for future pages: when PLUTO `retailarea` is 0 (condo lots) and no storefront-registry sq ft is filed, the page says "I could not see your floor area" and asks; it does not assume one.

Why: the per-foot rate was right ($15,000 × 12 on <1,000 sq ft is ≥ $180/sq ft, exactly the rate used); the floor area was wrong by >2.5× and had no data behind it — the record itself flagged `retailarea` 0 and no shop asking rents on Mott/Broome. A guessed area propagated into rent, the counter's rent share, and the break-even, and flipped the record's conclusion: at the true rent the counter's break-even re-computes to ≈$446k (footnote only; frozen $530k stands), below the $470k sales guess, so the counter plausibly carries itself. Declaring the unknown costs nothing and keeps the scoreable guesses honest. The Deux Luxe area (1,171 sq ft = a quarter of 4,685 sq ft over four registered food storefronts) is also an assumption and is now flagged as the weakest input on that page. Both pages copy the owner-approved Lion's Milk / El Punto CSS and order exactly (one sales number, sales → rent → costs → questions → how); method tables stay in the markdown records.

**D132 — Glossary audit: the charter is reconciled with what the code does; four owner rulings and one open definition.** *(2026-09-21, owner; Session 43, abenmayor-57)*

A read-only Opus audit compared every term in `docs/CONTEXT.md` against `src/loci/` and found nine contradictions, ~15 load-bearing undefined terms, 13 words carrying more than one meaning (`closure` has seven), and a dozen stale numbers. The owner ruled on the five that only the owner could:

1. **`act` needs grade ≥B; `diligence` is the grade-C verdict** (code is right, D74 stands). C4 had equated *act* with "worth hand-diligence" and AC-4 was titled "reaching *act*" while resolving on economics at C, which the ladder makes unreachable. AC-4 is re-titled "reaching *diligence* or better, with economics at C or better"; the economic bar is unchanged — the words were fixed, not the criterion.
2. **Co-location: the charter and the 2026-09-14 rule stand; the code is wrong.** Code groups only same-category records at one coordinate cell, triggers no check, and counts unresolved pairs as-is (`COLLAPSE_UNRESOLVED = False`). Fix ticketed on the existing D24 ticket. It moves the supply hash when run — announce to every peer first.
3. **A fourth radius is admitted: 400 m straight-line, the forecast/retrodiction outcome and feature radius, frozen for comparability with D88.** "Never Euclidean" now applies to walkable *measures* only. The allocator memo's 500 m straight-line POI list is a fifth radius on a card and is ticketed for removal in favour of the 400 m network catchment.
4. **Claim layers: the screen (gap score, supply ratio, character, recommendations) is REALIZED; only fitted quantities (p_opening) are MODELED.** §4.5 corrected; the warehouse's staging/measure/score/ledger/calib taxonomy is now called "warehouse layers" to stop the collision.
5. **Non-filtering signal: definition deferred.** The charter says bathhouse_sauna must leave gap_score / lead_category / n_missing byte-identical; the code puts the slug in ALLCATS and would move all three after ingest. The gap is ticketed; the term's meaning is decided when ingest (D124-1) forces it.

Also landed in CONTEXT.md: 16 code-derived definitions in §4.2 (POI, canonical POI, supply set, registry/floor anchor, closure gate, poi_status, evidence-aged, backfill-censored, rewind/rung, licence event, went-dark triangulation, paid closure check, censored nearest distance, frame, licensed lot vs registry premises, act/diligence); a "words that carry more than one meaning" table with canonical names; §4.3 rewritten for the address era; stale numbers refreshed (50 sources with the true class breakdown, eight feeds, hash ed55301203a4, as-of 2026-09-16, P-checks recorded as run, four survival sources, four demoted categories, licence-event locus at the BBL with a business arm, AC-1 baseline superseded by D123, hexes as calibration input only).

**Why a decision and not just an edit.** Two of the rulings change what a shipped number means (a card at C says *diligence*, never *act*; the D88 coefficient is straight-line, not walkable) and one commits to a hash-moving code change. Recording them here stops a later session re-deriving "act means diligence" from the old C4.

No warehouse writes; supply hash ed55301203a4 unchanged.

**D133 — Codex peer session: agent hand-off tooling, commercial-listings collector, and public trade-area signals landed uncommitted.** *(2026-09-21, read-only audit by a separate session)*

Uncommitted tree work built by the Codex peer session already referenced in D131/D132 ("a peer's uncommitted whole-file reformat," "a peer's eight uncatalogued staging.public_* tables") gets its first description here; nothing below was committed by this audit, which was read-only end to end.

**Agent hand-off layer**: `src/loci/agent_memory.py` (5 tests) builds a bounded, read-only `Brief` from the document trio (charter/phase/next actions/scope note) and renders either a Markdown resume packet or a role-specific Codex hand-off (`dispatch()`) for one of five roles — coordinator/data-engineer/research-reviewer/developer/red-team — each keyed to a new `.codex/skills/loci-*/SKILL.md` mirroring `docs/AGENTS.md`'s existing role cards verbatim. New CLI: `loci agent-brief [--next-actions N] [--json]`, `loci agent-dispatch <role> --task ...`; new Makefile target `agent-memory` runs `agent-brief` as a document-contract smoke check (not yet wired into `make check`). New root `AGENTS.md` names this the required session-start command for Codex.

**Commercial-listings collector**: `src/loci/sources/cities/nyc/commercial_listings.py` (1,992 lines, 24 tests) collects advertised availability from 13 permitted broker/owner sites (Sinvin, RTL, Leasync, Titan, Verada, MOD, UAL, Mona, ARA, Walker, Roseman, Tri State, Meridian), checking robots.txt per source, pacing per-source minimums, and hard-refusing LoopNet/CoStar live scraping (`BLOCKED_LIVE_DOMAINS`; only enters via an authorized CSV export with a recorded `--authorization` string). Amenity fields are TRUE-or-unknown, never inferred FALSE. Rows are geocoded and location-verified against the official NYC borough boundary; near-duplicate postings across sources are deduplicated by content and address. New CLI: `loci commercial-listings` (collect + write JSONL + GeoJSON, gated on `--min-listings`/`--min-coverage` against a stated market benchmark of 1,823 active MN+BK listings) and `loci commercial-listings-map` (rebuild the map export from a saved snapshot + current warehouse, joining screen context only on an exact tax-lot BBL match). Webmap: new "Advertised spaces" overlay (toggle, circle layer, hover/click popups) showing address/space/rent/amenity claims plus, on an exact BBL match, the headline reach-gap category and restaurant revenue scenario — captioned "a reach-gap signal, not a recommendation" and never an input to `gap_score` or supply ratios. One collector run on disk: `data/commercial_listings/latest.jsonl` (2.68 MB) and `webmap/data/commercial-listings.json` (3.1 MB, regenerated 2026-09-21).

**Trade-area profile**: `src/loci/model/trade_area_profile.py` (1 test) is a pure read-only SQL projection over existing ACS/LODES/MTA/Citi Bike/PLUTO address-grain measures into a fixed 36-column table; explicitly not a customer-origin or sales model, changes no grade. New CLI: `loci trade-area-profile [--boroughs MN,BK] [--out PATH] [--dry-run]`. One MN+BK export on disk: `data/exports/trade_area_profile.csv` (105 MB).

**Public signals ingest**: `src/loci/sources/public_signals.py` lands six newly admitted public trade-area signal families (NYC mobility survey, permitted events, open restaurants, DOE demographics via Socrata; Census Opportunity Atlas tract covariates/outcomes; IRS ZIP income archive) into `staging.public_*` tables — this is the source of the "eight uncatalogued staging.public_* tables" that D131's `check-warehouse` already flagged red. New CLI: `loci ingest-public-signals [--signals ...] [--root PATH] [--dry-run]`. No source is coerced into `staging.poi`; nothing here touches the screen grade.

**cli.py's ~10,300-line diff is a mechanical reformat** (typer.Option signatures re-wrapped) plus exactly the 6 new commands above — confirmed by function-order comparison against HEAD (195 → 201 top-level defs), not a divergent rewrite.

**Owner rulings on scope-fit (2026-09-21)**: `.codex/` kept local-only for now (gitignored, not shipped) pending confirmation it's meant to ship. The two GTM research docs found alongside this work — `docs/loci-location-intelligence-prospects.csv` and `docs/nyc-retail-chain-investors-2026-09-18.md` (a PE/VC-backed-chain investor list and site-selection-contact prospect list, dated 2026-09-18) — are cleared to ship as-is despite unclear fit to this branch's theme. Code commit itself (agent_memory.py, trade_area_profile.py, commercial_listings.py, public_signals.py, the cli.py/webmap/Makefile diffs, AGENTS.md files) is ON HOLD pending the owner confirming with the Codex peer session that built it — not committed by this entry.

**Not yet done / flagged for the owner**: `data/chains/`, `data/commercial_listings/`, `data/exports/` were untracked and matched no `.gitignore` rule (105 MB CSV export currently unexcluded) — fixed by this entry's `.gitignore` addition. `data/loci.duckdb.pre-reclaim-2026-09-16` (9.6 GB) remains present, still un-ignored by any exact-match rule, already flagged for deletion pending owner confirmation per D131. No warehouse writes were made by this audit or this entry; supply hash untouched; check-tickets/check-warehouse were NOT re-run as part of the audit itself.

**D134 — D132/GTM-202 live re-run: blocked at `address-gaps` by a pre-existing defect (GTM-209), then the owner authorized fixing GTM-209 itself; fixed at both call sites, re-run completed end to end, screen now stamped on the collapse-fix hash.** *(2026-09-22, owner-authorized hash-moving run; Opus data-engineer)*

The peer-announcement window on D132's collapse-fix code (`COLLAPSE_UNRESOLVED=True`, `SINK_GROUP_SIZE=5`) cleared with no objections and the owner authorized the live re-run. `loci supply-ratio --boroughs MN,BK --fit-baseline` ran clean: 136,563 → 133,356 → 130,536 canonical POIs (gate then collapse, matching the pre-run preview exactly), `supply_baseline.yaml` re-fit and hash-stamped `ed55301203a4` → `1898163ac8ce`, 332,041 `analysis.address` rows and 4,980,615 `analysis.address_category` rows UPDATE-only refreshed (five + three disjoint columns, per D73's contract) for the 15 real categories; `tests/test_supply_ratio.py::test_baseline_supply_hash_matches_the_live_poi_supply` now PASSES.

`loci address-gaps` then refused outright: `EmptyCategoryError: 1 of 16 categories have no POI in the supply set: bathhouse_sauna`. This is not caused by the collapse fix — it is GTM-209 (Urgent, opened the same day as this ruling under D132), which already names this exact failure mode for `address_gaps.py`: `bathhouse_sauna` was admitted to `categories.py`/`ALLCATS` at D124 (2026-09-17) as a non-filtering signal with ingest deliberately deferred (D124-1), and nobody had re-run `address-gaps` (or, it turns out, `supply-ratio --fit-baseline`) since, so the gap sat undiscovered. Widened GTM-209 (tickets.py) with a second instance found in the same session: `model/supply_ratio.fit_baselines()` has no zero-supply guard either, so my `--fit-baseline` run wrote a functionally-inert but policy-forbidden `baseline: 0.0` row for `bathhouse_sauna` (`baseline_of()` nulls any non-positive baseline out of every ratio, so no category's numbers besides bathhouse_sauna's own are affected) — this trips `tests/test_supply_ratio.py::test_baseline_yaml_declares_its_radius_and_universe` and `tests/test_category_registry.py::test_generated_model_tables_cover_every_slug`, both of which assert the unfitted set is exactly `{bathhouse_sauna}` per the GTM-198 G8 rule ("a hand-written row would be a fabrication"). `supply_baseline.yaml` is header-marked GENERATED / do-not-hand-edit, so this was left as the honest output of the command rather than hand-stripped — reverting the yaml entirely was considered and rejected, because it would only trade this one new failure for reintroducing the hash-mismatch failure this whole re-run exists to fix, without actually restoring a clean state (`test_category_registry` and `test_bike_od`'s 16×16-vs-15×15 failure are driven by `ALLCATS` having 16 slugs, not by the yaml's contents, so reverting the yaml would not have fixed them either). `tests/test_street_frame.py::test_the_warehouse_holds_exactly_two_frames_and_the_namespaces_are_disjoint` fails for the same root cause and was *already* broken before this session touched anything: `analysis.address_category` has held exactly 15 rows per address (4,980,615 = 15 × 332,041) since the last successful `address-gaps` run, predating D124 — the canonical screen has been silently stale on category count since 2026-09-17 and nobody had checked.

**Net effect**: `score.supply.supply_hash()` reads `1898163ac8ce` (the collapse fix is live and correct in code), and `supply_baseline.yaml` plus the 15 real categories' `supply_per_1k`/`homes_400m`/`supply_ratio_vs_base` columns are refreshed against it. But `analysis.address_gaps` (`gap_score`, `lead_category`, `nearest_m`, `n_missing`) — and everything downstream in the canonical order (address-demand, pipeline, storefronts, age-fit, the second supply-ratio apply, address-access, transit-profile, citibike, address-character, storefront-pipeline, dot-counts, address-legality, revenue, export-webmap) — did NOT run and still reflect the PRE-collapse-fix supply set. D132/GTM-202's implementation is code-complete but its live re-run is still NOT DONE; QUESTIONS D24 stays open. Test suite: 2,810 passed / 1 skipped / 6 failed (pre-run baseline was 2,808 / 1 / 4: the hash test that was expected to flip did flip; the 3 pre-existing-unrelated failures the task briefing named were `test_no_positional_inserts` (a separate Codex peer session's uncommitted `public_signals.py`, D133) and, it now appears, two of `test_category_registry`/`test_bike_od`/`test_street_frame`/`test_supply_ratio::test_baseline_yaml_declares_its_radius_and_universe` — all four of which are really ONE root cause (GTM-209) counted four ways, and all four predate this session; `test_paid_sources` failed only in the full-suite run and passed in isolation, so it reads as order-dependent/flaky, not something this session caused).

**RESOLVED, same session (owner ruling): "fix GTM-209 now, then finish the canonical re-run."** Before writing code, the design question the fix turned on — should a zero-POI category read as a universal gap, or be excluded from scoring — was checked against three sources rather than guessed: (1) `docs/CATEGORY-EXPANSION.md` §4, the signal-vs-filter rule, already answers it in writing: "the test of a signal: removing it must leave `gap_score`, `lead_category`, `n_missing` and the missing set byte-identical" — a category admitted as `ships_as: signal` (bathhouse_sauna, D124/GTM-198) is excluded from those three aggregates as a matter of charter, independent of whether it has any supply at all; a zero-POI signal category is simply the earliest point on that same exclusion, not a special case needing its own ruling. (2) No prior category was ever admitted with zero POIs — every one of the original 15 had real supply from OSM/Overture/DOHMH on day one; bathhouse_sauna is the precedent-setter, which is exactly why GTM-198 built the `EmptyCategoryError` guard as a stopgap in the first place (its own commit message: "an unmapped slug would otherwise lead everywhere"). (3) The guard's docstring already stated its own reasoning in full — a zero-supply category "becomes `lead_category` for the whole city and `n_missing` gains one at every address — silently" — confirming the guard is deliberate fail-loud design, not an oversight to route around. All three pointed the same direction with no tension between them, so this did not need to come back as a fork.

Built: `model/conveniences.signal_categories()` — a small, cached, shared reader of categories.yaml's `ships_as` field (same pattern as `model/recommend.non_headline_categories()`, a different flag on the same file) — imported by both `address_gaps.py` and `supply_ratio.py` so the definition lives in exactly one place. `address_gaps._refuse_empty_categories` exempts a signal slug from the empty-category guard. `compute_gap_metrics` gained a `signal_cats` parameter: `ratio` is computed unmasked for every category including a signal one ("keep nearest_m/ratio computed," per GTM-209's own text), but the row-max/argmax/`>1`-count that produce `gap_score`/`lead_category`/`n_missing` mask signal columns out first. `supply_ratio.fit_baselines()` now skips (never zero-fills) a category that was swept but has zero POIs anywhere in the sweep — distinct from the pre-existing "not swept at all" test fixture, which still gets an explicit `None`, so the existing `test_fit_baselines_emits_every_category_even_with_no_rows` needed no change. Four tests: the old `test_db_engine_refuses_a_category_with_no_supply_pois` hardcoded `ALLCATS[-1]` as the must-raise example — true only by accident until bathhouse_sauna became `ALLCATS[-1]` — rewritten to pick a genuine filtering category explicitly; a new test proves a signal category at zero supply does NOT raise; a new test rigs the signal column to ratio 999,999 (would trivially win every argmax if it counted) and proves `gap_score`/`lead_category`/`n_missing` do not move while `ratio` itself does; a new `fit_baselines` test (via `monkeypatch` on `ALLCATS`) pins the skip-when-swept-and-zero behaviour independent of which real slug is unfitted today.

Re-run from `supply-ratio --fit-baseline` through `export-webmap` (skipping `street-frame`: street geometry does not depend on POI supply and the cached parquet from 2026-09-16 is still current) completed with no further errors. `supply_baseline.yaml`'s categories block is back to excluding exactly `{bathhouse_sauna}`; `loci address-gaps` wrote 332,041 rows (281,842 lot + 50,199 street) with NO EmptyCategoryError. Screen result: 190,413 lot addresses with a gap (median `gap_score` 1.26) + 33,934 street (median 1.29); lead-category distribution bar 76,066 / laundry 56,735 / tailor_repair 43,211 / bank 20,609 / convenience 16,699 / cafe_bakery 9,348 / pharmacy 604 / nails_beauty 340 / restaurant 315 / fitness 198 / hair_barber 158 / childcare 62 / hardware 2 — grocery and clinic lead nowhere, and `bathhouse_sauna` correctly leads NOWHERE (332,041/332,041 addresses read `ratio > 1` for it per the raw per-category printout, all correctly excluded from `n_missing`/`lead_category`). **No valid "before" screen snapshot exists to diff these numbers against**: `address-gaps` had not completed successfully since D121 (2026-09-17, pre-bathhouse_sauna, pre-collapse-fix) — this is the first screen computation on the new hash, not a delta from a prior one. The collapse fix's own isolated effect remains the colocation preview, reconfirmed unchanged post-run: gated supply 133,356 → 130,536 (−2,820, −2.11%). age-fit refit clean on the new hash (bar dispersion 3.63×, childcare 3.21×, pharmacy refused on its own regressor — all matching precedent exactly); `export-webmap` wrote 151 files, 176.5 MB.

Full suite: 2,819 passed / 1 skipped / 2 failed, both pre-existing and unrelated to this work — `test_bike_od.py`'s own hardcoded `(15, 15)` matrix-shape assertion (a separate gap this ticket did not scope, since `bike_od.py`'s placebo was never asked to honour `signal_categories()`) and `test_no_positional_inserts.py` on a different Codex peer session's uncommitted `public_signals.py` (D133). `test_category_registry` and `test_street_frame`, both failing on the GTM-209 root cause, now pass, as predicted. `test_paid_sources` did not fail in this run either, confirming the earlier read of it as an order-dependent flake.

**Scoping call, noted not acted on**: `model/address_demand.py` still annotates a signal category's `ratio > 1` rows (143,127 for bathhouse_sauna this run) — it is a sibling table governed by the D48/D57 "graded, never filtered" rule, a different mechanism from the signal-vs-filter exclusion GTM-209 covers, and GTM-209's own text scoped the fix to `address_gaps.py` and its aggregates only. Left as a candidate follow-up ticket, not assumed in scope. `loci revenue` also printed a pre-existing, unrelated warning (`revenue.yaml` spec_hash drift `9bda94515a2e` → `43b8334c2c7b`) and a *correctly expected* one (supply hash drift, since lambda was calibrated on the old set) — `revenue fit` was NOT run, since it is not part of the documented canonical order and re-calibrating lambda is its own gated decision outside this task's scope.

D132/GTM-202's live re-run is now COMPLETE. QUESTIONS D24 is answered. GTM-202 and GTM-209 both marked Done in `tickets.py`, with the completion detail carried in their descriptions per this project's convention. No files staged or committed by this session.

No files were staged or committed by this session. Changed: `src/loci/model/supply_baseline.yaml` (the fit-baseline output), `docs/CONTEXT.md` §9.7, `src/loci/tickets.py` (GTM-202 and GTM-209 descriptions), this entry.

**D135 — Stone Street Coffee operator answers scored: sales HIT, and the second floor-area MISS, opposite in direction to Fazenda's.** *(2026-09-22, GTM-214; committed 64b8d8a before this entry, CHECKPOINT/tickets held by a concurrent session for D134)*

The former manager of Stone Street Coffee (379 Broome) answered by text on 2026-09-21; the owner relayed the thread as a screenshot. This closes four of the five questions in the 2026-09-15 pre-registration (`docs/recommendations/predictions/stone-street-coffee-379-broome-2026-09-15.md` §6) and is the third operator record scored, after El Punto (D91) and Fazenda (D130).

**What the operator said.** Annual sales ≈**$1,400,000** on **107,000 transactions**, ~$13.08 average ticket — "because we're big on food"; delivery ≈5% of sales, under 3% in the most recent week (2,300 sales at $12.90); Saturday and Sunday dominate; staff is 2 baristas on the floor, 3 in the kitchen and 1 porter; rent was "a little low" against the $10,500/month guess, with no figure given beyond "almost nobody in Nolita has rent under 10k unless they're on a very old lease"; and the shop holds **the whole 2,013 sq ft ground floor plus a basement of equal size**.

**Sales: all three guesses hit, and the winner was disqualified in advance.** Scored on the record's own rule (`ln(our ÷ true)`, plus hit/miss on the stated range): the Loci model (a) $1,409,100 → **+0.006**; bottom-up (c) $1,160,000 → −0.188; judgment (b) $1,000,000 → −0.336. All three fall inside their ranges. But §4 of the record pre-registered the disqualification: *"(a) is high; the truth sits between $800,000 and $1,300,000. Above $1.4M, our order count was far too low and the model was right by accident."* The truth landed at ~$1.4M, at or just above that self-declared ceiling, so **(a)'s near-exact figure is logged as the coincidence the record called ahead of time, not as evidence the `capacity_bound` heuristic works.** It remains floor area × a price band with no demand term (`revenue-v0.2`, `cafe_bakery` unmodelled by design), and this result must not be cited to promote it to a `cafe_bakery` estimator. The record's own tighter band was ~7–8% low, which is the honest headline. The order-count check supports that reading rather than the model's: back-solving gives ≈2,058 orders/week against the bottom-up assumption of 1,960, about 5% low — a modest miss, not the "far too low" count that would have been needed.

**Floor area: MISS, and the second one, in the opposite direction to the first.** The record assumed the 2,013 sq ft PLUTO retail floor was split roughly in half with Greecologies 1 m away, putting the shop at ~1,000 sq ft. It holds the entire ground floor plus an equal-size basement. Fazenda (D130, GTM-207) missed the other way — 2,500 sq ft assumed against under 1,000 true — because PLUTO carried `retailarea` 0 on a condominium lot and a typical footprint was substituted. Here `retailarea` was present and real, and the error came from an *extra* inference layered on good data: that an adjacent second business implied a shared demise. **Ruling, generalising both: never infer floor area or how it is split, from adjacency, from a lot-typical footprint, or from the presence of a second business at the lot — ask, or state it unknown.** D130's retailarea-0 rule is the narrow case of this one, not the whole rule. A present `retailarea` figure says how much retail floor the lot has, never who holds it, and never whether a basement doubles the usable space.

**Rent and staff: directional only, not ln-scored.** No figure was given for either, so neither can be divided into a point estimate. Rent's "a little low" places the truth above $10,500 and, inferentially, inside the $7,500–$15,000 range — logged as a directional hit with the point guess low. Staff cost's "close" is a hit on the $340,000 total, but the composition is wrong: the record modelled two to three people at the busy hours in a counter-serve shop, and the real operation runs a three-person kitchen crew. A right total reached through a wrong shape is worth recording as such — the operator's own framing is that the kitchen "positions us differently than a standard cafe," which §1 and §4 did not distinguish.

**Why this matters beyond one shop.** Both scored misses on this project are now **inputs** — rent and floor area — not model form. That is consistent with D118's challenger null (HistGradientBoosting failed its pre-registered ΔAUC floor) and it locates the binding constraint: the warehouse holds no shop rents (the listings table holds flats), so the screen's economics stay grade D until real rents arrive. Owner's reading, same day: more ground truth beats a smarter model.

Files: §7 evidence log filled in the frozen 09-15 record; scoring and error analysis in `docs/recommendations/predictions/stone-street-coffee-379-broome-2026-09-21-answer.md`. Both committed as 64b8d8a. No warehouse writes; supply hash untouched by this work.

**D137 — Admit `brewery` as a 17th category, its own slug, non-filtering signal.** *(2026-09-22, owner rulings; subagent session via Fable coordinator; ticket TBD (not yet pushed to Linear) Todo)*

Owner rulings (2026-09-22): (1) `brewery` is its own category — NOT folded into `bar`. (2) WIDE definition: includes ALL breweries — taprooms, brewpubs-as-breweries, AND production-only brewers with no public tasting room. Owner ruled 2026-09-22 to re-derive tests/test_bike_od_specificity.py for the 17-category family in the same change rather than xfail it.

Landed through the fail-closed expansion checklist (docs/CATEGORY-EXPANSION.md) only as far as G0 (registry/adapter completeness, no warehouse needed) — mirroring exactly how `bathhouse_sauna` (D124, GTM-198) was admitted. G3-G8 (anchor coverage, dedup rank, census validation, demand, density-elasticity, G9 coverage grade) all need a fit against real ingested supply and are explicitly NOT passed; no warehouse write, rebuild, migration apply, or poi_status/supply_hash change was made or attempted this session — a peer session holds the hash move.

Touch points landed (mirroring bathhouse_sauna's ~25-file pattern): `categories.py` (17th slug, tier 3 — food & gathering, alongside bar/restaurant/cafe_bakery, not tier 4 like bathhouse_sauna: a brewery is a food-and-drink venue, not a destination amenity), `categories.yaml` (`ships_as: signal`, `headline: false`, NAICS 312120, OSM `craft=brewery`/`microbrewery=yes`, Overture `brewery` — verified against the live NYC extract, `brewpub` does NOT exist as an Overture primary category — Foursquare leaf "Dining and Drinking > Brewery"), the three universal adapters (osm_overpass.py, overture_places.py, foursquare_places.py — the last gained a brewery-priority pre-check in `map_leaf()` because ~10% of Foursquare rows list a generic Bar/Restaurant label before the Brewery leaf, which would otherwise shadow it under first-path-wins traversal), validation/google_places.py (`brewery`+`brewpub`, both real Table A types), viz/webmap_export.py (COLORS entry, dE76 33.7 from the other 16), sql/036 + sql/007 CHECK literals widened (sql/007's is a NEW class, `brewer` — see below), reach.yaml/reach_tiers.yaml/conveniences.yaml/spend.yaml/demand.yaml/benchmarks.yaml/zbp_naics.yaml/model/revenue.yaml/model/density_elasticity.yaml all gained a brewery row, several explicitly flagged `TODO(brewery)` placeholders (see below — no number was invented silently), tests updated (`test_category_registry.py`, `test_demand_caveat.py`, `test_carrying_capacity.py`, `test_supply_ratio.py`, `test_recommend.py` incl. the live-warehouse G9 grade test, confirmed read-only: brewery grades C, same as bathhouse_sauna), `docs/CATEGORIES.md` regenerated (`loci gen-categories`, 17 categories), `docs/CONTEXT.md` §4.2 glossary updated (17 categories, 5 demoted).

NYS SLA licence types Brewer/Micro-Brewer/Micro-Brewer 3 Year/Farm Brewer (sources/cities/nyc/alcohol_licences.yaml) moved off `other` onto a NEW overlay classification named `brewer` (deliberately NOT `brewery` — that string is the Loci POI category, and `test_the_overlay_is_not_a_sixteenth_category` pins that the two vocabularies never collide; caught by running the adapter test suite, not by inspection). This is the standalone alcohol-OVERLAY map layer only (sources/cities/nyc/nys_sla.py's docstring scope) — NOT wired into staging.poi, so `brewery` the POI category still has NO registry.yaml anchor, same P1-open state bathhouse_sauna shipped in. `sql/007`'s CHECK constraint was widened in source (reaches only a fresh warehouse) and a matching `sql/056_brewery.sql.draft` was written, NOT applied, for an existing warehouse's migration. "Restaurant Brewer" left mapped to `restaurant` in model/filing_categories.yaml, one-line comment added noting this is intentional (a brewpub serves food first) — unchanged from before this session.

Dedup verdict (no code change): `score/dedup.py`'s cross-category merge is category-PAIR-agnostic already (any two categories merge on name/distance rules, not a hardcoded bar/restaurant special case); `_VENUE_WORDS` already lists "brewery"/"brew"/"taproom". The ONE pair-specific mechanism, `CATEGORY_PRECEDENCE = "finer_food"` / `_FINER_FOOD = ("cafe_bakery", "bar")`, was deliberately NOT extended to include `brewery`: doing so would make an aggregator's `brewery` tag override a DOHMH "restaurant" permit label for a brewpub, which contradicts the owner's own "Restaurant Brewer serves food first, stays restaurant" ruling above. Left alone on purpose, not an oversight.

Every open placeholder (owner/real-data follow-up, not invented silently): `reach.yaml` brewery=2450 (query right-censor value, same convention as bathhouse_sauna, not measured); `reach_tiers.yaml` brewery reach_m=400/owner_m=640 (analog to `bar`, no fresh literature search); `conveniences.yaml` brewery=8 min (analog to `bar`, flagged `TODO(brewery): owner to confirm`); `spend.yaml` brewery annual_spend=556 (flagged placeholder copy of bar's own row — CEX has no bar/brewery split); `zbp_naics.yaml` brewery NAICS 312120 confidence medium, no live CBP pull run (`TODO(brewery)`); `model/revenue.yaml` brewery added to `benchmark_only` (real CEX line + real EC cell exist, unlike bathhouse_sauna, but no fit has run — move out once one has).

Full test run (targeted, not full suite, per instruction): every touched adapter/registry/demand/recommend/legality/migrate/webmap/revenue/density test file passes. `tests/test_bike_od_specificity.py` (untouched, NOT in the ~25-touch-point list) now fails 7 assertions that hardcode the BH "family of sixteen" from `sorted(CATEGORIES)` growing to 17 real synthetic categories — confirmed pre-existing-shape breakage caused by this session, left UNFIXED and flagged rather than hand-guessed: the seed-specific permutation numbers in that file (e.g. "seeds 9, 10, 12, 13 fire at sixteen") need re-derivation against the new 17-category synthetic family, which is a statistical recalibration task, not a mirroring one, and was out of the declared code-and-tests scope this session ran under. `uv run loci check-sources` passes (registry.yaml note-only edit, docs regenerated, no drift).

**D137 addendum — the deferred full re-run, executed** *(2026-09-22, same day, owner approved; peers cleared the hash)*. `sql/056_brewery.sql.draft` renamed to `sql/056_brewery.sql` and confirmed harmless under `db.init_schema`'s `*.sql` glob (the file is comment-only — the real migration is code, per migrate.py's own header on why heavy rebuilds are never `.sql`). Applied `loci migrate-warehouse --step observation_category_check --apply` (analysis.address_observation 103→103 rows, CHECK now names all 17 slugs) and, by hand, the `staging.alcohol_licences` CREATE-INSERT-count-DROP-RENAME the draft documented (24,412→24,412 rows, CHECK now includes `brewer`; no existing migrate.py step covered this table). `loci ingest-alcohol` (full) then reclassified the four-licence-type set: 55 `brewer` rows (BK 29 / QN 17 / MN 4 / SI 4 / BX 1), up from 0.

Pre-flight (dry-run, small `--limit`, nothing written) confirmed brewery rows land from every live-adapter path before the real pulls: Overture 16/12,076 sampled, Foursquare 8/7,099, OSM 38/43,249 (OSM sampled only — see below), SLA 55/3,000 licences classified `brewer`. No crash on any adapter.

Full re-run, uncapped, canonical D78 order: `loci ingest --source overture_places` (134,140 rows, 168 brewery) and `--source foursquare_os_places` (109,231 rows, 182 brewery) re-ingested in full — `osm_overpass` was deliberately NOT re-ingested into the live warehouse: it has never been part of the live POI supply (E1-era decision, staging.poi carries zero osm_overpass rows today); adding it would be a new-source scope change, not a category admission, so it stayed at the pre-flight dry-run confirmation only. `loci dedup`: 308,706 → 219,364 canonical (28.9% collapsed); brewery 350 raw (168 Overture + 182 Foursquare) → 227 canonical (151 Overture + 76 Foursquare after cross-source collapse). Nearest-MN/BK-address nearest-neighbour split (≤500 m, `analysis.address`, approximate — POI supply is citywide, not MN+BK-scoped): BK 55, MN 39, 133 nearer no MN/BK address (QN/BX/SI or open water) — expected, since reach crosses borough lines and most breweries sit outside MN+BK.

`loci poi-snapshot` wrote the month cleanly (219,364 locations, 325 newly minted) but its `coverage_check` guard then FAILED: 94 `cluster_id`s are each claimed by two ledger rows ("two histories fused onto one storefront"), exit 1. Investigated: **not brewery-specific** — only 2 of the 94 fused clusters are brewery; the rest spread across restaurant (50), nails_beauty (23), cafe_bakery (12), childcare (9), grocery (7), bar (6), convenience (6), fitness (6), laundry (4), hair_barber (4), pharmacy (3), hardware (2), clinic (1), bank (1). This is the pre-existing cluster-id-instability risk D79 already named ("cluster_id is an input-order artefact, 0% survive a row-order shuffle") surfacing generically after two full source re-ingests, not a defect this session introduced into brewery's own path. Left UNFIXED (out of this session's scope, no ledger rows deleted or hand-edited) and flagged here for its own ticket; `loci check-presence` will currently report this same FAIL until it is.

`address-gaps` (332,040 rows, no `EmptyCategoryError`): brewery reads a gap on 307,738 of 332,040 MN+BK addresses (109,880 censored) but — like `bathhouse_sauna` — never once leads (`lead_category` is never `brewery` or `bathhouse_sauna` across all 332,040 rows), confirming D134's `signal_categories()` exemption generalizes correctly to the 17th category with no further code change. `age-fit apply --category all` first hit `AgeFitStale` on `bar` (curve pinned to the pre-run hash) and exited 1; re-fit `bar` and `childcare` with `loci age-fit fit` (both pass their own dispersion gates against the new hash) then re-ran `apply` clean; `pharmacy` stays refused (no curve, unchanged D66/D69 state) — brewery itself carries no age-fit curve and is unaffected. `supply-ratio`, `address-access`, `transit-profile --re-sweep`, `citibike address-measures`, `address-character build`, `storefront-pipeline openings`, `dot-counts address-context`, `address-legality build` (commercial 49,036 / grandfathered 19,038 / ineligible 213,586 / unknown 50,380 — materially unchanged from D106) and `revenue --boroughs MN,BK` (ships `restaurant` only; brewery still `benchmark_only`, unfitted) all ran clean. Two expected, pre-existing warnings carried through unchanged by this session: `supply-ratio`'s baseline is still pinned to the OLD hash (`--fit-baseline` deliberately NOT run, per the canonical order's own "NO --fit-baseline" note) and `revenue`'s lambda is likewise pinned to an older incumbent set. `export-webmap` wrote 153 files (207.8 MB) including new `gaps/brewery.json` (38.85 MB) and `pois/brewery.json`.

**Supply hash: `1898163ac8ce` → `5ee548b8c4ac`.** `loci check-sources` and `loci check-tickets` both pass clean. Targeted tests (`test_category_registry`, `test_supply_ratio`, `test_bike_od_specificity`, `test_carrying_capacity`, `test_demand_caveat`, `test_recommend`, `test_address_legality`, `test_migrate_warehouse`): 240 passed, 1 expected failure — `test_baseline_supply_hash_matches_the_live_poi_supply` (asserts `model/supply_baseline.yaml`'s pinned hash equals the live one; it will fail after any supply-hash-moving re-run until `loci supply-ratio --fit-baseline` is deliberately run, which this session did not do, matching the canonical order's own instruction). GTM-213's open items (a) and are now landed by this addendum; (b)–(e) (reach/conveniences/spend/zbp placeholders, revenue fit, dedup precedence) remain open exactly as GTM-213 described them.

### 2026-09-17 — Session 37 (Codex): FOIL-only tenant-space research close-out (D126)
- Drafted the send-ready LL157 request, recorded the owner ruling and flip condition, added one pending Linear ticket, and parked the six memo questions plus the Int 0090-2026 caveat. No warehouse write, supply-hash move, tenant-facing build, or FOIL submission occurred.

---

### 2026-09-15 — Session 32 (abenmayor-chains, Opus build + Sonnet alias pass, Fable executive): auto-admission built, ruled and run (D113)
- Owner's ask (2026-09-15): auto-admit candidates monthly; rank and prune the queue; three rulings on supermarket co-op banners (admitted with `sales_role: incumbent`), generic names (17 compound names stoplisted, drift test enforces fixed point), and the stash incident (new rule in agent briefs). Offered candidate-tier-only / internal-until-confirmed; owner chose full auto-admission.
- Built in one pass: `src/loci/chains/exclusions.py` (eight exclusion classes with tests), `candidates.py` (the monthly diff engine), `generic_keys.txt` (48 + 13 seed entries, extensible), CLI `loci chains candidates`, `admit`, `reject`, `auto-admit`. Schema changes: watchlist.yaml gains optional tier / sales_role / admission_reason / rejection_reason / decided_on / decided_by / locations_at_decision fields; confidence value `auto` added; `reported`/`verified` rows without evidence now validation errors.
- Live 2026-09 rebuild twice for aliases: 741 auto-admitted, watchlist 161 → 902; candidates preview in prior session 31's scratchpad; tickets GTM-186–188 Done.
- Testing: 91 new in test_chains_exclusions.py and test_chains_candidates.py (full suite 2,194 passed / 1 skipped).
- Incident: build agent stash (recovered), new rule for agent briefs forbidding stash/checkout/reset.

### 2026-09-16 — Session 33 (abenmayor-chains, Opus build, Fable executive): chains schema shipped (GTM-189, D114); page served from the repo (GTM-191 page half)
- Chains schema landed (GTM-189, CHECKPOINT D114): watchlist.yaml gains five hand-entered fields (capital_events, signed_leases, store_count_source, expansion_contact_url, trajectory_state) with validate() enforcement and verified gate, capital-event escape hatch armed for rejected rows; sql/039 three pipeline/press snapshot signals (pipeline_filings_12m, pipeline_coverage, press_hits_12m) + brand_latest view re-bind.
- Live deployment: `loci init-db` and `loci chains detect --month 2026-09` re-run, 2026-09 snapshot re-issued with signals populated; 902 rows validate unchanged.
- Tests: +56 fixture-based (watchlist 20→47, candidates 57→70, detect 19→29, render_html 11→17); full suite 2,322 passed / 1 skipped.
- GTM-189 Done. GTM-191's page-half done (render --html written in prior session 32 commit 2d22407); Tavily raise GTM-192 unblocked but deferred pending owner's first pruning pass over 741 auto rows.

### 2026-09-16 — Session 34 (abenmayor-af, Fable executive): challenger forecast null (D118) then charter v2 landed (D116)
- Owner's ML question (should the recommendation engine be more intelligent?) → Sonnet briefing → challenger design (Opus data-scientist) → build + live run (Sonnet developer): HistGradientBoosting challenger fails the pre-registered floor (ΔAUC +0.0031 vs +0.010, 4 categories regress); `loci forecast challenge` harness shipped read-only, 15 tests, suite 2,328 passed / 1 skipped (D118).
- Owner stepped back from the modeling question to the higher-order charter question → charter brief → de facto inventory of the last 33 sessions → Opus charter draft → contrarian redraft verdict (14 findings) → four owner rulings by AskUserQuestion buttons → redraft → Sonnet verification pass → landed as docs/CONTEXT.md v2 (D116): owner as first customer, take-it first, decision value behind a dated survival gate (AUC ≥ 0.65), Gowanus bathhouse as AC-1; SCOPE CORRECTION banner retired.
- Peers sharing the tree this session: abenmayor-61 (D113/D114), abenmayor-db (D115), abenmayor-84 (D117).
- Rewind thread: owner reframed success as a business that moves in and survives, not just an opening, and asked for a 2020→2023 backtest with "never ever ever limit data pulls" — probe (Opus data-engineer) → pre-registration (Opus statistician) → wave one landed (commit 70cbd55: full-history filing backfill, LL157 recode crosswalk, DCWP licence intervals, `loci rewind` CLI) → wave two (ingest agent) in flight on the eleven remaining ingest caps (D119).
- DCWP correction found mid-thread: the roster publishes no status-change date, its licence vocabulary maps to one Loci category, and the licence→ledger identity join matches only 0.9% — recorded as defects on survival-gate source #4 rather than hidden (D119, CONTEXT.md §3.1).
- Audit of eleven remaining ingest caps for wave two: LL157 MN+BK-only write, DCWP Active-only, Foursquare closed+stale drop, SLA active-file-only, Citi Bike 2023-01 default / 2021-02 floor, MTA 3 months, CBP 2023 only, ZBP 2023 only, LODES 3 vintages, stale registry window text — wave two removes them.
- Owner: "can we audit our database?" — Opus data-engineer audit (read-only, 76 objects vs D61's 26; double-counting, unenforced scope, positional INSERTs, a lexicographic-vintage bug, unread objects) → owner rulings by AskUserQuestion buttons (keep every forecast vintage but drop features_json/forecast_id and reclaim, enforce scope in schema, consolidate now, street points get everything the lot frame has, storefront grain keyed, NULL-borough keep-and-flag, fix laundry source_rank and re-run tonight).
- Wave two landed (commit b0665c0): eleven ingest caps removed in code, DCWP roster ingest reverted pending the source_rank ruling, five silent double-count/silent-zero bugs fixed, supply hash unchanged end to end; 2,548 tests passed.
- Reshape (commit d2135a2): laundry source_rank branch fixed and re-run, supply as-of advanced to 2026-09-16 (9 licence-expiry closures, 3 evidence-aged unknowns), hash ba944e18c57b → a0b8ac9a8a52 → ed55301203a4, canonical order re-run on the new hash, TIGER tract ingest for street demographics, forecast vintage 0.1.1+67e28b04 issued lot+street, sql/045+sql/046 schema reshape (objects 81 → 83), 13 positional INSERTs → named columns, two agent-caused bugs caught and fixed; suite 2,669 passed / 1 skipped.
- Phase A/B: statistician v2 pre-registration re-sources the survival gate off the DCWP roster onto NYS SLA licence intervals for restaurant/bar/grocery (provisionally pharmacy), landed in CONTEXT.md §3.1; Phase A agent incident — `git checkout -- src/loci/sql/028_forecast.sql` run on a file only it had modified, a rule violation, recorded rather than hidden.
- Reclaim: DB file 8.97 GiB → 5.17 GiB via COPY FROM DATABASE with FK-ordered inserts (DuckDB 1.5.5 does not topologically sort FK children); pre-reclaim copy kept at data/loci.duckdb.pre-reclaim-2026-09-16 for the owner.
- D121 landed (GTM-205 Done); owner's list has two items still pending: (2) all additional calculations on existing data — inventory every computed measure vs what the warehouse now holds, unread; (3) camera and satellite data — next.

**D138 — Fix the D79 cluster_id-instability bug that fused 94 ledger rows after the D137 brewery re-run; repair the live ledger.** *(2026-09-22, owner ruling: "fix it this session" — `check-presence` was FAILING; a D139 OSM re-ingest runs right after)*

What: `loci poi-snapshot`'s `coverage_check` failed after the D137 brewery re-run (commit 16cbb48) — 94 `cluster_id`s each claimed by two `analysis.poi_presence` rows (restaurant 50, nails_beauty 23, cafe_bakery 12, brewery 2, others), flagged but left unfixed in the D137 addendum for its own ticket.

Root cause: D79 already named the mechanism ("`cluster_id` is an input-order artefact, 0% survive a row-order shuffle") and `snapshot()`'s upsert already had a guard for it — an UPDATE nulling `cluster_id_latest`/`poi_id_latest` on any ledger row not carried forward, so a stale integer can never be misread as still pointing at its old storefront. The guard's staleness test was `WHERE last_seen_month < ?` (bound to the snapshot's own `month`), a MONTH-GRANULARITY comparison. That is a no-op when `poi-snapshot` is re-run for the SAME calendar month after a full source re-ingest reshuffles `cluster_id` numbering — exactly what the D137 brewery re-run did (two full re-ingests, one dedup re-run, one `poi-snapshot` re-run, all inside September). A ledger row not re-matched by this run's hash/name+distance link still had `last_seen_month = '2026-09'` from its EARLIER run that same month, so `last_seen_month < month` was false and the row's stale `cluster_id_latest` was never cleared. Its old integer then collided with whatever different storefront today's fresh dedup numbering happened to reuse it for, and `coverage_check`'s "one ledger row per live cluster" invariant caught two ledger rows — two distinct histories — claiming one `cluster_id`. Confirmed empirically: all 94 stale rows carried `last_snapshot_at` from an earlier run (2026-09-14 21:51, the D134/GTM-202 collapse-fix re-run) while every other one of the 219,364 current rows carried today's run's timestamp (2026-09-22 18:02) — i.e. `last_snapshot_at` already distinguishes "touched by this run" correctly where `last_seen_month` cannot.

Fix (code, `src/loci/model/poi_presence.py`, `snapshot()`): the stale-nulling guard now reads `WHERE last_snapshot_at < ?` bound to this run's own `now` (already computed for the upsert) instead of `WHERE last_seen_month < ?` bound to `month`. `last_snapshot_at` moves to `now` on every row the upsert actually touches, so comparing against it finds "not written just now" regardless of whether two runs land in the same calendar month. Module docstring (point 2) and the inline comment at the guard updated to explain the failure mode. A regression test, `tests/test_poi_presence.py::test_a_same_month_reingest_does_not_fuse_cluster_ids`, reproduces it directly: snapshot a month, re-ingest with one location's source ids shuffled away and its old integer `cluster_id` reused by an unrelated new POI, re-run `snapshot()` for the SAME month, and assert `coverage_check` is clean and the departed location's `cluster_id_latest` is NULL rather than fused onto the newcomer's. 20/20 tests in that file pass, including the new one.

Repair of the live warehouse (not code — a targeted data fix matching what the corrected guard would have produced): backed up `data/loci.duckdb` (7.26 GB) to the scratchpad before any write. Verified the 94×2=188-row fusion was exactly and only the 94 rows whose `last_snapshot_at` (2026-09-14 21:51:03.848768) predated the live warehouse's max `last_snapshot_at` (2026-09-22 18:02:44.236300, this run's own timestamp) — `UPDATE analysis.poi_presence SET cluster_id_latest = NULL, poi_id_latest = NULL WHERE last_snapshot_at < (SELECT max(last_snapshot_at) FROM analysis.poi_presence)` affected exactly 94 rows. Split, not deleted: ledger row count is unchanged at 219,458 before and after; every column except the two "latest" pointers is untouched on every row; `first_seen_month`/`first_seen_kind`/`first_seen_src_date` — the actual history — survive on both the newly-orphaned row and its unrelated newcomer. `dupes: 94 → 0`. `loci check-presence`'s `coverage_check` portion: FAIL → `ok 219,458 ledger rows, 100.00% of 219,364 deduped locations covered`.

Side effect, recorded per instruction: nulling `poi_id_latest` (necessary — six other modules join `poi_presence.poi_id_latest` directly without the colocation view's own defensive `GROUP BY`, so a stale row sharing a live POI's `poi_id_latest` is a real fan-out risk, not just a cosmetic one) also un-derives 2 of the 94 rows' closure evidence from `analysis.poi_supply_status`, because `poi_closure.apply_to_ledger`'s `resolve()` had attached those 2 storefronts' Foursquare closures to their PRE-recategorization (bathhouse_sauna admission, D124) ledger identity rather than their current one. Effect: `poi_status` for those 2 live POIs (one is "K-Town Sauna") moves `closed → unknown` — the safe/conservative direction per D79's own default, not `open`. **Supply hash: `5ee548b8c4ac` → `c36a96a86cab`.** `poi_status` distribution: closed 14,922 → 14,920, unknown 161,598 → 161,600, open 42,844 → 42,844 (unchanged). Of the other 92 rows, 49 belong to POIs still in `analysis.poi_supply` today (mostly `bathhouse_sauna`/`brewery`, i.e. this session's own D124/D137 category admissions recategorizing a POI that used to sit under a different slug — category is part of `location_key`, so a category change breaks both the hash-match and the name+distance link by design, same mechanism D101/D103's `poi_key_map` migration exists for); none of those 49 carry a closure, so this is the full and only supply-affecting consequence.

Flagged, NOT fixed here (separate, pre-existing, unrelated scope): `loci check-presence` still exits 1 after this fix, on a DIFFERENT, older surface — "12 ledger rows still carry a location_key that an APPLIED poi_key_map moved away from — the migration half-landed" (the D101/D103 key-migration mechanism, `last applied 2026-09-14 21:50:37`). Confirmed present in the pre-D138 backup too, so it predates this session and is not the D79 cluster-fusion risk the owner ruled to fix. Needs its own ticket.

Targeted tests run (not the full suite, per instruction): `tests/test_poi_presence.py` (20 passed), `tests/test_dedup.py` + `tests/test_dedup_cross_category.py` + `tests/test_poi_colocation.py` + `tests/test_report_ledger.py` + `tests/test_supply_snapshot.py` (98 passed).

### 2026-09-16 — Session 35 (abenmayor-map, Opus executive): webmap neighborhood scope fixed (5e743c7), DOE snapshot registered (D122, GTM-204), Bed-Stuy read-only check

- Owner report: picking a neighborhood in single-business mode still drew every other neighborhood's gap dots (only All-opportunities was NTA-scoped, via the per-NTA files). Developer agent: gap files now carry a dictionary-encoded `nta` parallel array (nta_code per point, null if unresolved — none were), gapFC filters on it, sidebar shows "<name> only · N addresses · clear", banner counts what is drawn, old files degrade to unscoped; known locations stay unscoped as context. Re-exported on ba944e18c57b (unchanged), webmap suite 154 passed, browser-verified on Bushwick (West) laundry (261 addresses). Committed 5e743c7 by explicit path and pushed; Railway redeploys only if the service watches this branch.
- Owner shared the Gothamist/Furman Bed-Stuy piece and chose all three: Bed-Stuy read (data-scientist, read-only), DOE enrollment source ticket (developer: live-portal probe first, registry + tickets.py + Linear GTM-204 + QUESTIONS D57), and the QUESTIONS entry. Findings recorded as D122.
- Peer abenmayor-af announced a hash move off ba944e18c57b to H1 tonight with one canonical re-run and a file reclaim; this session held all warehouse writes, used the tree only after the peer cleared registry/tickets/QUESTIONS, and claimed D122 by chat (D121 is the peer's). CHECKPOINT hash to be messaged to the peer, who appends D121 after.
- Continued 2026-09-17 (owner "continue"; chose GTM-198 over the DOE ingest): Opus developer ran P1–P3 + G0 for `bathhouse_sauna` live against Open Data and the cached feeds, coordinated the inclusion rule and vocabulary with abenmayor-db's GTM-199 base-rate enumeration (D123 cites D124; one definition, two decisions), owner ruled signal / narrow / three pins; landed as D124. Freeze lifted mid-session by abenmayor-af (hash ed55301203a4, forecast 0.1.1+67e28b04); no warehouse writes from this session all day.

### 2026-09-15 — Session 31 (abenmayor-chains, Sonnet ×5 research/preview, Fable executive): candidates preview and 38-brand batch add (D109 first run, by hand)
- Owner asked "what other places are missing?" — the D109 predicate was run ad hoc, read-only, on the 2026-09-15 rebuilt snapshot as a preview of `loci chains candidates` (GTM-186): **892 unlisted brands** clear it after exclusions (bank/clinic 68, health systems 16, fuel 4); 11 of the top 40 are 100+ location incumbents. Preview file in the session scratchpad (`candidates_preview_2026-09.md`).
- Owner ruled "add everything above except incumbents": four parallel Sonnet research passes on 43 brands → **38 rows added** (all `reported`, ≥2 evidence links, `first_added` 2026-09-15), list now **161**. Dropped with reasons: SuperFresh (Key Food co-op banner, no single site-selector), Burger Spot (generic-name collision), Pop's Pizza (two conflicting owners), "? Tea" (forced June 2026 rebrand of four Molly Tea stores after a trademark injunction — zero unit growth, and `brand_key` collapses to the bare word "tea"), Sweet & Favorite Cake Shop (one storefront emitting four permit records).
- Six more brand-key splits aliased in `normalize.py` with tests (Blank Street, Chip City, Dos Toros keep the locator's full name; Guacado and Teriyaki One the short one; "Moka & Co" pinned because the &→and rule plus the legal-suffix strip reduced it to "moka and"); detect re-run twice for the aliases; CHAINS.md re-rendered at 161 brands / 20,984 detected.
- Re-dating artefacts the research surfaced, carried as caveats in the rows: Earthbar (Equinox-lobby concession, 47 US units vs detect's 28 in NYC) and Kikka Sushi (in-grocer counters, 16 of 17 "new") are concession models, not storefront tenants; PureGym's "growth" is the Blink Fitness rebrand; Gristedes is shrinking (owner threatening closure) despite "11 new"; Xi'an Famous Foods self-reports 19 NYC vs detect 32; Equis Pica Pollo self-reports 7 total vs detect 14. Teppanyaki One and Teriyaki One share an operator (Harmony Dining Group).
- Commits b68c7aa (D109), 4beeb60 (D110), 879f8ed (batch add). Warehouse access coordinated by message with abenmayor-db's Citi Bike back-ingest (pause at month boundaries); D46–D48 Answered-by lines were swept into abenmayor-db's f56326c.

### 2026-09-17 — Session 36 (abenmayor-db): Gowanus bathhouse base rate enumerated, AC-1 Baseline field ruled (D123, GTM-199 Done)
- Session-start ritual (Sonnet brief; peers abenmayor-af mid-reclaim, abenmayor-1d on GTM-198). Owner chose GTM-199. Two parallel Opus enumerations (warehouse read-only, web) on the definition agreed by chat with GTM-198; two owner rulings by buttons (citywide baseline + pipeline; bathing-primary day spas count); Opus investor wrote the memo. D123. Warehouse: read-only only; abenmayor-af's detached ingests hold the lock intermittently.

### 2026-09-21 — Session 41 (abenmayor-cam, Opus developer; build 2026-09-17, recovery 2026-09-21): camera and aerial instruments landed (D131)
- Item 3 was built on 2026-09-17 by an agent that stalled before reporting: sweep/flush/report/schedule, footprints ingest, aerial pull/change/awnings/convertible, sql/053–054, tests, registry entries, the DOT ask and the three review pages were all on disk, unstaged, with the whole of cli.py reformatted in the tree by a peer. Recovered 2026-09-21: cli.py reconciled from HEAD + item-3 hunks only; every number in D131 re-verified read-only against the warehouse and the logs; one sweep defect (overrun past the window close) fixed with a test.
- Verification: suite 2,792 passed / 3 peer failures; check-sources green; check-warehouse red on the peer's `staging.public_*` only; supply hash ed55301203a4; camera validation N = 45, ρ +0.413; launchd not installed; overnight ingests read (LODES ok, ZBP ok, CBP report crash on a stale 2023 cache, Citi Bike legacy abort on a `NULL` dock id).
- Not done: the owner's hand-check of the three review pages (the gates in memo §5 rows 3–5); `schedule --install`; the CBP and Citi Bike fixes; the Mapillary coverage probe (memo §5 item 2).

### 2026-09-17 — Session 42 (abenmayor-5b, Opus executive; closed 2026-09-21): Gowanus commuter-data briefing, Deux Luxe + Fazenda operator pages (D130), Fazenda rent MISS scored

- Gowanus commuter-data read (Sonnet, read-only): Loci holds MTA entries at the address grain (`analysis.address_transit_profile`, `transit_entries_400m`; 2020–24 and 2025+ feeds never summed), LODES **WAC only** (`jobs_400m`, 2023) — no OD/RAC, no ACS journey-to-work — and Citi Bike 2021-02→2026-08 (`staging.citibike_station_month`, `analysis.bike_od_leakage`). Legacy 2013–2020 Citi Bike tables exist in code but hold 0 rows in this snapshot; `registry.yaml` still lists LODES as `planned`. 545 Sackett St (AC-1 site): 0 subway entries within 400 m, 1,485 homes / 2,046 jobs at 400 m, ~136 bike starts; Gowanus core only 62% of addresses have any subway access at 400 m. Transit/jobs remain card-context only (D76).
- Deux Luxe page (Opus ×2): record + HTML; live warehouse on supply ed55301203a4, as-of 2026-09-16, read-only.
- Fazenda page (Opus ×2): owner corrected mid-run "part coffee shop part men's clothing store"; re-framed as cafe_bakery with the Stone Street page as sibling; predictive tense. Resolved by ledger (DOHMH CAMIS 50191163) + web for the address text.
- Owner reported rent $15,000/mo on <1,000 sq ft → MISS ×2 logged (Sonnet), correction file written; frozen pages untouched.
- Peers: abenmayor-af mid-edit on CHECKPOINT/tickets (takes D131); abenmayor-1d closed; edit held until af's commit landed. Nothing committed by this session.

### 2026-09-21 — Session 43 (abenmayor-57, Opus executive): glossary audit, charter reconciled with code (D132)
- Opus read-only audit of CONTEXT.md vs src/loci/ (75 terms); owner ruled on act/diligence, co-location, radii, claim layers; non-filtering signal deferred.
- Sonnet applied the rulings and factual fixes to docs/CONTEXT.md (98-line diff); executive fixed the §4.6/§11 source-class breakdown from registry.yaml (universal 7, national 11, state 7, city open data 19, city-unique 6 = 50) and the §8 hex row.
- Peers: abenmayor-af (D131) and abenmayor-5b (D130) landed first by agreement; D132 claimed by chat. Nothing committed by this session yet.

### 2026-09-22 — Session 45 (Opus executive): source-candidate sweep and competitor capability-gap memo (no D-id)

- **Asked:** the owner asked whether StreetLight Data, Vexcel, Facteus, Advan, Dataplor and BatchData are on the source list, and what other sources Loci needs, benchmarked against competitors' claims, public sources first, then paid.
- **Built:** paid wishlist +4 (`paid_vexcel_intelligence` P3, whose EULA names site selection as prohibited commercial use; `paid_facteus_transaction_data` P2, ranked behind SafeGraph Spend because its licence is unverified and its panel-size claims are inconsistent; `paid_dataplor_poi` P3, whose sample agreement bars redistribution and ML training; `paid_batchdata_property` P3, which duplicates free PLUTO/ACRIS/DOF). Wishlist is now 39 entries, $343,565/yr booked. StreetLight (P2) and Advan (P1) were already listed. Free planned sources +2: `fema_nfhl` (flood-zone polygons, public domain, point-in-polygon join to lots) and `rebny_retail_reports` (corridor asking rent for MN+BK, internal use only; corridors need hand-drawn geometry). Memo `docs/competitor-capability-gaps-2026-09-22.md` covers 15 web-sourced competitors plus 13 from the owner's Notion Competitors database: Loci has 3 claimed capabilities, partly has 6, is missing 6. CenterCheck is a new paid lead ($4k/yr and up, but weak on independents). MapZot.AI appears to resell GrowthFactor.
- **Commits:** 65a383a, c780f7d, 291bea9, ca2971c. No warehouse writes; the supply hash was not touched by this session.
- **Process:** one commit (292d907) swept in 145 files a peer session had staged, and was backed out with a soft reset. Every commit since uses `git commit --only <paths>`.

## Next actions

D130-1) **Score Fazenda's remaining answers as they arrive (sales so far, staff, orders/ticket, clothing share, counter's floor share) into §7 of the record; full first-year comparison 2027-09-17.** Deux Luxe: all four questions open.
D130-2) **Operator-page template change: on `retailarea` 0 lots with no storefront-registry sq ft filed, the rent section says "I could not see your floor area" and the sq-ft question moves first; re-check Deux Luxe's 1,171 sq ft assumption when its answer arrives.**
D130-3) **Gowanus commuter data: load LODES OD (flows, not just WAC), run the code-landed pre-2021 Citi Bike ingest (tables exist, 0 rows), and fix `registry.yaml` LODES `status: planned` → measured extents (D121-3 follow-through).**

D131-1) **Promote or not the 4,668 triangulated closures** (`analysis.closure_triangulation`, D125): a hash-moving write — announce to every peer first.
D131-2) **Pick the binding P5 statistic** (joinable PPV restaurant 0.29 / bar 0.24 / grocery 0.04 vs determined PPV 0.52 / 0.53 / 0.07, D125) before any survival fit.
D131-3) **Delete `data/loci.duckdb.pre-reclaim-2026-09-16`** (9.6 GB) after the owner confirms (D121-4).
D131-4) **Owner hand-checks the three review pages** — `data/aerial/gowanus_change_review.html` (100 lots: agreement ≥ 0.8 with DOB status, memo §5 row 3), `awning_review.html` (100 faces: precision ≥ 0.8 vs LL157 occupied), `convertible_review.html` + `convertible_gowanus_review.html` (top 30 each); then flip the catalog comments off "ungated" or drop the layer.
D131-5) **Camera AM sample continues** — the AM window at the 15 validation pairs holds one weekday (09-18); a second weekday and `loci sidewalk-count schedule --install` (owner step) start the weekly series.
D131-6) **CBP NaN fix:** `loci ingest-cbp-national --years 2023 --refresh`, and make the cached-parquet read treat a file without `year` as stale.
D131-7) **Citi Bike legacy re-run:** NULLIF the literal `NULL` / `\N` dock ids in `legacy_projection_sql`, then `loci citibike ingest --start 2013-06 --end 2021-01` (it holds the warehouse lock for hours: announce).
D131-8) **Push the provisional GTM-206 to Linear** and stamp the real id into the tickets.py description; remove the PUSHED_WITHOUT_ID exemption.

D132-1) **Honour `ships_as: signal` in the screen aggregates** — a signal slug must not enter gap_score / lead_category / n_missing / the missing set; add the byte-identical test from CATEGORY-EXPANSION.md. Blocks D124-1 ingest.
D132-2) **Collapse or resolve unresolved co-located pairs before counting** (D24 ticket, now ruled) — hash-moving; announce to every peer, freeze protocol per D115.
D132-3) **Allocator memo: drop the 500 m straight-line POI list; quote the 400 m network catchment only.**
D132-4) **Stale code docstrings:** cli.py address-gaps help still describes the retired 800 m / 12-of-15 gate; sql/028 says frame='lot' only; categories.py and score/access.py cite v1 section numbers; TIER_WEIGHTS retired by A4 but still rendered (QUESTIONS D63).
D132-5) **Commit the glossary edit** (docs/CONTEXT.md, CHECKPOINT.md D132, tickets.py, QUESTIONS.md, TICKETS.md) — stage by path, peers' hunks stay out.

D133-1) **GTM-212: confirm with Codex peer and decide on code commit for D133.**

D126-1) **Owner sends the LL157 FOIL** at `docs/asks/foil-ll157-rent-fields-2026-09-17.md`; record the request ID, acknowledgement, response date, and any appeal deadline without changing warehouse or product scope.
D126-2) **Push the FOIL ticket to Linear** when a Linear connection is available; it is intentionally pending sync, not represented as live.

D121-1) **Owner list item (2): inventory every computed measure against what the warehouse now holds** — unread.
D121-2) **Owner list item (3): camera and satellite data** — next.
D121-3) **Long ingests launched (LODES, CBP, ZBP, MTA ridership, Citi Bike) — verify row counts by year on completion.**
D121-4) **Delete `data/loci.duckdb.pre-reclaim-2026-09-16` after the owner confirms.**
D121-5) **P3′/P5/P6 survival prerequisites due 2027-01-31** (CONTEXT.md §3.1 v2 pre-registration).
D121-6) **Carry DCWP inspections `first_inspection_date` for laundry (P6).**

wave two: eleven ingest caps + per-category survival-source inventory (in flight); then re-source rung (iii) with the statistician; then run the entry rewind at t0 2021-01-01 and 2020-01-01.

D116-1) **Admit `bathhouse_sauna` through the GTM-112 fail-closed category-expansion checklist (AC-1 step 1; anchor source, coverage-validation plan, reach tier, Google type map).**
D116-2) **AC-1 trade-area sheet for Gowanus within two weeks** — Appendix A8 method: hand enumeration of bathhouse/sauna/banya supply, a premium demand pool, drive/transit-time catchment swept 10/15/20/30 min, and a PLUTO feasibility gate; feeds the AC-1 pre-registration. The Baseline field and the competitor set are ready in docs/bathhouse-base-rate-2026-09.md (D123); the sheet still needs the premium demand pool, drive/transit catchment sweep and the PLUTO feasibility gate.
D116-3) **Reconcile the bank 0.67x vs 1.97x divergence** between the two Gowanus cards on the same supply hash 767b28674e30 (QUESTIONS D55); no bank ratio prints until this resolves.
D116-4) **Score El Punto's remaining answers (sales, staff, order counts) when they arrive (AC-6, D91/D117).**
D120-1) **The five Todo tickets (GTM-198–202) are the queue** — bathhouse_sauna checklist (AC-1 Step 1, Urgent) first, then ~~Gowanus bathhouse base rate (D56)~~ done (D123, GTM-199), filing-blind amenity anchor (T10), Gowanus bank ratio reconcile (D55), co-located pair policy (D24). Queue is now: GTM-198 checklist (abenmayor-1d, D124, landing) → GTM-200 filing-blind anchor (largely answered by D123's source anatomy + GTM-198's verdict; confirm and close) → GTM-201 bank reconcile → GTM-202 pair policy.

D122-1) **GTM-204 DOE Demographic Snapshot ingest** — full history of all four Open Data files (no window), zone join via cmjf-yawu, `k_enrollment_zone` + 5-year change as CONTEXT columns, Bed-Stuy / Borough Park / East Village sanity read; enters no grade and no score. Check DOE InfoHub for the 2022-23..2024-25 Excel releases Open Data lacks and record them as a second feed if the schema matches.
D122-2) **Add B11003/B11005 (households with children / couples without children) to grid/acs.py GETVARS** on the next ACS refresh so the composition read can answer the "childless couples" question directly rather than by proxy.

D135-1) **Write adapters for the two free planned sources from session 45: `fema_nfhl` (flood zone per address via point-in-polygon on PLUTO lot points) and `rebny_retail_reports` (digitise the MN+BK corridor geometry, then assign corridor asking rent by proximity; internal use only). Each is its own deliverable.** Needs a tickets.py definition and a QUESTIONS entry when scheduled.

D124-1) **GTM-198 ingest step (announced hash move):** announce to every peer, then `loci migrate-warehouse --step observation_category_check --apply`, the Overture/Foursquare/OSM ingests (emit the slug; ~43 rows leave nails_beauty), dedup with a `bathhouse_sauna` branch in score/dedup.py:source_rank ranking nyc_dohmh_pool_permits first, record the new hash, then G1–G12 in checklist order; empty `UNFITTED_PENDING_INGEST` as each artifact lands. Expect G3 unmeasurable, G8 ≈ 0.00×, G11 D.
D124-2) **AC-1 Step 2 — the A8 hand trade-area sheet for Gowanus** on the D123 base rate (1.7/yr citywide 2019–2026, CI 0.9–2.9; 3.4/yr since 2024) and the named pipeline entrants (Reunion Bathhouse 272 Seigel St, SaunaLounge 45 S 3rd St); the DOHMH pool-permit roster + café permits are the enumeration source.
D124-3) **Wide-slug ruling stays open:** whether day spa / health spa leave nails_beauty is not decided; the hand list (Juvenex, Great Jones, Fountain of Youth) is the only boundary applied.

D123-1) **Re-pull Overture/FSQ after 2026-08-21 to pick up Connexion Flushing; map FSQ `Bath House`/`Sauna` + Overture `sauna`/`onsen` into the slug (GTM-198).**

00) ~~Re-baseline on ba944e18c57b (baseline → canonical order incl. legality → age-fit → revenue → forecast re-issue → export-webmap) — owed by the next session needing stamped artefacts; drift test red until then (D107).~~ done — canonical order re-run in ~23 min, drift test green (D112).
00-bis) **GTM-185 ground-truth follow-ups (a)-(d): storefront distance in the observation grain, name-alias reconciliation of the 45 miss candidates, deli/bodega convenience search terms, anchor eligibility (building-class) check (D107).**

0) ~~Re-baseline the supply hash (d993b3802c6a after the ledger/036 re-render); abenmayor-cf running it 2026-09-15; every stamped artefact must be re-stamped or its discrepancy recorded.~~ done — 17900c2 (D106).

0-bis) ~~Install Interceptor and run the first ground-truth session on the four address anchors + Gowanus centroid (GTM-183)~~ done — first session run 2026-09-15, 19 anchors, run gt-2026-09-15-session1 (D107, GTM-183 Done); follow-ups at GTM-185 (a)-(d) above.

0-ter) **abenmayor-cc: settle which ratio supply_ratio_at_issue records and log the outcome under D105.**
0-quater) ~~address-legality build is the canonical order's long pole (GTM-169, D106): 3,449 s in the 2026-09-15 re-baseline against 345 s for the next longest step; the open-POI join in model/address_legality.open_poi_match_sql needs an index on poi_supply_status(lat, lon) or a vectorised spatial join before the next re-run (abenmayor-29).~~ done — grid-bucketed equi-join, 3,449 s → 3 s, byte-identical (commit c36cd18, D112, GTM-169).

0-quinquies) **Promote the re-baseline runner to `loci rebaseline` (D112):** per-step timing, a `--from STEP` resume flag, and the age-fit pharmacy refusal (exit 1) treated as designed rather than fatal — this session ran the D106 order as a scratch zsh script (scratchpad rebaseline.sh); ticket to open.

1) ~~GTM-172: act on the 2026-09-15 investor re-review (docs/reviews/investor-review-2026-09-15.md); exercise the full-memo path on a grade-C-or-better anchor, since all three current anchors grade D and produced no-trade notes.~~ done — R1–R3 + ask 4 built, forecast_latest fixed, three notes regenerated (two NO CALL, one REFUSED), first full memo rendered at grade C (D115, GTM-172 Done).

1a) The first full memo exists — send docs/recommendations/3004260001-2026-09-16.md to the investor agent for a third-round review before it reaches an operator; fix the special-district name rendering; decide review ask 5.

1b) The supply as-of is pinned at 2026-09-15 (D115). Advancing it (`loci supply-asof advance --to …`) is a hash-moving event: announce it like a poi_status write, then run the canonical order behind it. Extend the freeze marker (data/SUPPLY_FREEZE) to direct evidence writes (ground-truth record, closure checks) — ticket.

2) After the re-baseline: confirm legality columns survived the canonical order (`loci address-legality stats` must not exit 1) and that webmap/data reflects spdist1 if wanted on the map.

3) Widen `analysis.spend_ledger`'s provider CHECK to include claude_cli (next free migration) so plan-billed prose rows stop being recorded under 'anthropic'.

4) Add legality coverage to `make check` so an unapplied layer fails loudly.

5) Decide the successor-business question (QUESTIONS entry below).

6) Owner adds ANTHROPIC_API_KEY when ready.

0j. **Forecast retention before 2026-10-01 (D92, ticket a, GTM-163):** DB grew 1.8 → 4.6 GB in one issue+score cycle (~1.4 GB/cycle) with no retention rule; adopt keep-latest-3-vintages'-predictions + all outcomes/runs (or an equivalent) before the next monthly `make chains-refresh` issues a third vintage.
0k. **Point the retrodiction cohort, the recommendations-ledger fill check and the forecast scorer at the peer's `poi_is_open` predicate once it lands (D92):** abenmayor-cf's colocation work (sql/029_poi_colocation.sql) is landing a real open/closed predicate; all three currently infer "open" from presence/absence in the first-seen ledger.
0l. **Send planner Packets A and B; record verdicts (D92/D93, ticket c, GTM-165):** docs/planner-packets-2026-09.md is send-ready (Packet A legality vs herding, Packet B the ten held-out corridors); verdicts enter QUESTIONS.md and, if a rule changes, the decision log.
0m. **Closed-catchment carrying-capacity fit (D93, ticket b, GTM-164):** fit the gated forms on NTA/CD partitions instead of the overlapping 400 m shed; publish the CBP-vs-POI ratio as the portability parameter a second city must reproduce; test the grocery flattening point against the D91 capacity bound.
0n. ~~Wire the modeled/realized/scored layers onto the map and CLI~~ done (e578470, D92) — `loci forecast issue|score|report|distribution`, Off/Modeled/Realized/Surprise segmented control on the webmap, `make chains-refresh` issues and scores forecasts monthly.
0o. **Second weekday camera sampling via launchd, not a session scheduler (GTM-170, D90):** install a launchd plist so `loci sidewalk-count` runs the AM/MD/PM windows on scheduled weekdays independent of any interactive session; the 2026-09-14 12:03 and 16:02 batches both failed on their first try (machine sleep; DNS failure to webcams.nyctmc.org) and needed a manual relaunch, and the morning window was missed outright because the session itself started at 09:06.
0p. **Coverage-validation follow-ups: implement the four ruled changes and settle the one still-open question (D90):** owner ruled "yes to all 4" — demote tailor_repair/hair_barber from headline (D30 precedent), adopt the G9 Wilson-upper ladder (GTM-174), drop sports_club/marina from fitness's GOOGLE_TYPES (GTM-173), and run cross-category dedup before per-category dedup (GTM-175, GTM-153 proper); land all four and re-fit/re-run the screen. Still open and NOT part of this ruling: whether unresolved co-located POI pairs (2,527 groups / 7,597 POIs) collapse or count as-is (QUESTIONS D24, default off).
0q. **D94 docs after the co-location closure gate's screen re-run:** the closure gate (`poi_is_open`, `analysis.poi_colocation`, commit a67f03e, supply 767b28674e30 → 9a11a2f5b9e7) landed this session; write up D94 once the downstream re-run this implies is done.
0r. **Revenue re-fit and forecast re-issue on supply 9a11a2f5b9e7 (peers' steps):** the site-revenue model (D91) and the forecast ledger (D92) were both fit on the pre-closure-gate supply hash; re-fit and re-issue on the new gated universe.
0s. **Second weekday camera sampling via launchd (GTM-177):** install a launchd plist so `loci sidewalk-count` samples the AM/MD/PM windows on scheduled weekdays independent of any interactive session; unblocks GTM-157's remaining second-weekday AM/MD/PM sample.
0t. **84's revenue fit/apply and 88's forecast re-issue / export-webmap on supply a7c531ebca17 (D101, in flight tonight):** both the site-revenue model (D91) and the forecast ledger (D92) were fit on pre-D101 supply hashes; re-fit/re-issue and re-export the webmap on the dedup'd, closure-gated universe.
0u. **Fill the 379 Broome St report's model revenue after 84's revenue apply:** docs/recommendations/379-broome-st-2026-09-14.md is pre-registered with category and demand predictions but no model revenue_p50, pending the D91 apply on a7c531ebca17.
0v. **Owner decisions outstanding from session 25 (D94/D101):** (a) `init_schema` must apply only HEAD-tracked migrations, not every `*.sql` on disk (QUESTIONS D25, abenmayor-88 to propose); (b) whether unresolved co-located POI pairs collapse or count as-is (QUESTIONS D24, still open); (c) whether cafe_bakery's newly-anchored principled set under category-precedence B is right, or café should keep aggregator members (QUESTIONS D26); (d) hair_barber's beauty_salon Google type inclusion (QUESTIONS D23, still open).
0w. **GTM-176 stale state:** add the fourth `poi_is_open` state for an inspection-gap beside a fresher co-located record (Okozushi at 376 Graham, 384 d since last inspection, no published closure) — a candidate predicate refinement, not a closure rule (D79 still forbids absence-as-closure).
0x. **has_open_commercial_poi legality join fix (abenmayor-29):** the 379 Broome St report found `has_open_commercial_poi` reads False beside an open café 12.6 m away — reported to abenmayor-29, whose D97 legality label needs the fix before the ineligible-address exclusion can be trusted.
0y. **Ticket GTM-181 — re-issue the 2026-09 forecast vintage on the final supply hash, then export (D96).** *(done 2026-09-15 (4b6841f))* Re-run #3 finished at ~23:10 on supply_hash `467cd5969200`; a peer began the re-issue at session close but it had not landed in `analysis.forecast_run` as of this write — confirm the new `model_version`, run `forecast score`, `forecast-export`, `citibike export`, `export-webmap` (the last after the revenue re-fit, item 0r/0t, also lands), and mark GTM-181 Done.
0z. ~~Citi Bike phase 1~~ **done** (D102, GTM-166, commits f7f3d29/ac914da); ~~Citi Bike phase 2~~ **done** (D108, GTM-167, origin-destination leakage per NTA × category, CONTEXT ONLY); ~~Citi Bike phase 3~~ **done** (D111, GTM-168, activity-growth retrodiction feature CONTEXT ONLY, reader proven portable to Divvy) — the Citi Bike plan is complete.
0z1. **Card wording for bike_od_supplied_share (D120):** the four passing categories (bar, fitness, childcare, pharmacy) may be read as category-specific; the other eleven print "unresolved at this power" — power grows only with more destinations, i.e. a wider measurable universe.
0z2. **Owner calls opened this session:** (a) ~~whether the 2026-02-23 system-wide-zero storm day stays in Citi Bike's per-weekday divisor~~ **answered** (D108 addendum) (D102; QUESTIONS entry below); (b) whether bike dayparts extend `analysis.address_transit_profile` or get a sibling table — two builders writing one DELETE scope needs a decision before either lands (D102; watch-table-proliferation caution); (c) GTM-162 recommendations-ledger settings (radius, expiry, rubric weights — carried from 0h, still unanswered); (d) `loci forecast prune`'s first REAL (non-dry-run) execution — retention code shipped (D96) but `LOCI_FORECAST_PRUNE_REAL=1` has not been set on any run yet, and the DB is now 5.0 GB.
0z3. **After any `loci address-gaps` re-run, also run `loci citibike od-measures --re-sweep` (D108):** `bike_od_run_at IS NULL` is the flag; `needs_rebuild` also fires on a supply-hash move. Growth-measures follows the same re-sweep rule after an address-gaps re-run (`loci citibike growth-measures --asof … --re-sweep`).

0z4. **Chains watchlist next phases:** GTM-186/187/188 Done (D113, candidates/admit/reject/auto-admit, exclusions, generic_keys.txt). GTM-189 Done (D114, hand-evidence fields + verified gate on YAML, sql/039 three snapshot signals + brand_latest re-bind, capital-event escape hatch armed). GTM-191 page-half Done (commit 2d22407, render --html into data/chains/, press domains). Remaining: GTM-190 (trajectory_state classifier, needs 2026-10 snapshot), GTM-191 Tavily raise + GTM-192 (tier 3 `watch`, now unblocked by GTM-189), owner's first pruning pass over 741 auto rows (next human intake point after auto-admission).

6) **GTM-192 (D110): build tier 3 `watch`.** Land it *after* GTM-186 (`loci chains candidates`) and GTM-189 (schema), which it depends on. First run is the calibration run — the 168/22 pool figures were measured on the 2026-09-15 filing snapshot and must be re-derived. `research.MAX_QUERIES` goes 60 → 68 in the same change as the new `query_kind = 'watch'`, never before it, so the raised ceiling never sits unused. Carry the open question with it: whether `watch` accepts categories the filings cannot see at all (the Mink Padel case) — decide it on the first real run, with signal (f)'s yield in hand rather than in the abstract.

0. **Score the Lion's Milk and El Punto pre-registrations (D91):** when the owner answers (revenue, rent, headcount, operating hours, delivery share), enter the two real comps into data/benchmarks/ and re-grade the Roebling and Graham cards (D74, `loci recommend`). P5 on the GTM memos until the owner provides closure.
0a. **Headcount/size term for the revenue model (D91):** the v0.2 median anchor describes a 2–3 person shop; a fully-staffed 1,000 sq ft café requires headcount or team productivity as an input the model lacks. Scoped as a low-priority expansion (not breaking the current gate), but necessary before any operator can rely on the p50 figure.
0b. **GTM-153 dedup measurement before fix (D91):** a café with a DOHMH permit was counted twice in the supply pool. Measure the size of the category-assignment rule defect (how many storefronts are incorrectly classed as multiple categories) before applying a per-category dedup fix.
0c. **Retrodiction — done — verdict D88 (GTM-158 Done, QUESTIONS T11 answered):** entry test AUC 0.866 vs 0.854 no-score, supply coefficient +1.17 [0.87, 1.47] after NTA FE + other-category conditioning — the screen ranks retail streets, not unmet demand; LL157 go-dark null; business-level survival not identified (Foursquare ~3% ascertainment). See docs/retrodiction-2026-09.md.
0g. **Survival ascertainment (GTM-161, D88):** estimate Foursquare closure-ascertainment rate per category against the LL157 go-dark base rate and DOHMH absence, then either an ascertainment-weighted hazard model or Google Places Insights (GTM-159) as the closure source — only then can any survival claim be made.
0h. **Recommendations ledger owner decisions (GTM-162, D89):** radius (400 m straight-line vs network), expiry after N months, whether a regraded card auto-expires the old row, rubric weights, pre-existing build-outs as context; first real fill check lands 2026-10-01 inside the monthly `make chains-refresh` job.
0i. **Update docs/GTM.md value-prop language to cite D88** (in progress in parallel) — cost-of-search framing confirmed, not superseded, by the retrodiction verdict.
0d. License riders (GTM-159): get redistribution/derivative-use quotes from Placer, Data Axle, Esri, Advan/Dewey before any purchase — license terms, not price, are binding (D86).
0e. Trial Google Places Insights in BigQuery (monthly POI snapshots since 2024-01) against the first-seen ledger (D79) to see how many of the 47.7% `backfill_censored` rows it resolves (GTM-159, D86).
0f. Write to NYC DOT for the VivaCity sensor data-sharing ask (~100 sites incl. residential) — the non-corridor validation set GTM-147 needs (GTM-159, D86).
1. Verify the 2026-10-01 launchd run's logs (`data/chains/logs/`) — confirm storefront-pipeline build → poi-snapshot → detect → research → render all completed for the first unattended monthly cycle covering `loci poi-snapshot` (D79), `analysis.storefront_pipeline` (D80), and `loci chains detect` (first month-over-month delta, D77); a skipped step is a permanent hole for any of these ledgers.
1b. ~~D80 filings stage two~~ done (commit 10a33e8): `analysis.storefront_pipeline` at bbl × business_name_key, cross-agency lead times, `openings_pipeline_400m`/`openings_recent_400m` context measure, `'gov_filing'` first-seen kind, chains pipeline column. Remaining: widen the filing-lifecycle coverage beyond food/drink (10 of 15 categories are structurally filing-blind — laundry, hair, nails, childcare, clinic, fitness, bank, hardware, convenience, tailor) and harden the cross-agency link (92% of reconciled pairs are sole-pair-in-BBL) — ticketed GTM-152.
1c. Surface transit share + daypart profile (D76 addendum), first-seen month (D79), and D80's pipeline counts + cross-agency lead time on the webmap popup and the `loci recommend` card as context — render a zero share as "no station within a 5-minute walk," a censored first-seen month as unknown never as a September opening, and a zero pipeline count in one of the 10 filing-blind categories as "not tracked by government filings" never as "nothing coming."
1d. Before any grade uses `'gov_filing'` as a first-seen kind: contrarian precedence review — an application is not an opening, and the 92% sole-pair reconciliation rate means most "confirmed" cross-agency lead times are really single-source.
2. Re-apply `loci address-access --boroughs MN,BK` after any MN+BK citywide re-run — the access columns (transit_entries_400m, jobs_400m) NULL out on every `loci address-gaps` re-run (RESOLVED for the canonical order, D78 — the peer's How-to-resume block now includes `address-access` and `transit-profile`; keep this line for any ad hoc re-run outside that order).
3. Put the access columns on the `analysis.address_gaps` view (extend `address_gaps_view_sql()`) and on the webmap popup as context — render a zero as "no station within a 5-minute walk," never as "low foot traffic" (D76 ruling).
4. Before any grade discussion: build the 800 m / distance-decay transit variant and clear the D76 graduation test (≥100 non-corridor validation points, Brooklyn-only ρ ≥ 0.6 with CI lower bound > 0.4, non-degenerate variable) — only then does transit/jobs leave card-context status.
5. Owner call: minimum member count for the density ranking (`loci clusters --min-addresses N`; 9 of the top 50 are single addresses, D83).
6. Re-fit the supply baseline once (`loci supply-ratio --fit-baseline`) so supply_baseline.yaml's universe string reads the lot-only wording (n stays 281,842) — sanctioned by D84; then plain apply.
7. P&L collection for the comps file (data/benchmarks/bizbuysell_nyc_listings.csv) — 3–5 per category from businesses for sale in Gowanus area (BizQuest/BizBuySell block automated and browser access so human collection honored).
8. ~~Owner approval for the Google coverage check on the Gowanus core for pharmacy/convenience/hardware (GTM-48, ~$170 budget)~~ done — GTM-48 ran 2026-09-14 (D90): docs/coverage-validation-2026-09.md, 5,633 rows, 5,985 calls ≈ $190, ledger 8,963/9,113.
9. Style the pipeline overlay by activity_status (dev_pipeline.activity_status: active/lapsed/stalled; see PIPELINE_COLUMNS in webmap_export.py).
10. Pharmacy double-count: join NPPES other_names (DBA) on the stored npi to fuse the 516 legal-name/awning pairs (D71).
11. `loci export-webmap` should echo meta.ageFit.warning to the terminal like supplyWarning (D71).
12. Webmap: expose gap_score_fit and age_fit_lead (with the supply-revealed caveat text untruncated) — viz/webmap_export.py _gap_sql is a hand-picked column list.
13. spend.yaml bar entry: replace the unverified 65.3% away-from-home share with CEX item 790420 ($279 All-CU 2024, 43.4%) — fair-value engine correction. Keep the existing items that are still valid; remove any that D63 closed (e.g. "fix subway ridership NULL" if present).
14. Decide whether the webmap export should carry the new demographic columns; viz/webmap_export.py _gap_sql is a hand-picked column list, so nothing new reaches the map until it is added.
15. Catchment-weighted demographics over the walk-shed (QUESTIONS entry) — owner call before building.
16. Merge DCWP laundries (`loci ingest-dcwp --no-stage --apply && loci dedup`), re-run score/address-gaps MN+BK, export-webmap; then grade adoption per D48/D51 with C ≡ 1 on the principled set; contrarian re-review.
17. StreetEasy sweep (running, run_id in session log): merge parquet into staging.listings when done; combine LL84 + listings + NYCHVS small-building washer rates into the laundry demand haircut for the grade.
18. D14 nails anchor inflation; then load anchors for bank (FDIC), pharmacy (NYS), childcare (DOHMH) so those categories leave the fail-open path.
19. Decide D7 density-class reach (D43): calibrate reach(c) per density class from complete addresses in that class, bounded below by the cited floors in reach_tiers.yaml; re-run the D34/D39 battery per class — density is a grade input within MN+BK, not a rule to extend to outer boroughs (D48).
20. ~~Contrarian review of `demand.yaml`~~ done — D49; ~~port to addresses (GTM-110)~~ done — D57. Remaining: viz/webmap_export.py joins analysis.address_demand on (address_id, reach_hash, supply_hash) filtered to is_lead and renders demand_caveat_text UNTRUNCATED (the X6 disclaimer is its tail).
21. ~~Before GTM-48 can run~~ GTM-48 RAN 2026-09-14 (D90; docs/coverage-validation-2026-09.md; 5,633 rows, 5,985 calls ≈ $190, ledger 8,963/9,113). Historical context kept below for what the run had to clear: re-budget the Google ledger (22 calls remain; corrected run ≈ 14,850 calls); resolve the three categories.yaml anchor-vs-adapter contradictions (clinic lists 621111 while foursquare_places.py excludes doctors' offices; hardware's 444140 anchor excludes the home centers the adapters ingest; bank's 522110 excludes the credit unions Overture feeds in — see item 26); decide tailor_repair's headline status. Sampling frame is now gap ADDRESSES (D58): 5,277 calls at 649 m for 20/stratum; grocery has zero missing addresses in eligible MN+BK (200 control calls buying no gap observations — drop it or treat as pure control); fitness/pharmacy/nails_beauty/hardware have income deciles with no missing address at all, so their missing arm is thinner than 10 strata. Also: plan()'s est_cost_usd treats the 5,000 free calls as untouched; 2,978 are already spent, so the true incremental cost of a 5,277-call run is nearer $170 than the printed $8.86 — budget off the ledger, not that number. CLI: `loci validate --per-stratum N --boroughs MN,BK [--unit-weighted] [--seed S] [--dry-run]`. Tests: tests/test_sample.py 16 (11 new); suite 333. **Remaining, per D90:** the three anchor-vs-adapter contradictions are STILL unresolved (QUESTIONS D23); tailor_repair's headline status is DECIDED (D90 ruling 1 — demoted, with hair_barber, per the D30 precedent); the est_cost printout defect noted above recurred on the actual D90 run ($16.70 printed vs ≈$177 true) and is now ticketed separately (GTM-171) rather than fixed.
22. Fitness anchor source for the ~20% true coverage hole (QUESTIONS M7).
23. Clinic re-anchor to licensed urgent care (D30).
24. Overture adapter: decide whether `bank_credit_union` belongs under the bank slug (NAICS 522110 excludes credit unions 522130) — see the comment in `categories.yaml`.
25. E9 ignition productionization — not before 1–6.
26. Comps (O7/O8): owner runs the manual export in benchmarks.yaml or approves a browser session;
    fix or remove the failing test_comps test. Fair-value: the spec and analysis.site_fairvalue model
    that spend.yaml cites do not exist — decide whether to write them (O6/O7) or retire spend.yaml to
    a reference file.
27. ZBP follow-up (D40/M9): rerun `loci zbp-compare` per source to attribute the overcount; then decide whether SLA and NYS DOS anchors need an active-license filter like the DOHMH one in (18). Add ZCTA polygons for a proper POI→ZIP join. Feed `zip_category_establishments` size bands into D9 and establishments-per-resident into O6.
28. Fold `src/loci/zbp_naics.yaml` into `src/loci/categories.yaml` (D42) so there is one NAICS mapping, or have zbp-compare read the anchor's `naics_2017` field; add the drift check.
29. Collect 3–5 P&Ls per target category (owner specification: real operators in Gowanus or comparables) — the only path from D81's grade C to grade B (model calibration cannot improve without local economics truth; see D81 caveats).
30. ~~Google coverage check on the Gowanus core for pharmacy/convenience/hardware (GTM-48, owner approval, ~$170 budget) — validate the gap addresses for whether a listing actually exists, untrusted-query fault vs measured absence.~~ done — see item 8 and D90.
31. Leakage (λ) out-of-sample test design — D81 caliates λ per county to match Economic Census totals, a right-by-construction property with no out-of-sample test; design one (e.g., holdout county, or fitted λ on training counties vs test county).
32. Extend the revenue backtest to a size-band likelihood (D81 currently targets establishment count per CBP employee band; a business-failure-rate or log-likelihood loss function may be more sensitive to distribution shape at small scales).
33. Neighborhood character (D82, GTM-154) on the recommend card: add the context line and the "weekday-office catchment" copy (with the more-homes-not-fewer-customers caveat) for corporate-labeled addresses; render a NULL transit AM/PM share as "no station within a 5-minute walk," never a zero.
34. Review the COMMERCIAL_OVERLAY_MIN_LOTS=20 threshold (D82, GTM-154) against a held-out corridor list — the twelve planner corridors used to tune it flip at every setting tried, so they cannot also validate the choice.
35. Weekday sidewalk-camera sampling at the 15 DOT-adjacent cameras (AM/MD/PM, two weekdays) then `loci sidewalk-count validate --round 2026-05`, recording rank correlation per window — ticketed GTM-157 (D85).
36. ~~Wire `webmap/data/dot.json` into the map~~ done (commit 089f1fe) — DOT count points sized by latest round with trend + COVID caveat, 969 cameras with per-camera sidewalk summaries, off by default; a bare-`NaN`-in-JSON export bug found and fixed, pinned by test (D85 addendum).
37. Re-run `loci dot-counts address-context` after any address rebuild — add to the canonical pipeline order alongside `address-access` and `transit-profile --re-sweep` (D85).
~~GTM-12~~ done — see D12.

If tickets change, edit `src/loci/tickets.py` and re-run `loci gen-tickets` — then push the
delta. Never hand-edit `docs/TICKETS.md`. If a ticket is renamed, `make check` will flag every
question in `docs/QUESTIONS.md` that cited the old title.

When a question gets answered, update its **Status** and **Current answer** in
`docs/QUESTIONS.md` in the same session.
