<!--
DRAFT — not applied to docs/CHECKPOINT.md. A peer session holds CHECKPOINT.md this
session (per the coordinator's instruction). When it's free:
  (a) paste the "D140 entry" block below into the decision log, in date order among
      the other 2026-09-22 entries (after D137's addendum, alongside/after D139 if
      that has landed by then — check with the peer holding the file first).
  (b) paste the "Session-log line" block into the session log, dated 2026-09-22.
  (c) add the two "Blockers table" rows to the existing Blocked table (~line 181 of
      CHECKPOINT.md today).
  (d) the D140 ticket definition (PARENT row in drafts/tickets_d140.py.txt) must land
      in src/loci/tickets.py in the SAME edit as (a), per this repo's check-tickets
      hook — do not commit the decision-log entry without it.
-->

## (a) Decision-log entry

**D140 — RQ-001 opened ("how long do favorable neighborhood conditions last for a restaurant buyer, and what ends them, and what causes the shift"); the research-question framework (interview → seed → data audit → notebook → answer → review → tickets → checkpoint, docs/research/README.md, `loci research new/check/run`) adopted as the mandatory pattern every future Loci research question must follow.** *(2026-09-22, owner interview + subagent session; ticket TBD (not yet pushed to Linear))*

**Why a framework, not just an answer.** The owner asked that this question — and whatever comes after it ("which neighborhoods are next," etc.) — be handled through the exact same repeatable sequence rather than each landing as a bespoke one-off analysis. The CLI gate (`loci research check`, wired into `make check` alongside check-sources/check-questions/check-tickets) is what makes that a standing rule instead of a documented intention: it fails non-zero when an RQ folder is missing a stage file it claims is done, or cites a `GTM-nn` ticket id that doesn't exist in `tickets.py` — the same drift-prevention shape as every other `check-*` gate in this repo, applied to research output instead of source registrations. `src/loci/research.py`, `src/loci/research_template/`, and `.claude/skills/research-question/SKILL.md` were built this session and are functionally complete; the one piece of real wiring left is registering `research_app` with `app.add_typer(...)` in `cli.py` and adding `loci research check` to the Makefile — see the framework ticket in `drafts/tickets_d140.py.txt`.

**Owner interview rulings, summarized** (full text in `docs/research/RQ-001-regime-durability/QUESTION.md`): outcome = composite regime of demand (foot traffic, residents/workers, income) + supply (restaurant density/saturation) + cost (rent); answered for owner (purchase) and tenant (lease) separately; unit of analysis = address trade area, aggregated for reporting, never a hex table; two tiers — ZIP-level 1994→present for the long base rate, address-level ~2010→present for calibration and per-site scoring; "full shift" measured two independent ways (data-driven Markov/HMM-family regime model vs a fixed hysteresis threshold) and compared, neither alone is the answer; durations are right-censored survival data, ongoing regimes never dropped; macro enters only through free FRED/BLS series and is tested as a hypothesis, never assumed; all four validation methods required (out-of-time backtest, named-neighborhood urban-planner pre-registration, contrarian + statistician verdict, operator-site spot check); session deliverable is the framework plus a provisional, explicitly-labeled v0 answer, not a finished one.

**Mid-session ruling: ingest every free data gap this session** (macro FRED/BLS series, Zillow ZORI/ZHVI, LODES WAC, the full multi-vintage ACS 5yr panel, historical PLUTO vintages) — no rolling windows, caps, or samples, per the owner's standing 2026-09-16 rule restated for this question. `DATA-AUDIT.md`'s read-only probe of `registry.yaml` and the warehouse found SUPPLY solid (ZBP/CBP give a real 26-year 1998–2023 ZIP×category panel already loaded) but COST effectively absent at ZIP grain (ZORI/ZHVI registered, zero rows loaded — the single binding constraint on a real composite v0) and DEMAND a single 2023 ACS cross-section rather than a trend; MACRO entirely unregistered, nothing ingested anywhere. This session's ingest pass got `fred_macro` fully pulled (11 FRED series verified live, full history, no caps — 9,322 monthly + 804 annual rows staged to parquet, adapter at `src/loci/sources/macro.py`, draft registry entry in `drafts/registry-fred_macro.yaml` pending a `registry.yaml` merge) while `registry.yaml`/warehouse access itself was held by a peer session; Zillow/ACS/LODES/PLUTO remain open ingest tickets (see below).

**v0 = ZIP tier, 2000→latest, per `METHOD.md`'s v0 recipe** (data-scientist judgment memo, design only, nothing run yet). Headline window starts 2000 because that's when ZHVI (the cost pillar) starts, not 1994 — 1994–99 demand+supply-only data exists solely to date the onset of spells already running at 2000 (left-truncated entry). Equal-weight (⅓ each) within-year percentile-rank composite; hysteresis enter ≥0.70/exit <0.60 for 2 consecutive years (sensitivity grid required, ≥50% median movement means "the answer is the grid, not a number"); Kaplan–Meier with delayed entry and PUMA cluster-bootstrap CIs; k-means(k=4) fit on ≤2013 data plus a persistence-filtered Markov matrix, compared against the KM median (AC-9); discrete-time hazard for ranked exit drivers, macro entering only via year-FE × exposure interactions (level macro is near-mechanically null against a relative/tercile outcome — METHOD.md §5); owner/tenant readings split by cause-specific (Aalen–Johansen) incidence, not by re-weighting the same spells. v0 notebook itself is not yet built — ticketed Urgent (see tickets draft) as the actual RQ-001 deliverable everything else in this milestone feeds or checks.

**Migrations 060–062 claimed** for landing this session's staged parquet (`data/interim/rq001/{macro,zillow,acs_zcta}/`) into the warehouse — `fred_macro`, `zillow_zhvi_zori`, `acs_5yr_zcta_panel` respectively. Reserved at 060 rather than the immediate next-free number (056 is the current highest landed migration, `sql/056_brewery.sql`) because 057–059 may already be claimed by concurrent peer work; claim/confirm by chat before applying, per this repo's concurrent-session convention.

**Tickets.** 19 rows drafted under a new "Research questions" Linear milestone (added to `EPICS`) — 1 parent (this decision), 1 framework-CLI-wiring row, 5 free-gap ingests, 1 warehouse-load row, 1 Urgent v0-notebook-build row, 1 address-tier-panel row, 1 paid-foot-traffic-quote row (quote only, owner budget ruling required before any purchase — SEED.yaml constraint), 3 Urgent validation rows (out-of-time backtest, named-neighborhood check, contrarian+statistician verdict), 1 High validation row (operator-site spot check), 3 Low-priority data-audit follow-ups (DOHMH ZIP rollup, the ZBP SIC 1994–97 crosswalk decision D42 left implicit, DOT pedcount ZIP feasibility), and 1 recurring drift-re-run row. Full text: `docs/research/RQ-001-regime-durability/drafts/tickets_d140.py.txt` (not yet pasted into `src/loci/tickets.py` — held by a peer session). None carries a real `GTM-nn` id yet; `loci check-tickets` will list all 19 as unpushed the moment they land, which is expected for a freshly-defined batch and clears once they're pushed to Linear (or exempted via `PUSHED_WITHOUT_ID`, mirroring the D126–D129 precedent) in the same sitting they're pasted in.

---

## (b) Session-log line

### 2026-09-22 — Session (subagent, drafts only; tickets.py/CHECKPOINT.md held by a peer for D139): RQ-001 opened, research-question framework adopted (D140)
- Read `SEED.yaml`, `DATA-AUDIT.md`, `METHOD.md`, `QUESTION.md`, `STATUS.yaml` for RQ-001 (regime durability — how long favorable neighborhood conditions last for a restaurant buyer). Drafted the D140 decision-log entry, 19 `tickets.py` rows under a new "Research questions" milestone (1 parent + 18 children: framework CLI wiring, 5 free-gap ingests, warehouse load, the Urgent v0-notebook build, the address-tier panel, a paid-foot-traffic quote-only row, 3 Urgent + 1 High validation rows, 3 Low-priority data-audit follow-ups, 1 recurring re-run), and this session-log/blockers text — none applied to the live files; all written to `docs/research/RQ-001-regime-durability/drafts/` for the peer holding `tickets.py`/`CHECKPOINT.md` to paste in.
- No code, warehouse, or registry changes. `src/loci/research.py`, `research_template/`, and the `research-question` skill already existed from earlier work this session-cluster; confirmed by direct read that `research_app` is NOT yet wired into `cli.py` and `loci research check` is NOT yet in the Makefile `check:` target — ticketed rather than fixed here (drafts-only scope).

---

## (c) Blockers-table rows

Add to the existing `### Blocked` table:

| Blocker | Blocks | Action |
|---|---|---|
| Paid historical foot-traffic source (Placer.ai/SafeGraph/Advan) not quoted or bought | RQ-001's address-tier demand pillar has no real foot-traffic series (free DOT pedcount is 114 fixed points citywide, `control`-role only, DATA-AUDIT.md) | Run the quote-gathering ticket (drafts/tickets_d140.py.txt), bring pricing/depth comparison to the owner for an explicit budget ruling — no purchase without one (SEED.yaml constraint) |
| RQ-001 warehouse load (migrations 060–062: fred_macro, zillow_zhvi_zori, acs_5yr_zcta_panel) waiting on `tickets.py`/`registry.yaml`/warehouse access | v0 notebook (AC-7) needs to rerun from the warehouse, not from hand-staged parquet in `data/interim/rq001/` | Confirm migration numbers 060–062 are still free by chat with whichever peer is running D139's re-run, then apply; merge `drafts/registry-fred_macro.yaml` into `registry.yaml` in the same pass |
| 19 draft `tickets.py` rows (D140, "Research questions" milestone) not yet pasted into the live file | `check-tickets` cannot yet confirm D140 coverage; RQ-001's SEED.yaml AC-13/14/15/16 all unmet | Whoever next holds `src/loci/tickets.py` pastes `drafts/tickets_d140.py.txt` in full (EPICS entry + 19 T rows), adds the CHECKPOINT.md D140 entry in the same edit, runs `loci gen-tickets`, then pushes to Linear (or exempts via `PUSHED_WITHOUT_ID`) |
