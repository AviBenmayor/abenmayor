# DRAFT — not applied. Written 2026-09-22 by a subagent session; tickets.py and
# CHECKPOINT.md are held by a concurrent session (D139 re-run). Whoever next has
# exclusive access to src/loci/tickets.py should:
#
#   1. Add the EPICS entry below to the EPICS list (after the E10 tuple, ~line 163).
#   2. Paste the 19 T-list rows below into T, in a new
#      `# ------------------------------------------------------------------ Research
#      question framework (RQ-001, D140)` block (house style: one banner comment per
#      themed group, see the existing file for the pattern).
#   3. None of these 19 rows carries a `GTM-nn` id yet (nothing has been pushed to
#      Linear this session). `loci check-tickets` will list every one of them under
#      UNPUSHED the moment they're pasted in — that is expected, not a bug, for a
#      freshly-defined batch. Two ways to clear it, same as the D126–D129 precedent
#      (see the PUSHED_WITHOUT_ID comment block near the top of tickets.py):
#        (a) push all 19 to Linear under the new "Research questions" milestone in
#            the same sitting and stamp each real GTM-nn id back into its
#            description (preferred — this is what AC-13/AC-14/AC-15 in SEED.yaml
#            ask for), or
#        (b) if Linear access isn't available yet, add all 19 titles to
#            PUSHED_WITHOUT_ID with a comment mirroring the D126–D129 one
#            ("pending Linear sync; remove this exemption once the real id is
#            stamped back"), and keep every description's closing line as written
#            below ("Pending Linear sync; no GTM id has been represented as live.").
#      Either way, `loci gen-tickets` must be re-run afterward to regenerate
#      docs/TICKETS.md, docs/linear-import.csv, docs/linear-tickets.json.
#   4. tickets.py has no native parent/child field (the CSV/JSON schema is Title /
#      Description / Status / Priority / Estimate / Labels / Project / Milestone —
#      no Parent column). The PARENT row below is a normal ticket; the Linear
#      parent-issue relationship on the 18 children has to be set by hand (or via
#      the Linear MCP connector) after all 19 are live, using the PARENT ticket's
#      GTM id as the parent. SEED.yaml AC-14 checks this with
#      `list_issues(parent)`, not with anything tickets.py emits.
#   5. Every one of the 19 rows closes with "Opened 2026-09-22 (CHECKPOINT D140)" —
#      matching house style, where a ticket cites the decision that spawned it (see
#      D131's or D125's tickets). That means all 19, not just the PARENT row, are
#      individually "cite D140" as far as decisions.py's cited_ids() is concerned;
#      loci check-tickets only needs ONE of them to carry a GTM-nn id (or a
#      PUSHED_WITHOUT_ID entry) for D140 itself to read as covered, but every row
#      that still lacks one shows up on its own UNPUSHED line regardless — see note
#      3, which already covers this (it says "every one of them," not just some).
#      The ZBP-SIC row additionally says "(D42 skipped it)" per the owner's dictated
#      text, so it also registers as (harmless, redundant) coverage for D42 — D42
#      already has real ticket coverage elsewhere — but is itself still unpushed
#      under D42 too until it gets a GTM id or a PUSHED_WITHOUT_ID entry, same as
#      every other row here is unpushed under D140.

# ============================================================ EPICS addition ====
# Append to the EPICS list (src/loci/tickets.py, after the E10 tuple):

    ("Research questions", "Structured research questions run through the fixed "
     "interview -> seed -> data-audit -> notebook -> answer -> review -> tickets -> "
     "checkpoint sequence (docs/research/README.md; `loci research new/check/run`). "
     "One docs/research/RQ-NNN-<slug>/ folder per question, gated by `loci research "
     "check` so no stage is silently skipped. Every RQ's subtickets land here as a "
     "parent issue plus children, never as one-off tickets scattered across the "
     "epic the RQ happens to touch.", "Ongoing"),

# ============================================================== T-list rows =====
# Paste into T (src/loci/tickets.py). (epic, title, priority, estimate, labels, description)

# ------------------------------------------------------------------ Research question framework (RQ-001, D140)
("Research questions", "RQ-001 -- How long do favorable neighborhood conditions last for a restaurant buyer, and what ends them? (D140)", H, 2, "model,validation,docs",
 "Parent/tracking issue for the first Loci research question run through the new fixed framework (docs/research/README.md). Owner's question, verbatim: 'how often does the landscape of a neighborhood fully shift... if a restaurant were to purchase a storefront in a neighborhood, how long should they anticipate for the favorable conditions to last?' Answered as (1) a citywide base-rate distribution of favorable-regime duration, (2) ranked drivers of regime exit with macro tested as a hypothesis not assumed, and (3) a per-address expected-duration function, reported separately for the owner (property-purchase) and tenant (lease) readings and split by restaurant segment where sample supports it.\n\nWhy a parent ticket at all: every future Loci RQ follows this exact same shape (SEED.yaml -> QUESTION.md -> DATA-AUDIT.md -> notebook -> ANSWER.md -> review -> tickets -> checkpoint, docs/research/README.md), and the owner asked that similar questions be handled identically rather than each getting its own ad-hoc ticket sprawl -- grouping the framework build and every RQ-001 follow-up under one parent, in its own 'Research questions' milestone, is what keeps that promise visible in Linear instead of just in a docs convention.\n\nArtifacts: docs/research/RQ-001-regime-durability/{SEED.yaml, QUESTION.md, DATA-AUDIT.md, METHOD.md, STATUS.yaml, notebook.ipynb (pending), ANSWER.md (pending)}. SEED.yaml's 16 acceptance criteria are the source of truth for scope; STATUS.yaml tracks per-stage done/pending/blocked so a missing file is never silently mistaken for 'not needed.'\n\ntickets.py has no parent/child field -- the 18 children below (framework CLI wiring, five source ingests, the warehouse load, the v0 notebook, the address tier, the paid-quote ask, four validations, and three lower-priority data-audit follow-ups) are separate T rows under this same milestone; the actual Linear parent-issue link is set by hand once all 19 are pushed.\n\nOpened 2026-09-22 (CHECKPOINT D140). Pending Linear sync; no GTM id has been represented as live."),
("Research questions", "Research-question framework: wire `loci research new/check/run` into the CLI and `make check`", H, 3, "infra,docs",
 "Why: the owner's ruling on RQ-001 was explicit -- every future Loci research question must follow the identical interview/seed/data-audit/notebook/answer/review/tickets/checkpoint sequence, not a bespoke one-off analysis. `src/loci/research.py` (327 lines: scaffold, next_number, STATUS.yaml loader, check_rq/check_all, resolve_rq, run_notebook, and a `research_app` Typer sub-app with new/check/run commands), `src/loci/research_template/` (the six-file template set: SEED.yaml, QUESTION.md, DATA-AUDIT.md, notebook.ipynb, ANSWER.md, STATUS.yaml) and `.claude/skills/research-question/SKILL.md` (the nine-step operator sequence) already exist and were used to build RQ-001 itself -- but `research_app` is not yet registered with `app.add_typer(...)` in cli.py, so `loci research new/check/run` do not exist as commands today, and `loci research check` is not yet one of the drift gates in the Makefile `check:` target alongside check-sources/check-questions/check-tickets/check-presence.\n\nThis is the CLI gate the owner's own words describe: 'check gate prevents drift' -- without it, a future RQ folder can skip a stage (an ANSWER.md deleted after being marked done, a dangling GTM-nn citation in QUESTION.md) with nothing catching it, exactly like check-tickets/check-questions catch drift everywhere else in this repo.\n\nScope: (1) `app.add_typer(research_mod.research_app, name='research')` in cli.py; (2) add `uv run loci research check` to the Makefile `check:` target; (3) confirm `loci research check` exits non-zero against the two pytest fixtures SEED.yaml's AC-4 already specifies (a folder missing a done-marked stage file, a folder citing a GTM id not in tickets.py) and passes against the real RQ-001 tree today; (4) `uv add nbformat nbclient` (README.md notes these are needed for `loci research run` and are not yet in pyproject.toml).\n\nOpened 2026-09-22 (CHECKPOINT D140). Pending Linear sync; no GTM id has been represented as live."),
# ------------------------------------------------------------------ Free-gap ingests (RQ-001 DATA-AUDIT, session 2026-09-22)
("Research questions", "Ingest fred_macro: FRED/BLS macro series (NYC unemployment, restaurant employment, rates, CPI food-away, NBER recession + COVID flags)", M, 3, "ingest,model",
 "Why: SEED.yaml AC-10 requires the notebook to test macro as a hazard driver on available data or state explicitly it is blocked; DATA-AUDIT.md found NOTHING registered or ingested for any of unemployment, restaurant-sector employment, rates, or CPI food-away-from-home -- a clean gap, not a partial one. Owner's mid-session ruling (2026-09-16 standing rule, reaffirmed for this session): ingest every free data gap in full, no rolling windows or caps.\n\nIdentification limit, from METHOD.md section 5, stated up front so this ingest is not oversold: level macro cannot move RELATIVE favorability (terciles are relative and the favorable share is fixed at about 1/3 by construction, so a shock that moves every ZIP together changes no ranks -- a macro main effect on the aggregate exit rate is close to mechanically null), and year fixed effects are perfectly collinear with any NYC-wide macro series (one value per year each; only one of the two can enter a model). What IS identifiable: H1 (macro raises churn), a descriptive-only regression of the year-level exit rate on the macro series, effective n around 24 years and about 3 episodes (2001, 2008-09, COVID) -- no significance claim; and H2 (macro reorders ranks), modelled with year FE plus macro x exposure interactions, identified cross-sectionally (recession x a ZIP's restaurant employment share; COVID x office-job share via LODES CNS codes; rates x ZHVI level). Absent this ingest, AC-10 ships BLOCKED, which SEED.yaml's exit conditions explicitly allow but which the owner's own question ('favorable macroeconomic conditions, if that's a true factor') asked to have tested, not skipped.\n\nStatus: a draft adapter (src/loci/sources/macro.py) and registry entry (docs/research/RQ-001-regime-durability/drafts/registry-fred_macro.yaml, NOT applied -- registry.yaml held by a peer) already exist from this session's ingest pass; all 11 FRED series verified live 2026-09-22 (HTTP 200, real observation columns) and pulled in full with no caps into data/interim/rq001/macro/{macro_monthly,macro_annual}.parquet (9,322 / 804 rows). Remaining: merge the draft registry entry into registry.yaml, confirm with `loci check-sources`, and land the warehouse table (see the migrations-060-062 ticket below, which this ticket's parquet feeds).\n\nOpened 2026-09-22 (CHECKPOINT D140). Pending Linear sync; no GTM id has been represented as live."),
("Research questions", "Ingest Zillow ZHVI/ZORI to a ZIP monthly panel, 2000-present", H, 3, "ingest,model",
 "Why: DATA-AUDIT.md names this the single highest-value ticket for the entire RQ-001 COST pillar and its binding constraint -- ZORI/ZHVI is REGISTERED in registry.yaml (status: planned) but has zero rows anywhere in the warehouse (grepped every table name for zori/zhvi/rent). Without it there is no ZIP-grain cost series before 2019 (DOF storefront vacancy only starts 2019, a 7-year window well short of the >=15-year bar) and METHOD.md's own v0 pillar table (section 1) makes ZHVI the literal COST pillar input, available from 2000 -- which is WHY the v0 recipe's headline window is 2000-to-latest rather than 1994: the cost pillar cannot go back further than ZHVI does. This is not a v1 nice-to-have; the notebook's headline number depends on it directly.\n\nZORI (2015-present) is explicitly NOT a composite input per METHOD.md section 1 (a definition that changes mid-panel would manufacture spurious exits); it is ingested alongside ZHVI only as a rank-correlation check, reported but not scored.\n\nStatus: data/interim/rq001/zillow/ exists as a staged output directory from this session's ingest pass but is currently empty -- the adapter/pull has not actually run yet, unlike fred_macro's. Scope: adapter (ZHVI ZIP-level, all-homes tier; ZORI ZIP-level), full history with no caps (owner rule), land as parquet under data/interim/rq001/zillow/, then the warehouse load ticket below picks it up.\n\nOpened 2026-09-22 (CHECKPOINT D140). Pending Linear sync; no GTM id has been represented as live."),
("Research questions", "Ingest ACS 5-year ZCTA panel, all vintages (not the single 2023 cross-section)", M, 5, "ingest,panel",
 "Why: DATA-AUDIT.md found the DEMAND pillar's income/population/worker inputs backed by exactly ONE ACS vintage in the warehouse (acs_year = 2023, analysis.address_demographics / analysis.hex_demographics) despite registry.yaml claiming 2013-2024 sliding-5yr-window coverage is servable -- a level, not a trend, everywhere the pillar needs one. The RQ-001 owner ruling was explicit: DEMAND is one of three pillars the composite regime score is built from every panel-year back to 2000 (METHOD.md section 1); a single repeated cross-section understates real demand variation and cannot distinguish a ZIP that has been rising for a decade from one that just arrived at the same 2023 level.\n\nThreat this closes, named directly in METHOD.md section 9 (top threats to v0's trustworthiness): 'measurement breaks masquerading as exits' lists the ACS 5-year overlap and the 2020 ZCTA redraw first among the five candidate break sources that can reshuffle ranks and manufacture synchronized fake exits, and separately notes overlapping ACS windows inflate persistence. Pulling every available vintage (not just enough for one cross-section) is what lets the notebook's break-year diagnostic (exit count in a break year > 2x the adjacent-year mean) actually test for this rather than assume it away. ZCTA vintages before the 2020 redraw and after it are NOT the same geography -- the ingest must carry the vintage-specific ZCTA boundary, not just relabel a fixed crosswalk across the break.\n\nOpened 2026-09-22 (CHECKPOINT D140). Pending Linear sync; no GTM id has been represented as live."),
("Research questions", "Ingest LODES WAC 2002-present as a ZIP jobs panel", M, 3, "ingest,panel",
 "Why: worker demand. LODES WAC is registered `verified`/servable 2002-2023 in registry.yaml but DATA-AUDIT.md found NO staging or analysis table for it anywhere in the warehouse -- it was never actually ingested despite the registry marking it ready. METHOD.md's v0 ZIP pillar table already substitutes ZBP total employment per km2 for 'workers' in the demand pillar, so this ingest is not blocking the ZIP-tier v0 headline number the way ZHVI or the ACS panel are -- but it is the input the owner's address-tier ('per-site') reading depends on: METHOD.md section 1 names LODES WAC explicitly as the address-panel-tier demand input (alongside ACS block-group data inside the travel-time trade area), which ZBP-derived ZIP employment cannot substitute for at address grain.\n\nA real 22-year annual block-level jobs panel, aggregable to ZIP, is worth having in its own right for cross-checking ZBP's establishment-count-only view of demand, and DATA-AUDIT.md flags it as a same-shape bounded ingest job (registry already says verified) rather than exploratory work.\n\nOpened 2026-09-22 (CHECKPOINT D140). Pending Linear sync; no GTM id has been represented as live."),
("Research questions", "Ingest PLUTO historical vintages to a ZIP land-use panel", L, 5, "ingest,panel",
 "Why: commercial/retail floor area and assessed value are the supply-capacity and cost-adjacent inputs the current warehouse cannot provide as a time series -- `analysis.address` is derived from PLUTO but only carries PLUTO's CURRENT vintage per address, not year-over-year attribute history, so it is a universe, not a panel. DATA-AUDIT.md frames this as a second COST proxy alongside ZORI/ZHVI, not the primary one, and METHOD.md scopes it explicitly to the address panel tier ('2010+, calibration only, not v0') -- so this ticket does not block the ZIP-tier v0 notebook the way the Zillow or ACS ingests do.\n\nScope: evaluate whether NYC DCP's archived semiannual PLUTO releases can actually be assembled into a multi-year assessed-value/FAR panel before committing to a full ingest -- this is explicitly a feasibility check first (per DATA-AUDIT.md's own framing: 'evaluate whether...'), not a guaranteed-buildable panel, since PLUTO's historical release format and completeness across decades is unverified.\n\nOpened 2026-09-22 (CHECKPOINT D140). Pending Linear sync; no GTM id has been represented as live."),
# ------------------------------------------------------------------ Warehouse load + v0 build
("Research questions", "Load RQ-001 staged parquet into the warehouse (migrations 060-062)", H, 3, "ingest,infra",
 "Why: reproducibility. SEED.yaml AC-7 requires the notebook to rerun end to end from the WAREHOUSE with one command (`loci research run RQ-001`), not from hand-staged parquet files sitting outside version control -- but this session's ingest pass (fred_macro fully pulled, Zillow/ACS/LODES ingests defined above) had to stage its outputs under data/interim/rq001/{macro,zillow,acs_zcta}/ as parquet because a peer session held the warehouse/migration-number lock for the D139 re-run at the same time. Landing them properly means three numbered migrations, one per new staging table, so a fresh warehouse init and an existing one both pick them up the same way every other source in this repo does (sql/NNN_name.sql convention, src/loci/sql/).\n\nScope: 060_macro.sql (staging.fred_macro_monthly / _annual from the existing macro.sql.draft in this RQ's drafts/ folder -- confirm it's harmless under db.init_schema's *.sql glob the way sql/056_brewery.sql's draft was, per the D137 precedent), 061_zillow.sql (staging.zillow_zhvi_zori), 062_acs_panel.sql (staging.acs_5yr_zcta_panel, vintage-keyed). Claim these three numbers by chat with any peer before applying -- sql/056_brewery.sql is the current highest landed migration; 057-059 may already be claimed by concurrent work, hence reserving 060-062 rather than the immediate next-free numbers.\n\nOpened 2026-09-22 (CHECKPOINT D140). Pending Linear sync; no GTM id has been represented as live."),
("Research questions", "Build RQ-001 v0 ZIP-tier notebook per METHOD.md's v0 recipe", U, 8, "model,panel",
 "Why URGENT: this is the actual deliverable SEED.yaml's goal names first -- 'this session ships the framework, data audit, a provisional v0 ZIP-tier answer, and the Linear ticket tree' -- and every other row in this milestone either feeds it (the five ingests, the warehouse load) or checks it after the fact (the four validations). Without this ticket landing, RQ-001 is a framework and an audit with no answer, which fails AC-8/AC-9/AC-11 outright.\n\nScope is METHOD.md's own 10-step v0 recipe, verbatim: (1) build the balanced ZIP universe and frozen ZIP<->ZCTA<->PUMA crosswalk, assert constant ZIP count 1994-latest; (2) pull ZBP employment/722-counts, ACS income/population, decennial-2000 population, ZHVI from the warehouse, deflate income, apply the NAICS-2012 crosswalk; (3) within-year percentile ranks -> pillars -> equal-weight composite c (2-pillar composite for 1994-99); (4) apply the section-2 hysteresis rule (enter >=0.70 x2yr, exit <0.60 x2yr) to derive spells with left-truncated entry ages for pre-2000 onsets; (5) Kaplan-Meier with delayed entry (lifelines `entry=`), median/RMST-20, IQR, censored share, PUMA cluster-bootstrap CI, stock-flow check; (6) the section-2 sensitivity grid (4 bands x 3 persistence levels) and leave-one-pillar-out, flagging >50% median movement; (7) k-means (k=4) on data <=2013, persistence-filtered Markov matrix, implied mean sojourn vs KM median (AC-9); (8) discrete-time hazard (duration spline, lag-2-to-4 pillar changes, borough FE, PUMA-clustered SEs), year FE + macro x exposure if fred_macro landed else marked BLOCKED, ranked driver table; (9) Aalen-Johansen cause-specific incidence for the tenant reading (10-yr conditional survival, cost-led incidence, rent-reset flag) and owner reading (demand/supply-led incidence, appreciation exits), segments where median count >=10; (10) the 2013-origin backtest against the median-for-all and naive-persistence comparators (handed off to the dedicated backtest validation ticket below, but the notebook must emit the tables it needs).\n\nBlocked on the Zillow and ACS ingests landing in the warehouse (or, per SEED.yaml's data_insufficient_for_v0 exit condition, ships as SUPPLY-only with COST/DEMAND explicitly marked degraded if they have not).\n\nOpened 2026-09-22 (CHECKPOINT D140). Pending Linear sync; no GTM id has been represented as live."),
# ------------------------------------------------------------------ Address tier + paid-source quote
("Research questions", "Address-tier panel (~2010-present) and a per-address expected-duration function", M, 8, "model,panel",
 "Why: the owner explicitly asked for a per-site answer ('if a restaurant were to purchase a storefront... how long should they anticipate'), and a ZIP averages 20,000-100,000 people -- METHOD.md section 9's third named threat states plainly that a favorable corridor can sit inside an unfavorable ZIP, so the ZIP-tier v0 answers 'how long do you stay in the top third of NYC ZIPs,' a relative and citywide statement, not 'how long will conditions stay favorable at 379 Broome.' The ZIP tier is explicitly not the buyer-facing answer; it is the base rate the buyer-facing answer has to be calibrated against.\n\nScope, per METHOD.md section 1's address-panel-tier row: demand from LODES WAC plus ACS block-group data inside the travel-time trade area, with DOT pedestrian counts and MTA ridership where present; supply from DOHMH and SLA restaurant counts; cost from ZORI/ZHVI OF THE CONTAINING ZIP (there is no address-level rent series in this warehouse) -- stated on every output, not silently implied. Calibration validation: METHOD.md section 9's threat #3 requires the address-tier and ZIP-tier spell definitions to show kappa >= 0.4 agreement before the address tier is trusted for a real site decision; until that check passes, this ticket's output stays a v0/provisional calibration, same caveat class as the ZIP-tier v0 itself.\n\nSequenced after the ZIP-tier v0 notebook (needs the same hysteresis/regime machinery) and the LODES ingest above.\n\nOpened 2026-09-22 (CHECKPOINT D140). Pending Linear sync; no GTM id has been represented as live."),
("Research questions", "Quote a paid historical foot-traffic source (Placer.ai / SafeGraph / Advan) for RQ-001's address tier", L, 1, "ingest,blocker",
 "Why: DATA-AUDIT.md found the only free foot-traffic series in the warehouse (NYC DOT pedestrian counts, staging.dot_pedestrian_count) covers 114 FIXED points citywide, not a ZIP or address sample -- registry.yaml scopes it to `control`/validation only, and it cannot serve as a real demand-pillar input at either panel tier without heavy, unvalidated imputation. A real address-tier demand series needs a paid panel product.\n\nSEED.yaml's constraint is explicit and binding: 'a paid historical foot-traffic source is scoped with a quote only, nothing bought without an explicit owner budget ruling' -- this ticket is the quote-gathering step, not a purchase authorization. Scope: get pricing and historical-depth terms from at least Placer.ai, SafeGraph (via its successor/distributor), and Advan for NYC address-grain or near-address-grain foot traffic reaching back toward 2010, and bring the comparison to the owner for a budget ruling. No data is ingested and no contract is signed under this ticket.\n\nOpened 2026-09-22 (CHECKPOINT D140). Pending Linear sync; no GTM id has been represented as live."),
# ------------------------------------------------------------------ Validation (the four SEED.yaml AC-12 methods)
("Research questions", "Validation: RQ-001 out-of-time backtest (freeze 2013, score 2018/2023 vs median-for-all and naive persistence)", U, 3, "validation,rigor",
 "Why URGENT: this is one of the two checks that decide whether RQ-001's v0 answer is valid at all, not a nice-to-have polish pass. METHOD.md section 8 states the failure criterion directly: 'the hazard model ships only if its 10-year Brier beats comparator (a) [the median-for-all unconditional training KM curve], with a PUMA-bootstrap CI excluding 0, and it is not worse on emerging spells. Otherwise the per-address function is the conditional KM alone.' Skipping this backtest means shipping a hazard/driver model with no evidence it beats the dumbest possible baseline -- exactly the failure mode CLAUDE.md's verification-habits section warns against ('verify before committing to a dependency').\n\nScope, per METHOD.md section 8: freeze everything on data <=2013 (hysteresis rule, k-means centroids, transition matrix, hazard coefficients, the ZHVI-only cost pillar); score spells at risk at the 2013 origin for P(still favorable in 2018) and P(still favorable in 2023); report IPCW Brier at 5 and 10 years plus Uno's C and decile calibration, split separately for emerging spells (age <=3 at origin, per the named threat that models systematically undercall the youngest spells) and mature spells; apply the failure criterion and print PASS or FAIL -- a FAIL does not block shipping RQ-001, it demotes the driver model to context and the per-address function to conditional KM alone, which is itself a real and reportable finding.\n\nOpened 2026-09-22 (CHECKPOINT D140). Pending Linear sync; no GTM id has been represented as live."),
("Research questions", "Validation: RQ-001 named-neighborhood check vs the urban-planner's pre-registration", U, 2, "validation,rigor",
 "Why URGENT: the second of the two checks that decide whether v0's regime labels mean anything to someone who knows the city on the ground, not just to the model. METHOD.md section 8: the urban-planner PRE-REGISTERS, blind to model outputs, expected onset and end years for roughly ten named ZIPs (11211/11249, 11237/11206, 10002, 10026/10027, 11101, 11215, 11216/11225, 11102/11103, 11372, plus 11207/11208 as a deliberate late-or-never case) -- a hit is an onset within +/-3 years. This is the check that catches a technically-well-fit model whose regime timing still contradicts what a planner already knows happened in Williamsburg, Bushwick, or the Lower East Side; CLAUDE.md's urban-planner agent exists precisely for this kind of ground-truth sanity check.\n\nMust run BEFORE ANSWER.md is written, per docs/research/README.md's fixed sequence (review is stage 6, before stage 5's answer is finalized) and SEED.yaml AC-12, which requires this to appear in ANSWER.md as RUN-with-result or TICKETED-with-id.\n\nOpened 2026-09-22 (CHECKPOINT D140). Pending Linear sync; no GTM id has been represented as live."),
("Research questions", "Validation: contrarian + statistician verdict on RQ-001's regime definition and survival model", U, 2, "validation,rigor",
 "Why URGENT: the third load-bearing check -- docs/research/README.md names contrarian + statistician review as the MINIMUM bar for stage 6 of every RQ, before stage 5's ANSWER.md ships, and this is the question whose entire output is a survival-analysis regime definition, i.e. exactly the statistician agent's charter (identification, MAUP, multiple testing, backtest/placebo design) and the contrarian agent's charter (is this a data gap wearing a costume, a parameter artifact, a saturation effect).\n\nScope: stress-test METHOD.md section 2 (is the 0.70-enter/0.60-exit hysteresis band defensible, or is the >50%-median-movement grid test itself under-powered) and section 5 (does the discrete-time hazard's lag-2-to-4 window actually avoid the contemporaneous-change-is-the-definition-of-exit accounting-identity trap it claims to avoid; is the D1 guardrail -- saturation as accounting, not a claim that retail predicts neighborhood change -- actually being honored in how the driver table gets written up). This is a methodology/verdict call, which per CLAUDE.md's model-routing standard belongs on Opus-tier judgment (contrarian/statistician), not on the Sonnet agent that ran the analysis.\n\nOpened 2026-09-22 (CHECKPOINT D140). Pending Linear sync; no GTM id has been represented as live."),
("Research questions", "Validation: RQ-001 operator-site spot check (Lion's Milk, El Punto, Stone Street, Deux Luxe, Fazenda)", H, 2, "validation",
 "Why: the fourth of the SEED.yaml AC-12 validation methods (not owner-flagged Urgent, unlike the three above, but still a required row -- ANSWER.md must mark it RUN or TICKETED, never silently skipped). METHOD.md section 8: read the regime state and trajectory of the ZIPs containing each of these five already-instrumented operator sites at that site's own opening year, and check whether RQ-001's regime label at that moment is consistent with what the site's own ground-truth record (rent, sales, floor area) already showed.\n\nThis reuses ground truth this project has already paid for rather than collecting new evidence: three of the five sites already carry scored operator records with real sales/rent/floor-area figures from prior sessions (a former-manager interview each), and the other two have open pre-registered questions. Read-only against RQ-001's own output tables -- no new data collection, no new operator outreach.\n\nOpened 2026-09-22 (CHECKPOINT D140). Pending Linear sync; no GTM id has been represented as live."),
# ------------------------------------------------------------------ Lower-priority data-audit follow-ups
("Research questions", "DOHMH restaurant opens/closes -> ZIP rollup for address-tier supply dynamics", L, 3, "ingest,panel",
 "Why: DATA-AUDIT.md found analysis.poi_first_seen already spans 1978-2026 (219,458 rows) but pooled across every POI type, not filtered to restaurant/bar categories or rolled up to ZIP-year -- a near-census independent cross-check on the CBP/ZBP-derived SUPPLY panel sits unused. Filtering and rolling it up gives a second, independently-sourced opening/closing series to validate the SUPPLY pillar against, extending further back than the registry's stated 2022-current DOHMH window (the underlying data actually reaches 1978).\n\nLower priority than the five ingests above: this is a cross-validation improvement on a pillar (SUPPLY) that DATA-AUDIT.md already rates 'solid' from ZBP/CBP alone, not a pillar with a coverage gap.\n\nOpened 2026-09-22 (CHECKPOINT D140). Pending Linear sync; no GTM id has been represented as live."),
("Research questions", "Decide the ZBP SIC->NAICS 1994-97 crosswalk for RQ-001 (D42 skipped it)", L, 2, "model,decision",
 "Why: DATA-AUDIT.md flags this as currently an IMPLICIT skip, not a decision actually scoped to this question -- D42 (2026-09-05, NAICS 2022 adopted as the category anchor) chose NAICS over SIC for Loci's category taxonomy generally, but never ruled on whether RQ-001 should hand-build a SIC-5812-to-NAICS-722 crosswalk specifically to recover 1994-1997 ZBP restaurant counts. Doing so would extend the SUPPLY panel from 26 years (1998-2023) to 30 (1994-2023) and let the 1994-99 pre-headline period (METHOD.md section 1, currently used only to date onsets of spells already running in 2000) carry real SUPPLY-side spell detection of its own instead of being a dating aid only.\n\nThis is an owner ruling ticket, not a coding ticket: bring the tradeoff (four more years of SUPPLY history vs the crosswalk-mapping risk of a manual SIC-era recode) to the owner and record the decision, then either build the crosswalk or formally cap the ZIP tier at 1998 in METHOD.md.\n\nOpened 2026-09-22 (CHECKPOINT D140). Pending Linear sync; no GTM id has been represented as live."),
("Research questions", "Assess DOT pedestrian-count ZIP feasibility for RQ-001's demand pillar", L, 1, "ingest,validation",
 "Why: DATA-AUDIT.md is explicit that this should be ASSESSED, not assumed useless -- the 114-point DOT pedestrian-count series (staging.dot_pedestrian_count, 2007-2026, 37 biannual rounds) is registry-scoped `control`/validation-only and structurally cannot become a ZIP-tier demand PILLAR input (114 points is not a ZIP sample), but it may still contribute something narrower: a sparse validation check on whichever ZIPs happen to contain a DOT count point, cross-checking the ZBP/ACS-derived demand pillar's direction of travel in those ZIPs against real measured foot traffic.\n\nLowest priority of the three data-audit follow-ups: this is a feasibility question with a plausible 'no, stays validation-only as the registry already says' answer, not a committed build.\n\nOpened 2026-09-22 (CHECKPOINT D140). Pending Linear sync; no GTM id has been represented as live."),
# ------------------------------------------------------------------ Recurring maintenance
("Research questions", "Recurring: re-run `loci research run RQ-001` + `loci research check` on each annual ZBP/ACS/ZHVI release", M, 2, "validation,deferred",
 "Why: RQ-001's answer is a base rate computed from a rolling panel (ZBP/CBP, ACS 5yr, ZHVI/ZORI), not a static fact -- each source's annual/regular release shifts the ZIP universe, the most recent spell-censoring point, and potentially the hysteresis-grid sensitivity result. Without a recurring re-run, the shipped v0 answer silently goes stale the first time any of ZBP, ACS, or ZHVI publishes a new vintage, and nothing in this repo's drift-check suite (check-sources/check-questions/check-tickets/research check) would catch a STALE answer the way they catch a missing file or a dangling ticket id -- staleness is a different failure mode from drift, and this ticket is what closes it.\n\nScope: re-run the full RQ-001 notebook end to end and `loci research check` whenever ZBP/CBP, ACS 5yr, or Zillow ZHVI/ZORI publish a new annual/regular vintage (not on a fixed calendar date -- tied to each source's actual release cadence, mirroring how the chains-refresh launchd job (D80/D92) ties to a monthly cadence rather than an arbitrary interval); diff the new spell table and headline median/IQR against the prior run and flag if the movement exceeds the section-2 sensitivity grid's own 50% threshold, the same bar used to judge internal hysteresis-band sensitivity.\n\nOpened 2026-09-22 (CHECKPOINT D140). Pending Linear sync; no GTM id has been represented as live."),
