# RQ-003 — Answer

Scaffolded 2026-09-23 by `loci research new`.

## Answer (v0, provisional)

TODO — plain-language answer to the question as asked, owner vs tenant / segment split
where the seed calls for it. Label the version (`v0`, `v1`, …) and whether it is
provisional.

## Confidence

TODO — what the confidence is, and what would raise or lower it (sample size,
censoring, coverage bias, the specific things the contrarian/statistician review
should probe).

## Validation

| Check | Status | Result / ticket |
|---|---|---|
| TODO validation check | RUN | TODO result |
| TODO validation check | TICKETED | GTM-TODO |

Each row is either `RUN` (with the result inline) or `TICKETED` (with the id that will
run it later) — never silently skipped.

## Gaps → tickets

TODO — every known gap in this answer, each mapped to a ticket id (`GTM-\d+`) defined
in `src/loci/tickets.py`.
